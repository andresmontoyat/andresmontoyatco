---
phase: 21-foundation-astro-scaffold-i18n-routing-layout-shell
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - vercel.json
  - middleware.ts
autonomous: true
requirements: [ROUTE-02, DEPLOY-02]
must_haves:
  truths:
    - "vercel.json no longer contains any rewrites block (SPA fallback removed)"
    - "vercel.json framework is astro and cache headers are scoped to /_astro/* only"
    - "middleware.ts at repo root redirects / to /en or /es (cookie first, else Accept-Language) with 302"
    - "The redirect target is validated against a literal ['en','es'] allowlist before entering the Location header"
    - "On /en/* and /es/* the middleware refreshes the cam-lang cookie"
    - "D-01: The redirect/refresh cookie is named cam-lang (same name as today's localStorage key) and is now a real cookie the middleware reads edge-side before any JS runs (Task 2)"
    - "D-02: Accept-Language resolution uses the same simple heuristic as LanguageContext.readInitialLang (header contains 'es' → 'es', else 'en'), no q-value parser (Task 2)"
    - "D-03: The bare-/ redirect uses status 302 (temporary — destination is visitor-dependent, never 301) (Task 2)"
    - "D-04: The middleware runs on every page route and refreshes the cam-lang cookie per visited locale (/en/* → en, /es/* → es) (Task 2)"
  artifacts:
    - path: "middleware.ts"
      provides: "Vercel-native Edge Middleware: / redirect + cookie refresh + open-redirect guard"
      contains: "isKnownLocale"
    - path: "vercel.json"
      provides: "Astro deploy config: no rewrites, framework astro, /_astro/* cache header"
      contains: "\"framework\": \"astro\""
  key_links:
    - from: "middleware.ts"
      to: "Location redirect header"
      via: "isKnownLocale allowlist gate"
      pattern: "isKnownLocale"
    - from: "middleware.ts"
      to: "cam-lang cookie"
      via: "Set-Cookie response header"
      pattern: "Set-Cookie"
---

<objective>
Replace the SPA-era `vercel.json` and author the Vercel-native Edge Middleware (`middleware.ts` at repo root) that owns the `/` → locale redirect. `vercel.json`: drop the entire `rewrites` block (Vercel explicitly does not support `vercel.json` rewrites for Astro), switch `framework` to `astro`, and scope cache headers to `/_astro/*` (DEPLOY-02). `middleware.ts`: 302-redirect `/` to `/en` or `/es` — `cam-lang` cookie first (D-01), else `Accept-Language` substring heuristic (D-02) — with an `isKnownLocale()` allowlist guard against open redirects, and a `cam-lang` cookie refresh on `/en/*`/`/es/*` visits (D-04).

Purpose: This is the phase's single highest-risk deliverable (flagged by 3 of 4 research passes). The redirect is the only capability in Phase 21 that requires the Edge tier at all. This plan writes the code; Plan 21-05 validates it against a real Vercel preview deploy (the only place the pass-through behavior can be confirmed).
Output: A clean Astro `vercel.json` and a working `middleware.ts` redirect + cookie-refresh handler with an open-redirect guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-RESEARCH.md
@.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-PATTERNS.md
@.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-01-SUMMARY.md

<interfaces>
<!-- middleware.ts target syntax: 21-RESEARCH.md § "middleware.ts — Vercel-native Edge Middleware" (lines ~170-232) -->
<!-- vercel.json corrected shape: 21-RESEARCH.md § "vercel.json — corrected shape" (lines ~350-374) -->
<!-- Locale heuristic to mirror: src/i18n/LanguageContext.jsx readInitialLang (lines 10-16) -->
<!-- Allowlist precedent: src/i18n/LanguageContext.jsx setLang guard (lines 41-47) -->

Locked decisions this file implements:
  D-01  cookie name = 'cam-lang' (real cookie now, read edge-side before JS)
  D-02  Accept-Language heuristic = header.toLowerCase().includes('es') ? 'es' : 'en'  (NO q-value parser)
  D-03  redirect status = 302 (temporary — destination is visitor-dependent, never 301)
  D-04  runs on every route; refreshes cam-lang cookie on /en/* → en, /es/* → es

CONSTRAINTS from 21-RESEARCH.md (do NOT copy Next.js patterns):
  - No request.cookies.get() / NextResponse — that API is Next.js-only. Parse
    request.headers.get('cookie') manually; set via a literal 'Set-Cookie' response header string.
  - Do NOT add @vercel/functions in this plan. The bare-200-Response pass-through
    assumption (Open Question 1 / Assumption A2) is UNRESOLVED and can only be validated
    against a real Vercel preview deploy (Plan 21-05). Implement the bare-200 approach now;
    Plan 21-05 adds `next()` from @vercel/functions ONLY if the preview proves pass-through fails.
  - Matcher scope: 21-RESEARCH.md flags matcher '/(.*)' also runs on /_astro/* assets (Edge
    cost). DECISION FOR THIS PLAN: use a negative-lookahead matcher excluding /_astro/ and common
    static extensions (mirroring Vercel's documented pattern, substituting _astro for _next/static),
    so the cookie refresh still runs on real page routes per D-04 without per-asset Edge invocations.
    Record the exact matcher string used in the SUMMARY.

SECURITY (open redirect): parsed locale MUST pass isKnownLocale() (matches literal ['en','es'])
before it is concatenated into the Location header. Never reflect a raw cookie/header value.

D-04 INTERPRETATION (flag for user sign-off): D-04 says the middleware "runs on every route".
This plan interprets that as every PAGE route: the matcher uses a negative-lookahead that excludes
/_astro/ and static-asset extensions, so the cam-lang cookie still refreshes on every /en/* and
/es/* page visit (D-04's actual purpose — cookie refresh on locale page visits) WITHOUT paying
per-asset Edge invocations. This narrows D-04's literal "every route" wording and is NOT explicitly
covered by CONTEXT.md's "Claude's Discretion" section, so the executor MUST call this interpretation
out explicitly in 21-03-SUMMARY.md (the exact matcher string + this rationale) for user confirmation
rather than narrowing D-04 silently.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite vercel.json for Astro static output</name>
  <files>vercel.json</files>
  <read_first>
    - vercel.json (current — framework vite, SPA rewrites, /assets/* + /index.html + / cache rules)
    - .planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-RESEARCH.md § "vercel.json — corrected shape for this phase"
    - .planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-PATTERNS.md § "vercel.json"
  </read_first>
  <action>
    Replace `vercel.json` with the corrected Astro shape from 21-RESEARCH.md: keep `$schema`, set `"framework": "astro"`, keep `"buildCommand": "npm run build"` and `"outputDirectory": "dist"`. Remove the entire `"rewrites"` block (DEPLOY-02 — Vercel's own Astro docs state `vercel.json` rewrites are unsupported for Astro; this is not just SPA cleanup but a documented anti-pattern). Replace the `"headers"` array with a single rule scoped to `"source": "/_astro/(.*)"` setting `Cache-Control: public, max-age=31536000, immutable` (Astro's real hashed-asset prefix, replacing the Vite-era `/assets/*`). Drop the old `/index.html`, `/`, and image-extension cache rules — HTML routes use Vercel's Astro-detected defaults. Do NOT add any `middleware` key (Vercel auto-detects `middleware.ts` at repo root).
  </action>
  <verify>
    <automated>node -e "const v=require('./vercel.json'); if(v.rewrites) throw new Error('rewrites still present'); if(v.framework!=='astro') throw new Error('framework not astro'); const srcs=(v.headers||[]).map(h=>h.source); if(!srcs.includes('/_astro/(.*)')) throw new Error('missing /_astro cache rule'); if(srcs.some(s=>s.includes('/assets/'))) throw new Error('stale /assets rule present'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `vercel.json` has no `rewrites` key (DEPLOY-02).
    - `framework === 'astro'`.
    - `headers` contains exactly one rule, `source === '/_astro/(.*)'`, with the immutable long-max-age Cache-Control.
    - No `/assets/*`, `/index.html`, or `/` cache rules remain.
    - Valid JSON (`require('./vercel.json')` does not throw).
  </acceptance_criteria>
  <done>vercel.json is Astro-shaped: no rewrites, framework astro, single /_astro/* cache header; assertion prints OK.</done>
</task>

<task type="auto">
  <name>Task 2: Author middleware.ts — / redirect + cookie refresh + open-redirect guard</name>
  <files>middleware.ts</files>
  <read_first>
    - src/i18n/LanguageContext.jsx (readInitialLang heuristic lines 10-16; setLang allowlist guard lines 41-47 — the security precedent to mirror server-side)
    - .planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-RESEARCH.md § "middleware.ts" AND § "Common Pitfalls" (cookie-parsing, bare-200 passthrough) AND § "Security Domain"
    - .planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-PATTERNS.md § "middleware.ts (repo root)"
  </read_first>
  <action>
    Create `middleware.ts` at the repo ROOT (sibling of `package.json`) — NOT `src/middleware.ts` (that path is Astro's own middleware, inert under `output: 'static'`; the two must not be confused). Use 21-RESEARCH.md's `middleware.ts` block as the primary source. Export `const config = { matcher: ... }` using a negative-lookahead that excludes `/_astro/` and common static extensions (record the exact string in SUMMARY) so cookie refresh runs on page routes only (D-04) without per-asset Edge cost. Define `KNOWN_LOCALES = ['en','es']`, `DEFAULT_LOCALE = 'en'`, `COOKIE_NAME = 'cam-lang'` (D-01). Hand-roll two pure helpers: `parseCookie(header, name)` reading `request.headers.get('cookie')` (raw `"a=b; c=d"` string — NO Next.js `request.cookies` API), and `resolveLocaleFromAcceptLanguage(header)` returning `header?.toLowerCase().includes('es') ? 'es' : 'en'` (D-02, no q-value parser). Add `isKnownLocale(value): value is Locale` that checks membership in `KNOWN_LOCALES` — SECURITY: the redirect target must pass this guard before entering the `Location` header (mirrors LanguageContext setLang's allowlist; prevents open redirect via a crafted `cam-lang` cookie or `Accept-Language` value). Default export `middleware(request)`: match `/^\/(en|es)(\/|$)/` on the path — if a locale prefix is present, return a bare `new Response(null, { status: 200 })` with a `Set-Cookie: cam-lang=<pathLocale>; Path=/; Max-Age=31536000; SameSite=Lax` header (D-04 cookie refresh). If `pathname === '/'`, pick `target = isKnownLocale(cookieLocale) ? cookieLocale : resolveLocaleFromAcceptLanguage(...)` and return `new Response(null, { status: 302, headers: { Location: '/' + target, 'Set-Cookie': 'cam-lang=' + target + '; Path=/; Max-Age=31536000; SameSite=Lax' } })` (D-03 302). Otherwise return a bare `new Response(null, { status: 200 })`. Always set `SameSite=Lax` and the 1-year `Max-Age` (cookie hygiene — do not omit). Add a code comment flagging that the bare-200 pass-through (Assumption A2 / Open Question 1) is validated in Plan 21-05 and that `next()` from `@vercel/functions` is the documented fallback if it fails. `.ts` may use semicolons (Astro/TS convention).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('middleware.ts','utf8'); ['export const config','matcher','isKnownLocale','cam-lang','Set-Cookie','SameSite=Lax','status: 302','Location','accept-language'].forEach(t=>{ if(!s.toLowerCase().includes(t.toLowerCase())) throw new Error('missing '+t) }); if(s.includes('request.cookies')) throw new Error('Next.js-only request.cookies API used — parse header manually'); if(s.includes('@vercel/functions')) throw new Error('do not add @vercel/functions in this plan'); console.log('OK')" && npx tsc --noEmit middleware.ts 2>/dev/null || echo "tsc check skipped (no tsconfig) — source assertions above are the gate"</automated>
  </verify>
  <acceptance_criteria>
    - `middleware.ts` exists at repo root (NOT under `src/`).
    - Exports `config` with a negative-lookahead `matcher` excluding `/_astro/`.
    - `isKnownLocale()` gate is applied to the redirect target before the `Location` header (open-redirect mitigation, T-21-03-01).
    - Cookie read via manual `request.headers.get('cookie')` parse — no `request.cookies` / `NextResponse`.
    - `/` returns status 302 with `Location: /en` or `/es` and a `cam-lang` `Set-Cookie` (D-01, D-03).
    - `/en/*` and `/es/*` return a cookie-refreshing response (D-04).
    - All `Set-Cookie` strings include `SameSite=Lax` and `Max-Age=31536000`.
    - No `@vercel/functions` import (deferred to 21-05 fallback).
    - SUMMARY records the exact matcher string and reiterates the Plan 21-05 pass-through validation dependency.
    - SUMMARY explicitly flags the D-04 "every route" -> "every page route" interpretation (matcher excludes /_astro/ + static assets) for user sign-off, rather than narrowing D-04 silently.
  </acceptance_criteria>
  <done>middleware.ts at repo root redirects / (302, allowlist-guarded) and refreshes cam-lang on locale routes, with no Next.js-only APIs; assertion prints OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Edge Middleware | Untrusted `cam-lang` cookie and `Accept-Language` header cross into the redirect handler |
| Edge → CDN static file | Middleware response must let Vercel serve the underlying static page (pass-through) on locale routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Spoofing (open redirect) | `Location` header built from `cam-lang` cookie / `Accept-Language` | mitigate | `isKnownLocale()` matches the value against literal `['en','es']` before it enters `Location`; a crafted cookie/header value falls through to `DEFAULT_LOCALE` — never reflected (21-RESEARCH.md Security Domain, V5 Input Validation) |
| T-21-03-02 | Tampering / CSRF-adjacent | `cam-lang` cookie attributes | mitigate | Cookie set with `SameSite=Lax` + explicit `Max-Age`; it is a non-auth preference cookie so severity is low, but hygiene attributes are set regardless |
| T-21-03-03 | Denial of Service / cost | broad `matcher` invoking Edge on every static asset | mitigate | Negative-lookahead matcher excludes `/_astro/` and static extensions, cutting per-asset Edge invocations while preserving D-04 cookie refresh on page routes |
| T-21-03-04 | Tampering (content replacement) | bare-200 Response failing to pass through on `/en/*` | transfer | Cannot be validated locally; explicitly transferred to Plan 21-05's real-Vercel-preview checkpoint, with `next()` from `@vercel/functions` as the documented fallback (Assumption A2) |
</threat_model>

<verification>
- `vercel.json`: no rewrites, framework astro, single `/_astro/*` cache rule (assertion prints OK).
- `middleware.ts`: source assertions pass; no Next.js-only APIs; no `@vercel/functions`.
- Live redirect behavior (302 on `/`, pass-through on `/en`) is validated in Plan 21-05, not here.
</verification>

<success_criteria>
`vercel.json` matches the static-first Astro architecture (DEPLOY-02) and `middleware.ts` implements the ROUTE-02 redirect + cookie refresh with an open-redirect guard, ready for live validation in Plan 21-05.
</success_criteria>

<output>
Create `.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-03-SUMMARY.md` when done
</output>
