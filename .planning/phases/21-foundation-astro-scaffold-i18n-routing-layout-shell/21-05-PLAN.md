---
phase: 21-foundation-astro-scaffold-i18n-routing-layout-shell
plan: 05
type: execute
wave: 4
depends_on: ["21-02", "21-03", "21-04"]
files_modified:
  - middleware.ts
  - package.json
autonomous: false
requirements: [ROUTE-02]
user_setup:
  - service: vercel
    why: "The / redirect + bare-200 pass-through can only be validated against a real Vercel preview deploy (not astro dev / vercel dev) — the phase's single highest-risk item"
    dashboard_config:
      - task: "Ensure the v5 branch triggers a Vercel preview deploy (repo already deploys to Vercel; a preview URL is expected per push)"
        location: "Vercel project → Deployments (preview per branch/PR)"
must_haves:
  truths:
    - "On the real Vercel preview, GET / returns 302 to /en or /es driven by cam-lang cookie then Accept-Language"
    - "On the real Vercel preview, /en and /es serve their full static HTML (the bare-200 middleware response passes through, not an empty body)"
    - "A cam-lang cookie set on an /en or /es visit changes a later GET / redirect target"
    - "A direct hit on a nonexistent path serves the authored 404.html, not an SPA index.html"
    - "A crafted cam-lang cookie value (e.g. cam-lang=evil) is never reflected into the / redirect Location — the target falls through to the default or Accept-Language locale (open-redirect guard verified live)"
    - "If pass-through fails, the middleware is switched to next() from @vercel/functions and re-validated green"
  artifacts:
    - path: ".planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-05-SUMMARY.md"
      provides: "Recorded preview URL + redirect/pass-through/404 validation results (ROUTE-02 sign-off)"
  key_links:
    - from: "middleware.ts"
      to: "Vercel preview runtime"
      via: "live 302 redirect + static pass-through"
      pattern: "302"
---

<objective>
Validate the ROUTE-02 redirect against a REAL Vercel preview deploy — the single highest-risk item in this milestone, flagged independently by 3 of 4 research passes and by STATE.md's Blockers. Two assumptions can only be resolved here, not locally: (A2/Open Question 1) whether a bare `new Response(null, { status: 200 })` from `middleware.ts` passes through to the built static page on `/en`/`/es`, or replaces it with an empty body; and the live `/` → locale 302 behavior with cookie + `Accept-Language`. If pass-through fails, apply the documented fallback — add `@vercel/functions` and use `next({ headers })` for the cookie-refresh branch — and re-validate.

Purpose: Every later v5 phase assumes this route tree + redirect works in production. Signing it off against a live preview (not `astro dev`) is the gate that unblocks Phase 22+.
Output: A validated preview deploy with a recorded URL and pass/fail results for redirect, pass-through, cookie-refresh, and 404.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-RESEARCH.md
@.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-03-SUMMARY.md
@.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-04-SUMMARY.md

<interfaces>
<!-- Open Question 1 / Assumption A2: 21-RESEARCH.md § "Open Questions" #1 and § "Common Pitfalls" (bare-200 passthrough) -->
<!-- Fallback: import { next } from '@vercel/functions'; return next({ headers: { 'Set-Cookie': ... } }) -->
<!-- for the /en/* /es/* cookie-refresh branch ONLY if the bare-200 pass-through fails on the real preview. -->
<!-- @vercel/functions is ~1KB, NOT an SSR adapter — does not violate the adapter-free constraint. -->

Validation matrix (run against the PREVIEW URL, e.g. with curl -sI):
  1. GET /                      → 302, Location: /en or /es, Set-Cookie: cam-lang=...
  2. GET / (Cookie: cam-lang=es)→ 302, Location: /es   (cookie beats Accept-Language)
  3. GET / (Accept-Language: es-ES, no cookie) → 302, Location: /es  (D-02 heuristic)
  4. GET /en                    → 200 AND body contains the real page HTML (NOT empty) — pass-through proof
  5. GET /es                    → 200 AND body contains the real page HTML
  6. GET /this-path-does-not-exist → 404 status serving dist/404.html content (not index.html)
  7. GET / (Cookie: cam-lang=evil) → 302, Location: /en (default) or Accept-Language-derived — NEVER reflects 'evil' into Location (open-redirect guard, T-21-03-01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce a Vercel preview deploy of the v5 branch</name>
  <files>.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-03-SUMMARY.md (matcher string + pass-through caveat)
    - vercel.json (framework astro, no rewrites)
  </read_first>
  <action>
    Trigger a Vercel preview deploy of the current v5 branch. Prefer pushing the branch so Vercel's Git integration auto-builds a preview (the repo already deploys to Vercel per `@vercel/analytics`/`vercel.json`). If a `vercel` CLI is authenticated locally, `vercel deploy` (preview, non-prod) is an acceptable alternative. If the deploy trigger requires authentication that is not available, STOP and surface an auth gate (do not fake a URL). Capture the resulting preview URL and record it at the top of `21-05-SUMMARY.md`. Do NOT run this validation against `astro dev` or `vercel dev` alone — 21-RESEARCH.md is explicit that only a real Vercel preview can validate the middleware pass-through (Assumption A2).
  </action>
  <verify>
    <automated>MISSING — validated by the human checkpoint in Task 2 against the live preview URL; no local automated proxy exists for Vercel Edge Middleware behavior (documented in 21-RESEARCH.md Environment Availability)</automated>
  </verify>
  <acceptance_criteria>
    - A real Vercel preview URL is produced and recorded in `21-05-SUMMARY.md`.
    - The deploy used the Astro build (framework astro, `dist` output) — not the old SPA config.
    - If auth was unavailable, an auth gate was surfaced rather than a fabricated URL.
  </acceptance_criteria>
  <done>A live Vercel preview URL for the v5 branch is recorded in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate redirect + pass-through + 404 on the live Vercel preview</name>
  <files>middleware.ts, package.json</files>
  <action>
    Human-driven validation against the preview URL from Task 1. The executor presents the checklist below (curl commands), waits for the operator to confirm results, and — only if the pass-through check fails — applies the `@vercel/functions` `next()` fallback and redeploys. This task pauses for the operator; it does not auto-approve.
  </action>
  <what-built>The Astro route tree (`/en`, `/es`), the `middleware.ts` `/` redirect + `cam-lang` cookie refresh, the `vercel.json` cleanup, and the `404.astro` page — all deployed to the Vercel preview URL recorded in Task 1. This checkpoint validates the ROUTE-02 redirect + pass-through behavior that no local test can confirm.</what-built>
  <how-to-verify>
    Against the preview URL from Task 1 (substitute PREVIEW below), run and confirm each:

    1. `curl -sI PREVIEW/` → status `302`; `Location:` is `/en` or `/es`; a `Set-Cookie: cam-lang=...; SameSite=Lax` header is present.
    2. `curl -sI --cookie "cam-lang=es" PREVIEW/` → `302` with `Location: /es` (cookie beats Accept-Language, D-01/D-04).
    3. `curl -sI -H "Accept-Language: es-ES,es;q=0.9" PREVIEW/` (no cookie) → `302` with `Location: /es` (D-02 heuristic).
    4. `curl -s PREVIEW/en | head -c 2000` → shows the REAL page HTML (`<html lang="en">`, `<title>`, content) — NOT an empty body. This is the critical pass-through proof (Assumption A2).
    5. `curl -s PREVIEW/es | head -c 2000` → shows the real `<html lang="es">` page.
    6. `curl -sI PREVIEW/this-path-does-not-exist` → `404`; `curl -s` of it shows the authored 404 content ("404", the bilingual message), NOT the SPA index.
    7. `curl -sI --cookie "cam-lang=evil" PREVIEW/` → `302`; `Location:` is `/en` (default) or the Accept-Language-derived locale — it MUST NOT be `/evil` or otherwise reflect the crafted value. This is the behavioral open-redirect guard test for `isKnownLocale()` (T-21-03-01); a `Location: /evil` here is a FAIL, not a pass.

    If check 4 or 5 returns an EMPTY body (pass-through failed), the middleware needs the `@vercel/functions` `next()` fallback — see Resume signal.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms checks 1-7 pass against the live preview URL (or pass after the next() fallback is applied and redeployed).</human-check>
  </verify>
  <resume-signal>
    Type "approved" if checks 1-7 all pass. If check 4/5 failed (empty body on /en or /es), reply "passthrough-failed" — the executor will then: `npm install @vercel/functions` (verify legitimacy on npmjs.com/package/@vercel/functions first — official Vercel-published), change the `/en/*` `/es/*` cookie-refresh branch in `middleware.ts` from a bare 200 `Response` to `import { next } from '@vercel/functions'; return next({ headers: { 'Set-Cookie': ... } })` (keeping the `/` 302 branch unchanged), redeploy (Task 1), and re-run this checklist. If check 7 failed (a crafted cookie value was reflected into `Location`), the `isKnownLocale()` open-redirect guard in `21-03`'s `middleware.ts` regressed and MUST be fixed and redeployed before sign-off. If any other check failed, describe which and the observed output.
  </resume-signal>
  <done>Checks 1-7 confirmed passing on the live preview; SUMMARY records per-check results and whether the next() fallback was applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → Vercel Edge | Live untrusted requests hit `middleware.ts` on the preview deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05-01 | Spoofing (open redirect) | live `/` redirect from crafted cookie/header | mitigate | Validated live: checks 2/3 confirm valid `en`/`es` targets are honored and check 7 confirms a crafted `cam-lang=evil` value is NEVER reflected into `Location` (it falls through to the default) — the `isKnownLocale` guard from 21-03 is exercised behaviorally in production here |
| T-21-05-02 | Tampering (content replacement) | bare-200 pass-through returning empty page bodies | mitigate | Checks 4/5 directly assert the real page HTML is served; the `@vercel/functions` `next()` fallback is the resolution path if pass-through fails (Assumption A2 closed here) |
| T-21-05-03 | Tampering | `@vercel/functions` install (only if fallback triggered) | mitigate | If added, verify legitimacy against npmjs.com/package/@vercel/functions (official Vercel-published, ~1KB helper, not an SSR adapter) before install; record disposition in SUMMARY |
</threat_model>

<verification>
- Preview URL recorded; checks 1-7 pass (or pass after the `next()` fallback is applied and redeployed).
- ROUTE-02 signed off against a real Vercel preview, per STATE.md's mandatory-validation blocker.
</verification>

<success_criteria>
The `/` → `/en`/`/es` 302 redirect (cookie → Accept-Language), the `/en`/`/es` static pass-through, and the authored 404 are all confirmed working on a real Vercel preview deploy — closing Assumption A2 and clearing the milestone's highest-risk item (ROUTE-02).
</success_criteria>

<output>
Create `.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-05-SUMMARY.md` when done (with preview URL + per-check results + whether the next() fallback was needed)
</output>
