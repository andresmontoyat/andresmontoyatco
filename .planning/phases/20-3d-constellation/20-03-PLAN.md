---
phase: 20-3d-constellation
plan: 03
type: execute
wave: 3
depends_on: [20-01, 20-02]
files_modified:
  - src/game/useClickVsDrag.js
  - src/game/useClickVsDrag.test.js
  - src/game/renderers/WebGLConstellation.js
  - scripts/check-bundle-gate.mjs
  - .planning/phases/20-3d-constellation/20-UAT.md
autonomous: true
requirements: [DEPTH-01]
tags: [hook, click-vs-drag, uat, bundle-gate-close, lighthouse]

must_haves:
  truths:
    - "Click on a node within 5px+250ms (mouse) or 8px+250ms (touch) opens the ExperienceCard — GAME-04 preserved under OrbitControls gesture state"
    - "Drag past the threshold suppresses click selection — OrbitControls consumes the gesture without spurious ExperienceCard mount"
    - "useClickVsDrag is a pure jsdom-testable hook — 5+ unit tests cover threshold math, time threshold, touch-bump branch, no-op safety"
    - "WebGL chunk ≤130 kB gz WARN band — bundle-gate WARN threshold lands in check-bundle-gate.mjs"
    - "Lighthouse mobile re-verified: Perf ≥95 / A11y 100 / BP 100 / SEO 100 (HARD gate intact at Phase 17 baseline)"
    - "UAT.md scaffold lives at .planning/phases/20-3d-constellation/20-UAT.md with 8 manual checks per ROADMAP success criteria + v3.9 deferred items"
    - "All 272+ tests stay GREEN; ≥5 new useClickVsDrag tests pass; UAT 8/8 documented; bundle gate exits 0; milestone v3.10 ready for tag"
  artifacts:
    - path: "src/game/useClickVsDrag.js"
      provides: "Pure hook exposing { onPointerDown, onPointerUp, isDragRef } with deterministic 5px-mouse/8px-touch/250ms threshold math"
      contains: "DIST_THRESHOLD_TOUCH"
    - path: "src/game/useClickVsDrag.test.js"
      provides: "5+ jsdom unit tests: mouse-click within threshold, mouse-drag past threshold, touch-bump to 8px, long-press fails time threshold, no-op when onClick missing"
      contains: "useClickVsDrag"
    - path: "scripts/check-bundle-gate.mjs"
      provides: "WebGL chunk WARN band at ≤130 kB gz (Phase 17 +OrbitControls projection)"
      contains: "WEBGL_WARN_KB"
    - path: ".planning/phases/20-3d-constellation/20-UAT.md"
      provides: "8-item manual UAT checklist scaffold + v3.9 deferred bundling (above-fold + SVG twinkle real-device confirm)"
      contains: "## Test Matrix"
  key_links:
    - from: "src/game/useClickVsDrag.js threshold math"
      to: "src/game/renderers/WebGLConstellation.js pointer-pick handler"
      via: "Hook is consumed inside renderer useEffect; OrbitControls attached FIRST, pick handlers SECOND, useClickVsDrag arbitrates click vs drag BEFORE onSelectSkill fires (CRIT-02 mitigation)"
      pattern: "useClickVsDrag\\(|onPointerDown|onPointerUp"
    - from: "scripts/check-bundle-gate.mjs WEBGL_WARN_KB"
      to: "dist/assets/WebGLConstellation-*.js measured gz size"
      via: "Closes the Plan 20-03 WARN-band verdict; alarms above 130 kB gz"
      pattern: "WEBGL_WARN_KB"
    - from: ".planning/phases/20-3d-constellation/20-UAT.md"
      to: "Lighthouse mobile verdict + v3.9 carried debt"
      via: "Manual UAT sweep documents pass/fail per 8 checklist items; mobile chunk + Lighthouse re-verify close milestone"
      pattern: "Lighthouse|mobile"
---

<objective>
Close the milestone: extract the click-vs-drag threshold math into a pure jsdom-testable hook (Alert A6 / CRIT-02 / MOD-03), wire it into the renderer to arbitrate click vs drag BEFORE `onSelectSkill` fires, add the bundle-gate WebGL WARN-band threshold for the now-larger WebGL chunk, and scaffold + execute the 8-item UAT checklist that bundles the v3.9 deferred real-device confirm.

Purpose:
- Defends CRIT-02 (OrbitControls swallows click → onSelectSkill stops firing) with deterministic, isolated, jsdom-testable threshold math — not buried inside the renderer.
- Defends MOD-03 (capacitive touchscreen 1-3px jitter) via `pointerType === 'touch'` → 8px branch.
- Defends MOD-07 (jsdom 3D blind spot) by honestly documenting which UAT items are visual-only and cannot be unit-tested.
- Closes the bundle-gate verdict: WebGL chunk WARN band at ≤130 kB gz (Phase 17 117 kB + measured 6.85 kB gz OrbitControls + uniforms + Vector3.project = projected ~123 kB).
- Closes the Lighthouse mobile HARD gate verdict (no regression: mobile SVG path UNCHANGED; chunk size unchanged; expected GREEN by construction).
- Closes the v3.9 carried UAT debt (above-fold layout + SVG ambient twinkle real-device confirm) in a single device sweep per CONTEXT §deferred bullet.

Output: A passing UAT 8/8, bundle gate exit 0 with documented WARN-band threshold for WebGL chunk, Lighthouse mobile re-verified at Phase 17 baseline, milestone v3.10 ready for `git tag v3.10` ceremony; useClickVsDrag hook + tests committed; renderer integrates the hook with OrbitControls-first / pick-handlers-second ordering preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-3d-constellation/20-CONTEXT.md
@.planning/phases/20-3d-constellation/20-UI-SPEC.md
@.planning/phases/20-3d-constellation/20-PATTERNS.md
@.planning/phases/20-3d-constellation/20-01-SUMMARY.md
@.planning/phases/20-3d-constellation/20-02-SUMMARY.md
@.planning/phases/16-filters-floating-experiencecard/16-UAT.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@src/game/renderers/WebGLConstellation.js
@src/game/useConstellation.js
@src/game/useRendererCapability.js
@src/context/ViewModeContext.js
@scripts/check-bundle-gate.mjs

<interfaces>
<!-- LOCKED contracts. -->

## useClickVsDrag hook (D-20-CLICK-DRAG-THRESHOLD, Alert A6)

NEW file `src/game/useClickVsDrag.js`. Full shape per PATTERNS §"src/game/useClickVsDrag.js":

```
import { useRef, useCallback } from 'react'

// D-20-CLICK-DRAG-THRESHOLD — locked values
const DIST_THRESHOLD_MOUSE = 5    // px screen-space
const DIST_THRESHOLD_TOUCH = 8    // px — MOD-03 capacitive touch noise
const TIME_THRESHOLD_MS = 250     // industry convention (Cortico)

export default function useClickVsDrag({ onClick } = {}) {
  const dragRef = useRef({
    startX: 0,
    startY: 0,
    startT: 0,
    pointerType: 'mouse',
  })

  const onPointerDown = useCallback((e) => {
    dragRef.current = {
      startX: e.clientX,
      startY: e.clientY,
      startT: typeof performance !== 'undefined' ? performance.now() : Date.now(),
      pointerType: e.pointerType || 'mouse',
    }
  }, [])

  const onPointerUp = useCallback((e) => {
    const ref = dragRef.current
    const dx = e.clientX - ref.startX
    const dy = e.clientY - ref.startY
    const dist = Math.hypot(dx, dy)
    const now = typeof performance !== 'undefined' ? performance.now() : Date.now()
    const dt = now - ref.startT
    const distThreshold = ref.pointerType === 'touch'
      ? DIST_THRESHOLD_TOUCH
      : DIST_THRESHOLD_MOUSE
    const isClick = dist < distThreshold && dt < TIME_THRESHOLD_MS
    if (isClick && onClick) onClick(e)
  }, [onClick])

  return { onPointerDown, onPointerUp, isDragRef: dragRef }
}
```

Constraints:
- No DOM access at module scope (jsdom-testable).
- `performance.now()` guarded by `typeof performance !== 'undefined'` (jsdom-defensive).
- `useCallback` keeps handlers stable for `useEffect` deps in renderer.
- Default-export only — matches `src/game/` convention.
- `isDragRef` returned as the same `dragRef` so callers can read post-arbitration metadata if needed (not used in renderer wiring but exposed for future composition).

## useClickVsDrag tests (Plan 20-03 deliverable)

NEW file `src/game/useClickVsDrag.test.js`. Pattern per PATTERNS §"src/game/useClickVsDrag.test.js". ≥5 tests:

1. **mouse click within threshold** — pointerdown at (100, 100), pointerup at (102, 101) → onClick fires once.
2. **mouse drag past threshold** — pointerdown at (100, 100), pointerup at (110, 100) (Δx=10 ≥ 5) → onClick NOT called.
3. **touch click within bumped threshold** — pointerdown with `pointerType: 'touch'` at (100, 100), pointerup at (107, 100) (Δx=7 < 8 but ≥ 5) → onClick fires once (MOD-03 branch).
4. **touch drag past bumped threshold** — pointerdown touch at (100, 100), pointerup at (109, 100) (Δx=9 ≥ 8) → onClick NOT called.
5. **long-press fails time threshold** — pointerdown at (100, 100); `vi.spyOn(performance, 'now')` returns startT=0 on down, returns 300 on up → dt=300 ≥ 250 → onClick NOT called even though distance=0.
6. **no-op when onClick missing** — `renderHook(() => useClickVsDrag())` with no args; pointerdown→pointerup → no throw.

Use `vi.useFakeTimers()` only if needed for the time-threshold test; otherwise stubbing `performance.now` via `vi.spyOn(performance, 'now').mockReturnValueOnce(0).mockReturnValueOnce(300)` is cleaner.

## Renderer integration (CRIT-02 mitigation completion)

Inside `src/game/renderers/WebGLConstellation.js`, in the existing pointer event useEffect (currently ~line 615), replace the bare `onClick` and pointerdown/pointerup wiring with the hook:

```
import useClickVsDrag from '../useClickVsDrag'

// inside function body
const { onPointerDown, onPointerUp } = useClickVsDrag({
  onClick: (e) => {
    // existing pick math — Vector3.project(camera) per Plan 20-02
    const matched = pickAt(e.clientX, e.clientY)
    if (matched != null && props.onSelectSkill) props.onSelectSkill(matched)
  },
})

// inside the canvas event-setup useEffect
canvas.addEventListener('pointerdown', onPointerDown)
canvas.addEventListener('pointerup', onPointerUp)
// removeEventListener counterparts in cleanup
```

CRITICAL ordering preserved:
1. OrbitControls instantiation FIRST (Plan 20-02).
2. `onPointerDown` and `onPointerUp` attach SECOND.
3. `useClickVsDrag` arbitrates internally — `onClick` callback only fires when threshold passes.

The plain `'click'` listener that may have remained from Phase 17 MUST be removed in this step (search-and-replace `canvas.addEventListener('click'` → remove block). pointerup is now the sole click path.

`pickAt()` is a helper that wraps the `Vector3.project(camera)` + depthScale + Math.hypot logic Plan 20-02 lands. If Plan 20-02 inlined it, extract it to a local function here.

## Bundle-gate WARN-band threshold (close milestone verdict)

In `scripts/check-bundle-gate.mjs`:

- Existing constant `const WEBGL_TTI_WARN_KB = 60` (line 15) — this was Phase 17 baseline WARN at 60 kB. Phase 17 actual chunk landed at 117 kB gz, which was already silently above this — the WARN suppresses because `if (webglKB > WEBGL_TTI_WARN_KB)` console.warn but does not exit non-zero.
- Plan 20-02 lands ~123 kB gz projected. Plan 20-03 raises the WARN ceiling to 130 kB gz and adds a SECOND threshold above which a louder warning fires.

Replace:
```
const WEBGL_TTI_WARN_KB = 60
```
With:
```
const WEBGL_WARN_KB = 130      // v3.10 WARN band threshold (Plan 20-03 close)
const WEBGL_TTI_WARN_KB = 60   // Phase 17 baseline INFO threshold (kept for continuity)
```

Update the existing WebGL chunk WARN block (lines ~54-58) to check both:
```
if (webglKB > WEBGL_WARN_KB) {
  console.warn(`WARN: ${webglLabel} > ${WEBGL_WARN_KB} kB (v3.10 WARN band; HARD ceiling deferred to empirical data per REQUIREMENTS)`)
} else if (webglKB > WEBGL_TTI_WARN_KB) {
  console.log(`INFO: ${webglLabel} > ${WEBGL_TTI_WARN_KB} kB baseline (expected post-Phase-17)`)
} else {
  console.log(`INFO: ${webglLabel}`)
}
```

Soft WARN — does NOT exit non-zero. v3.10 REQUIREMENTS.md §Constraints explicitly defers HARD ceiling on desktop chunk pending empirical data.

## UAT scaffold (Plan 20-03 deliverable, MOD-07 honest documentation)

NEW file `.planning/phases/20-3d-constellation/20-UAT.md`. Use Phase 16 UAT (`.planning/phases/16-filters-floating-experiencecard/16-UAT.md`) as the structural template.

Required sections:

```
---
phase: 20-3d-constellation
status: pending
created: 2026-06-08
---

# Phase 20 — 3D Constellation Manual UAT

## Scope

Manual visual + interaction verification of v3.10 DEPTH-01 across capable-desktop browsers,
RM emulation, mobile viewport, and v3.9 carried deferred items (above-fold + SVG twinkle real-device confirm).

## Environment

| Surface | Configuration |
|---------|---------------|
| Browser (primary) | Chrome stable on macOS 14+ |
| Browser (secondary) | Safari 17+, Firefox stable |
| Viewport (desktop) | 1440x900 (default), 1920x1080 (widescreen) |
| Viewport (mobile) | 375x667 (iPhone SE), 768x1024 (iPad portrait) |
| Theme | dark default + light via toggle |
| Language | en default + es via toggle |
| Reduced motion | system default + DevTools "emulate prefers-reduced-motion: reduce" |
| Build | `npm run build` then `npx serve dist` (no `npm run dev` — production build only) |

## Test Matrix

### 1. Tilted 3D obvious at frame 0
- [ ] Load page on capable desktop browser, wait for game-mode renderer slot to mount.
- [ ] Verify perspective foreshortening is visible WITHOUT requiring drag — front nodes larger than back nodes; constellation reads as 3D, not flat disc.
- [ ] Pass criterion: take a screenshot at first paint; perspective depth is unambiguous.

### 2. Drag-rotate with damping + polar clamp
- [ ] Mouse-down on canvas; verify cursor flips `grab` → `grabbing`.
- [ ] Drag horizontally; verify constellation rotates around vertical axis with damping inertia.
- [ ] Release mid-drag; verify damping coasts the rotation to stop (<1s settle).
- [ ] Drag UP past polar clamp boundary; verify camera halts at clamp angle (no snap-back, no upside-down view).
- [ ] Drag DOWN past polar clamp boundary; verify symmetric halt.
- [ ] Release at any angle within clamp; verify constellation stays at release angle (no snap-back to initial).

### 3. Auto-rotate idle spin + permanent pause-on-first-drag
- [ ] On fresh page load, verify auto-rotate begins at ~30s/orbit (CCW around vertical axis).
- [ ] Verify auto-rotate pauses when pointer hovers over a node (`hoveredSkillId != null`).
- [ ] Verify auto-rotate pauses when ExperienceCard is open (`selectedSkillId != null`).
- [ ] Drag canvas once; verify auto-rotate STOPS PERMANENTLY for the session (no idle resume; D-20-CONTEXT-AUTOROTATE-RESUME).
- [ ] Reload page; verify auto-rotate restarts (new session).

### 4. Click selects, drag does not (GAME-04 preserved + click-vs-drag threshold)
- [ ] Quick click on a node (no movement, <250ms) → ExperienceCard opens for that skill.
- [ ] Click+drag 10px+ → ExperienceCard does NOT open; constellation rotates only.
- [ ] On Chrome devtools touch emulation, tap a node → ExperienceCard opens (8px threshold tolerates capacitive touch jitter).
- [ ] On Chrome devtools touch emulation, swipe → no spurious selection.
- [ ] On Surface / touch-iMac if available, real-touch tap → ExperienceCard opens.

### 5. prefers-reduced-motion path preserved (WCAG 2.1 AA + Lighthouse a11y 100)
- [ ] DevTools → Rendering → Emulate CSS media feature → prefers-reduced-motion: reduce → reload.
- [ ] Verify: SVG path renders, NO WebGL canvas mounts (capability tier gates to SVG).
- [ ] Verify: NO OnboardingHint pill visible (component returns null under RM).
- [ ] Verify: SVG ambient twinkle (Phase 19) is suppressed under RM (existing motion-safe gate).
- [ ] Run `npm run lighthouse:mobile` → verify accessibility score = 100.

### 6. Mobile SVG path UNCHANGED + Lighthouse mobile HARD gate
- [ ] At 375x667 viewport, load page → SVG path renders (capability tier gates WebGL to ≥768px).
- [ ] At 768x1024 viewport with touch input, load page → verify capability tier evaluates per `useRendererCapability` (may go either path depending on gate criteria; document the verdict).
- [ ] Run `npm run lighthouse:mobile && npm run lighthouse:check`. Verify HARD gate: Perf ≥ 95 / A11y 100 / BP 100 / SEO 100. exit code 0 = pass.
- [ ] Verify mobile chunk gz size unchanged at ~8.91 kB (`node scripts/check-bundle-gate.mjs` PASS).

### 7. All 272+ tests stay GREEN
- [ ] `npm test` exits 0.
- [ ] Confirm new tests added: Plan 20-01 ≥3 layout z-tests + 5 bundle-gate regex tests; Plan 20-02 6 OnboardingHint tests; Plan 20-03 ≥5 useClickVsDrag tests. Total ≥277 GREEN.

### 8. Bundle-gate regression fixture confirms three/addons/* leak HARD-FAILS
- [ ] Bundle gate exits 0 against the v3.10 production build.
- [ ] Mobile chunk does not contain any `three/...` import string (existing regex defense holds).
- [ ] WebGL chunk reported with size ≤130 kB gz (v3.10 WARN band).
- [ ] Regression fixture test (`scripts/check-bundle-gate.test.mjs`) confirms a synthetic mobile-chunk fixture containing `from 'three/addons/controls/OrbitControls.js'` HARD-FAILS the gate.

## v3.9 Carried Deferred Items (bundled per CONTEXT §deferred)

### v3.9 above-fold real-device confirm
- [ ] Open production build on real iPhone 14 / Pixel 7: above-the-fold layout reads — SkillFilters fixed at `bottom-0 z-30`, H1 compact, renderer slot fills `flex-1`. Document any layout shift.

### v3.9 SVG ambient twinkle real-device confirm
- [ ] Open production build on real desktop browser without prefers-reduced-motion: ambient twinkle visible on SVG path (e.g., capability tier downgrade or `cam-viewmode=game` on a low-power device). Confirm twinkle motion-safe.

## Pass Criteria

UAT 8/8 PASS required for milestone v3.10 close. Any FAIL routes to follow-up plan (20-04 or v3.10.1).

## Notes / Findings

(Executor fills in during sweep — screenshots, browser versions, deviations from expected behavior.)
```

## Forbidden anti-patterns (re-read before commit)

- Adding a Raycaster-based pointer pick (MIN-06 — throttled CPU; keep math-based pick).
- Bumping `WEBGL_WARN_KB` above 130 — REQUIREMENTS.md §Constraints locks the desktop WARN at ~125 (Plan 20-03 honors with 130 cap; above breaches the soft contract).
- Skipping the v3.9 carried UAT items — explicit deferred bullet in CONTEXT.md §deferred says they bundle here.
- Adding a HARD ceiling exit-non-zero to the WebGL chunk — REQUIREMENTS.md §"Out of Scope" line 53 says HARD ceiling waits for empirical data.
- Lighthouse desktop fail interpreted as gate fail — desktop is NOT a HARD gate; document but do not block.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useClickVsDrag hook + tests + renderer integration</name>
  <files>src/game/useClickVsDrag.js, src/game/useClickVsDrag.test.js, src/game/renderers/WebGLConstellation.js</files>
  <read_first>
    - src/game/renderers/WebGLConstellation.js (post-Plan-20-02 — pointer event useEffect ~line 615, look for the bare `onPointerDown` / `onPointerUp` / `onClick` wiring Plan 20-02 may have inlined)
    - src/game/useConstellation.js (lines 25-49 + 69-75 — useCallback stability + jsdom-defensive performance pattern)
    - src/context/ViewModeContext.js (lines 7-19, 27-45 — useCallback pattern reference)
    - .planning/phases/20-3d-constellation/20-PATTERNS.md §"src/game/useClickVsDrag.js" + §"src/game/useClickVsDrag.test.js" (full)
    - .planning/research/PITFALLS.md §CRIT-02 + §MOD-03
    - .planning/phases/20-3d-constellation/20-CONTEXT.md §"Phase Boundary" (NEW src/game/useClickVsDrag.js)
  </read_first>
  <behavior>
    - Test 1 (mouse click): pointerdown(100, 100, mouse) → pointerup(102, 101, mouse) → onClick called once.
    - Test 2 (mouse drag): pointerdown(100, 100, mouse) → pointerup(110, 100, mouse) → onClick NOT called.
    - Test 3 (touch click within bump): pointerdown(100, 100, touch) → pointerup(107, 100, touch) → onClick called once (Δ=7 < 8px touch threshold; would FAIL the 5px mouse threshold).
    - Test 4 (touch drag past bump): pointerdown(100, 100, touch) → pointerup(109, 100, touch) → onClick NOT called.
    - Test 5 (long-press): pointerdown at t=0, pointerup at t=300, no movement → onClick NOT called (dt ≥ 250ms).
    - Test 6 (no-op safety): `useClickVsDrag()` with no args; pointerdown → pointerup; no throw, no console error.
  </behavior>
  <action>
    RED. Create `src/game/useClickVsDrag.test.js` per the PATTERNS test-shape (lines 581-635 of 20-PATTERNS.md). Use `renderHook` + `act` from `@testing-library/react`. Implement all 6 cases per the behavior block. For Test 5 use `vi.spyOn(performance, 'now').mockReturnValueOnce(0).mockReturnValueOnce(300)`. Run `npm test -- useClickVsDrag` → confirm all 6 FAIL (module not found).

    GREEN. Create `src/game/useClickVsDrag.js` per the LOCKED shape in the `<interfaces>` block above. Default-export `useClickVsDrag({ onClick })`. Module-scope constants `DIST_THRESHOLD_MOUSE=5`, `DIST_THRESHOLD_TOUCH=8`, `TIME_THRESHOLD_MS=250`. useRef + useCallback for `onPointerDown` / `onPointerUp`. jsdom-defensive `typeof performance !== 'undefined'` guard.

    Run `npm test -- useClickVsDrag` → all 6 GREEN. Run `npm test` (full suite) — total ≥272 + 6 = 278 GREEN.

    Integrate into the renderer. In `src/game/renderers/WebGLConstellation.js`:
    - Add `import useClickVsDrag from '../useClickVsDrag'` at top imports.
    - In the function body, call `const { onPointerDown, onPointerUp } = useClickVsDrag({ onClick: (e) => { const matched = pickAt(e.clientX, e.clientY); if (matched != null && props.onSelectSkill) props.onSelectSkill(matched) } })`.
    - In the canvas event-setup useEffect: replace existing `canvas.addEventListener('pointerdown', ...)` / `canvas.addEventListener('pointerup', ...)` blocks Plan 20-02 wired with the hook-returned handlers.
    - If Plan 20-02 left a `canvas.addEventListener('click', ...)` block in place: REMOVE it. pointerup arbitrated via the hook is now the sole click path.
    - In cleanup: `canvas.removeEventListener('pointerdown', onPointerDown); canvas.removeEventListener('pointerup', onPointerUp)`.

    Run `npm test` again — must remain ≥278 GREEN (existing renderer code paths unaffected by the swap; jsdom mocks getContext null so the renderer's mount path returns early). Run `npm run build && node scripts/check-bundle-gate.mjs` to confirm bundle gate still exits 0.

    Manual dev-server smoke: `npm run dev`, capable desktop browser:
    - Quick tap (no move) on a node → ExperienceCard opens.
    - Click and drag past 10px → no ExperienceCard, constellation rotates.
    - Chrome devtools touch emulation, tap → ExperienceCard opens.
    - Chrome devtools touch emulation, swipe → no spurious selection.
  </action>
  <verify>
    <automated>npm test -- useClickVsDrag</automated>
  </verify>
  <acceptance_criteria>
    - `src/game/useClickVsDrag.js` exists with default export `useClickVsDrag`.
    - `rg "^const DIST_THRESHOLD_MOUSE = 5" src/game/useClickVsDrag.js` returns 1 hit.
    - `rg "^const DIST_THRESHOLD_TOUCH = 8" src/game/useClickVsDrag.js` returns 1 hit.
    - `rg "^const TIME_THRESHOLD_MS = 250" src/game/useClickVsDrag.js` returns 1 hit.
    - `rg "pointerType === 'touch'" src/game/useClickVsDrag.js` returns 1 hit (MOD-03 branch).
    - `rg "useClickVsDrag" src/game/renderers/WebGLConstellation.js` returns ≥2 hits (import + invocation).
    - `rg "addEventListener\\(['\\\"]click['\\\"]" src/game/renderers/WebGLConstellation.js` returns 0 hits (replaced by pointerup-via-hook path).
    - `npm test -- useClickVsDrag` exits 0; 6 tests pass.
    - `npm test` exits 0; total ≥278 GREEN.
    - `npm run build && node scripts/check-bundle-gate.mjs` exits 0.
    - Manual smoke: quick tap selects, drag rotates without selection, touch jitter ≤7px on emulated touch still selects.
  </acceptance_criteria>
  <done>
    useClickVsDrag hook + tests + renderer integration committed. CRIT-02 (OrbitControls swallows click) defended deterministically via the hook's threshold math; MOD-03 (touch jitter) defended via the pointerType branch. GAME-04 click-to-select preserved under OrbitControls gesture state. Commit `feat(20-03): useClickVsDrag hook + renderer integration (D-20-CLICK-DRAG-THRESHOLD, CRIT-02, MOD-03)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bundle-gate WARN band + UAT scaffold + UAT execution + milestone close</name>
  <files>scripts/check-bundle-gate.mjs, .planning/phases/20-3d-constellation/20-UAT.md</files>
  <read_first>
    - scripts/check-bundle-gate.mjs (post-Plan-20-01 — widened regex + named export at line 12-13; existing WEBGL_TTI_WARN_KB at line 15)
    - .planning/phases/16-filters-floating-experiencecard/16-UAT.md (full — UAT scaffold structural template)
    - .planning/phases/20-3d-constellation/20-CONTEXT.md §"Deferred Ideas" (Manual real-device UAT bundling — v3.9 above-fold + SVG twinkle bundle here)
    - .planning/REQUIREMENTS.md (§Constraints — desktop WebGL lazy chunk soft ceiling ~125 kB gz, WARN at 130 kB)
    - .planning/research/PITFALLS.md §MOD-07 (jsdom 3D blind spot — UAT is the honest test for shader + projection)
    - .planning/research/SUMMARY.md §"Plan 20-03" (UAT items list)
    - .planning/ROADMAP.md §"Phase 20: 3D Constellation" Success Criteria 1-8 (the 8 UAT items derive from these)
    - package.json (verify scripts: `lighthouse:mobile`, `lighthouse:check`, `check:bundle-gate` exist; if not, document the actual invocation in UAT)
  </read_first>
  <action>
    **Step A: bundle-gate WARN band.** Edit `scripts/check-bundle-gate.mjs` around line 15 (currently `const WEBGL_TTI_WARN_KB = 60`).

    Add the new constant ABOVE it (preserving WEBGL_TTI_WARN_KB for INFO continuity):

    ```
    const WEBGL_WARN_KB = 130      // v3.10 WARN band threshold (Plan 20-03)
    const WEBGL_TTI_WARN_KB = 60   // Phase 17 baseline INFO threshold
    ```

    Update the WebGL chunk evaluation block (currently lines ~54-58) to the three-tier ladder per `<interfaces>`. Soft WARN — does NOT exit non-zero. Run `npm run build && node scripts/check-bundle-gate.mjs` → confirm:
    - Mobile chunk PASS at ~8.91 kB gz.
    - WebGL chunk reported (target ~123 kB gz; if ≤60 = INFO; if 60 < x ≤ 130 = INFO baseline-exceeded; if > 130 = WARN v3.10 band).

    **Step B: UAT scaffold.** Create `.planning/phases/20-3d-constellation/20-UAT.md` per the full template in `<interfaces>` UAT scaffold section. Include all 8 test-matrix items + v3.9 carried deferred bullets + environment + pass criteria + empty notes section.

    Mirror the structure of `.planning/phases/16-filters-floating-experiencecard/16-UAT.md` (YAML front-matter, ## Scope, ## Environment, ## Test Matrix, ## Pass Criteria, ## Notes/Findings). Do not invent new sections.

    **Step C: UAT execution sweep.** Execute the 8 checklist items in the new 20-UAT.md against a fresh production build:

    1. `npm run build && npx serve dist` (or whatever production-preview command is in package.json — verify).
    2. Sweep capable desktop browser (Chrome stable on macOS):
       - Tilted 3D obvious frame 0 ✓
       - Drag-rotate damping + polar clamp ✓
       - Auto-rotate + permanent pause-on-first-drag ✓
       - Click selects / drag does not ✓
    3. DevTools emulate prefers-reduced-motion: reduce → SVG path / no pill / Lighthouse a11y 100 ✓
    4. Mobile viewport 375x667 + 768x1024 → SVG path / Lighthouse mobile HARD gate cleared.
    5. Run `npm run lighthouse:mobile && npm run lighthouse:check` against production build → exit code 0 required. Document Perf / A11y / BP / SEO scores in 20-UAT.md ## Notes section.
    6. `npm test` → confirm ≥278 GREEN.
    7. `node scripts/check-bundle-gate.mjs` exit 0; document mobile + WebGL chunk sizes.
    8. Synthetic regression fixture: confirm `npm test -- check-bundle-gate` still passes the 5 fixture cases from Plan 20-01.

    **v3.9 carried deferred** — open production build on a real desktop browser (or document why it cannot be done in this session); confirm above-the-fold reads + SVG twinkle motion-safe gating. If real-device access is constrained to the executor's session, document partial completion in 20-UAT.md ## Notes (`v3.9 above-fold visual confirmed on macOS Chrome at 1440x900; iPhone 14 real-device pending operator session`).

    **Step D: fill 20-UAT.md ## Notes / Findings with results.** Tick boxes for items that pass; document screenshots paths, browser versions, scores. Any FAIL → flag for follow-up plan 20-04 OR v3.10.1 micro-milestone.

    **Step E: final verification.** Run the full close-the-milestone sequence:
    ```
    npm test
    npm run build
    node scripts/check-bundle-gate.mjs
    npm run lighthouse:mobile && npm run lighthouse:check
    ```
    All 4 commands MUST exit 0 (or the equivalent existing CI invocation).

    Commit `docs(20-03): UAT 8/8 PASS + bundle-gate WARN band ≤130 kB gz + Lighthouse mobile HARD gate re-verified (v3.10 close)` containing the bundle-gate edit + new UAT.md with completed sweep.
  </action>
  <verify>
    <automated>npm test && npm run build && node scripts/check-bundle-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `rg "WEBGL_WARN_KB = 130" scripts/check-bundle-gate.mjs` returns 1 hit.
    - `rg "WEBGL_TTI_WARN_KB = 60" scripts/check-bundle-gate.mjs` returns 1 hit (preserved INFO threshold).
    - `node scripts/check-bundle-gate.mjs` exits 0 against current build.
    - `.planning/phases/20-3d-constellation/20-UAT.md` exists with YAML front-matter, ## Scope, ## Environment, ## Test Matrix (8 items), ## Pass Criteria, ## Notes/Findings + v3.9 carried deferred section.
    - 20-UAT.md ## Notes section is populated with results (not left empty).
    - `npm run lighthouse:mobile && npm run lighthouse:check` exit codes both = 0; scores documented in 20-UAT.md ## Notes (Perf ≥95 / A11y 100 / BP 100 / SEO 100).
    - `npm test` exits 0 with ≥278 GREEN.
    - `git log --oneline -1` shows the milestone-close commit.
    - 20-UAT.md ## Pass Criteria block confirms 8/8 PASS OR documents specific failing items routed to follow-up.
  </acceptance_criteria>
  <done>
    Bundle-gate WARN band threshold lands at 130 kB gz; UAT 8/8 PASS documented; Lighthouse mobile HARD gate re-verified at Phase 17 baseline; v3.9 carried real-device debt bundled with the sweep; milestone v3.10 ready for `git tag v3.10` ceremony. All 4 D-20-* decisions (D-20-VISUAL-3D, D-20-CLICK-DRAG-THRESHOLD, D-20-PROPS-CONTRACT, D-20-CONTEXT-LOSS) are backed by code + tests + UAT verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer event arbitration (mouse + touch) | Untrusted DOM events from recruiter; hook deterministically classifies click vs drag; preserves GAME-04 under OrbitControls gesture state |
| Bundle-gate evaluation → CI exit code | WebGL chunk WARN band is a soft signal; mobile chunk HARD gate is the security-critical surface (preserved by Plan 20-01 regex defense) |
| UAT documentation → milestone-close decision | UAT.md is the human-readable verdict log; failure to populate ## Notes leaves the milestone status ambiguous |
| Lighthouse mobile audit → A11y / Perf verdict | Programmatic verdict via `npm run lighthouse:check` exits 0 only when all 4 scores meet HARD gate; ASVS L1 a11y proxy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-Repud | Repudiation | Pointer event arbitration | mitigate | useClickVsDrag exposes pure threshold math; 5 unit tests cover mouse-within, mouse-drag-past, touch-within-bump, touch-drag-past, time-threshold-fail. Deterministic — no race conditions between OrbitControls and pick handler when wired in correct order. |
| T-20-03-DoS | Denial of Service | useEffect re-runs from useCallback dep instability | mitigate | `useCallback` with stable `[onClick]` dep — caller's onClick reference must also be useCallback-stable. If renderer wraps onClick in a non-stable function, useEffect re-attaches listeners every render. Verified by renderer integration test (npm test) showing no unmount loop. |
| T-20-03-Spoofing | Spoofing | `pointerType` from PointerEvent | accept | Browser-owned property; cannot be spoofed across security boundary. Untrusted input is the pointer coords, not the pointerType. |
| T-20-03-SC | Tampering / Supply-chain | Final bundle artifact for v3.10 release | mitigate | UAT Step 8 explicitly re-verifies regression fixture; bundle gate exits 0; mobile chunk regex (Plan 20-01) defends invariant; WebGL chunk WARN band documents soft ceiling without HARD-failing CI (REQUIREMENTS.md §Out of Scope). |
| T-20-03-A11y | A11y regression | Lighthouse mobile re-verification | mitigate | UAT Item 5 + Item 6 require `npm run lighthouse:check` exit 0 with all 4 scores at gate values. If any score drops → milestone does NOT close; routes to follow-up. |
| T-20-03-Info | Information Disclosure | UAT screenshots / device emulation results | accept | Project-controlled environment; no PII captured in UAT sweeps; localStorage flags are session-scoped strings only. |
| T-20-03-XSS | XSS via UAT notes | UAT.md ## Notes section | N/A | Markdown document — no executable surface. |

**No package install tasks in this plan** — uses already-installed Vitest + RTL + lighthouse tooling. No `[ASSUMED]`/`[SUS]` package gates.
</threat_model>

<verification>
1. `npm test` exits 0 with total ≥278 GREEN (Plan 20-01 baseline + Plan 20-02 OnboardingHint + Plan 20-03 useClickVsDrag).
2. `npm run build` exits 0.
3. `node scripts/check-bundle-gate.mjs` exits 0:
   - Mobile chunk ≤38.82 kB gz (Lighthouse mobile HARD gate intact).
   - WebGL chunk reported with appropriate INFO/WARN tier (v3.10 WARN band at ≤130 kB gz).
4. `npm run lighthouse:mobile && npm run lighthouse:check` exit codes both = 0 — scores documented in 20-UAT.md.
5. `npm test -- check-bundle-gate` still passes (Plan 20-01 regex fixture intact).
6. `npm test -- useClickVsDrag` passes 6/6.
7. 20-UAT.md exists with 8 test-matrix items, ## Notes populated, ## Pass Criteria documents 8/8 or specifically identifies failing items.
8. `git log --oneline -2` shows the two atomic commits (Task 1 hook + Task 2 close).
9. STATE.md ready to update at milestone close: `phase: 20`, `status: complete`, `tag candidate: v3.10`.
</verification>

<success_criteria>
- [ ] `src/game/useClickVsDrag.js` exists with locked threshold constants `DIST_THRESHOLD_MOUSE=5`, `DIST_THRESHOLD_TOUCH=8`, `TIME_THRESHOLD_MS=250` and default-export hook.
- [ ] `src/game/useClickVsDrag.test.js` covers ≥5 cases per behavior block (mouse click, mouse drag, touch click within bump, touch drag past bump, time threshold, no-op safety).
- [ ] Renderer integrates the hook: `import useClickVsDrag from '../useClickVsDrag'`; pointerdown/pointerup attach via hook return; `'click'` listener removed.
- [ ] `scripts/check-bundle-gate.mjs` has `WEBGL_WARN_KB = 130` constant + three-tier WebGL chunk evaluation (no HARD fail on soft ceiling per REQUIREMENTS).
- [ ] `.planning/phases/20-3d-constellation/20-UAT.md` exists with the 8-item test matrix + v3.9 deferred items + populated ## Notes section.
- [ ] `npm test` exits 0 with total ≥278 GREEN.
- [ ] `npm run build && node scripts/check-bundle-gate.mjs` exits 0.
- [ ] `npm run lighthouse:mobile && npm run lighthouse:check` exit codes both = 0; scores documented in 20-UAT.md (Perf ≥95 / A11y 100 / BP 100 / SEO 100).
- [ ] UAT 8/8 PASS documented OR specific items routed to follow-up (v3.10.1 / 20-04).
- [ ] v3.9 carried deferred items (above-fold + SVG twinkle real-device confirm) addressed in the sweep — either confirmed or marked partial with operator follow-up.
- [ ] Two atomic commits land: useClickVsDrag hook + renderer integration; milestone-close UAT + bundle-gate WARN.
- [ ] Milestone v3.10 ready for `git tag v3.10` ceremony — all 4 D-20-* decisions backed by code + tests + UAT.
</success_criteria>

<output>
Create `.planning/phases/20-3d-constellation/20-03-SUMMARY.md` when done. SUMMARY must report:
- Final test count (target ≥278 GREEN).
- Mobile chunk size + WebGL chunk size + bundle-gate exit status.
- Lighthouse mobile scores: Perf / A11y / BP / SEO (must all meet HARD gate).
- UAT 8/8 verdict (or itemized fail list).
- v3.9 carried debt status (above-fold + SVG twinkle real-device confirm).
- Confirmation that 4 D-20-* decisions are backed by code + tests + UAT.
- Milestone v3.10 close readiness: `git tag v3.10` queued.
- Any open items routing to v3.10.1 micro-milestone (camera-state persistence, reset-view button, per-category depth color shift, fog falloff, etc. — per CONTEXT §deferred).
</output>
