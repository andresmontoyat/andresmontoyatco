---
phase: 17-webgl-desktop-renderer-lighthouse-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/useRendererCapability.js
  - src/game/useRendererCapability.test.js
  - src/game/RendererErrorBoundary.js
  - src/game/RendererErrorBoundary.test.js
  - src/game/renderers/WebGLConstellation.js
  - src/game/renderers/WebGLConstellation.test.js
  - src/game/GameMode.js
  - package.json
autonomous: true
requirements: [GAME-01, GAME-07]

must_haves:
  truths:
    - "On a capable client (≥1024px viewport + motion OK + saveData OK + WebGL ctx + no ?renderer=svg) the page renders the WebGL canvas (1 Point node) in place of the SVG renderer"
    - "On any failed capability gate (mobile viewport, prefers-reduced-motion, saveData, no WebGL, ?renderer=svg) the existing Phase 15 SvgConstellation renders — pixel-identical to Phase 16 close-out"
    - "When the WebGL chunk fails to load or the WebGLConstellation throws on mount, RendererErrorBoundary catches and renders SvgConstellation as the fallback — no broken screen, no user-visible error"
    - "GameMode no longer contains inline ConstellationErrorBoundary class or inline detectCapabilities function — both lifted to standalone modules"
    - "Mobile bundle (GameMode chunk) still ships zero three.js — verified by grep on dist/assets/GameMode-*.js"
  artifacts:
    - path: "src/game/useRendererCapability.js"
      provides: "Reactive capability hook returning 'webgl' | 'svg' enum"
      exports: ["default useRendererCapability"]
    - path: "src/game/useRendererCapability.test.js"
      provides: "Vitest coverage for all 5 capability gates + matchMedia reactivity + URL override"
    - path: "src/game/RendererErrorBoundary.js"
      provides: "React 17 class ErrorBoundary that renders props.fallback on caught error"
      exports: ["default RendererErrorBoundary"]
    - path: "src/game/RendererErrorBoundary.test.js"
      provides: "Vitest coverage for children-render / fallback-render / sticky-state / [renderer-fallback] log prefix"
    - path: "src/game/renderers/WebGLConstellation.js"
      provides: "Slice 1 minimal WebGL renderer — props contract parity with SvgConstellation, mounts 1 Point node, aria-hidden canvas, accepts {nodes,edges,layout,highlightedSkillIds,selectedSkillId,yearRange,justFilteredId,theme,lang,t,onSelectSkill,onHoverSkill} props"
      exports: ["default WebGLConstellation"]
    - path: "src/game/renderers/WebGLConstellation.test.js"
      provides: "Smoke test via vi.mock('three') + getContext spy override (Pitfall 1/7)"
    - path: "src/game/GameMode.js"
      provides: "Renderer-slot conditional branching on useRendererCapability(); React.lazy import of WebGLConstellation at module scope; Suspense+ErrorBoundary wrap; inline ConstellationErrorBoundary class + detectCapabilities function deleted; data-prefers-reduced-motion wrapper attribute deleted"
    - path: "package.json"
      provides: "three ^0.169 in dependencies (named-imports tree-shake target ~40 kB gz per D-17-TREESHAKE); no peer additions"
  key_links:
    - from: "src/game/GameMode.js"
      to: "src/game/useRendererCapability.js"
      via: "default import + hook call inside component body returning 'webgl' | 'svg'"
      pattern: "import useRendererCapability"
    - from: "src/game/GameMode.js"
      to: "src/game/RendererErrorBoundary.js"
      via: "default import + JSX wrap around lazy WebGL branch"
      pattern: "import RendererErrorBoundary"
    - from: "src/game/GameMode.js"
      to: "src/game/renderers/WebGLConstellation.js"
      via: "React.lazy(() => import('./renderers/WebGLConstellation')) at module scope (Pattern G)"
      pattern: "React\\.lazy.*WebGLConstellation"
    - from: "src/game/renderers/WebGLConstellation.js"
      to: "three"
      via: "named imports only (D-17-TREESHAKE) — Scene, OrthographicCamera, WebGLRenderer, BufferGeometry, BufferAttribute, ShaderMaterial, Points, Color"
      pattern: "from 'three'"
---

<objective>
Slice 1 — Walking Thin. Ship the end-to-end renderer-selection backbone: a capability hook chooses 'webgl' vs 'svg', a lifted ErrorBoundary wraps the WebGL branch, GameMode lazy-imports a minimal WebGLConstellation that renders ONE Point node. On a capable desktop client the page renders 1 WebGL node where Phase 16 had 26 SVG nodes; on any gate failure or WebGL error the existing SvgConstellation renders unchanged.

Purpose: Prove the contract before scaling. Validate (a) capability detection routes correctly, (b) lazy chunk splits — three.js absent from mobile bundle, (c) ErrorBoundary swallows WebGL failures into SVG fallback, (d) Phase 15 SvgConstellation contract is honored by WebGLConstellation's props signature. Subsequent slices extend WebGLConstellation only — capability, ErrorBoundary, and GameMode wiring are LOCKED after Slice 1.

Output:
- src/game/useRendererCapability.js (+test)
- src/game/RendererErrorBoundary.js (+test) — LIFTED from GameMode.js:46-67
- src/game/renderers/WebGLConstellation.js (+test) — Slice 1 minimal 1-node smoke
- src/game/GameMode.js — extend renderer slot + delete inline ConstellationErrorBoundary + delete inline detectCapabilities + delete data-prefers-reduced-motion attribute
- package.json — add `three` dependency
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/17-webgl-desktop-renderer-lighthouse-gate/17-CONTEXT.md
@.planning/phases/17-webgl-desktop-renderer-lighthouse-gate/17-RESEARCH.md
@.planning/phases/17-webgl-desktop-renderer-lighthouse-gate/17-PATTERNS.md
@.planning/phases/17-webgl-desktop-renderer-lighthouse-gate/17-UI-SPEC.md

# Phase 15 contract source — SvgConstellation IS the contract document
@src/game/renderers/SvgConstellation.js

# Phase 16 wiring point being extended
@src/game/GameMode.js

# Test infra — Pitfall 1 source
@src/test/setup.js

# Lazy import pattern reference
@src/App.js

# Vite config — esbuild loader for .js+JSX, rollup-plugin-visualizer
@vite.config.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lift detectCapabilities → useRendererCapability hook + test (RED → GREEN)</name>
  <files>src/game/useRendererCapability.js, src/game/useRendererCapability.test.js, package.json</files>
  <read_first>
    - src/game/GameMode.js lines 29-44 (current detectCapabilities — LIFT verbatim and EXTEND per CONTEXT D-17-CAP-GATES)
    - src/game/renderers/SvgConstellation.js lines 32-59 (usePrefersReducedMotion — COPY SSR+addListener compat pattern, 17-PATTERNS Analog 1)
    - src/game/useConstellation.test.js (full — describe/renderHook/act test scaffolding pattern; 17-PATTERNS line 198)
    - src/test/setup.js lines 9-11 (HTMLCanvasElement.prototype.getContext = () => null — Pitfall 1 source)
    - 17-RESEARCH §6 lines 226-286 (canonical detect() + useRendererCapability sketch)
    - 17-RESEARCH §5 lines 192-214 (vi.spyOn getContext mockReturnValue pattern — Pitfall 1 mitigation)
    - 17-RESEARCH §7 lines 292-296 (?renderer URL override semantics — short-circuits all gates)
    - 17-PATTERNS lines 244-256 (12-test test plan: 5 gates × on/off + URL override + reactivity + SSR + memoization)
    - 17-CONTEXT D-17-BREAKPOINT (1024px = Tailwind lg:), D-17-CAP-GATES (4 mandatory gates), D-17-OVERRIDE (?renderer=svg|webgl, no localStorage)
  </read_first>
  <behavior>
    - RED test 1: `useRendererCapability()` returns `'webgl'` when (a) `window.matchMedia('(min-width: 1024px)').matches === true`, (b) `matchMedia('(prefers-reduced-motion: reduce)').matches === false`, (c) `navigator.connection?.saveData !== true`, (d) `navigator.connection?.effectiveType` not in ['2g','slow-2g'], (e) `getContext('webgl2') || getContext('webgl')` returns truthy. Must `vi.spyOn(HTMLCanvasElement.prototype, 'getContext').mockReturnValue({})` per Pitfall 1.
    - RED test 2: returns `'svg'` when viewport matchMedia matches=false.
    - RED test 3: returns `'svg'` when prefers-reduced-motion matchMedia matches=true.
    - RED test 4: returns `'svg'` when `navigator.connection = { saveData: true }`.
    - RED test 5: returns `'svg'` when `navigator.connection = { effectiveType: '2g' }`.
    - RED test 6: returns `'svg'` when getContext returns null.
    - RED test 7: `?renderer=svg` URL forces `'svg'` even when all gates pass (short-circuit per RESEARCH §7).
    - RED test 8: `?renderer=webgl` URL forces `'webgl'` (short-circuit — overrides gates).
    - RED test 9: SSR safety — when `typeof window === 'undefined'` returns `'svg'` (test via vi mock of detect's window check, OR document the SSR branch is covered by static analysis).
    - RED test 10: matchMedia 'change' dispatch re-evaluates capability — use mql.dispatchEvent + act(() => mqlListener({})).
    - RED test 11: cleanup detaches both viewport AND motion matchMedia listeners on unmount.
    - GREEN: implement per RESEARCH §6 canonical sketch — detect() pure fn with URL override → 4 gates → WebGL feature detect; default-exported useRendererCapability hook with useState(detect) + useEffect attaching matchMedia listeners with addEventListener/addListener compat shim (mirror SvgConstellation:32-59).
  </behavior>
  <action>
    Add `three` to `package.json` `dependencies` at version `^0.169` (named-imports target ~40 kB gz per D-17-TREESHAKE; no `react-three-fiber`, no `force-graph`, no `detect-gpu`). Run `npm install`. Do NOT add `@types/three` (project is JS, not TS — no JSDoc/types convention in repo per CLAUDE.md).

    Create `src/game/useRendererCapability.test.js` FIRST (RED), commit `test(17-01): RED useRendererCapability — 5 gates + URL override + reactivity`. Follow `useConstellation.test.js` describe/renderHook/act shape. matchMedia mock per `makeMockMatchMedia` from 17-PATTERNS line 227. `getContext` spy via `vi.spyOn(HTMLCanvasElement.prototype, 'getContext')` in beforeEach, `mockRestore()` in afterEach (Pitfall 1). `navigator.connection` saved/restored via `Object.defineProperty(navigator, 'connection', { value: ..., configurable: true })` per test. URL via `Object.defineProperty(window, 'location', { value: { search: '?renderer=svg' }, configurable: true })` per test. NO semicolons.

    Then create `src/game/useRendererCapability.js` (GREEN) — exact RESEARCH §6 sketch verbatim. SSR guard via `const isServer = typeof window === 'undefined'`. URL override read once via `new URLSearchParams(window.location.search).get('renderer')` (Pitfall 3 — no listener). 4 gates evaluated in order viewport→motion→saveData→effectiveType then WebGL ctx detect last. useState(detect) for synchronous initial. useEffect attaches matchMedia listeners on viewport + motion ONLY (saveData/effectiveType change requires page reload — not reactive per CONTEXT). addEventListener/addListener compat shim copy from SvgConstellation:32-59. Cleanup removes both listeners. Commit `feat(17-01): GREEN useRendererCapability hook — 4 gates + URL override + reactive matchMedia`.

    Decision recorded inline: URL override semantics CONFIRMED per RESEARCH §7 — `?renderer=webgl` short-circuits all capability gates (debug/UAT only; user accepts responsibility for forcing WebGL on incapable client). Add a comment in detect() noting this.
  </action>
  <verify>
    <automated>npm test -- --run src/game/useRendererCapability.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/game/useRendererCapability.js` exists with `export default function useRendererCapability()` (rg `^export default function useRendererCapability` matches once)
    - File `src/game/useRendererCapability.test.js` exists with ≥11 `it(...)` blocks (rg `^\s+it\(` count ≥ 11)
    - `npm test -- --run src/game/useRendererCapability.test.js` exits 0
    - rg `from 'three'` src/game/useRendererCapability.js returns 0 matches (pure-logic hook, no three.js import — Pattern J)
    - rg `localStorage` src/game/useRendererCapability.js returns 0 matches (D-17-OVERRIDE: no localStorage)
    - rg `addListener|removeListener` src/game/useRendererCapability.js returns ≥2 matches (Safari 13 compat shim per SvgConstellation pattern)
    - package.json dependencies contains `"three"` (rg `"three":` package.json returns ≥1 match)
    - 2 commits exist in git log: `test(17-01): RED useRendererCapability` then `feat(17-01): GREEN useRendererCapability`
  </acceptance_criteria>
  <done>
    useRendererCapability hook ships with all 11 RED tests now GREEN. Hook returns 'webgl' on capable client, 'svg' on any gate failure, honors ?renderer=svg|webgl override, reactive to viewport+motion matchMedia changes. `three` declared as dependency. Two atomic commits (RED → GREEN).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract RendererErrorBoundary from GameMode + test + minimal WebGLConstellation (Slice 1 1-node smoke) + GameMode wiring</name>
  <files>src/game/RendererErrorBoundary.js, src/game/RendererErrorBoundary.test.js, src/game/renderers/WebGLConstellation.js, src/game/renderers/WebGLConstellation.test.js, src/game/GameMode.js</files>
  <read_first>
    - src/game/GameMode.js lines 46-67 (ConstellationErrorBoundary class — LIFT verbatim, rename to RendererErrorBoundary)
    - src/game/GameMode.js lines 74 (useState(detectCapabilities) — DELETE; replaced by useRendererCapability)
    - src/game/GameMode.js lines 136-159 (current renderer slot — REPLACE with conditional)
    - src/game/GameMode.js line 142 (data-prefers-reduced-motion attribute — DELETE per Pitfall 10)
    - src/game/renderers/SvgConstellation.js lines 97-110 (props signature — WebGLConstellation MUST accept same 12 props identically)
    - src/game/renderers/SvgConstellation.js lines 7-26 (LIGHT_THEME_STROKES + SIZING + computeRadius + edgeStrokeWidth — copy module-scope helpers verbatim into WebGLConstellation.js for use in Slice 2; Slice 1 declares but does not yet consume the size formula)
    - src/App.js lines 11, 40-47 (React.lazy + Suspense pattern — copy verbatim; Pattern G: lazy import at module scope NOT inside component)
    - 17-RESEARCH §3 lines 111-152 (RendererErrorBoundary class + GameMode integration sketch)
    - 17-RESEARCH §5 lines 178-214 (Vitest WebGL strategy: vi.mock('three') + getContext spy)
    - 17-PATTERNS lines 260-298 (RendererErrorBoundary extract details — rename, change console prefix to '[renderer-fallback]', export default)
    - 17-PATTERNS lines 302-373 (RendererErrorBoundary.test.js full sketch — sticky behavior + console.error spy mandatory per Pitfall 6)
    - 17-PATTERNS lines 380-475 (WebGLConstellation Slice 1 scope: props contract, vi.mock('three'), canvas aria-hidden + tabIndex=-1)
    - 17-PATTERNS lines 480-580 (GameMode extension: shared rendererProps object, Suspense fallback=SvgConstellation per Pitfall 11/H, ErrorBoundary fallback=SvgConstellation per Pitfall 12, DELETE data-prefers-reduced-motion per Pitfall 10)
    - 17-UI-SPEC §"Loading State Contract" lines 305-330 (SVG IS the loading state — no spinner, Suspense fallback=SvgConstellation), §"Layout Contract" lines 442-461 (renderer-slot JSX structure verbatim), §"Bilingual / A11y Constraints" line 386 (canvas aria-hidden=true; outer role=application unchanged)
    - 17-CONTEXT D-17-SELECTION-MECH (Suspense+ErrorBoundary wrap when webgl, direct SvgConstellation when svg), D-17-ERRORBOUNDARY-INIT (catches chunk-fetch + shader-compile + ctx-creation), D-17-PRIMITIVES (Slice 1: 1 Point smoke only — no edges, no theme yet)
  </read_first>
  <behavior>
    - RED RendererErrorBoundary.test.js test 1: renders children when no error thrown (text "CHILD" in document, "FALLBACK" absent).
    - RED test 2: renders fallback when child component throws (text "FALLBACK" in document).
    - RED test 3: console.error called with first arg containing string '[renderer-fallback]' (Pattern I; per D-17-ERRORBOUNDARY-INIT + UI-SPEC §Copywriting line 432).
    - RED test 4: sticky behavior — after error, rerender with non-throwing child still shows fallback (UI-SPEC line 346 + Pitfall 5).
    - GREEN RendererErrorBoundary.js: React 17 class component with `{ hasError: false }` state, `static getDerivedStateFromError()`, `componentDidCatch(error, info) { console.error('[renderer-fallback]', error, info) }`, render returns props.fallback OR props.children. Export default. NO new dependency.
    - RED WebGLConstellation.test.js test 1: component mounts without throwing given minimal Phase 15 props (`{nodes, edges, layout, theme:'dark', lang:'en', t:{}, onSelectSkill: vi.fn(), onHoverSkill: vi.fn(), highlightedSkillIds:[], selectedSkillId:null, yearRange:null, justFilteredId:null}`).
    - RED test 2: renders a single `<canvas>` element with `aria-hidden="true"` and `tabIndex={-1}` (queryByRole + querySelector('canvas')).
    - RED test 3: calls `WebGLRenderer` constructor once on mount (vi mocked spy `.toHaveBeenCalledTimes(1)`).
    - RED test 4: calls `renderer.dispose()` on unmount (cleanup verification).
    - GREEN WebGLConstellation.js Slice 1 minimal: accept exact Phase 15 props signature; useRef for canvas; useEffect creates Scene+OrthographicCamera+WebGLRenderer(canvas), creates a `BufferGeometry` with one vertex at `layout[nodes[0].id]` (defensive `?? {x:0,y:0}`), creates a `ShaderMaterial` with minimal vertex/fragment shaders that just render a 10px point, mounts a `Points` mesh to the scene, calls renderer.render() once. Cleanup disposes geometry+material+renderer (Pitfall 8). Render returns `<canvas ref={canvasRef} aria-hidden="true" tabIndex={-1} className="webgl-canvas block w-full" />`. No rAF loop (Slice 3). No edges (Slice 2). No theme reactivity (Slice 2). No motion (Slice 3). NO semicolons.
    - GameMode.js: imports `Suspense` from react, import RendererErrorBoundary + useRendererCapability, declare `const WebGLConstellation = React.lazy(() => import('./renderers/WebGLConstellation'))` at module scope (Pattern G). DELETE inline class ConstellationErrorBoundary (lines 46-67). DELETE inline detectCapabilities function (lines 29-44). DELETE `useState(() => detectCapabilities())` (line ~74) — replace with `const capability = useRendererCapability()`. DELETE `data-prefers-reduced-motion` attribute (line ~142, Pitfall 10). Build shared `rendererProps` object once. Replace renderer slot lines 136-159: `<RendererErrorBoundary fallback={<SvgConstellation {...rendererProps} />}><div data-game-interactive ... data-renderer={capability}>{capability === 'webgl' ? (<Suspense fallback={<SvgConstellation {...rendererProps} />}><WebGLConstellation {...rendererProps} /></Suspense>) : (<SvgConstellation {...rendererProps} />)}</div></RendererErrorBoundary>`.
  </behavior>
  <action>
    Order: (a) RED RendererErrorBoundary.test.js → GREEN extract from GameMode.js:46-67 with rename+prefix change, commit `test(17-01): RED RendererErrorBoundary` then `feat(17-01): GREEN extract RendererErrorBoundary from GameMode + [renderer-fallback] log prefix`. (b) RED WebGLConstellation.test.js → GREEN minimal 1-node Slice 1 implementation, commit `test(17-01): RED WebGLConstellation Slice 1 smoke` then `feat(17-01): GREEN WebGLConstellation Slice 1 — 1 Point render + aria-hidden canvas`. (c) GameMode.js modification, commit `feat(17-01): wire useRendererCapability + lazy WebGL branch + delete inline ConstellationErrorBoundary/detectCapabilities/data-prefers-reduced-motion`.

    RendererErrorBoundary.test.js: use the verbatim sketch from 17-PATTERNS lines 302-369. `vi.spyOn(console, 'error').mockImplementation(() => {})` in beforeEach is MANDATORY (Pitfall 6 — React 17 logs caught errors to console regardless). `Thrower` helper component throws in render body. NO semicolons.

    RendererErrorBoundary.js: verbatim lift from GameMode.js:46-67, rename class to `RendererErrorBoundary`, change `console.error('Constellation error:', ...)` → `console.error('[renderer-fallback]', error, info)` (Pattern I, UI-SPEC §Copywriting line 432, greppable in DevTools). Export default. NO new dep. Reset behavior: NO auto-reset (Pitfall 5; UI-SPEC line 346 — only page reload escapes fallback).

    WebGLConstellation.test.js: `vi.mock('three', async (importOriginal) => { const actual = await importOriginal(); return { ...actual, WebGLRenderer: vi.fn(() => ({ setSize: vi.fn(), setPixelRatio: vi.fn(), render: vi.fn(), domElement: document.createElement('canvas'), dispose: vi.fn() })) } })` at top of file (Pitfall 7, RESEARCH §5 line 188). `vi.spyOn(HTMLCanvasElement.prototype, 'getContext').mockReturnValue({})` in beforeEach (Pitfall 1). Minimal props fixture: `{ nodes: [{id:'java', count:5, category:'lang', color:'#3b82f6'}], edges: [], layout: {java:{x:500,y:500}}, theme: 'dark', lang: 'en', t: {}, highlightedSkillIds: [], selectedSkillId: null, yearRange: null, justFilteredId: null, onSelectSkill: vi.fn(), onHoverSkill: vi.fn() }`. NO semicolons.

    WebGLConstellation.js Slice 1: import named symbols ONLY from 'three' (D-17-TREESHAKE) — `Scene, OrthographicCamera, WebGLRenderer, BufferGeometry, BufferAttribute, ShaderMaterial, Points, Color` (Slice 2 adds LineSegments+LineBasicMaterial+AdditiveBlending; Slice 1 declares only what it uses). Module-scope helpers LIFTED VERBATIM from SvgConstellation.js: LIGHT_THEME_STROKES (lines 7-16), SIZING table + computeRadius (lines 18-26), edgeStrokeWidth (lines 28-30). These are unused by Slice 1's 1-node render but declared now to avoid re-touching the file in Slice 2 (action: lift only — do NOT call). Component body: useRef canvasRef + sceneRef + rendererRef + cameraRef + materialRef + geometryRef. Single useEffect (cleanup-bearing): create Scene, OrthographicCamera(left=0, right=1000, top=0, bottom=1000, near=-1, far=1) over baked layout 0-1000 unit space, WebGLRenderer({ canvas: canvasRef.current, alpha: true, antialias: true }), setPixelRatio(Math.min(window.devicePixelRatio, 2)) (UI-SPEC §Node Geometry DPR clamp). Build minimal BufferGeometry: position attribute Float32Array([layout[nodes[0].id]?.x ?? 500, layout[nodes[0].id]?.y ?? 500, 0]) (defensive — Slice 2 fills all 26). ShaderMaterial with trivial vertex shader `gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0); gl_PointSize = 10.0;` and fragment shader `gl_FragColor = vec4(1.0, 1.0, 1.0, 1.0);`. Create Points(geometry, material), add to scene, renderer.render(scene, camera). Cleanup: geometry.dispose(), material.dispose(), renderer.dispose() (Pitfall 8). Return `<canvas ref={canvasRef} aria-hidden="true" tabIndex={-1} className="webgl-canvas block w-full" />` (UI-SPEC §Bilingual/A11y line 386).

    GameMode.js modification — surgical edits, preserve Phase 16 wiring (cardJobs memo, SkillFilters JSX, ExperienceCard conditional, justFilteredId pass-through, data-game-interactive on slot wrapper, position prop, Nav header data-game-interactive unchanged). DELETE lines 29-44 (detectCapabilities inline). DELETE lines 46-67 (ConstellationErrorBoundary inline class). At top imports: add `Suspense` to react import, `import RendererErrorBoundary from './RendererErrorBoundary'`, `import useRendererCapability from './useRendererCapability'`. At module scope BEFORE the GameMode component: `const WebGLConstellation = React.lazy(() => import('./renderers/WebGLConstellation'))` (Pattern G — module scope NOT inside body or React recreates the boundary every render → infinite re-fetch loop). Inside component body: replace `const [capabilities] = useState(...)` line with `const capability = useRendererCapability()`. DELETE data-prefers-reduced-motion attribute from renderer-slot wrapper (Pitfall 10 — no longer needed; reduced-motion users gated to SVG before WebGL mounts). Add `data-renderer={capability}` to the slot wrapper for QA debugging. Build `const rendererProps = { nodes: GRAPH_NODES, edges: GRAPH_EDGES, layout: LAYOUT, highlightedSkillIds: cons.highlightedSkillIds, selectedSkillId: cons.selectedSkillId, yearRange: cons.yearRange, justFilteredId: cons.justFilteredId, theme, lang, t, onSelectSkill: cons.onSelectSkill, onHoverSkill: cons.onHoverSkill }`. Replace renderer slot lines 136-159: `<RendererErrorBoundary fallback={<SvgConstellation {...rendererProps} />}><div data-game-interactive data-testid="renderer-slot" data-theme={theme} data-renderer={capability} className="w-full max-w-3xl relative">{capability === 'webgl' ? (<Suspense fallback={<SvgConstellation {...rendererProps} />}><WebGLConstellation {...rendererProps} /></Suspense>) : (<SvgConstellation {...rendererProps} />)}</div></RendererErrorBoundary>`. Both Suspense fallback AND ErrorBoundary fallback = SvgConstellation per UI-SPEC Pattern H + Pitfall 11/12 (SVG IS the loading state — not a spinner).
  </action>
  <verify>
    <automated>npm test -- --run src/game/RendererErrorBoundary.test.js src/game/renderers/WebGLConstellation.test.js src/game/GameMode.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File src/game/RendererErrorBoundary.js exists with `export default class RendererErrorBoundary` (rg `export default class RendererErrorBoundary` returns 1 match)
    - rg `'\[renderer-fallback\]'` src/game/RendererErrorBoundary.js returns ≥1 match (Pattern I prefix)
    - File src/game/RendererErrorBoundary.test.js exists with 4 `it(...)` blocks covering children/fallback/log-prefix/sticky
    - File src/game/renderers/WebGLConstellation.js exists with `export default function WebGLConstellation` (rg returns 1 match)
    - rg `from 'three'` src/game/renderers/WebGLConstellation.js returns exactly 1 match and that line contains `Scene` AND `WebGLRenderer` AND `Points` (named imports per D-17-TREESHAKE)
    - rg `aria-hidden="true"` src/game/renderers/WebGLConstellation.js returns 1 match (canvas a11y)
    - rg `tabIndex={-1}|tabIndex=\{-1\}` src/game/renderers/WebGLConstellation.js returns 1 match
    - rg `geometry\.dispose|material\.dispose|renderer\.dispose` src/game/renderers/WebGLConstellation.js returns ≥3 matches (Pitfall 8 cleanup)
    - rg `class ConstellationErrorBoundary` src/game/GameMode.js returns 0 matches (deleted — lifted to RendererErrorBoundary)
    - rg `function detectCapabilities` src/game/GameMode.js returns 0 matches (deleted — replaced by useRendererCapability)
    - rg `data-prefers-reduced-motion` src/game/GameMode.js returns 0 matches (Pitfall 10 — deleted)
    - rg `useRendererCapability` src/game/GameMode.js returns ≥2 matches (import + call)
    - rg `React\.lazy.*WebGLConstellation` src/game/GameMode.js returns 1 match AND the match is at module scope (line number < line of `function GameMode` — Pattern G; verify by reading both line numbers)
    - rg `RendererErrorBoundary` src/game/GameMode.js returns ≥2 matches (import + JSX usage)
    - rg `data-game-interactive` src/game/GameMode.js returns 1 match (renderer-slot wrapper preserved from Phase 16)
    - rg `import \{ Scene` OR `import \{` src/game/renderers/WebGLConstellation.js returns ≥1 match (single named-import block)
    - `npm test -- --run src/game/RendererErrorBoundary.test.js src/game/renderers/WebGLConstellation.test.js src/game/GameMode.test.js` exits 0
    - Full Vitest suite `npm test` exits 0 (no regression from Phase 16 — 183/183 GREEN baseline)
    - `npm run build` exits 0; `dist/assets/WebGLConstellation-*.js` exists as separate chunk (`fd 'WebGLConstellation-.*\.js$' dist/assets` returns ≥1 match — Pattern G + RESEARCH §11)
    - `rg -E 'THREE\.|from"three"|from'\''three'\''' dist/assets/GameMode-*.js` returns 0 matches (mobile chunk three.js absent — Slice 5 bundle gate pre-flight)
    - 5 commits in git log (Task 2 portion): test(17-01) RED RendererErrorBoundary → feat(17-01) GREEN RendererErrorBoundary → test(17-01) RED WebGLConstellation Slice 1 → feat(17-01) GREEN WebGLConstellation Slice 1 → feat(17-01) wire useRendererCapability + lazy WebGL branch
  </acceptance_criteria>
  <done>
    Slice 1 walking-thin shipped. On capable desktop (≥1024px viewport + motion OK + saveData OK + WebGL ctx) the page renders 1 WebGL Point; on any gate failure or WebGL error the SvgConstellation renders unchanged. RendererErrorBoundary lifted from GameMode + logs '[renderer-fallback]' prefix. WebGLConstellation chunk splits separately; mobile bundle (GameMode-*.js) contains zero three.js references. Phase 16 baseline 183/183 tests remain GREEN; new tests pass. Five atomic commits (RED→GREEN×2 + wire).
  </done>
</task>

</tasks>

<verification>
- `npm test` full suite exits 0 (no regression from Phase 16 baseline 183/183 GREEN)
- `npm run build` exits 0
- `fd '^GameMode-.*\.js$' dist/assets` finds exactly 1 file
- `fd '^WebGLConstellation-.*\.js$' dist/assets` finds exactly 1 file (separate lazy chunk per Pattern G + RESEARCH §11)
- `rg -E 'THREE\.|from"three"|from'\''three'\''' dist/assets/GameMode-*.js` returns 0 matches (mobile chunk three.js absent — pre-flight for Slice 5 bundle gate)
- Manual smoke check (recorded in plan SUMMARY): visit `http://localhost:5173/?renderer=webgl` in dev — canvas appears, 1 white point renders; visit `?renderer=svg` — SvgConstellation renders unchanged; visit with DevTools "prefers-reduced-motion: reduce" emulation — SvgConstellation renders
</verification>

<success_criteria>
- 5 new files exist (`useRendererCapability.{js,test.js}`, `RendererErrorBoundary.{js,test.js}`, `WebGLConstellation.{js,test.js}` × 2 + 1) — wait, 6 new files total (3 source + 3 test)
- `useRendererCapability` returns 'webgl' or 'svg' enum reactively (all 11 RED tests GREEN)
- `RendererErrorBoundary` extracted from GameMode.js, logs '[renderer-fallback]' prefix, sticky behavior verified by test
- `WebGLConstellation` Slice 1 renders 1 Point with aria-hidden canvas — smoke test GREEN via vi.mock('three')
- `GameMode.js` branches on capability via `useRendererCapability()`, lazy-imports WebGLConstellation at module scope, wraps in RendererErrorBoundary+Suspense (both fallback=SvgConstellation per UI-SPEC Pattern H)
- Inline ConstellationErrorBoundary class + inline detectCapabilities function + data-prefers-reduced-motion attribute all deleted from GameMode.js
- Mobile chunk (GameMode-*.js) ships zero three.js — verified by rg on dist
- `three` ^0.169 in package.json dependencies (no other new deps)
- 7 atomic commits in git log under `(17-01)` scope (RED→GREEN for each of 3 components + 1 GameMode wire)
- Full Vitest suite 183+N/183+N GREEN where N = new tests added (≥11 + 4 + 4 = ≥19 new tests)
</success_criteria>

<output>
Create `.planning/phases/17-webgl-desktop-renderer-lighthouse-gate/17-01-SUMMARY.md` when done.
</output>
