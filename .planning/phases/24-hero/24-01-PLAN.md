---
phase: 24-hero
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/count-up.js
  - src/scripts/count-up.test.js
  - src/scripts/details-dismiss.js
  - src/scripts/details-dismiss.test.js
autonomous: true
requirements: [STATIC-02]

must_haves:
  truths:
    - "D-02: a single shared vanilla module src/scripts/count-up.js animates every [data-count-up] element from 0 to its data-target using the same cubic ease-out curve (1 - (1 - p) ** 3) as the old useCountUp, triggered by IntersectionObserver, gated by prefers-reduced-motion"
    - "D-06: src/scripts/details-dismiss.js closes any <details class=\"details-dismiss\"> on Escape and on outside-click, generic (selector-based, zero Hero-specific identifiers) so Phase 26 can reuse it unmodified"
    - "D-04: count-up.js is structured so an IntersectionObserver threshold/offset can delay the animation start (constraint honored — no jarring counting during Hero's 850ms fade-in)"
    - "Both scripts guard matchMedia/requestAnimationFrame/IntersectionObserver with typeof checks before use (defensive browser-API access convention)"
    - "count-up.js never uses innerHTML/eval — only textContent assignment (DOM-XSS surface eliminated by design)"
  artifacts:
    - path: "src/scripts/count-up.js"
      provides: "Shared count-up enhancer (exports animate for testability, auto-inits on load)"
      contains: "IntersectionObserver"
    - path: "src/scripts/details-dismiss.js"
      provides: "Escape + outside-click close enhancer for details.details-dismiss"
      contains: "details.details-dismiss"
    - path: "src/scripts/count-up.test.js"
      provides: "jsdom unit coverage for animate() reduced-motion + template + guard paths"
    - path: "src/scripts/details-dismiss.test.js"
      provides: "jsdom unit coverage for Escape-close behavior"
  key_links:
    - from: "src/scripts/count-up.js"
      to: "[data-count-up] DOM nodes"
      via: "document.querySelectorAll + IntersectionObserver.observe"
      pattern: "data-count-up"
    - from: "src/scripts/details-dismiss.js"
      to: "details.details-dismiss elements"
      via: "querySelectorAll + keydown/click listeners"
      pattern: "details\\.details-dismiss"
---

<objective>
Author the two shared vanilla-JS enhancer modules that Hero.astro (Plan 24-02) and About.astro (Plan 24-03) both consume: `src/scripts/count-up.js` (D-02, consolidates the old per-component `useCountUp`) and `src/scripts/details-dismiss.js` (D-06, Escape + outside-click close for native `<details>`). This is the first `src/scripts/` directory in the migration.

Purpose: Establish the single-source-of-truth enhancers before any consumer references them, so downstream plans just add `<script src="../../scripts/…">` tags. Building these first prevents Astro build failures (a `<script src>` to a nonexistent file breaks the build).
Output: Two new vanilla ESM modules + two jsdom test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-hero/24-RESEARCH.md
@.planning/phases/24-hero/24-PATTERNS.md
@.planning/phases/24-hero/24-CONTEXT.md

<interfaces>
<!-- Full verified implementations already specified in 24-RESEARCH.md Pattern 2 (count-up, lines 160-214) and Pattern 3 (details-dismiss, lines 264-280), reproduced in 24-PATTERNS.md lines 146-198 and 214-228. Executor uses that code directly. Two deltas from the research code, required for testability: -->
<!-- 1. count-up.js MUST add `export { animate }` (named export) so the jsdom test can call animate() directly (the IntersectionObserver auto-path cannot fire under the repo's no-op IO stub). Auto-init side-effect on load is unchanged. -->
<!-- 2. details-dismiss.js MUST wrap its wiring in an exported `initDismiss(root = document)` function, called once on load; the test calls initDismiss() after building its own DOM. No behavior change vs the research code. -->

Cubic ease-out curve (verbatim from Hero.jsx useCountUp line 55): Math.round(target * (1 - (1 - p) ** 3))
Existing repo convention (BaseLayout.astro inline script, Hero.jsx lines 37-41): no semicolons, typeof guards before browser-API use.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: count-up.js shared enhancer + jsdom test</name>
  <files>src/scripts/count-up.js, src/scripts/count-up.test.js</files>
  <read_first>
    - .planning/phases/24-hero/24-RESEARCH.md (Pattern 2, lines 155-234 — full verified module + rationale)
    - .planning/phases/24-hero/24-PATTERNS.md (count-up.js section, lines 140-204)
    - src/components/Hero.jsx (lines 43-80 — useCountUp + Stat, the source of the cubic ease-out curve and the num.match(/\d+/) / num.replace(/\d+/, n) statParts logic)
    - src/test/setup.jsx (confirm the existing IntersectionObserver + HTMLCanvasElement stubs; add a matchMedia stub only if absent)
    - src/layouts/BaseLayout.astro (lines 117-134 — repo vanilla-JS code style: no semicolons, typeof/try-catch guards)
  </read_first>
  <behavior>
    - animate(el) with a matchMedia reduced-motion match TRUE sets el.textContent to the settled template string immediately (e.g. data-count-up="18" + data-count-up-template="{n}+" → "18+"), no rAF loop
    - animate(el) with data-count-up-template omitted defaults the template to "{n}" (renders the bare number)
    - animate(el) with a non-numeric/absent data-count-up (Number.isFinite false) is a no-op — leaves el.textContent untouched
    - init() returns early (no throw) when there are zero [data-count-up] elements on the page
    - When IntersectionObserver is undefined, init() falls back to animating all matched elements directly (no crash)
  </behavior>
  <action>Create src/scripts/count-up.js reproducing 24-RESEARCH.md Pattern 2 verbatim: module-level DURATION_MS = 1100 and SELECTOR = '[data-count-up]'; a `prefersReducedMotion()` helper using `typeof window.matchMedia === 'function'`; an `animate(el)` reading `el.dataset.countUp` (Number) and `el.dataset.countUpTemplate` (default '{n}'), using `setDisplay(n)` = `el.textContent = template.replace('{n}', String(n))`, short-circuiting to the target when reduced-motion or no requestAnimationFrame, else running a `tick` rAF loop with the cubic ease-out `Math.round(target * (1 - (1 - p) ** 3))`; an `init()` that querySelectorAll's SELECTOR, returns early on zero matches, falls back to `els.forEach(animate)` when IntersectionObserver is unavailable, else observes each with `{ threshold: 0.4 }` and calls animate + unobserve on first intersect; the load-time bootstrap (`DOMContentLoaded` vs immediate). ADD `export { animate }` for the test. Honor D-04: keep the animate() entry structured so a fixed start offset can be inserted before the first rAF if visual QA (Plan 24-05) shows counting during the 850ms fade-in — leave a one-line comment marking that seam. Never use innerHTML. Then create src/scripts/count-up.test.js (default jsdom environment — do NOT add the `// @vitest-environment node` pragma) importing `{ animate }`, mocking window.matchMedia, covering the 5 behaviors above.</action>
  <verify>
    <automated>npx vitest run src/scripts/count-up.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/scripts/count-up.test.js` passes with ≥5 assertions covering the behaviors listed
    - `grep -n "export { animate }" src/scripts/count-up.js` matches
    - `grep -c "innerHTML\|eval(" src/scripts/count-up.js` returns 0
    - `grep -n "1 - (1 - p) \*\* 3\|(1 - p) \*\* 3" src/scripts/count-up.js` matches (cubic ease-out ported verbatim)
  </acceptance_criteria>
  <done>count-up.js exists, exports animate, uses only textContent, ports the cubic ease-out curve, and its jsdom test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: details-dismiss.js enhancer + jsdom test</name>
  <files>src/scripts/details-dismiss.js, src/scripts/details-dismiss.test.js</files>
  <read_first>
    - .planning/phases/24-hero/24-RESEARCH.md (Pattern 3 script, lines 264-280 + Pitfall 2)
    - .planning/phases/24-hero/24-PATTERNS.md (details-dismiss.js section, lines 208-243 — direct mapping from Hero.jsx lines 100-110)
    - src/components/Hero.jsx (lines 96-135 — CvDownload useEffect being ported: mousedown→click, setOpen(false)→el.open=false)
  </read_first>
  <action>Create src/scripts/details-dismiss.js reproducing 24-RESEARCH.md Pattern 3's script: for every `details.details-dismiss` element, a `keydown` listener that on `e.key === 'Escape'` while `el.open` sets `el.open = false` and refocuses `el.querySelector('summary')`; a `document` `click` listener that closes the element when `el.open` and `!el.contains(e.target)`. Wrap the wiring in an exported `initDismiss(root = document)` (query `root.querySelectorAll('details.details-dismiss')`) and call `initDismiss()` once at load. Keep the selector strictly `details.details-dismiss` — zero Hero-specific class/id (D-06 generic-by-design so Phase 26 Experience can reuse it). Match repo style (no semicolons, optional chaining on the summary focus). Then create src/scripts/details-dismiss.test.js (default jsdom environment) that builds a `<details class="details-dismiss">` with a `<summary>` and a link, sets `el.open = true`, calls `initDismiss()`, dispatches a `keydown` Escape event, and asserts `el.open === false`; add a second case asserting an outside `document` click closes an open element.</action>
  <verify>
    <automated>npx vitest run src/scripts/details-dismiss.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/scripts/details-dismiss.test.js` passes: Escape-close case + outside-click-close case both GREEN
    - `grep -n "details.details-dismiss" src/scripts/details-dismiss.js` matches
    - `grep -n "export" src/scripts/details-dismiss.js` shows initDismiss exported
    - No Hero-specific identifier present: `grep -ci "hero\|cv-download\|#hero" src/scripts/details-dismiss.js` returns 0
  </acceptance_criteria>
  <done>details-dismiss.js exists with a generic selector, exports initDismiss, and its Escape + outside-click jsdom tests are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time attrs → client script | count-up.js reads `data-count-up`/`data-count-up-template` written by Astro frontmatter at build time — never runtime/attacker-controlled |
| DOM events → client script | details-dismiss.js reacts to Escape keydown + document clicks — no data extracted from the events beyond `e.key`/`e.target` identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Tampering (DOM-XSS) | count-up.js textContent write | mitigate | Use `el.textContent = template.replace(...)` only — never innerHTML/eval; values originate from build-time frontmatter integers/strings, never from location.hash/URLSearchParams (24-RESEARCH.md Security Domain) |
| T-24-01-02 | Denial of Service | count-up.js rAF loop | accept | Loop is bounded by DURATION_MS=1100 and self-terminates at p>=1; unobserve after first intersect prevents re-trigger — no runaway loop |
| T-24-01-SC | Tampering | npm installs | accept | This plan installs ZERO packages (24-RESEARCH.md Package Legitimacy Audit: N/A) — no supply-chain surface introduced |
</threat_model>

<verification>
- `npx vitest run src/scripts/` is GREEN (both new suites)
- `npx vitest run` shows no regressions against the 115 baseline suites
- Both modules are pure vanilla ESM (no React, no imports from src/i18n or src/components)
</verification>

<success_criteria>
- src/scripts/count-up.js and src/scripts/details-dismiss.js exist, each with a passing jsdom test
- count-up.js consolidates the count-up logic into one shared module (D-02) with the verbatim cubic ease-out curve
- details-dismiss.js is generic (D-06), reusable by Phase 26
- Zero innerHTML/eval; typeof guards present on all browser APIs
- Full test suite GREEN, no regressions
</success_criteria>

<output>
Create `.planning/phases/24-hero/24-01-SUMMARY.md` when done
</output>
