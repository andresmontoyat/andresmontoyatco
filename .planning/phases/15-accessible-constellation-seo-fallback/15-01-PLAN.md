---
phase: 15-accessible-constellation-seo-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/GameMode.js
  - src/game/GameMode.test.js
  - src/game/ConstellationFallback.js
  - src/game/ConstellationFallback.test.js
  - src/i18n/translations.js
  - src/App.js
autonomous: true
requirements:
  - GAME-01
  - GAME-06
tags: [game-mode, orchestrator, seo, sr-only, i18n, h1-derivation, mvp-slice-1]

must_haves:
  truths:
    - "On default landing the visitor sees a bilingual H1 with live-derived numbers (`{years} years/años. {skills} skills. One/Una constelación.`) above where the constellation will paint."
    - "The H1 integers (yearsActive, skillCount) are derived at render time from EXPERIENCE.period + buildConstellationGraph node count — never hardcoded."
    - "A visually-hidden but DOM-present `<section className=\"sr-only\">` lists all 12 experiences with title, company, date, location, bullets, and tech in the active language."
    - "Switching language re-renders the fallback in the new language without remounting."
    - "If the graph is empty (no nodes) the game-mode section renders `t.game.empty` instead of crashing."
    - "`viewMode === 'game'` mounts `<GameMode />` (lazy-loaded with Suspense fallback) replacing the Phase 14 placeholder."
  artifacts:
    - path: "src/game/GameMode.js"
      provides: "Orchestrator: capability detection, H1 derivation, ErrorBoundary wrapping renderer slot, ConstellationFallback always rendered outside boundary"
      exports: ["default GameMode"]
    - path: "src/game/GameMode.test.js"
      provides: "H1 derivation unit tests (yearsActive=19, skillCount=26 against live data), ConstellationFallback presence assertion, ErrorBoundary fallback render"
    - path: "src/game/ConstellationFallback.js"
      provides: "Pure presentational sr-only semantic experience list (active-lang only)"
      exports: ["default ConstellationFallback"]
    - path: "src/game/ConstellationFallback.test.js"
      provides: "Renders all 12 experiences in active language; language switch test"
    - path: "src/i18n/translations.js"
      provides: "game.* namespace extended with constellationLabel, constellationRoleDesc, subCopy, fallbackHeading, h1Years, h1Skills, h1Tagline (EN + ES)"
      contains: "h1Tagline"
    - path: "src/App.js"
      provides: "viewMode='game' branch mounts <Suspense><GameMode /></Suspense> using existing SectionFallback"
  key_links:
    - from: "src/App.js"
      to: "src/game/GameMode"
      via: "React.lazy import + Suspense"
      pattern: "React\\.lazy\\(\\(\\) => import\\('\\./game/GameMode'\\)\\)"
    - from: "src/game/GameMode.js"
      to: "src/game/constellation.graph.js (buildConstellationGraph)"
      via: "skillCount = buildConstellationGraph(EXPERIENCE, SKILLS).nodes.length"
      pattern: "buildConstellationGraph"
    - from: "src/game/GameMode.js"
      to: "src/game/ConstellationFallback"
      via: "<ConstellationFallback experiences={EXPERIENCE} lang={lang} /> rendered OUTSIDE ErrorBoundary"
      pattern: "ConstellationFallback"
    - from: "src/game/ConstellationFallback.js"
      to: "src/data/experience.js"
      via: "props.experiences[i].bullets[lang].map render"
      pattern: "bullets\\[lang\\]"
---

<objective>
Ship the MVP Slice 1 of Phase 15: the orchestrator (`GameMode`) replaces the Phase 14 placeholder in `App.js`, renders the bilingual derived-H1 + sub-copy above a renderer slot, and ALWAYS renders the sr-only semantic experience fallback outside the ErrorBoundary. Slice 1 deliberately ships a renderer placeholder (a sized `<div>` showing `t.game.empty` styled copy) so that the slice is end-to-end without the SVG: visitor lands → sees H1 with live numbers → sees crawlable career content via sr-only fallback → SR/ATS get the full content regardless of any later renderer behavior.

Purpose: Establish the orchestrator + SEO/SR safety net first. This unblocks Slices 2/3 (renderer + interaction) and guarantees that even if SVG rendering breaks later, the recruiter/ATS/SR experience is intact. Locks the H1 derivation contract per D-15-LAND-COPY/ATS so Slice 2 only adds the SVG canvas.

Output: `src/game/GameMode.js`, `src/game/ConstellationFallback.js`, their colocated tests, extended `game.*` translation keys, and the `App.js` plug-point swap from placeholder to lazy `<GameMode />`. Slice ends with all 71+ Vitest tests green and `npm run build` passing.
</objective>

## Phase Goal

**As a** recruiter or ATS crawler, **I want to** land on the portfolio and immediately see Carlos's full bilingual career content plus a derived "19 years / 26 skills" headline, **so that** I can absorb the substance of his experience even if the interactive constellation never renders for me.

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-CONTEXT.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-RESEARCH.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-UI-SPEC.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-PATTERNS.md
@.planning/phases/14-foundation-data-layer-viewmode-toggle-test-infra/14-01-SUMMARY.md
@.planning/phases/14-foundation-data-layer-viewmode-toggle-test-infra/14-02-SUMMARY.md
@CLAUDE.md
@src/App.js
@src/context/ViewModeContext.js
@src/i18n/translations.js
@src/game/constellation.graph.js
@src/game/constellation.graph.test.js
@src/data/experience.js
@src/data/skills.js
@src/components/Experience.js
</context>

<interfaces>
<!-- Contracts and exports needed for this plan. Extracted from Phase 14 outputs + ViewMode wiring. -->
<!-- Executor uses these directly — no codebase exploration needed for these surfaces. -->

From `src/game/constellation.graph.js`:
```js
export function buildConstellationGraph(experience, skills) // returns { nodes, edges }
//   nodes: [{ id, label, category, color, count, period: { start, end }, experienceIndices }, ...]
//   edges: [{ source, target, weight }, ...]
export const CURRENT_YEAR = 2026
```

From `src/data/skills.js`:
```js
export const SKILLS // { [canonicalKey]: { category, color, aliases } } — 26 canonical keys
export const SKILL_CATEGORIES // { [k]: { en, es, color } } — 8 categories
export const SKILL_CATEGORY_COLORS // legacy alias surface; same hex palette
```

From `src/data/experience.js`:
```js
export default EXPERIENCE // 12 entries, each: { title:{en,es}, company, date:{en,es}, location:{en,es}, bullets:{en,es:[...]}, tech:[...], period:{ start, end|null }, featured }
```

From `src/i18n/LanguageContext.js`:
```js
export function useLanguage() // returns { lang: 'en'|'es', t: translations[lang], setLang }
```

From `src/context/ViewModeContext.js`:
```js
export function useViewMode() // returns { viewMode: 'game'|'dev', setViewMode, toggleViewMode }
```

From `src/i18n/ThemeContext.js`:
```js
export function useTheme() // returns { theme: 'dark'|'light', setTheme, toggleTheme }
```

Existing `src/i18n/translations.js` `game.*` block (lines 24–29 EN, 206–211 ES, per 15-PATTERNS.md):
```js
game: { loadingTitle, loadingBody, empty, error }
```

App.js plug point: `src/App.js` lines 40–53 (`MainContent`'s `viewMode === 'game'` branch — current placeholder section).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 1, RED→GREEN): Extend game.* translation keys + write ConstellationFallback + tests</name>
  <files>src/i18n/translations.js, src/game/ConstellationFallback.js, src/game/ConstellationFallback.test.js</files>
  <read_first>
    - src/i18n/translations.js (full file — must locate existing `game:` blocks at EN/ES sections without disturbing surrounding keys)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-UI-SPEC.md (§ARIA Contract lines 488-531, §Copywriting Contract lines 783-806, §sr-only Fallback Contract lines 712-779)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-PATTERNS.md (§`src/i18n/translations.js` lines 445-495, §`src/game/ConstellationFallback.js` lines 275-308, §ConstellationFallback.test.js lines 382-405)
    - src/components/Experience.js (bilingual `job[lang]` field access pattern — `job.date[lang]`, `job.title[lang]`, `job.bullets[lang]` lines 60-90)
    - src/data/experience.js (verify shape of `bullets[lang]`, `date[lang]`, `location[lang]`, `title[lang]`, `tech[]`)
  </read_first>
  <behavior>
    - ConstellationFallback receives `{ experiences, lang, t }` props. NO hook calls.
    - Renders `<section aria-labelledby="constellation-fallback-heading" className="sr-only">`.
    - Section heading `<h2 id="constellation-fallback-heading">{t.game.fallbackHeading}</h2>`.
    - Ordered `<ol>` of all entries in array order (most recent first — `EXPERIENCE` is already ordered).
    - Each `<li>` contains an `<article>` with: `<h3>{exp.title[lang]} — {exp.company}</h3>`, `<p>{exp.date[lang]} · {exp.location[lang]}</p>`, `<ul>` of `<li>{bullet}</li>` from `exp.bullets[lang]`, `<p>{exp.tech.join(', ')}</p>`.
    - Test: renders all 12 experiences when given the live `EXPERIENCE` array (count `<li>` direct children of the `<ol>` = 12).
    - Test: heading text matches `t.game.fallbackHeading` in EN ("Full career experience") when `lang='en'`.
    - Test: heading text matches in ES ("Experiencia profesional completa") when `lang='es'`.
    - Test: when `lang='es'`, the first experience's bullets render the ES strings (assert at least one Spanish bullet substring is in the DOM).
    - Test: missing or null `experiences` prop renders nothing (returns null) — no crash. Use `expect(container.firstChild).toBeNull()`.
    - Translation keys added under `game.*` (EN + ES blocks, both):
      - `constellationLabel` (EN: "Interactive skill constellation. Use arrow keys to navigate between skills. Press Enter or Space to select a skill." / ES: "Constelación interactiva de habilidades. Usa las teclas de flecha para navegar entre habilidades. Presiona Enter o Espacio para seleccionar.")
      - `constellationRoleDesc` (EN: "skill constellation" / ES: "constelación de habilidades")
      - `subCopy` (EN: "Click a star to explore where Carlos used it." / ES: "Haz clic en una estrella para explorar dónde la usó Carlos.")
      - `fallbackHeading` (EN: "Full career experience" / ES: "Experiencia profesional completa")
      - `h1Years` (EN: "years" / ES: "años")
      - `h1Skills` (EN: "skills" / ES: "skills")
      - `h1Tagline` (EN: "One constellation." / ES: "Una constelación.")
      - Existing keys (`loadingTitle`, `loadingBody`, `empty`, `error`) MUST be preserved unchanged.
  </behavior>
  <action>
    Per D-15-SEO-DEFAULTS (active-lang only) and D-15-LAND-COPY (bilingual H1 keys derived at render time), do the following exactly:

    1. **RED — write `src/game/ConstellationFallback.test.js` first** (per CLAUDE.md TDD rule). Use the pattern at 15-PATTERNS.md §ConstellationFallback.test.js lines 386-404. Import the helpers from `@testing-library/react` and `vitest`. Import the live `EXPERIENCE` from `../data/experience.js` and the live `translations` object from `../i18n/translations.js`. Tests required:
       - `it('renders heading in active EN lang')` — render with `lang='en'`, assert `getByText('Full career experience')` is in the document via the `<h2>` matched by `getByRole('heading', { name: 'Full career experience' })`.
       - `it('renders heading in active ES lang')` — same with `lang='es'` and the ES heading.
       - `it('renders all 12 experience entries in document order')` — assert `container.querySelectorAll('section.sr-only > ol > li').length === EXPERIENCE.length` AND `EXPERIENCE.length === 12`.
       - `it('renders bullets in active language ES')` — render with `lang='es'`; assert that the first experience's first Spanish bullet text (read from `EXPERIENCE[0].bullets.es[0]`) appears in the DOM.
       - `it('renders nothing when experiences is null')` — `const { container } = render(<ConstellationFallback experiences={null} lang='en' t={...} />)` → `expect(container.firstChild).toBeNull()`.
       Provide `t` as `translations.en` / `translations.es` directly (post-extension) — the test imports `translations` and indexes by lang. Run `npm run test:run` and confirm tests fail (module not found OR keys not yet added — RED).

    2. **GREEN step A — add the 7 translation keys** to `src/i18n/translations.js`. Locate the EN `game:` object and merge the new keys without disturbing `loadingTitle`/`loadingBody`/`empty`/`error`. Repeat for the ES `game:` object. Follow the existing camelCase + comma-free style (no trailing commas — `comma-dangle: 0`). Verify with `rg -c "fallbackHeading" src/i18n/translations.js` → returns `2`.

    3. **GREEN step B — write `src/game/ConstellationFallback.js`**. Default export a pure functional component `function ConstellationFallback({ experiences, lang, t })`. Guard `if (!experiences) return null`. Return:
       ```
       <section aria-labelledby="constellation-fallback-heading" className="sr-only">
         <h2 id="constellation-fallback-heading">{t.game.fallbackHeading}</h2>
         <ol>
           {experiences.map((exp, i) => (
             <li key={`${exp.company}-${i}`}>
               <article>
                 <h3>{exp.title[lang]} — {exp.company}</h3>
                 <p>{exp.date[lang]} · {exp.location[lang]}</p>
                 <ul>
                   {exp.bullets[lang].map((b, j) => <li key={j}>{b}</li>)}
                 </ul>
                 <p>{exp.tech.join(', ')}</p>
               </article>
             </li>
           ))}
         </ol>
       </section>
       ```
       Use the EXACT JSX shape from 15-UI-SPEC.md §sr-only Fallback Contract lines 736-767 (one deviation allowed: `key={`${exp.company}-${i}`}` so duplicate companies do not collide). NO semicolons. NO trailing commas. JSX in `.js` (no `.jsx`). NO `React` import needed unless eslint requires (React 17+ JSX transform handles it; check existing `Experience.js` — it imports React; mirror that).

    4. Run `npm run test:run`. Expect new tests to pass and existing 57 tests still green (total 62 expected after this task; the next task adds the GameMode tests).

    5. Per CLAUDE.md TDD gate: commit RED first (`test(15-01): add failing ConstellationFallback tests + game.* key extensions`) then GREEN (`feat(15-01): ConstellationFallback sr-only semantic experience list + 7 game.* translation keys`).
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | tee /tmp/15-01-task1.log && grep -E "Tests\s+\d+\s+passed" /tmp/15-01-task1.log && rg -c "fallbackHeading" src/i18n/translations.js | grep -E "^2$" && rg -c "constellationLabel" src/i18n/translations.js | grep -E "^2$" && rg -l "h1Tagline" src/i18n/translations.js</automated>
    <automated>node -e "const t = require('./src/i18n/translations.js'); const en = t.translations?.en || t.default?.en; console.log(en.game.h1Tagline === 'One constellation.' ? 'OK' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/game/ConstellationFallback.js` exists and default-exports a pure functional component with the exact prop signature `{ experiences, lang, t }`.
    - `src/game/ConstellationFallback.test.js` exists with 5 tests as specified; all pass.
    - `src/i18n/translations.js` contains all 7 new keys in BOTH `en.game` and `es.game` blocks. Existing 4 keys (`loadingTitle`, `loadingBody`, `empty`, `error`) preserved verbatim.
    - `npm run test:run` exits 0 with total count = 57 (Phase 14) + 5 (new) = 62 passing tests.
    - `rg -n "^;" src/game/ConstellationFallback.js` returns no matches (no-semi rule).
    - `fd -e jsx src/` returns no `.jsx` files (JSX-in-.js convention).
    - Two separate commits exist in `git log --oneline -3`: RED commit message starts with `test(15-01)`, GREEN with `feat(15-01)`.
  </acceptance_criteria>
  <done>ConstellationFallback shippable as a pure presentational sr-only component; the 7 new translation keys are wired into the bilingual dictionary; tests prove rendering + language switching; TDD gate satisfied with explicit RED→GREEN commits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1, RED→GREEN): Write GameMode orchestrator + H1 derivation + ErrorBoundary + tests</name>
  <files>src/game/GameMode.js, src/game/GameMode.test.js</files>
  <read_first>
    - .planning/phases/15-accessible-constellation-seo-fallback/15-PATTERNS.md (§`src/game/GameMode.js` lines 30-114, §`src/game/GameMode.test.js` lines 311-346)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-UI-SPEC.md (§Layout Contract lines 640-687, §Active-State Contract lines 213-225, §Error / Empty State Contract lines 809-828, §ARIA Contract H1 assembly lines 534-565)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-RESEARCH.md (§Pattern 5 usePrefersReducedMotion lines 372-394, §Pattern 6 detectCapabilities lines 398-418, §Pattern 7 ErrorBoundary lines 420-437, §H1 Derivation lines 544-560, §Code Examples lines 649-666)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-CONTEXT.md (D-15-LAND-COPY lines 43-49, D-15-LAND-ATS line 50, decisions block lines 30-72)
    - src/App.js (current placeholder pattern at lines 40-53 — the slot we are filling)
    - src/context/ViewModeContext.js (existing window guard pattern + hook export — to mirror in capability detection)
    - src/game/constellation.graph.js (exported `buildConstellationGraph` + `CURRENT_YEAR`)
    - src/game/constellation.graph.test.js (test file pattern for assertion style on live data)
  </read_first>
  <behavior>
    - `GameMode` is a default-exported functional React component, no props.
    - Calls `useLanguage()` for `{ lang, t }`, `useTheme()` for `{ theme }`, `useViewMode()` for `{ setViewMode }`.
    - Derives `yearsActive = Math.max(...EXPERIENCE.map(e => e.period.end ?? CURRENT_YEAR)) - Math.min(...EXPERIENCE.map(e => e.period.start))` at module level (so it's a constant per build; values: 19).
    - Derives `skillCount = buildConstellationGraph(EXPERIENCE, SKILLS).nodes.length` at module level (value: 26).
    - Renders a `<section id="game-mode" className="min-h-screen flex flex-col items-center px-6 pt-12 pb-8">`.
    - Inside, a `<div className="text-center mb-8 md:mb-12 max-w-2xl">` containing:
      - `<h1 className="text-2xl md:text-4xl font-bold text-text-primary leading-tight mb-3">` with text: `` `${yearsActive} ${t.game.h1Years}. ${skillCount} ${t.game.h1Skills}. ${t.game.h1Tagline}` ``
      - `<p className="text-base text-text-secondary leading-relaxed">{t.game.subCopy}</p>`
    - A renderer slot wrapper `<div className="w-full max-w-3xl relative" data-testid="renderer-slot">` — for Slice 1, render a placeholder `<p className="text-text-secondary text-sm py-12 text-center" data-testid="renderer-placeholder">{t.game.empty}</p>`. Slice 2 replaces this placeholder body with `<SvgConstellation ... />`. The wrapper element and `data-testid="renderer-slot"` MUST exist now so Slice 2 can target it.
    - `<ConstellationFallback experiences={EXPERIENCE} lang={lang} t={t} />` rendered AFTER the renderer wrapper, INSIDE the `<section>` BUT OUTSIDE `ConstellationErrorBoundary`. Per 15-PATTERNS.md and 15-RESEARCH.md Pattern 7: fallback survives renderer errors.
    - A `ConstellationErrorBoundary` class component (defined in same file or local) wraps ONLY the renderer slot. Fallback UI = `<p className="text-text-secondary text-base text-center py-8">{t.game.error} <button onClick={() => setViewMode('dev')} className="text-brand underline">{t.nav.modeDev}</button></p>`. For Slice 1, the boundary wraps the placeholder `<p>` (boundary is in place even though placeholder won't throw).
    - Empty-graph guard: if `skillCount === 0`, render `<p className="sr-only">{t.game.empty}</p>` AND a visible centered `<p>` with the same text inside the renderer slot; do NOT crash.
    - Test: `it('renders H1 with yearsActive=19 derived from live data')` — render `<GameMode />` wrapped in all three providers (Theme + Language + ViewMode); assert the H1 textContent includes `19 years.` (with `lang='en'`). Setup a small render helper in the test file.
    - Test: `it('renders H1 with skillCount=26 derived from live data')` — same render; assert H1 textContent includes ` 26 skills.`.
    - Test: `it('renders H1 in Spanish when lang=es')` — render with provider that initializes `lang='es'` (use `useLanguage()` initial — set localStorage key `cam-lang` to `es` in beforeEach). Assert textContent includes `años` AND `Una constelación.`.
    - Test: `it('renders ConstellationFallback outside the error boundary')` — assert `getByRole('heading', { name: 'Full career experience' })` is present in document.
    - Test: `it('renders renderer slot with data-testid="renderer-slot"')` — `getByTestId('renderer-slot')` resolves; this slot is the Slice 2 plug point.
    - Test: `it('h1 derivation matches live data')` — pure assertion (no render): `expect(yearsActive).toBe(19); expect(skillCount).toBe(26)`. (Export the derivation as named constants for testability OR import from a shared `h1.js` helper — choose the simpler: export `yearsActive` and `skillCount` as named exports from `GameMode.js` alongside the default.)
  </behavior>
  <action>
    Per D-15-LAND-COPY (mandatory live derivation, never hardcoded) and D-15-LAND-ATS (H1 above renderer, always in DOM), do the following exactly:

    1. **RED — write `src/game/GameMode.test.js` first**. File pattern from 15-PATTERNS.md lines 311-346. Imports: `describe, it, expect, beforeEach` from `vitest`; `render, screen` from `@testing-library/react`; `LanguageProvider` from `../i18n/LanguageContext`; `ThemeProvider` from `../i18n/ThemeContext`; `ViewModeProvider` from `../context/ViewModeContext`; default + named `{ yearsActive, skillCount }` from `./GameMode`; `EXPERIENCE` from `../data/experience` (default export); `SKILLS` from `../data/skills`; `buildConstellationGraph, CURRENT_YEAR` from `./constellation.graph`. Build a local helper `function renderWithProviders(ui, { lang = 'en' } = {})` that sets `localStorage.setItem('cam-lang', lang)` BEFORE rendering, then wraps in `<ThemeProvider><LanguageProvider><ViewModeProvider>{ui}</ViewModeProvider></LanguageProvider></ThemeProvider>`. Add `beforeEach(() => { localStorage.clear() })`. Write the 6 tests listed in <behavior>. For the matchMedia-dependent path, add `beforeEach(() => { window.matchMedia = vi.fn().mockImplementation(q => ({ matches: false, media: q, onchange: null, addEventListener: vi.fn(), removeEventListener: vi.fn(), addListener: vi.fn(), removeListener: vi.fn(), dispatchEvent: vi.fn() })) })` (matchMedia is needed because GameMode will eventually call `usePrefersReducedMotion`; even though Slice 1 doesn't need it, add the mock proactively so Slice 2/3 don't break this test file).

       Run `npm run test:run`. Expect all 6 new tests to fail (`Cannot resolve module ./GameMode`). Commit: `test(15-01): add failing GameMode orchestrator tests (H1 derivation, fallback presence, renderer slot)`.

    2. **GREEN — write `src/game/GameMode.js`**.

       Top-level imports (mirror 15-PATTERNS.md §GameMode.js imports lines 35-47):
       - `import React from 'react'`
       - `import { useLanguage } from '../i18n/LanguageContext'`
       - `import { useTheme } from '../i18n/ThemeContext'`
       - `import { useViewMode } from '../context/ViewModeContext'`
       - `import EXPERIENCE from '../data/experience'`
       - `import { SKILLS } from '../data/skills'`
       - `import { buildConstellationGraph, CURRENT_YEAR } from './constellation.graph'`
       - `import ConstellationFallback from './ConstellationFallback'`

       Note: Slice 1 does NOT import `SvgConstellation` or `useConstellation` — Slice 2 will add those.

       Module-level derivation (export as named):
       ```
       export const yearsActive =
         Math.max(...EXPERIENCE.map(e => e.period.end ?? CURRENT_YEAR)) -
         Math.min(...EXPERIENCE.map(e => e.period.start))

       const { nodes: __graphNodes } = buildConstellationGraph(EXPERIENCE, SKILLS)
       export const skillCount = __graphNodes.length
       ```

       Class component (use the EXACT shape from 15-RESEARCH.md Pattern 7 lines 425-433):
       ```
       class ConstellationErrorBoundary extends React.Component {
         constructor(props) { super(props); this.state = { hasError: false } }
         static getDerivedStateFromError() { return { hasError: true } }
         componentDidCatch(error, info) {
           // eslint-disable-next-line no-console
           console.error('Constellation error:', error, info)
         }
         render() {
           if (this.state.hasError) return this.props.fallback
           return this.props.children
         }
       }
       ```

       Default export `function GameMode()`:
       - Read `{ lang, t } = useLanguage()`, `{ theme } = useTheme()`, `{ setViewMode } = useViewMode()`. `theme` is intentionally read here (will be passed to SvgConstellation in Slice 2) — silence any unused-var lint with a usage like passing it to the renderer slot's `data-theme` attribute.
       - Build the H1 text: `const h1Text = `${yearsActive} ${t.game.h1Years}. ${skillCount} ${t.game.h1Skills}. ${t.game.h1Tagline}``
       - Build error fallback element via a const: `const errorFallback = (<p ...>{t.game.error} <button ...>{t.nav.modeDev}</button></p>)` using the EXACT className strings from 15-UI-SPEC.md §Error/Empty State Contract lines 819-828.
       - Return the structure from 15-UI-SPEC.md §Layout Contract lines 643-686 BUT in Slice 1 the renderer slot body is `<p data-testid="renderer-placeholder" className="text-text-secondary text-sm py-12 text-center">{t.game.empty}</p>` inside `<ConstellationErrorBoundary fallback={errorFallback}>...</ConstellationErrorBoundary>`. The wrapping `<div className="w-full max-w-3xl relative" data-testid="renderer-slot" data-theme={theme}>` MUST be present.
       - `<ConstellationFallback experiences={EXPERIENCE} lang={lang} t={t} />` is the LAST child of the `<section>` (after the renderer-slot div), so it stays outside the boundary.

       Skip ALL of the following — they belong to Slice 2 or Slice 3, NOT Slice 1:
       - capability detection (`detectCapabilities`, `usePrefersReducedMotion`) — added in Slice 2
       - `useConstellation` hook usage — added in Slice 2
       - `SvgConstellation` import / render — added in Slice 2
       - role=application wrapper, roving tabindex, aria-live region — added in Slice 3

       Code style: no semicolons, no trailing commas, JSX in `.js`. Two-space indent. Run `npm run lint` to confirm zero airbnb violations.

       Run `npm run test:run`. Expect all 6 new tests to pass. Commit: `feat(15-01): GameMode orchestrator with derived H1 + ErrorBoundary + sr-only fallback hook-in`.

    3. The expected total test count after this task: 62 (from Task 1) + 6 (new) = 68 passing.
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | tee /tmp/15-01-task2.log && grep -E "Tests\s+68\s+passed" /tmp/15-01-task2.log</automated>
    <automated>rg -n "export const yearsActive" src/game/GameMode.js && rg -n "export const skillCount" src/game/GameMode.js && rg -n "ConstellationErrorBoundary" src/game/GameMode.js | head -3</automated>
    <automated>node -e "const m = require('./src/game/GameMode.js'); console.log('yearsActive='+m.yearsActive,'skillCount='+m.skillCount)"</automated>
    <automated>rg -n "^;|;\s*$" src/game/GameMode.js src/game/GameMode.test.js | grep -v "^\s*//" || echo "no-semi OK"</automated>
  </verify>
  <acceptance_criteria>
    - `src/game/GameMode.js` exports `default GameMode`, named `yearsActive` (=19), and named `skillCount` (=26) — derivations evaluated at module load against live `EXPERIENCE` and `SKILLS`.
    - `src/game/GameMode.test.js` has 6 tests; all pass; uses `renderWithProviders` helper and `localStorage` setup pattern.
    - No `import SvgConstellation` or `import useConstellation` in `GameMode.js` (those land in Slice 2).
    - The renderer slot exists with `data-testid="renderer-slot"` (Slice 2 plug point) and contains `<ConstellationErrorBoundary>` wrapping a placeholder `<p data-testid="renderer-placeholder">`.
    - `<ConstellationFallback ...>` is rendered as a sibling of the renderer-slot `<div>` (verified by DOM order: querying `section > .w-full ~ section.sr-only` returns the fallback).
    - `npm run test:run` exits 0 with 68 passing tests.
    - `npm run lint` exits 0 (airbnb + react/recommended clean).
    - Two commits in `git log --oneline -3`: RED `test(15-01)`, GREEN `feat(15-01)`.
  </acceptance_criteria>
  <done>The orchestrator is in place with a stable plug-in surface for Slice 2's renderer; H1 numbers are live-derived per D-15-LAND-COPY; sr-only fallback is reachable for SR/ATS/crawlers regardless of any future renderer error; ErrorBoundary is wired but inert (no renderer to throw yet).</done>
</task>

<task type="auto">
  <name>Task 3 (Wave 1): Replace App.js placeholder with lazy-loaded GameMode + smoke test</name>
  <files>src/App.js</files>
  <read_first>
    - src/App.js (full file — current placeholder lives at lines 40-53; lazy-import pattern exists at lines 11-18)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-PATTERNS.md (§`src/App.js` lines 577-614)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-UI-SPEC.md (§Layout Contract lines 640-687 — the layout that GameMode now ships)
    - .planning/phases/14-foundation-data-layer-viewmode-toggle-test-infra/14-02-SUMMARY.md (Note for Phase 15 line 132 — the plug point definition)
  </read_first>
  <behavior>
    - `src/App.js` adds a new lazy import `const GameMode = React.lazy(() => import('./game/GameMode'))` alongside the existing lazy imports (lines 11-18).
    - `MainContent` `viewMode === 'game'` branch replaced from the current placeholder `<section>` block to:
      ```
      if (viewMode === 'game') {
        return (
          <main id="main">
            <Suspense fallback={SectionFallback}>
              <GameMode />
            </Suspense>
          </main>
        )
      }
      ```
    - Existing `SectionFallback` (line 20) is REUSED — no new fallback component.
    - Dev-mode branch (lines 55-79) is UNCHANGED.
    - `t.game.loadingTitle` / `t.game.loadingBody` keys are still present in `translations.js` (Task 1 preserved them); they are now used by the Suspense fallback only if the JS chunk loads slowly. The `SectionFallback` is the existing visual placeholder (a `py-20` `div`) — that's the intended UX during chunk load.
    - After change: in a real browser at `/` with default `viewMode='game'`, the user sees `<GameMode />` (H1 + renderer placeholder + sr-only fallback) instead of the old `loadingTitle` heading.
    - No new test file added for App.js this task; existing ViewModeToggle + ViewModeContext component tests already exercise viewMode branching at the integration level. A smoke test is added: a new test inside `GameMode.test.js` (extend Task 2's file) `it('mounts inside Suspense when imported lazily')` is optional — skip unless straightforward; smoke is satisfied by `npm run build` succeeding.
  </behavior>
  <action>
    Per the Phase 14 plug-point spec (14-02-SUMMARY.md line 132) and 15-PATTERNS.md §App.js, do the following exactly:

    1. Open `src/App.js`. After the existing lazy import block (lines 11-18), add a single line: `const GameMode = React.lazy(() => import('./game/GameMode'))`. Order it logically: insert it AFTER the dev-mode component imports OR group it under a comment header — choose the cleaner placement based on the file's current spacing. Mirror the no-semicolon, single-line style of the surrounding lazy imports.

    2. Locate `MainContent` (lines 36-80). Replace lines 40-53 (the entire game branch) with:
       ```
       if (viewMode === 'game') {
         return (
           <main id="main">
             <Suspense fallback={SectionFallback}>
               <GameMode />
             </Suspense>
           </main>
         )
       }
       ```
       Do NOT remove the `const { t } = useLanguage()` line at line 38 — it is still used by SkipLink semantically and the dev branch's nothing-but-leave-it-untouched. Actually CHECK: if `t` is only used by the removed `t.game.loadingTitle` / `t.game.loadingBody`, then `t` becomes unused and airbnb-eslint will flag `no-unused-vars`. In that case, change the destructure to drop `t`: `function MainContent() { const { viewMode } = useViewMode() ... }` and delete the `const { t } = useLanguage()` line. Run `npm run lint` after the change and adjust if any unused-import warning appears.

    3. Do NOT touch the dev-mode branch (`return (<main id="main"> ... <Suspense>...</Suspense> ... </main>)`).

    4. Run `npm run test:run` — all 68 tests must still pass (no regression). Run `npm run build` — must exit 0 (verifies the lazy chunk splits without bundler errors). Run `npm run lint` — must exit 0.

    5. Single commit: `feat(15-01): replace App.js game-mode placeholder with lazy-loaded GameMode (Slice 1 wire)`.
  </action>
  <verify>
    <automated>rg -n "React\.lazy\(\(\) => import\('\./game/GameMode'\)\)" src/App.js | grep -v '^#' | grep -c lazy</automated>
    <automated>rg -n "loadingTitle|loadingBody" src/App.js | grep -v '^#' || echo "placeholder removed OK"</automated>
    <automated>npm run test:run 2>&1 | tee /tmp/15-01-task3.log && grep -E "Tests\s+68\s+passed" /tmp/15-01-task3.log</automated>
    <automated>npm run build 2>&1 | tail -20 | grep -E "built in|Done|✓"</automated>
    <automated>npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.js` contains exactly one `React.lazy(() => import('./game/GameMode'))` declaration.
    - `src/App.js` no longer references `t.game.loadingTitle` or `t.game.loadingBody`.
    - The `viewMode === 'game'` branch returns `<main id="main"><Suspense fallback={SectionFallback}><GameMode /></Suspense></main>`.
    - `npm run test:run` exits 0 with 68 passing tests (no test count change vs Task 2).
    - `npm run build` exits 0; the build emits a separate chunk for `GameMode.js` (verifiable via `ls -la dist/assets/ | grep -i game` showing at least one `GameMode-*.js`).
    - `npm run lint` exits 0.
    - One commit in `git log --oneline -1`: `feat(15-01): replace App.js game-mode placeholder with lazy-loaded GameMode (Slice 1 wire)`.
  </acceptance_criteria>
  <done>The Slice 1 vertical is shippable: visitor lands at `/`, the JS chunk loads, `<GameMode />` renders the bilingual derived-H1 + sub-copy + the renderer-slot placeholder + the always-DOM-present sr-only experience fallback. Slice 2 has a stable mount point (`data-testid="renderer-slot"`) to plug the SVG renderer into.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client React tree | All Slice 1 work is pure render; no network, no auth, no PII writes. `experience.js` content originates from this repo and is committed source. |
| i18n string boundary | Translation strings are imported from a static `.js` module; no dynamic interpolation of user input. |
| SR/ATS DOM boundary | `sr-only` content is exposed to assistive tech and crawlers; intentionally NOT visible to sighted users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-S | Spoofing | sr-only fallback content (ATS could interpret hidden content as cloaking) | mitigate | Active-lang only (per D-15-SEO-DEFAULTS + 15-RESEARCH.md §sr-only SEO/ATS Policy): hidden content matches visible language, no discrepancy with visible page. No prompt-injection-style payloads in `experience.js` (committed source). |
| T-15-01-T | Tampering | translations.js + experience.js (build-time data) | accept | Source-controlled; no runtime mutation path. Standard repo trust. |
| T-15-01-I | Information Disclosure | sr-only DOM dump of full career content | accept | Same career content is already visible in dev-view Experience.js. No new disclosure surface. |
| T-15-01-XSS | XSS | bullet rendering via `{exp.bullets[lang].map((b, j) => <li>{b}</li>)}` | mitigate | React escapes text children by default. NO `dangerouslySetInnerHTML` introduced. Bullets are plain strings in `experience.js`; about-section's `dangerouslySetInnerHTML` (T-Phase14-XSS, accepted) is unchanged. |
| T-15-01-D | DoS | rendering 12 entries × ~5-7 bullets ≈ 70 `<li>` elements in `sr-only` | accept | Trivial DOM size. No iteration explosion. |
| T-15-01-E | Elevation | ErrorBoundary fallback button calls `setViewMode('dev')` | mitigate | Allowlist enforced by Phase 14 `VALID_MODES`; same path used by ViewModeToggle. No privilege escalation. |
| T-15-01-CSP | CSP | No inline `<script>` or `<style>` introduced. SVG-inline JSX has no event handlers via string attrs. | mitigate | All event handlers are JSX `onClick`/`onKeyDown` props (CSP-safe). No `eval`. Inline styles allowed by existing CSP (Vite default — no CSP header set in production at this phase per Phase 11 deferral). |
</threat_model>

<verification>
- Total Vitest test count: **57 (Phase 14 baseline) + 5 (ConstellationFallback) + 6 (GameMode) = 68 passing**. Any deviation requires investigation before Slice 2 begins.
- `npm run build` exits 0 and emits a separate `GameMode-*.js` lazy chunk (confirms Slice 2 will be added to a code-split point, not the main bundle).
- `npm run lint` exits 0 (airbnb + react/recommended clean).
- Manual smoke (optional, not gating): open `npm start`, default landing renders bilingual H1 with "19 years. 26 skills.", switch lang via Nav pill → re-renders ES "19 años. 26 skills. Una constelación."; the sr-only fallback updates language alongside (verify with browser devtools "Inspect Element" on the hidden section).
- `rg -c "fallbackHeading|constellationLabel|h1Tagline" src/i18n/translations.js` returns 6 (3 keys × 2 langs).
</verification>

<success_criteria>
1. `GameMode.js` mounts via lazy import inside the existing `Suspense` boundary in `App.js`; default landing path renders without runtime errors.
2. H1 derivation: `yearsActive === 19` AND `skillCount === 26` against the live `EXPERIENCE` + `SKILLS` data; numbers are NEVER hardcoded (verified by unit test reading the same derivation logic).
3. `ConstellationFallback` is DOM-present whether or not the renderer slot has been wired (Slice 2 will fill the slot). Verified by RTL `getByRole('heading', { name: /Full career experience|Experiencia profesional completa/ })`.
4. All 7 new `game.*` translation keys exist in BOTH `en` and `es` blocks; existing 4 keys preserved.
5. `data-testid="renderer-slot"` is on a `<div>` inside `<ConstellationErrorBoundary>`; this slot is the stable Slice 2 plug point.
6. Zero `.jsx` files exist; no semicolons in new code; no trailing commas; airbnb-eslint clean.
7. Total Vitest test count = 68 passing.
</success_criteria>

<output>
Create `.planning/phases/15-accessible-constellation-seo-fallback/15-01-SUMMARY.md` when the plan completes, per the standard summary template. Key fields:
- `provides`: `GameMode orchestrator with derived H1`, `ConstellationFallback (active-lang sr-only)`, `game.* keys (constellationLabel, constellationRoleDesc, subCopy, fallbackHeading, h1Years, h1Skills, h1Tagline)`, `App.js lazy mount of GameMode at viewMode='game' branch`.
- `affects`: list the 6 `files_modified` exactly.
- `note`: Slice 2 plugs SvgConstellation into `data-testid="renderer-slot"` inside `<ConstellationErrorBoundary>`; sr-only fallback is sibling of slot and must stay outside the boundary.
</output>
