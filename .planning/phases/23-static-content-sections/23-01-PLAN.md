---
phase: 23-static-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/astro/About.astro
  - src/components/astro/About.test.ts
  - src/components/About.test.jsx
autonomous: true
requirements: [STATIC-01, TEST-01]

must_haves:
  truths:
    - "D-01: About.astro imports src/data/about.json directly in frontmatter with the same {en,es} field shape and the same pick(field, lang) resolver — zero data-shape changes"
    - "D-04: About.jsx's useCountUp/AnimatedValue/reduceMotion are NOT ported — About.astro renders the raw static value string (e.g. '18+ years') directly; count-up re-introduction is deferred to Phase 24's shared vanilla enhancer"
    - "D-06: About.test.ts is an Astro Container API test with '// @vitest-environment node' as its literal first line, asserting bilingual (en+es) content and ARIA/landmark presence, with no assertion-count regression vs About.test.jsx"
    - "D-07: About is the designated coverage-parity spot-check — About.test.ts ports all 7 it-blocks of About.test.jsx (label/heading/paragraphs, 5 quick-facts, ES translation, no-<strong> guard, schema sanity, no-'undefined' guard) before the Container API pattern is treated as validated for the remaining four components"
    - "About section renders correct EN and ES content at build time consuming about.json unchanged (STATIC-01)"
    - "About ships zero client-side JavaScript — no client: directive, no React import, no hooks"
  artifacts:
    - path: "src/components/astro/About.astro"
      provides: "Zero-JS About section, build-time bilingual render from about.json"
      contains: "section id=\"about\""
    - path: "src/components/astro/About.test.ts"
      provides: "Container API coverage-parity test for About (D-07 spot-check)"
      contains: "@vitest-environment node"
  key_links:
    - from: "src/components/astro/About.astro"
      to: "src/data/about.json"
      via: "frontmatter import"
      pattern: "import data from '../../data/about.json'"
    - from: "src/components/astro/About.test.ts"
      to: "src/components/astro/About.astro"
      via: "Container API renderToString with { props: { locale } }"
      pattern: "renderToString\\(About"
---

<objective>
Port `src/components/About.jsx` to a zero-client-JS `src/components/astro/About.astro`, consuming `src/data/about.json` unchanged, and author `src/components/astro/About.test.ts` as an Astro Container API test that ports every assertion of the existing `About.test.jsx` (the D-07 designated coverage-parity spot-check).

Purpose: About is the most content-varied of the five sections (paragraphs + quick-facts) and is the phase's validation gate — proving the real-content Container API testing pattern works before the remaining four components (Plans 23-02, 23-03) rely on it.
Output: `src/components/astro/` directory created; `About.astro` + `About.test.ts` committed; the old `About.test.jsx` removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-static-content-sections/23-CONTEXT.md
@.planning/phases/23-static-content-sections/23-PATTERNS.md

<interfaces>
<!-- Container API signature confirmed from node_modules/astro/dist/container/index.d.ts:
     renderToString(component, options?) where options.props is the props object.
     Astro components render as "partials" by default (no full <html> wrapper) — fine for a <section>. -->

Harness pattern (from src/pages/_404.test.ts, the only prior Container API test in the repo):
- Literal first line MUST be: // @vitest-environment node
- import { experimental_AstroContainer as AstroContainer } from 'astro/container'
- const container = await AstroContainer.create()
- const result = await container.renderToString(About, { props: { locale: 'en' } })
- Assert on the returned HTML string via result.toContain(...) / result.toMatch(/regex/) — NOT @testing-library/react screen queries.

About.astro props contract: receives `locale` via Astro.props (matches sibling components; do NOT use Astro.currentLocale inside the component so the Container API test can drive locale explicitly).

pick() resolver to port verbatim into frontmatter (from About.jsx lines 5-8):
  function pick(field, lang) {
    if (typeof field === 'string') return field
    return field?.[lang] ?? field?.en ?? ''
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create About.astro (zero-JS port, count-up dropped)</name>
  <files>src/components/astro/About.astro</files>
  <read_first>
    - src/components/About.jsx (source of markup, pick() resolver, and the useCountUp logic to DROP)
    - src/pages/404.astro (Astro frontmatter + template syntax analog)
    - src/data/about.json (data shape — do NOT modify)
    - .planning/phases/23-static-content-sections/23-PATTERNS.md (About.astro section: exact port guidance)
  </read_first>
  <behavior>
    - Renders `<section id="about">` with the label `pick(data.label, locale)`, `<h2>` `pick(data.h2, locale)`, and 3 paragraphs from `data.paragraphs`.
    - Renders an `<aside>` quick-facts panel with `pick(data.quick, locale)` heading and 5 `<li>` fact rows, each showing `pick(fact.kLabel, locale)` and `pick(fact.value, locale)`.
    - locale='en' produces 'About' / 'Who I am' / 'Quick facts' / 'Medellín, CO'; locale='es' produces 'Sobre mí' / 'Quién soy' / 'Datos rápidos' / 'Remoto'.
    - No React import, no useState/useEffect, no useCountUp/AnimatedValue — the value string renders verbatim (e.g. '18+ years').
  </behavior>
  <action>
    Create `src/components/astro/About.astro` (creating the `src/components/astro/` directory). In the frontmatter fence: `import data from '../../data/about.json'`; destructure `const { locale } = Astro.props`; copy the `pick(field, lang)` resolver verbatim from About.jsx lines 5-8; `const { facts } = data`. Port the JSX return (About.jsx lines 66-94) to Astro template markup: convert every `className` to `class`, drop all `key={...}` props, keep every Tailwind class string byte-identical (`font-pixel text-[10px] uppercase tracking-[1px] text-accent ...` etc.). Per D-04, DO NOT port About.jsx lines 10-43 (`reduceMotion`, `useCountUp`, `AnimatedValue`) — inline the `<Row>` markup (About.jsx lines 45-61) directly into the `facts.map(...)` `<li>` template and render `pick(f.value, locale)` as a plain string (no AnimatedValue wrapper). Call `pick(..., locale)` (the prop) everywhere the original called `pick(..., lang)`. Astro auto-escapes `{...}` expressions — do NOT use `set:html`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && test -f src/components/astro/About.astro</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/astro/About.astro` exists and `npm run build` completes without error.
    - `rg -c "useState|useEffect|useCountUp|AnimatedValue|client:" src/components/astro/About.astro` returns 0 (grep counts on a file with no `#` comment lines are safe here).
    - `rg -c "set:html" src/components/astro/About.astro` returns 0.
    - File contains `section id="about"` and `import data from '../../data/about.json'`.
  </acceptance_criteria>
  <done>About.astro renders the About section from about.json at build time with zero client JS and no count-up logic (D-01, D-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author About.test.ts (Container API, D-07 coverage-parity spot-check) and remove old RTL test</name>
  <files>src/components/astro/About.test.ts, src/components/About.test.jsx</files>
  <read_first>
    - src/components/About.test.jsx (the 7 it-blocks to port for parity)
    - src/pages/_404.test.ts (Container API harness pattern + rationale for `// @vitest-environment node`)
    - src/data/about.json (schema-sanity assertions)
    - src/components/astro/About.astro (component under test, from Task 1)
  </read_first>
  <behavior>
    - Test 1: renders `<section id="about">` without throwing (result contains 'id="about"').
    - Test 2 (EN): result contains 'About', 'Who I am', and matches /Solutions Architect and Senior Backend Engineer/, /Spring Framework/, /wife Viky/.
    - Test 3 (EN quick-facts): result contains 'Location','Current role','Experience','Languages','Work mode','Medellín, CO','Solution Architect @ Soldife','ES · EN','Remote friendly'; result matches /\d+\+ years/ (static value present — intent shifted from 'count-up settled' per D-04).
    - Test 4 (ES): result contains 'Sobre mí','Quién soy','Datos rápidos','Ubicación','Rol actual','Remoto'; matches /\+\d+ años/.
    - Test 5 (no dangerouslySetInnerHTML equivalent): result does NOT contain '<strong>'.
    - Test 6 (schema sanity): about.json has 3 paragraphs + 5 facts, each with en/es keys (data-only, no render).
    - Test 7 (regression guard): EN result contains 'Quick facts' and does NOT contain 'undefined'.
  </behavior>
  <action>
    Create `src/components/astro/About.test.ts` with `// @vitest-environment node` as the literal first line (required — esbuild's startup invariant fails under the repo's jsdom default per _404.test.ts lines 22-29). Import `experimental_AstroContainer as AstroContainer` from `'astro/container'`, `{ describe, expect, it }` from `'vitest'`, `About` from `'./About.astro'`, and `data` from `'../../data/about.json'`. In each render-based `it`, call `await AstroContainer.create()` then `await container.renderToString(About, { props: { locale: 'en' } })` (or `'es'`), and convert each `screen.getByText('X')` from About.test.jsx into `expect(result).toContain('X')` and each `toMatch(/re/)` into `expect(result).toMatch(/re/)`. Port ALL 7 it-blocks listed in `<behavior>` (D-07 parity — no assertion-count regression). Then delete `src/components/About.test.jsx` (its Astro replacement now covers it; no stale duplicate left behind per D-06/CONTEXT Claude's-discretion rule).
  </action>
  <verify>
    <automated>npx vitest run src/components/astro/About.test.ts && test ! -f src/components/About.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/astro/About.test.ts` passes with >= 7 `it` blocks GREEN.
    - Line 1 of About.test.ts is exactly `// @vitest-environment node`.
    - `src/components/About.test.jsx` no longer exists.
    - `npx vitest run` (full suite) stays GREEN (no regression in other suites).
  </acceptance_criteria>
  <done>About has a passing Container API test with parity to its former RTL spec (D-06, D-07); old RTL test removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time data → rendered HTML | `about.json` (committed, not user input) is interpolated into HTML at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Tampering (XSS) | About.astro `{...}` interpolation | mitigate | Astro auto-escapes all `{expr}` output; no `set:html` used (grep-gated in Task 1 acceptance). Content originates from committed `about.json`, not runtime user input. |
| T-23-01-02 | Information disclosure | about.json contents | accept | Data is intentionally-public portfolio copy; no PII/secrets rendered. |
| T-23-01-SC | Tampering (supply chain) | npm installs | accept | No package installs in this plan — `astro`/`vitest` already present in package.json. No new dependency surface. |
</threat_model>

<verification>
- `npm run build` produces a clean build with About.astro compiled.
- `npx vitest run src/components/astro/About.test.ts` GREEN with >= 7 it-blocks.
- Full `npx vitest run` GREEN (About.test.jsx removal introduces no orphaned imports).
</verification>

<success_criteria>
- About renders bilingual content from about.json at build time with zero client JS (STATIC-01).
- About has a Container API test with coverage parity to its former RTL spec (TEST-01, D-07 validated).
- The `src/components/astro/` pattern is proven for the remaining four components.
</success_criteria>

<output>
Create `.planning/phases/23-static-content-sections/23-01-SUMMARY.md` when done.
</output>
