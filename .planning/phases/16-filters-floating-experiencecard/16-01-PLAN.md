---
phase: 16-filters-floating-experiencecard
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/game/filters.test.js
  - src/game/useConstellation.test.js
  - src/game/SkillFilters.test.js
  - src/game/ExperienceCard.test.js
  - src/game/renderers/SvgConstellation.test.js
  - src/game/GameMode.test.js
autonomous: true
requirements: [GAME-03, GAME-04]
tags: [phase-16, tests-first, tdd-red, filters, dialog]
must_haves:
  truths:
    - "Failing test suite scaffolds every Phase 16 behavior before any production code is written"
    - "filters.test.js covers all 6 selectors (filterByYearRange, filterBySkillIntersection, filterByCategory, composeFilters, yearBounds, visibleSkillIds) with AND semantics + honesty rule"
    - "useConstellation.test.js extends existing 10 tests with toggleSkill / setYearRange / setCategory / resetFilters / highlightedSkillIds derivation / isFilterActive"
    - "SkillFilters.test.js asserts chip toggle aria-pressed, dual-thumb slider arrow keys, Home/End jumps, dependent valuemin/max, reset disabled when no filter active"
    - "ExperienceCard.test.js asserts role=dialog + aria-modal + aria-labelledby, initial focus on heading, Esc → onClose, Tab cycle wrap, click-outside outside [data-game-interactive], CV CTA href per lang, locked-chip aria-disabled"
    - "SvgConstellation.test.js extends with highlightedSkillIds dim, yearRange dim, edge dim when both endpoints dimmed"
    - "GameMode.test.js extends with SkillFilters mount + ExperienceCard conditional mount on selectedSkillId"
    - "All new/extended tests are RED (failing) — production code arrives in waves 1–5 to flip them GREEN"
  artifacts:
    - path: "src/game/filters.test.js"
      provides: "Pure selector unit tests (RED)"
      contains: "describe('filterBySkillIntersection"
    - path: "src/game/useConstellation.test.js"
      provides: "Extended hook tests (RED for new behavior)"
      contains: "describe('useConstellation"
    - path: "src/game/SkillFilters.test.js"
      provides: "Filter UI component tests (RED)"
      contains: "role=group"
    - path: "src/game/ExperienceCard.test.js"
      provides: "Dialog component tests (RED)"
      contains: "role=dialog"
    - path: "src/game/renderers/SvgConstellation.test.js"
      provides: "Extended renderer tests (RED for highlightedSkillIds + yearRange consumers)"
      contains: "highlightedSkillIds"
    - path: "src/game/GameMode.test.js"
      provides: "Extended orchestrator tests (RED for filters + card mount)"
      contains: "SkillFilters"
  key_links:
    - from: "src/game/filters.test.js"
      to: "src/data/experience.js, src/data/skills.js"
      via: "import EXPERIENCE / SKILLS for fixture-free real-data testing"
      pattern: "import EXPERIENCE from '../data/experience"
---

<objective>
Establish the failing-test baseline for Phase 16 — write every Vitest + RTL test for filters.js, useConstellation extensions, SkillFilters, ExperienceCard, SvgConstellation extensions, and GameMode wiring BEFORE any production code lands.

Purpose: Phase 14/15 convention is RED-first TDD on pure-logic modules; Phase 16 extends that discipline to component tests so behavior contracts (AND semantics, focus trap, dependent slider bounds, click-outside, bilingual CV CTA, dim-others) are pinned to assertions, not prose. This plan produces a single RED commit covering all 6 test files.

Output: 4 new test files + 2 extended test files. Running `npm run test:run` should show the new tests failing (production code does not yet exist or is the Phase 15 placeholder). No production source changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md
@.planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md
@.planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md
@.planning/phases/16-filters-floating-experiencecard/16-UI-SPEC.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-02-SUMMARY.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-03-SUMMARY.md
@src/game/useConstellation.js
@src/game/useConstellation.test.js
@src/game/renderers/SvgConstellation.js
@src/game/renderers/SvgConstellation.test.js
@src/game/GameMode.js
@src/game/GameMode.test.js
@src/game/constellation.graph.test.js
@src/game/spatialNav.test.js
@src/data/experience.js
@src/data/skills.js
@src/i18n/translations.js

<interfaces>
<!-- Phase 15 useConstellation hook contract (from src/game/useConstellation.js) -->
<!-- Phase 16 EXTENDS this; tests must assert the extended shape, not invent new fields. -->

Existing returned shape (Phase 15):
{
  selectedSkillId, hoveredSkillId, highlightedSkillIds, yearRange,
  onSelectSkill, onHoverSkill
}

Phase 16 extended shape (to be implemented in Plan 02, asserted in tests now):
{
  ...Phase15Shape,
  selectedSkills: string[],          // multi-skill filter (AND)
  category: string | null,           // category filter key
  yearBounds: [number, number],      // derived once from EXPERIENCE
  isFilterActive: boolean,
  toggleSkill(id),
  setYearRange([from, to] | null),
  setCategory(key | null),
  resetFilters()
}

Phase 15 renderer prop contract (from SvgConstellation.js lines 68-80) — UNCHANGED:
{ nodes, edges, layout, highlightedSkillIds, selectedSkillId, yearRange, theme, lang, t, onSelectSkill, onHoverSkill }

Phase 16 new component contracts (to drive tests now):

SkillFilters props:
{ nodes, selectedSkills, yearRange, yearBounds, category, isFilterActive,
  onToggleSkill, onYearRangeChange, onCategoryChange, onReset, lang, t }

ExperienceCard props:
{ selectedNode, jobs, selectedSkills, lang, t, onClose, onToggleSkill }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create filters.test.js + extend useConstellation.test.js (pure-logic RED suite)</name>
  <files>src/game/filters.test.js, src/game/useConstellation.test.js</files>
  <read_first>
    - src/game/constellation.graph.test.js (analog: pure-data-module test pattern, imports + describe naming)
    - src/game/spatialNav.test.js (analog: zero-React pure-function test shape)
    - src/game/useConstellation.test.js (existing 10 tests to mirror — renderHook + act shape lines 26-37, memoization test lines 59-64)
    - src/data/experience.js (fixture-free data — EXPERIENCE used directly in tests)
    - src/data/skills.js (SKILLS catalog with category map for filterByCategory)
    - .planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md (D-16-INTERSECT-AND, D-16-YEAR-BOUNDS, D-16-YEAR-EFFECT)
    - .planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md §1 (filters.js function signatures), §9 (test pattern lines 870-903), §test plan lines 376-389
    - .planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md "Wave 0 — Tests First" filters.test.js + useConstellation.test.js sections (lines 38-107)
  </read_first>
  <behavior>
    - filters.test.js — ≥12 tests covering:
      - filterByYearRange: full-range pass-through; single-year boundary intersection; disjoint range returns []; period.end == null treated as 2026 (CURRENT_YEAR)
      - filterBySkillIntersection: empty skills[] pass-through; single skill membership; multi-skill AND (Java + Spring Boot returns only jobs with BOTH per D-16-INTERSECT-AND); non-existent skill returns []
      - filterByCategory: null pass-through; category match via SKILLS map
      - composeFilters: all three dimensions combined returns intersection of all
      - yearBounds(EXPERIENCE) returns [2007, 2026] EXACTLY (D-16-YEAR-BOUNDS honesty rule from live data, mirrors Phase 15 H1 derivation)
      - visibleSkillIds: dedupes skill ids across experiences
    - useConstellation.test.js — ADD ≥8 tests in same describe block:
      - toggleSkill('Java') adds Java to selectedSkills
      - toggleSkill('Java') twice removes it
      - setYearRange([2018, 2026]) updates yearRange
      - setCategory('cloud') updates category
      - resetFilters() clears selectedSkills + yearRange + category back to [] / null / null
      - highlightedSkillIds derives [] when no filter; non-empty when filter active
      - isFilterActive is false at init, true after any filter set
      - existing memoization test still passes (no regression from new useMemo deps)
  </behavior>
  <action>Create src/game/filters.test.js — colocated (NOT in __tests__/ subdir per repo convention). Import filterByYearRange, filterBySkillIntersection, filterByCategory, composeFilters, yearBounds, visibleSkillIds from './filters.js' (file does not yet exist — tests are RED). Import EXPERIENCE from '../data/experience.js' and SKILLS from '../data/skills.js'. Use the describe('ModuleName - concern') naming from constellation.graph.test.js line 20. Cover the 13-case test plan from 16-RESEARCH.md §1. Critical assertion: yearBounds(EXPERIENCE) === [2007, 2026] derived from live data per D-16-YEAR-BOUNDS. EXTEND src/game/useConstellation.test.js — add 8 new tests inside the existing describe('useConstellation') block. Mirror the renderHook + act shape from lines 26-37 verbatim. Tests will fail because the Phase 15 hook does not yet expose toggleSkill / setYearRange / setCategory / resetFilters / isFilterActive (only highlightedSkillIds + yearRange as frozen state). Do NOT touch production code yet. No semicolons, no comma-dangle, JSX-in-.js.</action>
  <verify>
    <automated>npx vitest run src/game/filters.test.js src/game/useConstellation.test.js 2>&amp;1 | tee /tmp/wave0-red-a.log; grep -E "FAIL|✕|filters" /tmp/wave0-red-a.log</automated>
  </verify>
  <acceptance_criteria>
    - src/game/filters.test.js exists, contains import statements for filterByYearRange / filterBySkillIntersection / filterByCategory / composeFilters / yearBounds / visibleSkillIds from './filters.js'
    - src/game/filters.test.js has at least one describe block per selector + at least 12 `it(...)` blocks total
    - src/game/filters.test.js contains an explicit `expect(yearBounds(EXPERIENCE)).toEqual([2007, 2026])` assertion (D-16-YEAR-BOUNDS)
    - src/game/filters.test.js contains an AND-semantics assertion: `expect(result.every((e) => e.tech.includes('Java') && e.tech.includes('Spring Boot'))).toBe(true)` (D-16-INTERSECT-AND)
    - src/game/useConstellation.test.js gains 8+ new `it(...)` blocks within the existing describe('useConstellation') block — total test count rises from 10 → 18+
    - `npx vitest run src/game/filters.test.js` exits non-zero (file not found / module not found / FAIL)
    - `npx vitest run src/game/useConstellation.test.js` shows the 10 prior tests pass and the 8+ new tests FAIL with messages indicating missing setters (toggleSkill, setYearRange, etc.) — not a syntax error
    - No production source files in src/game/ are modified in this task
    - All new tests follow no-semicolons + no-comma-dangle ESLint contract
  </acceptance_criteria>
  <done>filters.test.js + extended useConstellation.test.js committed; running vitest shows pure-logic RED suite (filters.js missing; hook missing 5 setters). Phase 14/15 pre-existing tests remain GREEN. Single atomic commit: `test(16): add filters + extend useConstellation RED suite (RED)`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SkillFilters.test.js + ExperienceCard.test.js (component RED suite)</name>
  <files>src/game/SkillFilters.test.js, src/game/ExperienceCard.test.js</files>
  <read_first>
    - src/game/renderers/SvgConstellation.test.js (analog: RTL render + fixture-builder + matchMedia mock + bilingual rerender — lines 22, 24-39, 41-58, 167-176, 189-237, 228-237, 239-263)
    - src/game/GameMode.test.js (analog: provider-wrapping renderWithProviders helper lines 9-20 if context needed; Phase 16 SkillFilters is prop-driven so likely not needed)
    - src/i18n/translations.js (existing t.game.* keys — tests use translations.en/translations.es)
    - src/data/experience.js (real fixtures: jobs for one skill — pick Java which has 9 jobs)
    - src/components/Nav.js MobileMenu lines 167-228 (portal + role=dialog pattern that ExperienceCard mirrors)
    - .planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md (D-16-FLAYOUT-RESET, D-16-YEAR-INPUT, D-16-CARD-A11Y, D-16-FLOW-CLOSE-EMPTY, D-16-CARD-CV-CTA, D-16-CARD-CHIPS)
    - .planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md §9 (test patterns lines 905-1013), §2 (WAI-ARIA dual-thumb slider contract lines 396-525), §3 (focus trap contract lines 528-657)
    - .planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md SkillFilters.test.js + ExperienceCard.test.js sections (lines 109-253)
    - .planning/phases/16-filters-floating-experiencecard/16-UI-SPEC.md §ARIA Contract (aria-pressed, aria-disabled on reset, role=dialog + aria-modal + aria-labelledby)
  </read_first>
  <behavior>
    SkillFilters.test.js — ≥10 tests using renderFilters helper with prop-builder pattern:
    - chip toggle calls onToggleSkill with node.id when clicked, aria-pressed reflects selectedSkills membership
    - skill chip group has role=group + aria-label = t.game.filterSkillsLabel
    - category chip group has role=group + aria-label = t.game.filterCategoryLabel; renders 8 categories from SKILL_CATEGORIES
    - YearRangeSlider: ArrowRight on start thumb (focused) calls onYearRangeChange([N+1, end])
    - YearRangeSlider: ArrowLeft floored at min for start, end for end
    - YearRangeSlider: Home jumps to thumb's aria-valuemin; End to aria-valuemax
    - YearRangeSlider: dependent aria-valuemax — start thumb cannot exceed end - 1 (no onChange when already at end-1)
    - reset button disabled (native + aria-disabled='true') when isFilterActive=false
    - reset button enabled and onClick fires onReset when isFilterActive=true
    - data-game-interactive attribute present on filter bar root

    ExperienceCard.test.js — ≥12 tests using portal-aware screen queries (NOT container.querySelector):
    - renders with role=dialog, aria-modal='true', aria-labelledby='card-skill-heading'
    - initial focus = h2 heading (document.activeElement === heading on mount)
    - Esc key triggers onClose
    - Tab cycle: from last focusable wraps to first
    - Shift+Tab from first focusable wraps to last
    - click on document.body (outside [data-game-interactive]) triggers onClose AFTER a frame delay (Pitfall 2 requestAnimationFrame defer)
    - click inside dialog does NOT trigger onClose
    - CV CTA href = '/CV_Carlos_Montoya_EN.docx' when lang='en'; '/CV_Carlos_Montoya_ES.docx' when lang='es' (rerender pattern)
    - tech chip click calls onToggleSkill with the tech string
    - currently-selected skill (tech === selectedNode.id) renders as aria-disabled='true', click does NOT call onToggleSkill
    - empty-state: jobs=[] renders t.game.filterEmpty with role='status', NO <ol>
    - card content uses key={selectedNode.id} on the wrapper so changing selectedNode triggers remount (swap)
    - bilingual job content: rerender with lang='es' shows Spanish bullets[es] not bullets[en]
  </behavior>
  <action>Create src/game/SkillFilters.test.js — colocated. Import SkillFilters from './SkillFilters.js' (file not yet existing). Mirror SvgConstellation.test.js test scaffolding verbatim: matchMedia mock factory (RESEARCH.md §9 lines 116-133), renderFilters({...overrides}) fixture-builder helper with defaults (selectedSkills:[], yearRange:null, yearBounds:[2007,2026], category:null, isFilterActive:false, callbacks=vi.fn(), lang:'en', t:translations.en). Cover the 10-test plan above per D-16-FLAYOUT-RESET + WAI-ARIA APG (RESEARCH.md §2). For slider keyboard tests, use `userEvent.setup()` + thumb.focus() + `user.keyboard('{ArrowRight}')` per RESEARCH.md §9 line 944.

  Create src/game/ExperienceCard.test.js — colocated. Import ExperienceCard from './ExperienceCard.js'. Build a defaults helper with selectedNode={id:'Java', label:'Java', category:'lang'}, jobs=[real Java entries from EXPERIENCE], selectedSkills=['Java'], lang='en', t=translations.en, onClose=vi.fn(), onToggleSkill=vi.fn(). CRITICAL: query the portal via `screen.getByRole('dialog')` NOT container.querySelector (RESEARCH.md §9 lines 1021-1023). Cover the 12-test plan above. For click-outside test, wrap the user.click(document.body) call in `await waitFor(() => expect(onClose).toHaveBeenCalled())` to absorb the requestAnimationFrame defer (Pitfall 2). For CV CTA, use the rerender pattern from SvgConstellation.test.js lines 239-263. Do NOT create the production files yet — these tests must FAIL because SkillFilters.js and ExperienceCard.js do not exist. No semicolons, JSX-in-.js.</action>
  <verify>
    <automated>npx vitest run src/game/SkillFilters.test.js src/game/ExperienceCard.test.js 2>&amp;1 | tee /tmp/wave0-red-b.log; grep -cE "✕|FAIL" /tmp/wave0-red-b.log</automated>
  </verify>
  <acceptance_criteria>
    - src/game/SkillFilters.test.js exists, imports SkillFilters from './SkillFilters.js'
    - SkillFilters.test.js declares at least 10 `it(...)` blocks across describe groups (chip toggle, year slider keyboard, reset state, role=group)
    - SkillFilters.test.js contains an assertion `expect(reset).toBeDisabled()` AND `expect(reset).toHaveAttribute('aria-disabled', 'true')` for isFilterActive=false (D-16-FLAYOUT-RESET)
    - SkillFilters.test.js contains an ArrowRight assertion: `expect(onYearRangeChange).toHaveBeenCalledWith([2019, 2026])` from start state [2018, 2026]
    - SkillFilters.test.js contains a dependent-max assertion: from [2025, 2026], ArrowRight on start does NOT call onYearRangeChange
    - src/game/ExperienceCard.test.js exists, imports ExperienceCard from './ExperienceCard.js'
    - ExperienceCard.test.js declares at least 12 `it(...)` blocks
    - ExperienceCard.test.js asserts `expect(dialog).toHaveAttribute('aria-modal', 'true')` and `toHaveAttribute('aria-labelledby', 'card-skill-heading')` (D-16-CARD-A11Y)
    - ExperienceCard.test.js asserts initial-focus: `expect(document.activeElement).toBe(screen.getByRole('heading', { level: 2 }))` (D-16-CARD-A11Y)
    - ExperienceCard.test.js asserts CV CTA href EN/ES rerender pattern (D-16-CARD-CV-CTA)
    - ExperienceCard.test.js asserts locked-skill: `expect(lockedChip).toHaveAttribute('aria-disabled', 'true')` AND click does NOT fire onToggleSkill (D-16-CARD-CHIPS)
    - `npx vitest run src/game/SkillFilters.test.js src/game/ExperienceCard.test.js` exits non-zero with module-not-found errors (production files do not exist)
    - No production source files modified
  </acceptance_criteria>
  <done>SkillFilters.test.js + ExperienceCard.test.js committed as RED. Module-resolution failures expected (production files arrive Waves 3+4). Atomic commit: `test(16): add SkillFilters + ExperienceCard RED component suite (RED)`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend SvgConstellation.test.js + GameMode.test.js (renderer/orchestrator RED suite)</name>
  <files>src/game/renderers/SvgConstellation.test.js, src/game/GameMode.test.js</files>
  <read_first>
    - src/game/renderers/SvgConstellation.test.js (existing 22 tests — extend; dim test at lines 122-129 is the closest analog; renderRenderer helper lines 24-39)
    - src/game/GameMode.test.js (existing 9 tests — extend; renderWithProviders helper lines 9-20)
    - src/game/renderers/SvgConstellation.js (current dim logic line 224; placeholder props lines 72-74; node.years field — verify present)
    - .planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md (D-16-YEAR-EFFECT, D-16-INTERSECT-HIGHLIGHT)
    - .planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md §"Pitfall 7" (highlightedSkillIds stale memoization), §10 Validation lines 1390-1393
    - .planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md SvgConstellation.test.js + GameMode.test.js extension sections (lines 254-333)
  </read_first>
  <behavior>
    SvgConstellation.test.js — ADD 3 tests inside existing describe(s):
    - dims nodes NOT in highlightedSkillIds when array non-empty: non-matching circles get fill-opacity ≈ 0.35, matching get 1 (per Phase 15 dim treatment, D-16-INTERSECT-HIGHLIGHT)
    - dims nodes whose `years` field does not intersect yearRange (D-16-YEAR-EFFECT) — node with years [2008, 2009] under yearRange [2020, 2026] gets 0.35; node with years [2007, 2026] gets 1
    - dims edges incident to a dimmed node when filters active (edge opacity reduced regardless of weight per D-16-YEAR-EFFECT)

    GameMode.test.js — ADD 3 tests inside existing describe(s):
    - renders <SkillFilters /> as a child below the H1 — getByRole('group', { name: t.game.filterBarLabel }) matches
    - does NOT render <ExperienceCard /> initially (selectedSkillId=null) — queryByRole('dialog') returns null
    - clicking a node <g> (use user-event) sets selectedSkillId and renders the dialog
  </behavior>
  <action>EXTEND src/game/renderers/SvgConstellation.test.js with 3 tests. Use the existing renderRenderer helper. For the highlightedSkillIds dim test: pass highlightedSkillIds=['Java'], assert that container.querySelector('g[data-node-id="Docker"] circle:first-child') has fill-opacity ≈ 0.35 and 'g[data-node-id="Java"] ...' has fill-opacity 1 (PATTERNS.md lines 271-284). For yearRange dim test: pass nodes with explicit `years` arrays (fixture override) and yearRange [2020, 2026]; assert dim on the node that doesn't intersect. For edge dim test: pass highlightedSkillIds=['Java'] and assert that an edge whose source OR target is not Java has a reduced opacity attribute.

  EXTEND src/game/GameMode.test.js with 3 tests using existing renderWithProviders helper. First test: renderWithProviders(<GameMode />, { lang: 'en' }); expect(screen.getByRole('group', { name: t.game.filterBarLabel })) to be in the document. Second test: expect(screen.queryByRole('dialog')) to be null when no selection. Third test: simulate node click via user-event on a `<g data-node-id="Java">` and expect dialog to appear with the skill heading. These tests will FAIL because Wave 2 (renderer extension) + Wave 5 (GameMode wiring) haven't shipped yet. No production source changes. No semicolons.</action>
  <verify>
    <automated>npx vitest run src/game/renderers/SvgConstellation.test.js src/game/GameMode.test.js 2>&amp;1 | tee /tmp/wave0-red-c.log; grep -cE "FAIL|✕" /tmp/wave0-red-c.log</automated>
  </verify>
  <acceptance_criteria>
    - src/game/renderers/SvgConstellation.test.js gains 3+ new `it(...)` blocks; existing 22 pass count rises to 25+ when measured
    - One new test asserts `parseFloat(circle.getAttribute('fill-opacity'))` ≈ 0.35 for a non-highlighted node when highlightedSkillIds=['Java']
    - One new test asserts dim behavior when node.years doesn't intersect yearRange ([2020, 2026] excludes [2008, 2009])
    - One new test asserts edge dim when one endpoint is not in highlightedSkillIds
    - src/game/GameMode.test.js gains 3+ new `it(...)` blocks; total rises from 9 → 12+
    - GameMode.test.js asserts `expect(screen.getByRole('group', { name: /skill filters|filtros/i })).toBeInTheDocument()`
    - GameMode.test.js asserts `expect(screen.queryByRole('dialog')).not.toBeInTheDocument()` at init
    - GameMode.test.js asserts dialog appears after user.click on a node element
    - The 3 new SvgConstellation tests + 3 new GameMode tests are RED (fail with assertion errors, not syntax errors)
    - Pre-existing 22 SvgConstellation tests + 9 GameMode tests remain GREEN
    - No production source files modified
  </acceptance_criteria>
  <done>SvgConstellation.test.js + GameMode.test.js extended; running vitest shows new tests RED while old ones GREEN. Atomic commit: `test(16): extend SvgConstellation + GameMode RED suite (RED)`. After this commit the Wave 0 RED baseline is complete.</done>
</task>

</tasks>

<verification>
- `npx vitest run` reports the new tests as failing (file or assertion failures)
- `npx vitest run src/game/constellation.graph.test.js src/game/spatialNav.test.js src/game/constellation.layout.test.js src/game/ConstellationFallback.test.js` — all Phase 14/15 tests remain GREEN (no regression from extension files)
- All 6 test files exist at the colocated paths (NOT under __tests__/)
- No production .js source files in src/game/ or src/components/ were modified
</verification>

<success_criteria>
- 6 test files committed (4 new + 2 extended)
- Wave 0 RED baseline produces at least 33 failing tests across the new behaviors (13 filters + 8 hook + 10 SkillFilters + 12 ExperienceCard + 3 renderer + 3 GameMode)
- Every Phase 16 acceptance criterion from CONTEXT.md is encoded as at least one assertion
- Three atomic commits, one per task, each labeled `test(16): ... (RED)`
</success_criteria>

<output>
Create `.planning/phases/16-filters-floating-experiencecard/16-01-SUMMARY.md` when done — list each test file, the assertions added (one-line each), and the vitest run summary showing N tests RED.
</output>
