---
phase: 16-filters-floating-experiencecard
plan: 03
type: execute
wave: 2
depends_on: ["16-02"]
files_modified:
  - src/game/useConstellation.js
  - src/game/renderers/SvgConstellation.js
autonomous: true
requirements: [GAME-03, GAME-04]
tags: [phase-16, hook-extension, renderer-consumer, tdd-green]
must_haves:
  truths:
    - "useConstellation owns filter state — selectedSkills, yearRange, category — and exposes toggleSkill / setYearRange / setCategory / resetFilters / isFilterActive setters"
    - "useConstellation exposes justFilteredId (string | null) — set on toggleSkill add, cleared via setTimeout(100ms); honors reduced-motion (instant clear at 0ms)"
    - "highlightedSkillIds is DERIVED via composeFilters + visibleSkillIds whenever filter state changes (no stale memoization, Pitfall 7 guarded)"
    - "yearBounds is computed once at module load from live EXPERIENCE data and exposed via the hook (D-16-YEAR-BOUNDS honesty rule)"
    - "SvgConstellation consumes highlightedSkillIds[] to dim non-matching nodes at fill-opacity 0.35 (D-16-INTERSECT-HIGHLIGHT, reuses Phase 15 dim treatment)"
    - "SvgConstellation consumes yearRange to dim nodes whose .years field does not intersect [from, to] (D-16-YEAR-EFFECT)"
    - "SvgConstellation consumes justFilteredId prop — applies motion-safe:animate-chip-flash class on matching node <g> when prop === node.id (RESEARCH §7 tech-chip flash; resolves checker BLOCKER 2)"
    - "Edges incident to dimmed nodes also dim regardless of weight when any filter is active"
    - "Phase 15 single-select dim behavior is unchanged — extending, not replacing"
    - "Reduced-motion path skips the fill-opacity transition AND skips chip-flash class (motion-safe: prefix gates both; existing global rule preserved)"
  artifacts:
    - path: "src/game/useConstellation.js"
      provides: "Extended hook with full Phase 16 filter state + justFilteredId chip-flash trigger"
      contains: "toggleSkill"
    - path: "src/game/renderers/SvgConstellation.js"
      provides: "Renderer with highlightedSkillIds + yearRange consumer logic + justFilteredId chip-flash class"
      contains: "shouldDimNode"
  key_links:
    - from: "src/game/useConstellation.js"
      to: "src/game/filters.js"
      via: "named import of composeFilters, visibleSkillIds, yearBounds"
      pattern: "from './filters"
    - from: "src/game/renderers/SvgConstellation.js"
      to: "highlightedSkillIds, yearRange, justFilteredId props"
      via: "render-loop dim logic on each node circle + edge line; conditional motion-safe:animate-chip-flash on node <g>"
      pattern: "justFilteredId"
---

<objective>
Wire the pure-logic foundation into runtime state + renderer behavior: extend useConstellation with full filter state ownership (including justFilteredId chip-flash trigger) and extend SvgConstellation to dim nodes/edges based on highlightedSkillIds + yearRange and to flash the matching node when toggleSkill fires.

Purpose: After this plan, the constellation visualization responds to filter state — even without the SkillFilters UI shipping yet, the useConstellation hook tests are GREEN and the renderer tests assert correct dim + chip-flash behavior. UI bar (Plan 04) and dialog (Plan 05) can be developed against a working state surface. This plan also resolves checker BLOCKER 2 (chip-flash wiring) by binding the trigger in the hook and the visual in the renderer.

Output: 2 extended files. Wave 0 hook + renderer RED suites flip GREEN. Phase 15 single-select dim still works (Phase 16 is additive, not replacing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md
@.planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md
@.planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md
@.planning/phases/16-filters-floating-experiencecard/16-UI-SPEC.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-02-SUMMARY.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-03-SUMMARY.md
@src/game/filters.js
@src/game/useConstellation.js
@src/game/useConstellation.test.js
@src/game/renderers/SvgConstellation.js
@src/game/renderers/SvgConstellation.test.js
@src/data/experience.js
@src/data/skills.js

<interfaces>
<!-- Current Phase 15 useConstellation (src/game/useConstellation.js) — placeholders for Phase 16 -->
const [highlightedSkillIds] = useState([])   // line 8 — replace with derived useMemo
const [yearRange] = useState(null)           // line 9 — replace with [state, setter]

Phase 16 final return object:
{
  // Phase 15 (preserved):
  selectedSkillId, hoveredSkillId, onSelectSkill, onHoverSkill,
  // Phase 16 (new):
  selectedSkills,       // string[] — toggled via toggleSkill
  yearRange,            // [from, to] | null — set via setYearRange
  category,             // string | null — set via setCategory
  yearBounds,           // [2007, 2026] — module-derived, never changes
  highlightedSkillIds,  // string[] — derived from composeFilters + visibleSkillIds
  isFilterActive,       // boolean — true when any filter set
  justFilteredId,       // string | null — set on toggleSkill add, cleared 100ms later (0ms under reduced-motion)
  toggleSkill(id),
  setYearRange(range | null),
  setCategory(key | null),
  resetFilters(),
}

<!-- Current Phase 15 SvgConstellation dim logic (line 224) -->
const fillOpacity = selectedSkillId !== null && !isSelected ? 0.35 : 1

<!-- Phase 16 extended dim logic — must preserve Phase 15 behavior as a fallback -->
function nodeMatchesYearRange(node, yearRange)
function shouldDimNode(node, { selectedSkillId, highlightedSkillIds, yearRange })

<!-- Phase 16 chip-flash visual (RESEARCH §7) -->
// Renderer accepts justFilteredId prop; on each node <g>:
className={node.id === justFilteredId ? 'motion-safe:animate-chip-flash' : ''}
// Class auto-removes on next render when prop !== node.id (no per-node imperative cleanup needed)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend useConstellation.js with full Phase 16 filter state + derived highlightedSkillIds + justFilteredId chip-flash trigger</name>
  <files>src/game/useConstellation.js</files>
  <read_first>
    - src/game/useConstellation.js (existing Phase 15 hook — lines 1-27; placeholders at lines 8-9 to REPLACE)
    - src/game/useConstellation.test.js (RED tests from Plan 01 — the contract: toggleSkill / setYearRange / setCategory / resetFilters / isFilterActive / highlightedSkillIds derivation / justFilteredId set+clear)
    - src/game/filters.js (composeFilters, visibleSkillIds, yearBounds — newly available from Plan 02)
    - src/data/experience.js (EXPERIENCE source for yearBounds derivation)
    - src/data/skills.js (SKILLS map needed by composeFilters)
    - src/game/GameMode.js lines 13-16 (Phase 15 module-level YEAR_BOUNDS derivation pattern to mirror)
    - src/game/renderers/SvgConstellation.js lines 32-59 (inline usePrefersReducedMotion hook — used to gate the 100ms chip-flash timer)
    - .planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md §1 (filters.js contracts), §7 (chip-flash trigger + reduced-motion handling, anti-pattern: do NOT stack timeouts — useRef + clearTimeout), §"Pitfall 7" (stale memo guard)
    - .planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md Wave 2 useConstellation.js section (lines 570-657)
    - .planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md (D-16-INTERSECT-AND, D-16-PERSIST-MEMORY — in-memory state only, no localStorage; Claude's Discretion area H — tech-chip flash on constellation, reduced-motion = instant dim update only)
  </read_first>
  <behavior>
    - Replace Phase 15 placeholders: `const [highlightedSkillIds] = useState([])` → derived useMemo; `const [yearRange] = useState(null)` → [state, setter] tuple
    - Add 4 useState hooks: selectedSkills (string[] default []), yearRange ([from,to]|null default null), category (string|null default null), justFilteredId (string|null default null)
    - Add 4 useCallback setters: toggleSkill (push/remove from selectedSkills array; on ADD path also sets justFilteredId + schedules setTimeout(100ms) clear via a useRef-tracked timer; on REMOVE path leaves justFilteredId untouched — flash signals NEW filter intent), setYearRange (state setter passthrough), setCategory (state setter passthrough), resetFilters (clears all 3 filter dims AND clears any pending flash timer)
    - useRef to hold the latest flash setTimeout handle so back-to-back toggleSkill calls clear the prior timer before scheduling a new one (RESEARCH §7 anti-pattern: do NOT stack timeouts)
    - Reduced-motion handling: import / inline the usePrefersReducedMotion hook (or accept the reduced-motion flag derivation pattern from SvgConstellation lines 32-59) — when prefers-reduced-motion matches, the flash clear timeout is 0ms (effectively no flash window) OR justFilteredId is never set. Either implementation is acceptable; pick the simpler one and document. Recommended: still set justFilteredId so an SR could announce "filter added" via the existing aria-live region, but use 0ms timeout so the visual class application/removal happens within the same microtask.
    - Cleanup on unmount: useEffect returning a cleanup that clears any pending flash timer (prevents setState-after-unmount warnings)
    - Add 1 useMemo deriving highlightedSkillIds: when no filter active returns []; otherwise calls composeFilters(EXPERIENCE, {skillIds: selectedSkills, yearRange, category}, SKILLS) → visibleSkillIds
    - Add module-level YEAR_BOUNDS = yearBounds(EXPERIENCE) once at module load (mirrors GameMode.js Phase 15 pattern)
    - Expose isFilterActive boolean (selectedSkills.length > 0 || yearRange !== null || category !== null)
    - Final useMemo on the returned object includes ALL filter dependencies + justFilteredId in deps array (Pitfall 7 guard)
    - NO localStorage writes — D-16-PERSIST-MEMORY mandates in-memory only
    - Phase 15 selectedSkillId / hoveredSkillId / onSelectSkill / onHoverSkill UNCHANGED
  </behavior>
  <action>EDIT src/game/useConstellation.js. Add imports at top: `import EXPERIENCE from '../data/experience.js'`; `import { SKILLS } from '../data/skills.js'`; `import { composeFilters, visibleSkillIds, yearBounds } from './filters.js'`. Add `import { useState, useCallback, useMemo, useRef, useEffect } from 'react'` (extend whatever Phase 15 imports). Add module-level constant `const YEAR_BOUNDS = yearBounds(EXPERIENCE)` BEFORE the hook function (per GameMode.js line 14-16 pattern). Module-level `const FLASH_DURATION_MS = 100` constant per RESEARCH §7. Inside the hook function: REMOVE the Phase 15 frozen placeholders (lines 8-9). ADD four useState hooks for selectedSkills / yearRange / category / justFilteredId (default null). ADD useRef `const flashTimerRef = useRef(null)`. Compute reduced-motion flag (option A: inline matchMedia check matching SvgConstellation lines 32-59; option B: extract usePrefersReducedMotion to a shared file — pick option A for v1 to avoid cross-file churn). ADD useCallback toggleSkill per RESEARCH §7 + PATTERNS.md lines 609-622, EXTENDED: `setSelectedSkills((prev) => { const willAdd = !prev.includes(id); if (willAdd) { if (flashTimerRef.current) clearTimeout(flashTimerRef.current); setJustFilteredId(id); flashTimerRef.current = setTimeout(() => setJustFilteredId(null), prefersReducedMotion ? 0 : FLASH_DURATION_MS); } return prev.includes(id) ? prev.filter((s) => s !== id) : [...prev, id] })`. ADD other setters (setYearRange / setCategory pass-through; resetFilters clears all 3 filter dims AND `if (flashTimerRef.current) clearTimeout(flashTimerRef.current); setJustFilteredId(null)`). ADD useEffect for unmount cleanup: `return () => { if (flashTimerRef.current) clearTimeout(flashTimerRef.current) }`. ADD `const highlightedSkillIds = useMemo(() => { if (selectedSkills.length === 0 && !yearRange && !category) return []; const matching = composeFilters(EXPERIENCE, { skillIds: selectedSkills, yearRange, category }, SKILLS); return visibleSkillIds(matching); }, [selectedSkills, yearRange, category])` — CRITICAL: include all three filter-state values in the deps array per Pitfall 7. ADD `const isFilterActive = selectedSkills.length > 0 || yearRange !== null || category !== null`. UPDATE the final useMemo return object to include all new fields (selectedSkills, yearRange, category, yearBounds: YEAR_BOUNDS, highlightedSkillIds, isFilterActive, justFilteredId, toggleSkill, setYearRange, setCategory, resetFilters) and EXTEND its deps array to match (include justFilteredId so the returned object identity updates when the flash starts/clears, allowing downstream renderer to re-render with the new class). Phase 15 fields (selectedSkillId, hoveredSkillId, onSelectSkill, onHoverSkill) remain. Per D-16-PERSIST-MEMORY: NO localStorage.setItem calls anywhere. No semicolons.</action>
  <verify>
    <automated>npx vitest run src/game/useConstellation.test.js 2>&amp;1 | tee /tmp/wave2-hook.log; grep -E "passed|failed" /tmp/wave2-hook.log | tail -3; grep -c "localStorage\\.setItem" src/game/useConstellation.js; grep -c "justFilteredId\\|flashTimerRef" src/game/useConstellation.js</automated>
  </verify>
  <acceptance_criteria>
    - src/game/useConstellation.js imports composeFilters, visibleSkillIds, yearBounds from './filters.js'
    - Module-level `const YEAR_BOUNDS = yearBounds(EXPERIENCE)` declared OUTSIDE the hook function
    - Module-level `const FLASH_DURATION_MS = 100` constant declared
    - Hook declares useState for selectedSkills, yearRange, category, justFilteredId (4 new state hooks)
    - Hook declares `flashTimerRef = useRef(null)` for setTimeout handle tracking (RESEARCH §7 anti-pattern guard)
    - Hook declares useCallback for toggleSkill, setYearRange, setCategory, resetFilters
    - toggleSkill body uses `prev.includes(id) ? prev.filter((s) => s !== id) : [...prev, id]` (AND-toggle semantics) AND on the ADD path: clears any pending flashTimerRef.current, calls setJustFilteredId(id), schedules setTimeout(() => setJustFilteredId(null), prefersReducedMotion ? 0 : 100) and assigns the handle to flashTimerRef.current
    - resetFilters clears the pending flash timer AND sets justFilteredId back to null
    - useEffect cleanup clears flashTimerRef.current on unmount (no setState-after-unmount)
    - highlightedSkillIds is a useMemo (NOT useState) and its deps array contains exactly `[selectedSkills, yearRange, category]`
    - Return object includes selectedSkills, yearRange, category, yearBounds, highlightedSkillIds, isFilterActive, justFilteredId, toggleSkill, setYearRange, setCategory, resetFilters (in addition to Phase 15 fields)
    - `grep -c "localStorage" src/game/useConstellation.js` returns 0 (D-16-PERSIST-MEMORY)
    - `grep -c "justFilteredId" src/game/useConstellation.js` returns ≥4 (useState declaration, ADD-path set, clear-path set in resetFilters, return-object include)
    - `npx vitest run src/game/useConstellation.test.js` exits 0 with all 18+ tests PASSING (Phase 15 10 + Phase 16 8+ including a RED test asserting `toggleSkill('Java')` sets `justFilteredId === 'Java'` and 150ms later it is null)
    - `npx vitest run` shows no regression on filters.test.js, spatialNav.test.js, constellation.graph.test.js, constellation.layout.test.js
  </acceptance_criteria>
  <done>useConstellation.js extended with full filter state + chip-flash trigger. Hook RED tests are now GREEN; Phase 15 tests remain GREEN. Commit: `feat(16): extend useConstellation with filter state + justFilteredId chip-flash trigger`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend SvgConstellation.js to consume highlightedSkillIds + yearRange (dim logic) + justFilteredId (chip-flash) + reduced-motion fill-opacity transition guard</name>
  <files>src/game/renderers/SvgConstellation.js</files>
  <read_first>
    - src/game/renderers/SvgConstellation.js (current dim logic at line 224; placeholder consumers at lines 72-74; edge logic lines 188-195; prefersReducedMotion line 81; transition pattern line 206)
    - src/game/renderers/SvgConstellation.test.js (new Phase 16 RED tests from Plan 01 — must turn GREEN: highlightedSkillIds dim, yearRange dim, edge dim, plus a NEW test asserting that under matchMedia('(prefers-reduced-motion: reduce)') the fill-opacity transition style is 'none' AND a NEW test asserting that when justFilteredId === a node.id, that node <g> carries the 'motion-safe:animate-chip-flash' class)
    - src/game/constellation.graph.js (node shape — confirm `.years` field is `[start, end ?? CURRENT_YEAR]`; if not present, this task must surface that as a blocker for Plan 03 retry)
    - .planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md (D-16-YEAR-EFFECT — note: "Reduced-motion path skips opacity transition" — must be enforced and tested; D-16-INTERSECT-HIGHLIGHT)
    - .planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md §"Architectural Responsibility Map" (renderer consumer responsibility), §7 (chip-flash class application — applied while prop matches; auto-removed on next render when prop !== node.id, no per-node imperative cleanup)
    - .planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md Wave 2 SvgConstellation.js section (lines 660-722, including line 718-721 chip-flash hook-in note)
    - Phase 15 reduced-motion test pattern in src/game/renderers/SvgConstellation.test.js (existing matchMedia mock factory + reduced-motion describe block)
  </read_first>
  <behavior>
    - Add helper `nodeMatchesYearRange(node, yearRange)`: true when yearRange null OR node.years intersects [from, to]; uses node.years[0] <= to && node.years[1] >= from
    - Add helper `shouldDimNode(node, { selectedSkillId, highlightedSkillIds, yearRange })`: returns true when filters active AND node fails any criterion; falls back to Phase 15 single-select dim when no filters
    - Replace render-loop fillOpacity line (current line 224) with `const fillOpacity = shouldDimNode(node, {...}) ? 0.35 : 1`
    - Extend edge-render logic: when filters active, dim any edge whose source OR target is dimmed (multiply final opacity by 0.35) — D-16-YEAR-EFFECT explicit
    - Remove `// eslint-disable-line no-unused-vars` from highlightedSkillIds and yearRange prop declarations (lines 72, 74) — they ARE used now
    - Accept new prop `justFilteredId` (string | null). On each node `<g>`, apply `className={node.id === justFilteredId && !prefersReducedMotion ? 'motion-safe:animate-chip-flash' : ''}` (RESEARCH §7 + PATTERNS.md line 720). The motion-safe: prefix already guards reduced-motion at the CSS layer; the explicit `&& !prefersReducedMotion` JS guard is belt-and-suspenders so SR-only or CSS-disabled environments behave identically.
    - Preserve Phase 15 transition pattern (line 206 verbatim): `transition: prefersReducedMotion ? 'none' : 'fill-opacity 200ms ease-out'` — D-16-YEAR-EFFECT explicit ("Reduced-motion path skips opacity transition"). The Phase 15 line already does this; the new dim-others path MUST inherit the same transition style on the same elements. If the new code introduces a separate inline style block, copy the same `prefersReducedMotion ? 'none' : ...` guard.
    - Preserve Phase 15 single-select behavior: when no Phase 16 filter is active, selectedSkillId dims non-selected per Phase 15 — additive logic only
  </behavior>
  <action>EDIT src/game/renderers/SvgConstellation.js. Remove the `eslint-disable-line no-unused-vars` comments on the highlightedSkillIds (line 72) and yearRange (line 74) prop declarations — they are now consumed. ADD `justFilteredId` to the destructured props signature (default null). Add two pure helper functions at the module top (above the component) per 16-PATTERNS.md lines 673-693: `function nodeMatchesYearRange(node, yearRange)` and `function shouldDimNode(node, { selectedSkillId, highlightedSkillIds, yearRange })`. shouldDimNode logic: compute `filtersActive = (highlightedSkillIds?.length > 0) || yearRange !== null`; if filtersActive: dim when node is NOT in highlightedSkillIds (or highlight list empty) AND not in yearRange; if NOT filtersActive: fall back to Phase 15 single-select dim (`selectedSkillId !== null && node.id !== selectedSkillId`). In the render loop, replace the current fillOpacity computation (line 224) with `const fillOpacity = shouldDimNode(node, { selectedSkillId, highlightedSkillIds, yearRange }) ? 0.35 : 1`. CRITICAL: keep the existing transition guard verbatim: `transition: prefersReducedMotion ? 'none' : 'fill-opacity 200ms ease-out'` — verify by grep that the only fill-opacity transition declaration in the file has the prefersReducedMotion ternary. On the node `<g>` element, add `className={node.id === justFilteredId && !prefersReducedMotion ? 'motion-safe:animate-chip-flash' : ''}` (per PATTERNS.md line 720). In the edge render loop (around lines 188-195), add an additional dim multiplier when filtersActive AND (shouldDimNode(sourceNode) || shouldDimNode(targetNode)): final edge opacity = (existing computed opacity) × 0.35. No semicolons.</action>
  <verify>
    <automated>npx vitest run src/game/renderers/SvgConstellation.test.js 2>&amp;1 | tee /tmp/wave2-renderer.log; grep -E "passed|failed" /tmp/wave2-renderer.log | tail -3; grep -c "shouldDimNode\\|nodeMatchesYearRange\\|justFilteredId\\|animate-chip-flash" src/game/renderers/SvgConstellation.js; grep -E "transition:.*prefersReducedMotion.*'none'" src/game/renderers/SvgConstellation.js | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - src/game/renderers/SvgConstellation.js defines `function nodeMatchesYearRange` and `function shouldDimNode` (module-level helpers)
    - The `eslint-disable-line no-unused-vars` comments are removed from highlightedSkillIds (line ~72) and yearRange (line ~74) prop declarations
    - Component destructures new prop `justFilteredId` (with safe default — null or `= null` in destructure)
    - The render-loop fillOpacity expression uses shouldDimNode (`grep "shouldDimNode" src/game/renderers/SvgConstellation.js` returns ≥2 — one in helper definition, ≥1 in render loop)
    - Edge render code reduces opacity when both endpoints dim under active filters (search for `shouldDimNode` in edge rendering section)
    - Node `<g>` element receives `motion-safe:animate-chip-flash` class when `node.id === justFilteredId && !prefersReducedMotion`: `grep "animate-chip-flash" src/game/renderers/SvgConstellation.js` returns ≥1 (the class application)
    - `grep -E "transition:.*prefersReducedMotion.*'none'" src/game/renderers/SvgConstellation.js | wc -l` returns ≥1 (the fill-opacity transition is gated by reduced-motion per D-16-YEAR-EFFECT)
    - `npx vitest run src/game/renderers/SvgConstellation.test.js` exits 0 with all 25+ tests PASSING (Phase 15 22 + Phase 16 3 dim tests + 1 chip-flash test + 1 reduced-motion transition test)
    - One Phase 16 test asserts that under `matchMedia('(prefers-reduced-motion: reduce)') === { matches: true }`, a non-matching node circle's inline `style.transition` is `'none'` (NOT the default 200ms fade)
    - One Phase 16 test asserts that when `justFilteredId='Java'`, the `<g data-node-id="Java">` element has class containing `animate-chip-flash`; when `justFilteredId=null` or different node, the class is absent
    - The Phase 15 "does not dim before selectedSkillId is set" test (line 122-129) still PASSES — Phase 16 is additive
    - The Phase 15 single-select dim test still PASSES — fallback path preserved
    - `npx vitest run` shows no regression on useConstellation.test.js (which is GREEN from Task 1)
  </acceptance_criteria>
  <done>SvgConstellation.js extended with dim logic + chip-flash + reduced-motion transition guard. Renderer RED tests are GREEN; Phase 15 renderer tests remain GREEN. Commit: `feat(16): extend SvgConstellation — highlightedSkillIds/yearRange dim + chip-flash class + reduced-motion transition guard`</done>
</task>

</tasks>

<verification>
- `npx vitest run src/game/useConstellation.test.js src/game/renderers/SvgConstellation.test.js src/game/filters.test.js` — all GREEN
- `npx vitest run` — no regression across the full suite
- `npm run build` — exits 0; bundle size logged (no significant growth — hook + renderer extensions are < 1 kB gz; chip-flash state ≈ +0.3 kB gz per RESEARCH §10)
- SkillFilters.test.js + ExperienceCard.test.js + GameMode.test.js (new tests) still RED — expected, waiting on Plans 04/05/06
</verification>

<success_criteria>
- useConstellation.js exposes the full Phase 16 filter state surface (11+ public fields including justFilteredId, 4 new setters, 1 useRef-tracked timer)
- SvgConstellation.js dims nodes/edges based on highlightedSkillIds + yearRange while preserving Phase 15 single-select behavior
- SvgConstellation.js applies motion-safe:animate-chip-flash on the matching node <g> when justFilteredId === node.id (resolves checker BLOCKER 2)
- SvgConstellation.js fill-opacity transition is `'none'` under prefers-reduced-motion (resolves checker WARNING 6)
- 2 atomic commits (one per task)
- The constellation now visually responds to filter state + flashes the just-clicked tech chip — even before SkillFilters UI ships
</success_criteria>

<output>
Create `.planning/phases/16-filters-floating-experiencecard/16-03-SUMMARY.md` — list new hook fields/setters, the justFilteredId timer mechanism (100ms / 0ms under reduced-motion), the helper functions in SvgConstellation, the dim threshold (0.35), the chip-flash class application path, and confirm Phase 15 single-select behavior + reduced-motion transition guard both unchanged from Phase 15 baseline.
</output>
</content>
</invoke>