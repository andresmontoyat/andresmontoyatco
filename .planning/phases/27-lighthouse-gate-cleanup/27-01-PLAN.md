# Phase 27 · Plan 27-01 — Remove CRA/Vite-CSR leftovers

**Requirement:** DEPLOY-04 (cleanup half — gate + merge halves are operator-gated, see CONTEXT.md)
**Decision:** D-27-CLEANUP-SWEEP — remove all CRA/Vite-CSR leftovers in one focused pass.

## Goal

Delete the dead React CSR app, the dead root Vite entry, and the Vite-era bundle-gate
tooling so the branch contains only the Astro SSG surface. Build + tests stay GREEN.

## Tasks

### T1 — Delete legacy React CSR app
Remove (all verified zero active imports):
- `src/App.jsx`, `src/App.test.jsx`, `src/index.jsx`, `src/reportWebVitals.js`
- `src/components/{About,Claude,Contact,Experience,Footer,Nav,Projects,SectionPager,Skill}.jsx`
- `src/components/{Contact,Experience,SectionPager}.test.jsx`
- `src/components/_shared/` (SectionLabel.jsx, ThemeToggle.jsx)

### T2 — Delete dead Vite entry
- root `index.html` (references deleted `/src/index.jsx`; unreferenced by astro.config/vercel.json)

### T3 — Remove Vite-CSR bundle-gate tooling
- `scripts/check-bundle-gate.mjs`, `scripts/check-bundle-gate.test.mjs`
- `package.json`: drop `build:analyze` + `build:gate` (invoke `vite build`, broken post-T2)
- `package.json`: `lighthouse:local` + `lighthouse:mobile` local fallback `vite preview` → `astro preview` (keep `--port=4173`; deployed-URL path unchanged)

### T4 — Verify
- `npx vitest run` GREEN (count drops by removed bundle-gate specs; zero failures)
- `npm run build` succeeds (4 pages)
- `rg` for surviving references to any deleted module (outside `docs/`) → none

## Gate

Green build + green suite + zero dangling references. Commit atomically.

## Hand-off (27-02 / 27-03 — operator-gated, NOT in this plan)

After this commit the branch is cleanup-complete. Remaining to close v5:
1. Set `PUBLIC_SITE_URL` in Vercel env.
2. Deploy `v5-astro-migration` preview + Protection Bypass secret (21-05).
3. Run `lighthouse:deployed` + `lighthouse:check` on `/`, `/en`, `/es` (DEPLOY-04 / 27-02).
   - PASS → STATIC-02 Experience-half satisfied (D-26); proceed to merge (27-03).
   - FAIL → refactor Experience expand/collapse to native `<details>` (details-dismiss.js ready), re-gate.
4. Also close 24-05 (CV dropdown cross-browser QA) during the live pass.
