---
phase: 03-content-animations
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-og-image.js
  - scripts/og-template.html
  - public/og-image.png
autonomous: false
requirements_addressed: [SEO-01]
user_setup:
  - tool: chromium-headless
    why: "OG image generation via Playwright/Puppeteer screenshot — requires a Chromium binary"
    install: "If not already in node_modules: `npx playwright install chromium` (one-shot, ~150MB) OR install puppeteer which bundles its own Chromium"
must_haves:
  truths:
    - "public/og-image.png exists at exactly 1200x630px"
    - "The image shows the </cam> logomark, Carlos's name, role, divider, stats line, URL, and the profile photo (circular crop) per UI-SPEC visual spec"
    - "Background is solid #0D0D1A (ink-950) with a subtle dot-grid overlay matching bg-grid-subtle"
    - "Image weight is acceptable for social previews (< 200KB)"
  artifacts:
    - path: public/og-image.png
      provides: "1200x630 branded social preview card"
      min_size_bytes: 20000
    - path: scripts/generate-og-image.js
      provides: "One-shot regenerator script (developer can re-run if domain or copy changes)"
      contains: "page.screenshot"
    - path: scripts/og-template.html
      provides: "Static HTML template the script renders to PNG"
      contains: "Carlos Andrés Montoya Tobón"
  key_links:
    - from: index.html (Plan 03-06)
      to: public/og-image.png
      via: og:image content URL
      pattern: og-image\\.png
---

<objective>
Generate `public/og-image.png` — a 1200x630 branded social preview card matching the UI-SPEC visual spec — and commit a small reproducible toolchain (`scripts/og-template.html` + `scripts/generate-og-image.js`) so the image can be regenerated later if Carlos's photo, role, or domain changes.

Purpose: Plan 03-06 references `public/og-image.png` from the og:image and twitter:image meta tags. Without this file, social shares render with no preview image (or worse, a broken-image placeholder). The image is a one-time asset but the generation script is the contract — running it always produces the same output.
Output: Three files: the rendered PNG (binary), the HTML template, and a Node script. The script uses Playwright (already present? check `package.json`; if absent, this plan's checkpoint asks the user before installing). No production runtime dependency — `scripts/` is dev-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-animations/03-CONTEXT.md
@.planning/phases/03-content-animations/03-UI-SPEC.md
@public/images/me.webp
@tailwind.config.js

<interfaces>
UI-SPEC visual contract for the OG image (lines 894–922):

  Canvas: 1200 × 630px
  Background: #0D0D1A (ink-950) solid + dot-grid overlay (radial-gradient 32px tile)
  Left column (x: 80–640px): logomark, name, role, divider, stats, URL
  Right column (x: 700–1120px): circular profile photo, 320px diameter, brand-muted ring + glow

Color tokens (matched against tailwind.config.js):
  ink-950          = #0D0D1A
  brand            = #6C63FF
  brand muted      = rgba(108,99,255,0.4)
  text-primary     = #F0F0FF
  text-secondary   = #A0A0C0
  text-muted       = #606080

Fonts: Inter (400 + 800), JetBrains Mono (400). Self-hosted via @fontsource — must include via CDN OR @import in template HTML for Chromium to render correctly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/og-template.html (the static HTML rendered to PNG)</name>
  <files>scripts/og-template.html</files>
  <read_first>
    - .planning/phases/03-content-animations/03-UI-SPEC.md section "OG Image Specification (D-17, SEO-01)" (lines 892–924)
    - public/images/me.webp (verify file exists — referenced by template)
    - tailwind.config.js (verify color tokens — used to fill template)
  </read_first>
  <action>
    Create new file `scripts/og-template.html` with this exact content (a self-contained HTML page that renders the OG card. Sized at 1200x630 viewport):

    ```html
    <!DOCTYPE html>
    <html>
    <head>
      <meta charset="utf-8">
      <title>OG Image</title>
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
      <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;800&family=JetBrains+Mono:wght@400&display=swap" rel="stylesheet">
      <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        html, body {
          width: 1200px;
          height: 630px;
          background: #0D0D1A;
          font-family: 'Inter', system-ui, sans-serif;
          color: #F0F0FF;
          overflow: hidden;
        }
        .canvas {
          position: relative;
          width: 1200px;
          height: 630px;
          background:
            radial-gradient(circle, rgba(108,99,255,0.06) 1px, transparent 1px) 0 0 / 32px 32px,
            #0D0D1A;
        }
        .left {
          position: absolute;
          left: 80px;
          top: 80px;
          width: 560px;
        }
        .logomark {
          font-family: 'JetBrains Mono', monospace;
          font-size: 18px;
          color: #A0A0C0;
        }
        .logomark .slash { color: #6C63FF; }
        .name {
          margin-top: 60px;
          font-weight: 800;
          font-size: 52px;
          line-height: 1.05;
          color: #F0F0FF;
          letter-spacing: -0.02em;
        }
        .role {
          margin-top: 18px;
          font-size: 28px;
          line-height: 1.2;
        }
        .role .line1 { font-weight: 400; color: #A0A0C0; display: block; }
        .role .line2 { font-weight: 800; color: #6C63FF; display: block; }
        .divider {
          margin-top: 30px;
          width: 80px;
          height: 3px;
          background: #6C63FF;
        }
        .stats {
          margin-top: 24px;
          font-family: 'JetBrains Mono', monospace;
          font-size: 16px;
          color: #606080;
        }
        .url {
          position: absolute;
          left: 80px;
          bottom: 80px;
          font-family: 'JetBrains Mono', monospace;
          font-size: 14px;
          color: #6C63FF;
        }
        .photo {
          position: absolute;
          right: 80px;
          top: 50%;
          transform: translateY(-50%);
          width: 320px;
          height: 320px;
          border-radius: 50%;
          overflow: hidden;
          border: 3px solid rgba(108,99,255,0.4);
          box-shadow: 0 0 60px rgba(108,99,255,0.2);
        }
        .photo img {
          width: 100%;
          height: 100%;
          object-fit: cover;
          display: block;
        }
      </style>
    </head>
    <body>
      <div class="canvas">
        <div class="left">
          <div class="logomark">&lt;<span class="slash">/</span>cam&gt;</div>
          <div class="name">Carlos Andrés<br>Montoya Tobón</div>
          <div class="role">
            <span class="line1">Solutions Architect &amp;</span>
            <span class="line2">Senior Backend Engineer</span>
          </div>
          <div class="divider"></div>
          <div class="stats">18+ years &nbsp;·&nbsp; Java &amp; Spring &nbsp;·&nbsp; Medellín, CO</div>
        </div>
        <div class="url">andresmontoyatco.com</div>
        <div class="photo">
          <img src="../public/images/me.webp" alt="Carlos Andrés Montoya Tobón">
        </div>
      </div>
    </body>
    </html>
    ```

    Notes:
    - Fonts loaded from Google Fonts CDN — only used at generation time (not in production runtime). This is a one-shot rendering tool, not a deployed page.
    - The image source uses a relative path `../public/images/me.webp` so the script can render from `scripts/` directory.
    - The dot-grid background uses the same recipe as `bg-grid-subtle` from src/index.css (32px tile, brand at 6% alpha).
    - Layout numbers (positions, sizes, colors) come verbatim from UI-SPEC lines 894–922.
    - The `<br>` in the name field breaks "Carlos Andrés" / "Montoya Tobón" across two lines (UI-SPEC implies wrapping at 52px / line-height 1.05 — explicit break ensures determinism across font fallbacks).

    Code style: 2-space indent. No JS in this file (purely static HTML + CSS).
  </action>
  <verify>
    <automated>test -f scripts/og-template.html && grep -c "Carlos Andrés" scripts/og-template.html</automated>
    Expected: file exists; grep returns >= 1.
    Also: `grep -c "1200px" scripts/og-template.html` >= 2 (html and .canvas dimensions); `grep -c "630px" scripts/og-template.html` >= 2; `grep -c "0D0D1A" scripts/og-template.html` >= 1; `grep -c "6C63FF" scripts/og-template.html` >= 3 (brand uses); `grep -c "me.webp" scripts/og-template.html` == 1.
  </verify>
  <acceptance_criteria>
    - File `scripts/og-template.html` exists
    - `grep -F "Carlos Andrés" scripts/og-template.html` returns matches
    - `grep -F "Senior Backend Engineer" scripts/og-template.html` returns 1 match
    - `grep -F "andresmontoyatco.com" scripts/og-template.html` returns 1 match
    - `grep -F "1200px" scripts/og-template.html` returns >= 2 matches
    - `grep -F "630px" scripts/og-template.html` returns >= 2 matches
    - `grep -F "#0D0D1A" scripts/og-template.html` returns >= 1 match
    - `grep -F "#6C63FF" scripts/og-template.html` returns >= 3 matches
    - `grep -F "me.webp" scripts/og-template.html` returns 1 match
    - `grep -F "32px 32px" scripts/og-template.html` returns 1 match (dot-grid tile)
    - `grep -F "Inter:wght@400;800" scripts/og-template.html` returns 1 match
  </acceptance_criteria>
  <done>
    scripts/og-template.html is a self-contained 1200x630 HTML page that renders the UI-SPEC OG card layout (logomark, name, role, divider, stats, URL, circular photo) with the exact color tokens from the design system.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/generate-og-image.js (Node + Playwright screenshot)</name>
  <files>scripts/generate-og-image.js</files>
  <read_first>
    - package.json (check whether `playwright` or `puppeteer` is already a devDependency — if not, prefer Playwright)
    - .planning/phases/03-content-animations/03-UI-SPEC.md section "Playwright script location" (line 924)
    - scripts/og-template.html (must exist from Task 1)
  </read_first>
  <action>
    Create new file `scripts/generate-og-image.js` with this exact content:

    ```js
    /* eslint-disable no-console, import/no-extraneous-dependencies */
    // One-shot OG image generator. NOT part of the production build.
    // Run: `node scripts/generate-og-image.js`
    //
    // Requires Playwright (devDependency). Install with:
    //   npm install --save-dev playwright
    //   npx playwright install chromium
    //
    // Output: ./public/og-image.png (1200x630)
    const path = require('path')
    const { chromium } = require('playwright')

    const TEMPLATE = path.resolve(__dirname, 'og-template.html')
    const OUTPUT   = path.resolve(__dirname, '..', 'public', 'og-image.png')

    async function run() {
      const browser = await chromium.launch()
      const context = await browser.newContext({
        viewport: { width: 1200, height: 630 },
        deviceScaleFactor: 1,
      })
      const page = await context.newPage()
      await page.goto(`file://${TEMPLATE}`, { waitUntil: 'networkidle' })
      // Give web fonts a moment to settle
      await page.waitForTimeout(500)
      await page.screenshot({
        path: OUTPUT,
        type: 'png',
        omitBackground: false,
        clip: { x: 0, y: 0, width: 1200, height: 630 },
      })
      await browser.close()
      console.log(`OG image written: ${OUTPUT}`)
    }

    run().catch((err) => {
      console.error(err)
      process.exit(1)
    })
    ```

    Notes:
    - Standard CommonJS (`require`) — `scripts/` is a dev-only directory and Vite does not transpile it. Node runs it directly.
    - The eslint-disable comment is needed because (a) `console.log` is used and (b) `playwright` is a devDependency — both intentional.
    - The script reads the static template via `file://` URL and screenshots it.
    - `waitForTimeout(500)` gives Google Fonts enough time to load and apply (alternative: use `page.evaluate(() => document.fonts.ready)` — equivalent for this purpose).
    - Output path is `public/og-image.png` — referenced by the og:image meta in index.html (Plan 03-06).

    Code style: requires-style imports (Node CJS), 2-space indent, no semicolons.

    DO NOT add `playwright` to `package.json` automatically — that triggers a 150MB download. The next task is a CHECKPOINT that asks the user explicitly.
  </action>
  <verify>
    <automated>test -f scripts/generate-og-image.js && grep -c "page.screenshot" scripts/generate-og-image.js</automated>
    Expected: file exists; grep returns 1.
    Also: `grep -c "viewport: { width: 1200, height: 630" scripts/generate-og-image.js` returns 1; `grep -c "public/og-image.png\|og-image.png" scripts/generate-og-image.js` returns >= 1; `grep -c "chromium.launch" scripts/generate-og-image.js` returns 1.
  </verify>
  <acceptance_criteria>
    - File `scripts/generate-og-image.js` exists
    - `grep -F "page.screenshot" scripts/generate-og-image.js` returns 1 match
    - `grep -F "chromium.launch" scripts/generate-og-image.js` returns 1 match
    - `grep -F "width: 1200, height: 630" scripts/generate-og-image.js` returns >= 1 match (viewport + clip)
    - `grep -F "og-image.png" scripts/generate-og-image.js` returns 1 match
    - `grep -F "og-template.html" scripts/generate-og-image.js` returns 1 match
    - `grep -F "require('playwright')" scripts/generate-og-image.js` returns 1 match
  </acceptance_criteria>
  <done>
    scripts/generate-og-image.js exists and is a self-contained Playwright script that screenshots scripts/og-template.html at 1200x630 and writes the result to public/og-image.png.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Install Playwright (one-shot ~150MB), generate the image, and verify it</name>
  <what-built>
    Plan 03-07 has produced:
    - scripts/og-template.html (static HTML template — Task 1)
    - scripts/generate-og-image.js (Playwright screenshot script — Task 2)

    The user must now generate the actual `public/og-image.png` binary. This is gated as a checkpoint because the image generation requires installing Playwright (~150MB) which is a developer-environment decision.
  </what-built>
  <how-to-verify>
    1. Install Playwright as a devDependency (one-shot — adds to package.json devDependencies):
       ```
       npm install --save-dev playwright
       ```

    2. Install the Chromium binary that Playwright needs (~150MB download):
       ```
       npx playwright install chromium
       ```

    3. Run the generator:
       ```
       node scripts/generate-og-image.js
       ```
       Expected output: `OG image written: /absolute/path/to/public/og-image.png`

    4. Verify the file:
       ```
       file public/og-image.png
       ```
       Expected: `PNG image data, 1200 x 630, 8-bit/color RGBA, non-interlaced` (or similar — the key is "1200 x 630").

       ```
       ls -lh public/og-image.png
       ```
       Expected: file size between ~30KB and ~200KB.

    5. Open the file in any image viewer (Preview / Finder / web browser file://) — confirm visually:
       - Dark background (#0D0D1A)
       - "</cam>" logomark in top-left
       - "Carlos Andrés Montoya Tobón" in large white extrabold
       - "Solutions Architect &" in grey, "Senior Backend Engineer" in indigo (#6C63FF)
       - 80px brand-color divider line
       - "18+ years · Java & Spring · Medellín, CO" stats line in mono mid-grey
       - Profile photo on the right, circular, brand-tinted ring
       - "andresmontoyatco.com" in indigo at bottom-left

    6. (Optional) Test the OG card on a sharing tool: paste `https://andresmontoyatco.com/` into https://www.opengraph.xyz/ AFTER deploying — confirm the rendered preview matches.

    If the image looks wrong:
    - Check fonts loaded: try increasing `waitForTimeout` from 500 to 2000 in scripts/generate-og-image.js
    - Check photo: verify `public/images/me.webp` exists and is readable
    - Check colors: the template uses hex literals; if any look off, compare against `tailwind.config.js`

    7. Commit `public/og-image.png` to git. (It is a binary asset; small enough to be tracked.)
  </how-to-verify>
  <resume-signal>
    Type "approved" once `public/og-image.png` exists at 1200x630 and the visual content matches UI-SPEC. Or describe any issues so the template / script can be adjusted.
  </resume-signal>
</task>

</tasks>

<verification>
- `public/og-image.png` exists, is exactly 1200x630, and visually matches the UI-SPEC layout.
- Sharing the production URL on Slack / LinkedIn / X / iMessage produces a rich card preview using this image (verifiable post-deploy via opengraph.xyz).
- The script + template are committed so the asset can be regenerated deterministically.
</verification>

<success_criteria>
- File `scripts/og-template.html` exists and contains the spec-compliant layout.
- File `scripts/generate-og-image.js` exists and is a runnable Playwright screenshot script.
- File `public/og-image.png` exists at 1200x630 (after the user runs the checkpoint steps).
- The user has confirmed visual fidelity to UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-animations/03-07-SUMMARY.md` documenting: (a) the toolchain (template + script), (b) the runbook (`npm install --save-dev playwright && npx playwright install chromium && node scripts/generate-og-image.js`), (c) the resulting public/og-image.png file size and dimensions, (d) the relationship to Plan 03-06 (index.html references this image at the og:image and twitter:image meta tags).
</output>
