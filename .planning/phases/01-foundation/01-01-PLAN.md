---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - index.html
  - src/index.js
  - src/App.js
  - jsconfig.json
  - craco.config.js
  - postcss.config.js
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-04

must_haves:
  truths:
    - "npm run dev starts a Vite dev server on localhost:5173 and the app renders in the browser"
    - "npm run build completes without errors and produces a dist/ directory"
    - "React 18 createRoot is used — no ReactDOM.render call remains"
    - "Dead dependencies are absent from package.json and node_modules"
    - "The @ path alias resolves in both Vite and editor (jsconfig.json)"
  artifacts:
    - path: "vite.config.js"
      provides: "Vite 6 build config with React plugin and @ alias"
      contains: "plugin-react"
    - path: "index.html"
      provides: "Vite HTML entry point (moved to project root)"
      contains: "/src/index.js"
    - path: "src/index.js"
      provides: "React 18 mount using createRoot"
      contains: "createRoot"
  key_links:
    - from: "index.html"
      to: "src/index.js"
      via: "<script type=\"module\" src=\"/src/index.js\">"
      pattern: "src.*index.js"
    - from: "src/index.js"
      to: "src/App.js"
      via: "createRoot(document.getElementById('root')).render"
      pattern: "createRoot"
---

<objective>
Migrate the build system from Create React App (CRACO + react-scripts) to Vite 6 and upgrade React from 17 to 18. Remove all dead dependencies that add bundle weight with zero functionality.

Purpose: CRA is unmaintained. Every downstream task (Tailwind v3, font self-hosting, animation libraries) needs a working Vite build first. This plan unblocks all other Phase 1 work.
Output: A running Vite dev server, a passing production build, React 18 createRoot, and a lean package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vite 6 + React 18, remove dead dependencies, write vite.config.js</name>
  <files>package.json, vite.config.js, jsconfig.json, craco.config.js, postcss.config.js</files>

  <read_first>
    - package.json (understand current deps and scripts before touching)
    - craco.config.js (understand @ alias — must replicate in Vite)
    - jsconfig.json (understand editor path alias)
  </read_first>

  <action>
**Step 1 — Remove CRA + dead deps (all in one npm command):**
```
npm uninstall react-scripts @craco/craco react-router-dom react-hook-form axios react-scroll @headlessui/react web-vitals @fortawesome/fontawesome-svg-core @fortawesome/free-brands-svg-icons @fortawesome/free-solid-svg-icons @fortawesome/react-fontawesome
```

**Step 2 — Install Vite 6 + React 18 + @vitejs/plugin-react:**
```
npm install react@^18.3.1 react-dom@^18.3.1
npm install --save-dev vite@^6.0.0 @vitejs/plugin-react@^4.0.0
```

**Step 3 — Update package.json scripts** (replace craco-based scripts):
```json
"scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "lint": "eslint src --ext .js,.jsx"
}
```
Remove the `"eject"` script. Remove the `"eslintConfig"` block (it is CRA-specific; config lives in .eslintrc.js).

**Step 4 — Create vite.config.js** at project root:
```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { fileURLToPath, URL } from 'node:url'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
})
```

**Step 5 — Update jsconfig.json** to match Vite alias (same value as before, just verifying):
```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
```

**Step 6 — Delete craco.config.js and postcss.config.js.** Vite handles PostCSS via vite.config.js. The PostCSS config will be recreated in Plan 02 when Tailwind v3 is installed.

Do NOT touch src/ files in this task — App.js and index.js are updated in Task 2.
  </action>

  <verify>
    <automated>node -e "const p = require('./package.json'); const bad = ['react-scripts','@craco/craco','axios','react-router-dom','react-hook-form','react-scroll','@headlessui/react','web-vitals']; const found = bad.filter(d => p.dependencies[d] || p.devDependencies[d]); if(found.length) { console.error('FAIL still present:', found); process.exit(1); } console.log('OK — dead deps absent')"</automated>
  </verify>

  <acceptance_criteria>
    - package.json scripts contains "dev": "vite" and "build": "vite build" — grep verifiable: `grep '"dev": "vite"' package.json`
    - vite.config.js exists at project root with `@vitejs/plugin-react` import — `grep 'plugin-react' vite.config.js`
    - None of these appear in package.json dependencies or devDependencies: react-scripts, @craco/craco, axios, react-router-dom, react-hook-form, react-scroll, @headlessui/react, web-vitals — `node -e "..." ` check above passes
    - craco.config.js does not exist: `test ! -f craco.config.js && echo OK`
  </acceptance_criteria>

  <done>Vite 6 configured, React 18 packages installed, dead deps uninstalled, scripts updated, craco deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate HTML entry point and React 18 mount</name>
  <files>index.html, src/index.js, src/App.js</files>

  <read_first>
    - public/index.html (current HTML shell — must preserve meta tags, title, GA script placement)
    - src/index.js (current React 17 mount using ReactDOM.render)
    - src/App.js (verify no CRA-specific imports like reportWebVitals)
  </read_first>

  <action>
**Step 1 — Move public/index.html to project root** (Vite requires HTML at root, not in /public).

Copy `public/index.html` to `./index.html` and modify it for Vite:
- Change `%PUBLIC_URL%/favicon.ico` → `/favicon.ico`
- Change `%PUBLIC_URL%/logo192.png` → `/logo192.png`
- Change `%PUBLIC_URL%/manifest.json` → `/manifest.json`
- Remove the Google Fonts `<link>` tags entirely (will be replaced with self-hosted fonts in Plan 04)
- Add Vite module entry point INSIDE `<body>`, before `</body>`: `<script type="module" src="/src/index.js"></script>`
- Keep the GA script block and `<meta>` tags unchanged
- Keep `<div id="root"></div>` unchanged

The GA measurement ID in the current file is wrong (G-1GDDRC3G12). Fix it to G-4TZJGR3MXR in both gtag script locations.

Keep `public/index.html` in place as well — delete it only after verifying the build works (the Vite build copies files from `public/` automatically).

**Step 2 — Rewrite src/index.js** for React 18:
```js
import React from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App'

const root = createRoot(document.getElementById('root'))
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```
Remove the `reportWebVitals` import and call (reportWebVitals.js file can remain but will not be called).

**Step 3 — Check App.js** for any import that no longer exists (e.g., if it imported anything from removed packages). Current App.js imports only from `./i18n/LanguageContext` and `./components/*` — no changes needed. Confirm by reading App.js before writing.

**Step 4 — Delete public/index.html** after the file at `./index.html` is confirmed correct. Vite uses `./index.html` at root; files in `public/` are copied verbatim to output. Having both would result in a duplicate.
  </action>

  <verify>
    <automated>grep -n "createRoot" src/index.js && grep -n 'type="module"' index.html && grep -n "G-4TZJGR3MXR" index.html && echo "ALL CHECKS PASSED"</automated>
  </verify>

  <acceptance_criteria>
    - `index.html` exists at project root (not only in public/): `test -f index.html && echo OK`
    - `index.html` contains `<script type="module" src="/src/index.js">`: `grep 'type="module"' index.html`
    - `index.html` does NOT contain `%PUBLIC_URL%`: `grep -v '%PUBLIC_URL%' index.html | wc -l` equals total line count
    - `src/index.js` uses `createRoot` (React 18 API): `grep 'createRoot' src/index.js`
    - `src/index.js` does NOT contain `ReactDOM.render`: `grep -L 'ReactDOM.render' src/index.js`
    - GA ID is G-4TZJGR3MXR (not old wrong ID G-1GDDRC3G12): `grep 'G-4TZJGR3MXR' index.html`
    - `npm run dev` starts without errors (manual verification after task completes)
  </acceptance_criteria>

  <done>index.html at project root with Vite module entry and correct GA ID. src/index.js uses React 18 createRoot. App renders in browser via npm run dev.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Vite build passes end-to-end</name>
  <files>dist/ (generated, not committed)</files>

  <read_first>
    - vite.config.js (verify alias config is correct before build)
    - src/index.css (check for any CRA-specific syntax that would break Vite's CSS processor)
  </read_first>

  <action>
Run the build and interpret output:

```bash
npm run build
```

**Expected:** Build completes. `dist/` directory is created with `index.html`, `assets/*.js`, `assets/*.css`.

**If build fails with "Cannot find module" for an @/ import:** The alias config in vite.config.js is correct but the source file has an import error. Read the failing file, check the import path, fix the path.

**If build fails with CSS processing error:** The tailwind directives (`@tailwind base`) require Tailwind to be installed. If Tailwind v2 compat shim is still present in devDependencies, the build will fail. Remove tailwindcss from devDependencies and add a temporary empty postcss.config.js with `{}` so CSS passes through until Plan 02 installs Tailwind v3.

**If build succeeds but `npm run dev` shows import errors in browser console:** Check for any remaining `reportWebVitals` references or CRA env var patterns (`process.env.REACT_APP_*`). Remove them.

After a passing build, also verify dev server:
```bash
npm run dev
```
The terminal should output `VITE v6.x.x ready` and `Local: http://localhost:5173/`.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `npm run build` exits with code 0: `npm run build && echo "BUILD OK"`
    - `dist/index.html` exists after build: `test -f dist/index.html && echo OK`
    - `dist/assets/` contains at least one `.js` file: `ls dist/assets/*.js | head -1`
    - No "Cannot resolve" or "Module not found" in build output
  </acceptance_criteria>

  <done>npm run build exits 0. dist/ directory contains bundled assets. App is confirmed runnable with Vite.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` exits 0 with dist/ output
2. `npm run dev` starts on localhost:5173 and app renders
3. `grep 'createRoot' src/index.js` returns a match
4. `grep 'G-4TZJGR3MXR' index.html` returns two matches (one per gtag call)
5. None of the removed packages appear in package.json
</verification>

<success_criteria>
- Vite 6 dev server starts and serves the React app
- Production build completes with no errors
- React 18 createRoot in use — ReactDOM.render gone
- Dead packages (axios, react-router-dom, react-hook-form, react-scroll, @headlessui/react, web-vitals, @craco/craco, react-scripts, all @fortawesome/*) absent from package.json
- GA ID corrected to G-4TZJGR3MXR in index.html
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template.
</output>
