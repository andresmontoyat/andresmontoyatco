---
phase: 16-filters-floating-experiencecard
plan: 02
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - src/game/filters.js
  - src/i18n/translations.js
  - tailwind.config.js
  - src/index.css
autonomous: true
requirements: [GAME-03, GAME-04]
tags: [phase-16, pure-logic, tdd-green, i18n, tokens]
must_haves:
  truths:
    - "filters.js is a pure module — zero React/DOM imports, deterministic, near-100% unit-test coverage"
    - "yearBounds(EXPERIENCE) returns [2007, 2026] derived from live data (D-16-YEAR-BOUNDS honesty rule)"
    - "filterBySkillIntersection enforces AND semantics: Java + Spring Boot returns ONLY jobs where Carlos used BOTH (D-16-INTERSECT-AND)"
    - "All 13+ filters.test.js assertions pass; the pure-logic RED suite from Plan 01 is now GREEN"
    - "t.game.* extended with 20+ bilingual keys for filter bar, year slider, card heading, CV CTA, empty state, close button, locked-skill help text"
    - "Tailwind config gains chip/slider/card/cvCta color tokens + cardFadeIn/cardSlideUp/cardSwap*/chipFlash keyframes + matching motion-safe animation utilities"
    - "src/index.css gains Phase 16 CSS-var block in :root + [data-theme='light'] override + .scrollbar-hide utility"
  artifacts:
    - path: "src/game/filters.js"
      provides: "Pure selector module with 6 named exports"
      contains: "export function filterBySkillIntersection"
    - path: "src/i18n/translations.js"
      provides: "Extended t.game.* namespace (20+ new keys, EN+ES symmetric)"
      contains: "filterReset"
    - path: "tailwind.config.js"
      provides: "chip/slider/card/cvCta color tokens + Phase 16 keyframes/animations"
      contains: "cardFadeIn"
    - path: "src/index.css"
      provides: "Phase 16 CSS vars + scrollbar-hide utility"
      contains: "--color-chip-active-bg"
  key_links:
    - from: "src/game/filters.js"
      to: "src/data/experience.js, src/data/skills.js"
      via: "named imports (zero React; pure module)"
      pattern: "export function composeFilters"
    - from: "tailwind.config.js"
      to: "src/index.css"
      via: "var(--color-chip-active-bg) etc — Tailwind utilities resolve via CSS vars"
      pattern: "var\\(--color-chip"
---

<objective>
Ship the pure-logic foundation and design-token surface for Phase 16: the filters.js selector module + bilingual i18n keys + Tailwind colors/keyframes + CSS variables. After this plan, the Wave 0 RED pure-logic suite (filters.test.js) is GREEN and all subsequent component plans have the tokens, animations, and copy strings they need.

Purpose: filters.js is the single source of truth for AND-intersection / year-range / category filtering — pure, deterministic, and unit-tested near-100%. Locking the i18n and design tokens in Wave 1 means SkillFilters / ExperienceCard / SvgConstellation extensions in Waves 2-4 only consume them — no token coordination across plans, no late translation key additions.

Output: 1 new file (filters.js) + 3 extended config/data files. Pure-logic test suite GREEN; component test suites still RED until their wave lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md
@.planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md
@.planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md
@.planning/phases/16-filters-floating-experiencecard/16-UI-SPEC.md
@.planning/phases/14-foundation-data-layer-viewmode-toggle-test-infra/14-01-SUMMARY.md
@src/game/spatialNav.js
@src/game/constellation.graph.js
@src/data/experience.js
@src/data/skills.js
@src/i18n/translations.js
@tailwind.config.js
@src/index.css

<interfaces>
<!-- Public API of filters.js (consumed by Plan 03 hook + Plan 06 GameMode wiring) -->

const CURRENT_YEAR = 2026  // module constant, matches constellation.graph.js — no new Date()

export function filterByYearRange(experiences, [from, to])
  // [from, to] may be null/undefined → pass-through
  // intersection rule: start <= to && end >= from where end = period.end ?? CURRENT_YEAR

export function filterBySkillIntersection(experiences, skillIds)
  // skillIds = [] or null → pass-through
  // AND: experiences.filter((e) => skillIds.every((s) => e.tech.includes(s)))

export function filterByCategory(experiences, category, skills)
  // category=null → pass-through
  // experiences.filter((e) => e.tech.some((t) => skills[t]?.category === category))

export function composeFilters(experiences, { skillIds, yearRange, category }, skills)
  // Chains the 3 above; applies skill intersection first (most-selective), then year, then category

export function visibleSkillIds(matchingExperiences)
  // Returns Array.from(new Set) of all e.tech strings across matchingExperiences — dedupe + flatten

export function yearBounds(experiences)
  // Returns [min(period.start), max(period.end ?? CURRENT_YEAR)] — used to derive YEAR_BOUNDS once at module load

<!-- Phase 14 SKILLS data shape (from skills.js — confirmed by 14-01-SUMMARY): -->
SKILLS = { 'Java': { id, label, category, color, aliases }, ... }
SKILL_CATEGORIES = { 'lang': { en, es, color }, 'cloud': { en, es, color }, ... }  // 8 entries
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement src/game/filters.js (pure selectors — flip filters.test.js GREEN)</name>
  <files>src/game/filters.js</files>
  <read_first>
    - src/game/filters.test.js (RED suite from Plan 01 — the contract this file must satisfy)
    - src/game/spatialNav.js (analog: pure-function module shape, named exports, zero React)
    - src/game/constellation.graph.js (analog: module-level CURRENT_YEAR constant — DO NOT use `new Date()`, must be deterministic)
    - src/data/experience.js (shape: period.start, period.end, tech[])
    - src/data/skills.js (SKILLS map shape: skills[techString]?.category)
    - .planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md §1 (function bodies lines 302-372)
    - .planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md Wave 1 filters.js section (lines 339-385)
    - .planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md (D-16-INTERSECT-AND, D-16-YEAR-BOUNDS)
  </read_first>
  <behavior>
    - filterByYearRange returns pass-through when from/to nullish; otherwise returns experiences whose period [start, end ?? CURRENT_YEAR] intersects [from, to]
    - filterBySkillIntersection enforces AND: every skill in skillIds must appear in experience.tech
    - filterByCategory passes through when category nullish; otherwise keeps experiences where at least one tech maps to the category via skills[t]?.category
    - composeFilters chains the 3 selectors over a single accumulator; order: skill intersection → year → category
    - visibleSkillIds dedupes via Set across all matching experiences' tech arrays
    - yearBounds derives [min, max] purely from EXPERIENCE — DO NOT hardcode [2007, 2026]
    - All functions are pure (no mutation, return new arrays) and import zero React/DOM
  </behavior>
  <action>Create src/game/filters.js (named exports only, NO export default — mirrors spatialNav.js). Module-level `const CURRENT_YEAR = 2026` matching constellation.graph.js per D-16-YEAR-BOUNDS determinism. Implement filterByYearRange, filterBySkillIntersection, filterByCategory, composeFilters, visibleSkillIds, yearBounds per the function bodies in 16-RESEARCH.md §1 lines 302-372. composeFilters signature: `(experiences, { skillIds, yearRange, category }, skills)` — note third arg is the SKILLS catalog so filterByCategory can resolve tech → category without importing SKILLS inside the module. Zero React imports. Zero DOM imports. Zero memoization (per RESEARCH.md §8 — 84 string ops/change is negligible). No semicolons, no comma-dangle. Run `npx vitest run src/game/filters.test.js` and confirm GREEN.</action>
  <verify>
    <automated>npx vitest run src/game/filters.test.js 2>&amp;1 | tee /tmp/wave1-filters.log; grep -E "passed|failed" /tmp/wave1-filters.log | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/game/filters.js exists with exactly these named exports: filterByYearRange, filterBySkillIntersection, filterByCategory, composeFilters, visibleSkillIds, yearBounds (no default export)
    - Module declares `const CURRENT_YEAR = 2026` at top scope (NOT `new Date().getFullYear()`)
    - File has zero React imports: `grep -v '^//' src/game/filters.js | grep -E "from 'react'|from '@testing"` returns 0 lines
    - filterBySkillIntersection uses `.every` not `.some` for AND semantics (D-16-INTERSECT-AND verified by grep)
    - `npx vitest run src/game/filters.test.js` exits 0 with all 13+ tests PASSING
    - `yearBounds(EXPERIENCE)` returns `[2007, 2026]` as asserted by the honesty-rule test
    - No production files outside src/game/filters.js are modified in this task
    - File follows no-semicolons + no-comma-dangle ESLint contract; running `npm run lint -- src/game/filters.js` (if available) returns clean or only existing-baseline warnings
  </acceptance_criteria>
  <done>filters.js committed and filters.test.js is GREEN (13+ tests passing). Single commit: `feat(16): add filters.js pure selectors (GREEN)`. Wave 0 pure-logic RED → GREEN cycle complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend src/i18n/translations.js with t.game.* Phase 16 bilingual keys</name>
  <files>src/i18n/translations.js</files>
  <read_first>
    - src/i18n/translations.js (existing t.game.* block in en + es — Phase 15 keys lines 24-42 EN + symmetric ES; mirror the shape, do not break)
    - .planning/phases/16-filters-floating-experiencecard/16-UI-SPEC.md §Copywriting Contract (the locked EN/ES table — 20+ keys)
    - .planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md (D-16-FLAYOUT-RESET locks EN 'Reset' / ES 'Limpiar'; D-16-CARD-CV-CTA locks EN 'Download CV (English)' / ES 'Descargar CV (Español)')
    - .planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md Wave 1 translations.js section (lines 388-446)
    - src/game/renderers/SvgConstellation.js line 115-118 (.replace('{name}', node.label) substitution pattern to mirror for cardHeading/cardJobsCount/cardJobsListLabel)
  </read_first>
  <behavior>
    - Add 20+ new keys to t.game in both en and es blocks; existing Phase 15 keys are preserved (no rename, no delete)
    - Bilingual symmetry: every new EN key has a corresponding ES key with the same name; ES values are real Spanish (not English placeholders)
    - Template strings use `{var}` placeholders to be substituted via `.replace('{var}', value)` at call site (mirrors SvgConstellation.js line 115)
    - D-16-FLAYOUT-RESET copy: filterReset = 'Reset' / 'Limpiar'
    - D-16-CARD-CV-CTA copy: cvCtaLabel = 'Download CV (English)' / 'Descargar CV (Español)'
  </behavior>
  <action>EXTEND src/i18n/translations.js. Inside the existing `en: { game: { ... } }` block (after Phase 15 keys, before closing brace), append the 20 Phase 16 keys with exact EN values from 16-UI-SPEC.md Copywriting Contract: filterBarLabel, filterSkillsLabel, filterCategoryLabel, yearLabel, yearStartLabel, yearEndLabel, yearFrom, yearTo, filterReset ('Reset'), filterEmpty ('No matches — try fewer filters'), filterEmptyHint, cardClose ('Close'), cardHeading ('{skill}'), cardJobsListLabel ('Jobs using {skill}'), cardJobsCount ('{n} jobs'), cardJobsCountSingular ('1 job'), cvCtaLabel ('Download CV (English)'), techChipsLabel ('Technologies used'), yearsChipLabel ('Years'), skillActiveAndLocked. MIRROR EXACTLY in the `es: { game: { ... } }` block with Spanish translations: filterReset ('Limpiar'), filterEmpty ('Sin coincidencias — prueba menos filtros'), cardClose ('Cerrar'), cvCtaLabel ('Descargar CV (Español)'), etc. — pull all ES values from the UI-SPEC table verbatim. Do NOT rename or remove any Phase 15 keys. No semicolons, no comma-dangle on the function-call/return level (object literal trailing commas are fine per airbnb).</action>
  <verify>
    <automated>node -e "const t = require('./src/i18n/translations.js').default || require('./src/i18n/translations.js'); const keys = ['filterReset','filterBarLabel','cardHeading','cvCtaLabel','filterEmpty','cardClose','yearLabel','yearStartLabel','yearEndLabel','techChipsLabel','cardJobsCount','skillActiveAndLocked']; const missing = keys.flatMap(k => [['en', t.en?.game?.[k] ?? t.translations?.en?.game?.[k]], ['es', t.es?.game?.[k] ?? t.translations?.es?.game?.[k]]].filter(([,v]) => !v).map(([l]) => l+'.'+k)); console.log(missing.length === 0 ? 'OK all 12 keys present in both langs' : 'MISSING: '+missing.join(','))" 2>&amp;1 || grep -c "filterReset\\|cvCtaLabel\\|cardHeading\\|filterEmpty" src/i18n/translations.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "filterReset" src/i18n/translations.js` returns 2 (one for en, one for es)
    - `grep -c "cvCtaLabel" src/i18n/translations.js` returns 2
    - `grep -c "cardHeading" src/i18n/translations.js` returns 2
    - `grep -c "filterBarLabel" src/i18n/translations.js` returns 2
    - `grep "filterReset.*Reset" src/i18n/translations.js` matches (EN value)
    - `grep "filterReset.*Limpiar" src/i18n/translations.js` matches (ES value)
    - `grep "cvCtaLabel.*Download CV" src/i18n/translations.js` matches
    - `grep "cvCtaLabel.*Descargar CV" src/i18n/translations.js` matches
    - `grep "Sin coincidencias — prueba menos filtros" src/i18n/translations.js | wc -l` returns exactly 1 (ES filterEmpty matches UI-SPEC verbatim — addresses checker WARNING 3)
    - All Phase 15 keys still present: `grep -c "constellationLabel" src/i18n/translations.js` still returns 2
    - `npm run test:run -- --reporter=basic 2>&1 | grep -E "translations|i18n"` shows no new failures (existing tests still GREEN)
  </acceptance_criteria>
  <done>translations.js committed with 20+ new t.game.* keys in symmetric EN/ES blocks. Existing Phase 15 keys unchanged. Commit: `feat(16): extend t.game.* with filter + card + CV CTA keys`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend tailwind.config.js + src/index.css with Phase 16 tokens + keyframes</name>
  <files>tailwind.config.js, src/index.css</files>
  <read_first>
    - tailwind.config.js (existing Phase 15 keyframes lines 70-110 + colors.constellation/hintPill lines 40-49 — mirror shape)
    - src/index.css (Phase 15 :root vars lines 62-66 + [data-theme="light"] overrides lines 129-133 — mirror block structure; existing prefers-reduced-motion suppression rule)
    - .planning/phases/16-filters-floating-experiencecard/16-UI-SPEC.md §Color tokens + Keyframes (the locked CSS-var + keyframe definitions)
    - .planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md Wave 1 tailwind.config.js + src/index.css sections (lines 449-566)
    - .planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md §4 (cardSlideUp keyframe), §6 (cardSwap-out/in), §7 (chipFlash)
  </read_first>
  <behavior>
    - tailwind.config.js theme.extend.colors gains nested objects: chip, slider, card, cvCta — each value reads from a CSS-var (no raw hex except SKILL_CATEGORY_COLORS exception which stays as data, not config)
    - tailwind.config.js theme.extend.keyframes gains cardFadeIn, cardSlideUp, cardSwapOut, cardSwapIn, chipFlash
    - tailwind.config.js theme.extend.animation gains 'card-fade-in', 'card-slide-up', 'card-swap-out', 'card-swap-in', 'chip-flash' utilities matching the keyframes (kebab class name from camelCase keyframe)
    - src/index.css :root gains a Phase 16 token block (chip-active-bg, chip-outline-border/text, slider-track/range/thumb/thumbBorder, card-bg/border/shadow/overlay-bg, cv-cta-bg/text/hover-bg) with values from UI-SPEC
    - src/index.css [data-theme="light"] gains the matching Phase 16 light overrides
    - src/index.css gains a .scrollbar-hide global utility for mobile horizontal-scroll chip row (mobile toolbar)
    - Existing global `prefers-reduced-motion` rule from Phase 15 already suppresses these new animations — do NOT add a new media query
  </behavior>
  <action>EXTEND tailwind.config.js. Add to theme.extend.colors (after existing constellation/hintPill block): chip {activeBg, activeText, outlineBorder, outlineText} all reading from `var(--color-chip-*)`; slider {track, range, thumb, thumbBorder} from `var(--color-slider-*)`; card {bg, border, overlay} from `var(--color-card-*)`; cvCta {bg, text, hoverBg} from `var(--color-cv-cta-*)`. Use exact key shapes from 16-PATTERNS.md lines 454-477. Add to theme.extend.keyframes: cardFadeIn (opacity 0→1 + scale 0.96→1.0), cardSlideUp (opacity 0→1 + translateY 100%→0%), cardSwapOut (opacity 1→0), cardSwapIn (opacity 0→1), chipFlash (opacity 1→0.5→1 + scale 0.94→1.0). Add to theme.extend.animation: 'card-fade-in': 'cardFadeIn 200ms cubic-bezier(0.22, 1, 0.36, 1) both', 'card-slide-up': 'cardSlideUp 250ms cubic-bezier(0.22, 1, 0.36, 1) both', 'card-swap-out': 'cardSwapOut 75ms ease-in both', 'card-swap-in': 'cardSwapIn 75ms ease-out both', 'chip-flash': 'chipFlash 100ms ease-out both' (16-PATTERNS.md lines 501-508 verbatim). Include the existing comment-style mandate "MUST be applied with motion-safe: prefix" (mirror line 88).

  EXTEND src/index.css. Inside `:root { ... }` AFTER the Phase 15 constellation token block, insert a comment-header block `/* ── Filter chips + slider + card surfaces (Phase 16) ── */` then the 14 CSS vars from UI-SPEC (--color-chip-active-bg through --color-cv-cta-hover-bg). Inside `[data-theme="light"] { ... }` AFTER the existing Phase 15 light overrides, add a matching `/* ── Filter + card light-theme overrides (Phase 16) ── */` block with the light values. Append a global `.scrollbar-hide` utility class (scrollbar-width: none + ::-webkit-scrollbar { display: none } + -webkit-overflow-scrolling: touch). Do NOT add new prefers-reduced-motion rules — existing global rule covers the new keyframes. No semicolons in JS edit; CSS is unchanged stylistically.</action>
  <verify>
    <automated>grep -c "var(--color-chip-active-bg)\\|var(--color-card-bg)\\|var(--color-slider-thumb)\\|var(--color-cv-cta-bg)" tailwind.config.js; grep -c "cardFadeIn\\|cardSlideUp\\|chipFlash" tailwind.config.js; grep -c "card-fade-in\\|card-slide-up\\|chip-flash" tailwind.config.js; grep -c -- "--color-chip-active-bg\\|--color-card-bg\\|--color-slider-thumb\\|--color-cv-cta-bg" src/index.css; grep -c "scrollbar-hide" src/index.css &amp;&amp; npm run build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "chip:" tailwind.config.js` returns ≥1 inside the extend.colors block
    - `grep -E "card-fade-in|card-slide-up|card-swap-out|card-swap-in|chip-flash" tailwind.config.js` returns 5 utility names
    - `grep -E "cardFadeIn|cardSlideUp|cardSwapOut|cardSwapIn|chipFlash" tailwind.config.js` returns 5 keyframe names
    - `grep -c -- "--color-chip-active-bg" src/index.css` returns ≥2 (in :root and in [data-theme="light"])
    - `grep -c -- "--color-card-overlay-bg" src/index.css` returns ≥2
    - `grep -c "scrollbar-hide" src/index.css` returns ≥1 (the utility class definition)
    - `npm run build` exits 0 with no Tailwind config errors and produces a dist/ — game-mode chunk size logged for Wave 5 budget check
    - Phase 15 constellation tokens still present: `grep -c "constellation-node-fill" src/index.css` returns ≥2
  </acceptance_criteria>
  <done>tailwind.config.js + src/index.css extended with Phase 16 tokens. `npm run build` succeeds. Commit: `feat(16): add Phase 16 tokens + keyframes + scrollbar-hide utility`</done>
</task>

</tasks>

<verification>
- `npx vitest run src/game/filters.test.js` — 13+ tests GREEN
- `npx vitest run` — no regression on Phase 14/15 tests
- `npm run build` — exits 0; dist/assets/GameMode-*.js still ≤ 6 kB gz (no production hook/component code yet, only tokens)
- All 6 Phase 16 component test files (Plan 01) still RED — filters.js GREEN but other production code missing
</verification>

<success_criteria>
- filters.js committed with 6 named exports + CURRENT_YEAR constant
- translations.js gains 20+ symmetric EN/ES keys
- tailwind.config.js + src/index.css gain matching Phase 16 tokens, keyframes, animation utilities
- 3 atomic commits, one per task
- Pure-logic RED suite GREEN; component RED suites still RED (expected — they wait for Waves 2-5)
</success_criteria>

<output>
Create `.planning/phases/16-filters-floating-experiencecard/16-02-SUMMARY.md` — list filters.js exports, count of new translation keys per lang, list of tokens/keyframes added, and confirm bundle size impact (CSS may grow slightly; JS untouched).
</output>
