---
phase: 15-accessible-constellation-seo-fallback
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/index.css
  - tailwind.config.js
  - src/game/useConstellation.js
  - src/game/useConstellation.test.js
  - src/game/renderers/SvgConstellation.js
  - src/game/renderers/SvgConstellation.test.js
  - src/game/GameMode.js
  - src/game/GameMode.test.js
autonomous: true
requirements:
  - GAME-01
  - GAME-02
tags: [game-mode, renderer, svg, tokens, keyframes, capability-detection, reduced-motion, mvp-slice-2]

must_haves:
  truths:
    - "The renderer slot from Slice 1 now paints an SVG constellation: skill nodes sized by `√count`, edges of weight≥2 always visible, weight-1 edges hidden by default, all in category colors against a token-driven backdrop."
    - "The renderer is driven by the locked props contract `{ nodes, edges, layout, highlightedSkillIds, selectedSkillId, yearRange, theme, lang, t, onSelectSkill, onHoverSkill }` — no SVG-specific props leak through; a future WebGL adapter plugs into the same surface."
    - "`GameMode` performs capability detection (prefers-reduced-motion, saveData, isMobile, hasWebGL) once on mount; Phase 15 always resolves to `SvgConstellation`; the decision shape is forward-compatible with Phase 17 WebGL lazy chunk."
    - "Reduced-motion path: animation classes are gated by `motion-safe:` prefix and `animationDelay`/`animationName` are conditionally null on reduced-motion; the global `index.css @media (prefers-reduced-motion: reduce)` rule remains the safety net."
    - "Constellation tokens (`--color-constellation-edge`, `--color-constellation-edge-heavy`, `--color-constellation-halo`, `--color-hint-pill-bg`, `--color-hint-pill-text`) exist in both `:root` (dark) and `[data-theme=\"light\"]` blocks of `src/index.css`."
    - "AA contrast holds on light theme: nodes from the failing/borderline color list (arch cyan, devops amber, hardware pink, etc.) render with a one-shade-darker `stroke` of `1.5` viewBox units to lift contrast to ≥3:1 for UI elements (per 15-UI-SPEC.md §WCAG AA Contrast verification)."
  artifacts:
    - path: "src/game/renderers/SvgConstellation.js"
      provides: "Pure visual renderer behind the locked props contract: SVG with viewBox 0 0 1000 1000, nodes (with √count radius + theme-aware light-mode stroke), edges (weight gating), reveal animation (motion-safe), halo on selected (filter drop-shadow), hint pill placeholder. NO keyboard/role-application interaction yet — that's Slice 3."
      exports: ["default SvgConstellation"]
    - path: "src/game/renderers/SvgConstellation.test.js"
      provides: "Renders nodes/edges from fixture graph; weight-1 edges hidden; weight≥2 visible; reduced-motion path skips animationDelay; light-theme stroke applied; renderer contract props validation"
    - path: "src/game/useConstellation.js"
      provides: "Hook holding selection/hover state + Phase 16 filter placeholders (empty defaults); returns the props payload for SvgConstellation"
      exports: ["default useConstellation"]
    - path: "src/game/useConstellation.test.js"
      provides: "State transitions: initial null, onSelectSkill sets, second call toggles off, onHoverSkill updates hoveredSkillId"
    - path: "tailwind.config.js"
      provides: "Three new keyframes (nodeReveal, edgeReveal, hintFadeOut) + three new animation utilities + constellation/hintPill color tokens mapped to CSS vars"
    - path: "src/index.css"
      provides: "Five new CSS custom properties in `:root` (dark) and `[data-theme=\"light\"]` (light overrides)"
    - path: "src/game/GameMode.js"
      provides: "Slice 2 modification: replaces the Slice 1 placeholder `<p data-testid='renderer-placeholder'>` with `<SvgConstellation ...props />`; integrates `useConstellation(nodes)` + capability detection; reads `usePrefersReducedMotion`"
  key_links:
    - from: "src/game/GameMode.js"
      to: "src/game/renderers/SvgConstellation"
      via: "<SvgConstellation nodes={nodes} edges={edges} layout={layout} {...consHookValues} theme={theme} lang={lang} t={t} />"
      pattern: "<SvgConstellation"
    - from: "src/game/GameMode.js"
      to: "src/game/useConstellation"
      via: "default-imported hook, called with the same `nodes` from buildConstellationGraph"
      pattern: "useConstellation\\("
    - from: "src/game/renderers/SvgConstellation.js"
      to: "src/game/constellation.layout.js"
      via: "import { computeLayout } NOT at renderer level — `layout` is a prop passed in from GameMode"
      pattern: "layout\\["
    - from: "src/game/renderers/SvgConstellation.js"
      to: "tailwind.config.js keyframes"
      via: "className `motion-safe:animate-node-reveal` + `motion-safe:animate-edge-reveal`"
      pattern: "motion-safe:animate-(node|edge)-reveal"
    - from: "src/index.css"
      to: "tailwind.config.js theme.extend.colors"
      via: "var(--color-constellation-edge) referenced by `constellation.edge` Tailwind token"
      pattern: "var\\(--color-constellation-"
---

<objective>
Ship MVP Slice 2: the visual SvgConstellation renderer plugs into the Slice 1 plug-point. Visitor lands → sees the bilingual derived-H1 (from Slice 1) → AND NOW sees the constellation paint in with a reveal animation: ~26 category-colored nodes sized by `√count`, edges of weight≥2 always-visible, weight-1 edges hidden, on a token-driven backdrop that holds AA contrast in both themes. Reduced-motion users see the same constellation painted instantly with no halo. The renderer is pure — interaction (keyboard, selection, aria-live) is Slice 3.

Purpose: Lock in the renderer props contract per GAME-01 + render the graph data per GAME-02, both behind the surface that a Phase 17 WebGL adapter will reuse. Establish all visual tokens (CSS vars + Tailwind keyframes) the constellation needs so Slice 3 only adds interaction handlers.

Output: `SvgConstellation.js` + `useConstellation.js` (with tests), `tailwind.config.js` + `src/index.css` extensions (constellation tokens + 3 keyframes + 3 animations), and `GameMode.js` modifications that wire the renderer in place of the Slice 1 placeholder. Slice 2 ends with the full visual constellation rendering, the test count >=80, and `npm run build` passing.
</objective>

## Phase Goal (Slice 2 increment)

**As a** sighted visitor (recruiter on mobile), **I want to** see Carlos's skills bloom into a constellation with edges showing skill co-occurrences, **so that** I immediately grasp the breadth and connectedness of his 19-year career at a glance — even before I learn how to interact with it.

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-CONTEXT.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-RESEARCH.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-UI-SPEC.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-PATTERNS.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-01-PLAN.md
@CLAUDE.md
@src/game/GameMode.js
@src/game/constellation.graph.js
@src/game/constellation.layout.js
@src/data/skills.js
@src/index.css
@tailwind.config.js
@src/components/_shared/ViewModeToggle.js
@src/components/_shared/ThemeToggle.js
@src/context/ViewModeContext.js
</context>

<interfaces>
<!-- Contracts and outputs from prior plans + Phase 14 — executor uses directly. -->

From Slice 1 (`src/game/GameMode.js`, plan 15-01):
```js
// Slice 1 already imports + uses these:
import EXPERIENCE from '../data/experience'
import { SKILLS } from '../data/skills'
import { buildConstellationGraph, CURRENT_YEAR } from './constellation.graph'
import ConstellationFallback from './ConstellationFallback'
// Slice 1 derived constants exported:
export const yearsActive  // 19
export const skillCount   // 26
// Slice 1 rendered renderer slot:
// <div className="w-full max-w-3xl relative" data-testid="renderer-slot" data-theme={theme}>
//   <ConstellationErrorBoundary fallback={errorFallback}>
//     <p data-testid="renderer-placeholder" ...>{t.game.empty}</p>  ← REPLACE THIS WITH <SvgConstellation .../>
//   </ConstellationErrorBoundary>
// </div>
// <ConstellationFallback experiences={EXPERIENCE} lang={lang} t={t} />  ← STAYS OUTSIDE BOUNDARY
```

From Phase 14 (`src/game/constellation.graph.js`):
```js
export function buildConstellationGraph(experience, skills)
//   nodes: [{ id, label, category, color, count, period:{start,end|null}, experienceIndices }, ...]
//   edges: [{ source, target, weight }, ...]
```

From Phase 14 (`src/game/constellation.layout.js`):
```js
export function computeLayout(nodes)
//   returns { [id]: { x, y } } in viewBox 0..1000 coordinate space
```

From Phase 14 (`src/data/skills.js`):
```js
export const SKILL_CATEGORIES // { [k]: { en, es, color } }  — 8 entries
export const SKILL_CATEGORY_COLORS // { [k]: '#hex' }  — pinned semantic palette
```

**Locked renderer props contract (per GAME-01, 15-CONTEXT.md, 15-RESEARCH.md):**
```js
SvgConstellation({
  nodes,                  // from buildConstellationGraph
  edges,                  // from buildConstellationGraph
  layout,                 // from computeLayout(nodes) — { [id]: { x, y } } in 0..1000 space
  highlightedSkillIds,    // [] = no filter active (Phase 16 fills this)
  selectedSkillId,        // null until Slice 3 interaction
  yearRange,              // null until Phase 16 filter (currently ignored by renderer)
  theme,                  // 'dark' | 'light'
  lang,                   // 'en' | 'es'
  t,                      // translations[lang]
  onSelectSkill,          // (id) => void  — Slice 2 wires the callback but click handlers come in Slice 3; renderer simply receives the prop
  onHoverSkill,           // (id|null) => void
})
```

Phase 14 sample graph output (verified by `constellation.graph.test.js`): 26 nodes (Java count=11 is max). Layout produces deterministic positions in 0..1000 space.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 2): Extend tailwind.config.js keyframes/animations + src/index.css constellation tokens</name>
  <files>tailwind.config.js, src/index.css</files>
  <read_first>
    - tailwind.config.js (full file — locate `theme.extend.keyframes` block, the existing `pulse2` keyframe, and `theme.extend.colors` block)
    - src/index.css (full file — locate `:root` block with existing token vars, `[data-theme="light"]` overrides, and the `@media (prefers-reduced-motion: reduce)` global rule that already exists)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-PATTERNS.md (§`tailwind.config.js` lines 499-542, §`src/index.css` lines 545-572)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-UI-SPEC.md (§Color lines 92-210, §Animation Contract lines 296-389)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-RESEARCH.md (§Pattern 4 CSS Keyframe Animation lines 336-369, §Anti-Patterns lines 438-446)
  </read_first>
  <behavior>
    - `tailwind.config.js` `theme.extend.keyframes` gains three new keyframes verbatim from 15-UI-SPEC.md §Animation Contract lines 299-314 / 15-PATTERNS.md lines 506-514:
      - `nodeReveal`: `'0%': { opacity: '0', transform: 'scale(0.3)' }`, `'70%': { opacity: '1', transform: 'scale(1.1)' }`, `'100%': { opacity: '1', transform: 'scale(1.0)' }`
      - `edgeReveal`: `'0%': { opacity: '0' }`, `'100%': { opacity: '1' }` — **opacity-only**, NOT the 15-UI-SPEC stroke-dashoffset variant. Per 15-RESEARCH.md §Pattern 4 lines 354-357 + Pitfall 5: stroke-dashoffset with `'100%'` is invalid for SVG without pathLength normalization; opacity-fade is GPU-composited and safer for Lighthouse. 15-PATTERNS.md line 512 selects this variant.
      - `hintFadeOut`: `'0%': { opacity: '1' }`, `'80%': { opacity: '1' }`, `'100%': { opacity: '0', pointerEvents: 'none' }`
    - `tailwind.config.js` `theme.extend.animation` gains three new utilities:
      - `'node-reveal': 'nodeReveal 400ms cubic-bezier(0.34, 1.56, 0.64, 1) both'`
      - `'edge-reveal': 'edgeReveal 300ms ease-out both'`
      - `'hint-fade-out': 'hintFadeOut 600ms ease-in forwards'`
    - `tailwind.config.js` `theme.extend.colors` gains nested objects:
      - `constellation: { edge: 'var(--color-constellation-edge)', edgeHeavy: 'var(--color-constellation-edge-heavy)', halo: 'var(--color-constellation-halo)' }`
      - `hintPill: { bg: 'var(--color-hint-pill-bg)', text: 'var(--color-hint-pill-text)' }`
    - `src/index.css` `:root` block (after existing vars, before the `}` close) gains 5 new CSS vars (dark defaults):
      - `--color-constellation-edge: rgba(160, 160, 192, 0.25);`
      - `--color-constellation-edge-heavy: rgba(160, 160, 192, 0.55);`
      - `--color-constellation-halo: rgba(255, 255, 255, 0.18);`
      - `--color-hint-pill-bg: rgba(20, 20, 50, 0.95);`
      - `--color-hint-pill-text: #A0A0C0;`
    - `src/index.css` `[data-theme="light"]` block (after existing light overrides) gains 5 overrides:
      - `--color-constellation-edge: rgba(80, 80, 120, 0.20);`
      - `--color-constellation-edge-heavy: rgba(80, 80, 120, 0.45);`
      - `--color-constellation-halo: rgba(0, 0, 40, 0.12);`
      - `--color-hint-pill-bg: rgba(200, 200, 214, 0.85);`
      - `--color-hint-pill-text: #525269;`
    - Existing reduced-motion CSS rule is UNCHANGED. Do NOT add a constellation-specific `@media (prefers-reduced-motion)` block (per 15-PATTERNS.md line 571 — JS-side guard handles `animationDelay`/`animationName` stagger suppression).
    - Existing `pulse2` keyframe is UNCHANGED (Slice 3 will use it via `motion-safe:animate-pulse2` on the Java node).
  </behavior>
  <action>
    Per 15-UI-SPEC.md §Color + §Animation Contract and 15-PATTERNS.md §tailwind.config.js + §src/index.css, do the following exactly. NO TDD step for config changes (configs are infrastructure; behavior tests follow in Task 2).

    1. Open `tailwind.config.js`. Locate `theme.extend.keyframes` (around line 65-80 per 15-PATTERNS.md line 503). Add the three new keyframes from <behavior>. Preserve existing `fadeIn`, `slideUp`, `pulse2` entries verbatim. Locate `theme.extend.animation`. Add the three new entries from <behavior>. Preserve existing `fade-in`, `slide-up`, `pulse2` entries verbatim. Locate `theme.extend.colors`. Add the `constellation` and `hintPill` nested objects from <behavior>. Preserve all existing color tokens (ink, brand, accent, text). Keep the existing trailing-comma-free style.

    2. Open `src/index.css`. Locate the `:root` block (per 15-PATTERNS.md line 547 around lines 8-43). After the LAST existing token line and BEFORE the closing `}`, add a comment header `/* ── Constellation-specific tokens (Phase 15) ── */` followed by the 5 dark-default vars from <behavior>. Locate `[data-theme="light"]` block (around lines 64-98). After the last existing override, BEFORE the closing `}`, add the 5 light overrides from <behavior>. Do NOT touch the `@media (prefers-reduced-motion: reduce)` block.

    3. Run `npm run test:run` — must still pass 68 (no regression from Slice 1; configs don't change rendering until Task 2 imports them).

    4. Run `npm run build` — must exit 0. Search the emitted CSS for the new vars:
       `rg "color-constellation-edge|color-hint-pill-bg" dist/assets/*.css | head -10`
       — confirm at least 2 hits (the dark default + the light override).

    5. Single commit: `chore(15-02): add constellation tokens + nodeReveal/edgeReveal/hintFadeOut keyframes`.
  </action>
  <verify>
    <automated>rg -n "nodeReveal|edgeReveal|hintFadeOut" tailwind.config.js | grep -c -E "(nodeReveal|edgeReveal|hintFadeOut)" | awk '$1 >= 6 {print "OK"; exit 0} {exit 1}'</automated>
    <automated>rg -n "color-constellation-edge" src/index.css | grep -v '^#' | wc -l | awk '$1 >= 2 {print "OK"; exit 0} {exit 1}'</automated>
    <automated>rg -n "color-hint-pill-bg" src/index.css | grep -v '^#' | wc -l | awk '$1 >= 2 {print "OK"; exit 0} {exit 1}'</automated>
    <automated>rg -n "constellation: \{|hintPill: \{" tailwind.config.js</automated>
    <automated>npm run build 2>&1 | tail -10 | grep -E "built in|✓"</automated>
    <automated>rg -l "color-constellation-edge|color-hint-pill-bg" dist/assets/ 2>/dev/null | head -3</automated>
    <automated>npm run test:run 2>&1 | grep -E "Tests\s+68\s+passed"</automated>
  </verify>
  <acceptance_criteria>
    - `tailwind.config.js` contains the literal strings `nodeReveal:`, `edgeReveal:`, `hintFadeOut:`, `'node-reveal':`, `'edge-reveal':`, `'hint-fade-out':`, `constellation: {`, `hintPill: {`.
    - `src/index.css` contains `--color-constellation-edge`, `--color-constellation-edge-heavy`, `--color-constellation-halo`, `--color-hint-pill-bg`, `--color-hint-pill-text` exactly twice each (once in `:root`, once under `[data-theme="light"]`).
    - Existing `pulse2` keyframe, `fadeIn`, `slideUp` keyframes, and animation entries are untouched (verified by `rg "pulse2:" tailwind.config.js` matching the pre-existing line — line content unchanged).
    - `npm run build` exits 0 and the emitted CSS includes the new var names.
    - `npm run test:run` exits 0 with 68 passing tests (no regression; config-only change).
    - One commit `chore(15-02)`.
  </acceptance_criteria>
  <done>Visual tokens and keyframes available to the renderer. Slice 2 Task 2 and Slice 3 (pulse on Java + hint pill fade) can now reference `motion-safe:animate-node-reveal`, `motion-safe:animate-edge-reveal`, `animate-hint-fade-out`, `bg-constellation-edge`, `bg-hintPill-bg`, etc., without further config changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 2, RED→GREEN): Write useConstellation hook + tests</name>
  <files>src/game/useConstellation.js, src/game/useConstellation.test.js</files>
  <read_first>
    - .planning/phases/15-accessible-constellation-seo-fallback/15-PATTERNS.md (§`src/game/useConstellation.js` lines 227-272, §`src/game/useConstellation.test.js` lines 349-378)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-RESEARCH.md (§Renderer Contract lines 534-540, §`highlightedSkillIds` vs `selectedSkillId` lines 540)
    - src/context/ViewModeContext.js (analog: useState + useCallback + useMemo shape lines 28-53)
    - src/context/ViewModeContext.test.js (analog test file pattern)
  </read_first>
  <behavior>
    - Default export `function useConstellation(nodes)`. Hook signature is the EXACT shape from 15-PATTERNS.md lines 237-263.
    - State held: `selectedSkillId` (initial `null`), `hoveredSkillId` (initial `null`), `highlightedSkillIds` (initial `[]`; placeholder for Phase 16 filters), `yearRange` (initial `null`; placeholder for Phase 16 timeline).
    - Callback `onSelectSkill(id)` is `useCallback` — toggles: if current === id, set null; else set id.
    - Callback `onHoverSkill(id)` is `useCallback` — sets `hoveredSkillId` to `id` (id may be `null` to clear).
    - Return value is a `useMemo` of `{ selectedSkillId, hoveredSkillId, highlightedSkillIds, yearRange, onSelectSkill, onHoverSkill }` with dependency array containing every value.
    - Tests (build on 15-PATTERNS.md lines 358-377):
      1. `it('initializes with selectedSkillId null')` — `renderHook(() => useConstellation([]))` → `result.current.selectedSkillId === null`.
      2. `it('initializes with hoveredSkillId null')`.
      3. `it('initializes highlightedSkillIds as empty array')` — `result.current.highlightedSkillIds.length === 0`.
      4. `it('initializes yearRange as null')`.
      5. `it('sets selectedSkillId on onSelectSkill call')` — `act(() => result.current.onSelectSkill('Java')); expect(result.current.selectedSkillId).toBe('Java')`.
      6. `it('toggles selectedSkillId off when called twice with same id')` — call twice, expect null.
      7. `it('replaces selectedSkillId when called with different id')` — call with 'Java', then 'Docker'; expect 'Docker'.
      8. `it('updates hoveredSkillId on onHoverSkill')`.
      9. `it('clears hoveredSkillId when onHoverSkill called with null')`.
      10. `it('memoizes returned value when state unchanged across renders')` — `renderHook` + `result.current === result.current` after a no-op rerender. (Use `rerender()` from `renderHook` return.)
  </behavior>
  <action>
    Per 15-PATTERNS.md (role-match analog: ViewModeContext) and the renderer contract spec:

    1. **RED — write `src/game/useConstellation.test.js`** with all 10 tests. Imports: `describe, it, expect` from `vitest`; `renderHook, act` from `@testing-library/react`; default-import `useConstellation` from `./useConstellation`. Use the shape from 15-PATTERNS.md lines 353-377 as the seed; expand to all 10 tests. Run `npm run test:run` — expect tests to fail with module-not-found. Commit: `test(15-02): add failing useConstellation hook tests (10 specs)`.

    2. **GREEN — write `src/game/useConstellation.js`**. Imports: `import { useState, useCallback, useMemo } from 'react'` (no React import needed — this file is .js with no JSX). Implement the hook EXACTLY per 15-PATTERNS.md lines 238-263:

       ```
       export default function useConstellation(nodes) {
         const [selectedSkillId, setSelectedSkillId] = useState(null)
         const [hoveredSkillId, setHoveredSkillId] = useState(null)
         const [highlightedSkillIds] = useState([])
         const [yearRange] = useState(null)

         const onSelectSkill = useCallback((id) => {
           setSelectedSkillId((prev) => (prev === id ? null : id))
         }, [])

         const onHoverSkill = useCallback((id) => {
           setHoveredSkillId(id)
         }, [])

         return useMemo(() => ({
           selectedSkillId,
           hoveredSkillId,
           highlightedSkillIds,
           yearRange,
           onSelectSkill,
           onHoverSkill,
         }), [selectedSkillId, hoveredSkillId, highlightedSkillIds, yearRange, onSelectSkill, onHoverSkill])
       }
       ```

       (Note: trailing commas inside `useMemo` object literal are fine — they are not function-call comma-dangle. Mirror the existing `ViewModeContext.js useMemo` style; if eslint flags, remove.)

       Run `npm run test:run` — expect 10 new tests to pass. Total expected: 68 + 10 = 78.

       Commit: `feat(15-02): useConstellation hook with selection/hover state + Phase 16 filter placeholders`.

    3. Do NOT integrate the hook into GameMode yet — Task 4 will wire it. Task 3 builds the renderer.
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | grep -E "Tests\s+78\s+passed"</automated>
    <automated>rg -n "export default function useConstellation" src/game/useConstellation.js</automated>
    <automated>rg -n "useMemo|useCallback|useState" src/game/useConstellation.js | wc -l | awk '$1 >= 5 {print "OK"; exit 0} {exit 1}'</automated>
    <automated>rg -n "from 'react'" src/game/useConstellation.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/game/useConstellation.js` exists with the exact hook shape from 15-PATTERNS.md lines 238-263.
    - Imports only `useState, useCallback, useMemo` from React. No JSX in this file.
    - Default export.
    - `src/game/useConstellation.test.js` has 10 tests covering init state, set/toggle/replace selection, hover update/clear, memoization.
    - Two commits: RED `test(15-02)`, GREEN `feat(15-02)`.
    - `npm run test:run` exits 0 with 78 passing tests.
  </acceptance_criteria>
  <done>The selection/hover state hook is in place behind the renderer contract; Slice 3 will only add the keyboard/aria-live binding atop these callbacks — the state shape will not change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (Wave 2, RED→GREEN): Write SvgConstellation visual renderer + reveal animation + theme-aware light-mode stroke + tests</name>
  <files>src/game/renderers/SvgConstellation.js, src/game/renderers/SvgConstellation.test.js</files>
  <read_first>
    - .planning/phases/15-accessible-constellation-seo-fallback/15-PATTERNS.md (§`src/game/renderers/SvgConstellation.js` lines 117-223, §`SvgConstellation.test.js` lines 409-441)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-UI-SPEC.md (§Active-State Contract lines 213-225, §Sizing Scale lines 229-292, §Animation Contract lines 296-389, §Layout Contract lines 638-708, §Color §WCAG AA Contrast verification lines 161-200)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-RESEARCH.md (§Pattern 4 CSS Keyframe Animation lines 336-369, §Pattern 5 usePrefersReducedMotion lines 372-394, §Pitfall 1 SVG Transform Origin lines 561-572, §Pitfall 3 animationDelay under reduced-motion lines 588-602, §Pitfall 4 0.35 dimming pre-selection lines 605-616, §Pitfall 5 stroke-dashoffset lines 619-635, §Code Examples lines 647-728)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-CONTEXT.md (D-15-VIS-* decisions lines 33-39, D-15-LAND-PAINT/LAND-HINT lines 41-42)
    - src/game/constellation.graph.js (node shape) and src/game/constellation.layout.js (layout shape)
    - src/data/skills.js (SKILL_CATEGORIES + SKILL_CATEGORY_COLORS)
    - src/components/_shared/ViewModeToggle.js (analog: aria-pressed pattern + tokens-only colors)
  </read_first>
  <behavior>
    - Default export `function SvgConstellation({ nodes, edges, layout, highlightedSkillIds, selectedSkillId, yearRange, theme, lang, t, onSelectSkill, onHoverSkill })`.
    - Slice 2 ignores `yearRange` (Phase 16 territory). `highlightedSkillIds` default treatment per 15-RESEARCH.md A8: empty array means "no filter active" → all nodes full opacity; non-empty array would dim non-highlighted nodes BUT Slice 2 does not yet wire dimming since no filter UI exists. Document this in a `// Phase 16` comment.
    - `selectedSkillId` is read but stays `null` in Slice 2 (Slice 3 wires the user-triggered transitions). Renderer must still HANDLE non-null defensively: when non-null, apply halo + dim per 15-UI-SPEC.md §Active-State Contract. Per Pitfall 4: guard `selectedSkillId !== null` before dimming non-selected nodes.
    - Empty graph: if `nodes.length === 0`, return `<p className="text-text-secondary text-sm py-12 text-center">{t.game.empty}</p>` instead of an SVG.
    - SVG element: `<svg viewBox="0 0 1000 1000" preserveAspectRatio="xMidYMid meet" width="100%" style={{ minHeight: 240, maxHeight: 600 }} aria-hidden="true">`. Slice 2 keeps `aria-hidden="true"` per 15-UI-SPEC.md line 662 (Slice 3 adds the `role="application"` wrapper at a higher level).
    - Inside SVG, render in this order: (1) `<g className="edges">` containing all edges, (2) `<g className="nodes">` containing all nodes. (Edges first = paint underneath.)
    - Each edge: `<line key={`${e.source}-${e.target}`} x1={layout[e.source].x} y1={layout[e.source].y} x2={layout[e.target].x} y2={layout[e.target].y} stroke={e.weight >= 2 ? 'var(--color-constellation-edge-heavy)' : 'var(--color-constellation-edge)'} strokeWidth={edgeStrokeWidth(e.weight)} style={{ opacity: e.weight >= 2 ? 1 : 0, pointerEvents: 'none' }} className={isMotionSafe(reduced) ? 'motion-safe:animate-edge-reveal' : ''} />`. Weight=1 edges are positioned with `opacity:0` so they don't paint by default; Slice 3 will toggle to 1 on incident-node focus/hover. The `edgeStrokeWidth` helper from 15-UI-SPEC.md §Edge stroke width lines 271-279: `weight === 1 ? 1.0 : Math.min(1.0 + (weight - 1) * 1.0, 4.0)`.
    - Mobile-vs-desktop node radius: detect viewport once via `useState(() => typeof window !== 'undefined' && window.innerWidth >= 768 ? 'desktop' : 'mobile')`. Per 15-UI-SPEC.md §Node radius lines 233-257: mobile FLOOR=6, CEIL=14; desktop FLOOR=10, CEIL=23. Compute radius `r = FLOOR + (Math.sqrt(node.count) / Math.sqrt(MAX_COUNT)) * (CEIL - FLOOR)` where MAX_COUNT is `Math.max(...nodes.map(n => n.count))`.
    - Each node `<g>`: `<g key={node.id} data-node-id={node.id} data-category={node.category} style={{ transformBox: 'fill-box', transformOrigin: 'center', animationDelay: prefersReducedMotion ? '0ms' : `${sortIndex * 30}ms`, animationName: prefersReducedMotion ? 'none' : undefined }} className={prefersReducedMotion ? '' : 'motion-safe:animate-node-reveal'}>`. `sortIndex` is the index in the count-DESC-sorted nodes array (biggest first). `transformBox`/`transformOrigin` are MANDATORY per Pitfall 1 — without them nodes fly in from `(0,0)`.
    - Inside each `<g>`, render:
      - Visible circle: `<circle cx={pos.x} cy={pos.y} r={r} fill={SKILL_CATEGORY_COLORS[node.category]} fillOpacity={selectedSkillId !== null && node.id !== selectedSkillId ? 0.35 : 1} stroke={theme === 'light' ? LIGHT_THEME_STROKES[node.category] : 'none'} strokeWidth={theme === 'light' ? 1.5 : 0} style={{ filter: node.id === selectedSkillId && !prefersReducedMotion ? 'drop-shadow(0 0 8px var(--color-constellation-halo)) drop-shadow(0 0 16px rgba(255,255,255,0.08))' : 'none' }} />`.
      - `LIGHT_THEME_STROKES` is a module-level const map from 15-UI-SPEC.md lines 180-188: `{ lang: '#2563EB', ai: '#a855f7', arch: '#0891b2', cloud: '#059669', devops: '#d97706', security: '#ef4444', data: '#8b5cf6', hardware: '#db2777' }`. The 5 categories already passing AA on light theme map to their own fill color (no visual ring change); the 3 failing/borderline categories (arch=cyan, cloud=emerald, devops=amber, hardware=pink) get their darker shade as a 1.5 viewBox-unit stroke.
      - 44px touch-target circle: `<circle cx={pos.x} cy={pos.y} r={22} fill="transparent" data-touch-target />`. Slice 2 includes this for layout completeness; click handler wires in Slice 3.
      - (Slice 3 will add the focus ring `<circle>` + `role="button"` + `aria-label` + `tabIndex` + `onClick`/`onKeyDown`. Slice 2 leaves the `<g>` as a decorative wrapper.)
    - Inline `usePrefersReducedMotion` hook per 15-RESEARCH.md Pattern 5 lines 372-394 (Josh Comeau pattern, SSR-safe). Define inside `SvgConstellation.js` (or co-locate in a `_hooks.js` neighbor — choose inline to keep the file count minimal; the hook is ~20 lines).
    - Tests:
      1. `it('renders one <g> per node')` — fixture with 3 nodes; assert `container.querySelectorAll('g.nodes > g').length === 3`. Provide `layout` fixture with positions for all 3 ids.
      2. `it('renders one <line> per edge')` — fixture with 2 edges; assert `container.querySelectorAll('g.edges > line').length === 2`.
      3. `it('hides weight-1 edges via opacity:0')` — fixture with 1 edge of weight 1 and 1 edge of weight 3; assert weight-1 line `style.opacity === '0'`, weight-3 line `style.opacity === '1'`.
      4. `it('renders empty state when nodes is []')` — assert `getByText(t.game.empty)`.
      5. `it('skips animation delay under reduced-motion')` — mock matchMedia to return matches=true for `(prefers-reduced-motion: reduce)`; assert no `animation-delay` style or `animation-name: 'none'` on the node group.
      6. `it('applies light-theme stroke on borderline categories')` — render with `theme='light'`, fixture with a node of category `arch`; assert the visible circle has `stroke="#0891b2"` and `stroke-width="1.5"`.
      7. `it('uses √count scaling for radius')` — fixture with two nodes, count=1 and count=4; assert the count=4 node's `r` attribute is 2× the count=1 node's `r` minus the floor (i.e., the formula: count=4 radius = FLOOR + (2/sqrt(4)) * (CEIL-FLOOR) — for desktop FLOOR=10, CEIL=23, with MAX_COUNT=4 in fixture: count=1 → 10 + (1/2)*13 = 16.5; count=4 → 10 + (2/2)*13 = 23. The growth direction is what's tested, not exact pixels).
      8. `it('does not dim any node before selectedSkillId is set')` — render with `selectedSkillId={null}`; assert ALL visible circles have `fill-opacity="1"` (Pitfall 4 guard).
      9. `it('applies halo filter when a node is the selectedSkillId')` — render with `selectedSkillId='Java'`; assert the matching `<circle>` has `style.filter` containing `'drop-shadow'`.
      10. `it('omits halo under reduced-motion even when selected')` — mock matchMedia matches=true; render with `selectedSkillId='Java'`; assert filter is `'none'` or empty.
  </behavior>
  <action>
    Per all the locked decisions (D-15-VIS-* + D-15-LAND-PAINT), do the following exactly:

    1. **RED — write `src/game/renderers/SvgConstellation.test.js`** with all 10 tests. Test scaffolding pattern from 15-PATTERNS.md lines 415-427. Setup at top:
       ```
       import { describe, it, expect, beforeEach, vi } from 'vitest'
       import { render } from '@testing-library/react'
       import SvgConstellation from './SvgConstellation'
       import { translations } from '../../i18n/translations'

       const FIXTURE_NODES = [
         { id: 'Java', label: 'Java', category: 'lang', count: 4 },
         { id: 'Docker', label: 'Docker', category: 'devops', count: 2 },
         { id: 'AWS', label: 'AWS', category: 'cloud', count: 1 },
       ]
       const FIXTURE_LAYOUT = { Java: { x: 500, y: 500 }, Docker: { x: 200, y: 300 }, AWS: { x: 800, y: 700 } }
       const FIXTURE_EDGES = [
         { source: 'Java', target: 'Docker', weight: 3 },
         { source: 'Docker', target: 'AWS', weight: 1 },
       ]

       beforeEach(() => {
         window.matchMedia = vi.fn().mockImplementation((q) => ({
           matches: false, media: q, onchange: null,
           addEventListener: vi.fn(), removeEventListener: vi.fn(),
           addListener: vi.fn(), removeListener: vi.fn(),
           dispatchEvent: vi.fn(),
         }))
       })
       ```
       For reduced-motion tests, override `window.matchMedia` per-test to return `matches: true` only when the query matches `(prefers-reduced-motion: reduce)`. Build a helper `function renderRenderer(overrides = {})` that supplies all required props with sane defaults so each test only overrides what it cares about. Run `npm run test:run` — expect fail (module not found). Commit: `test(15-02): add failing SvgConstellation renderer tests (10 specs covering nodes, edges, theme, reduced-motion, halo)`.

    2. **GREEN — write `src/game/renderers/SvgConstellation.js`**. File at `src/game/renderers/SvgConstellation.js` (NEW directory `renderers/`). Imports: `import React, { useState, useEffect } from 'react'`; `import { SKILL_CATEGORY_COLORS } from '../../data/skills'`.

       Module-level constants (above the default export):
       - `const LIGHT_THEME_STROKES = { lang: '#2563EB', ai: '#a855f7', arch: '#0891b2', cloud: '#059669', devops: '#d97706', security: '#ef4444', data: '#8b5cf6', hardware: '#db2777' }`
       - `const SIZING = { mobile: { floor: 6, ceil: 14 }, desktop: { floor: 10, ceil: 23 } }`
       - `function computeRadius(count, maxCount, breakpoint) { const { floor, ceil } = SIZING[breakpoint]; return floor + (Math.sqrt(count) / Math.sqrt(maxCount)) * (ceil - floor) }`
       - `function edgeStrokeWidth(weight) { return weight === 1 ? 1.0 : Math.min(1.0 + (weight - 1) * 1.0, 4.0) }`
       - The Josh Comeau `usePrefersReducedMotion` hook inline per 15-RESEARCH.md Pattern 5 lines 376-394.

       Default export `SvgConstellation`. Steps inside:
       - Read `prefersReducedMotion` from the inline hook.
       - `useState(() => typeof window !== 'undefined' && window.innerWidth >= 768 ? 'desktop' : 'mobile')` for `breakpoint`; no resize listener in Slice 2 (per UI-SPEC simplicity — viewport responsiveness is acceptable as fixed at mount; Phase 17 can add resize handling if needed).
       - Empty-graph guard: `if (!nodes || nodes.length === 0) return <p ...>{t.game.empty}</p>`.
       - `const maxCount = Math.max(...nodes.map(n => n.count))`.
       - `const nodesByCount = [...nodes].sort((a, b) => b.count - a.count)` for stagger order.
       - Render the SVG with the structure from <behavior>. Use the exact className strings and inline styles.

       Code style: no semicolons, no trailing commas in arrays/function calls (object literals in JSX may have trailing commas — match existing Experience.js style; verify with `npm run lint`). JSX-in-.js (no `.jsx`). Two-space indent.

       Run `npm run test:run` — expect 10 new tests to pass. Total expected: 78 + 10 = 88.

       Commit: `feat(15-02): SvgConstellation pure renderer with √count sizing, theme-aware stroke, reveal animation, reduced-motion guards`.
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | grep -E "Tests\s+88\s+passed"</automated>
    <automated>rg -n "export default function SvgConstellation" src/game/renderers/SvgConstellation.js</automated>
    <automated>rg -n "transformBox|transformOrigin" src/game/renderers/SvgConstellation.js | wc -l | awk '$1 >= 2 {print "OK"; exit 0} {exit 1}'</automated>
    <automated>rg -n "LIGHT_THEME_STROKES" src/game/renderers/SvgConstellation.js | head -3</automated>
    <automated>rg -n "usePrefersReducedMotion" src/game/renderers/SvgConstellation.js</automated>
    <automated>rg -n "selectedSkillId !== null" src/game/renderers/SvgConstellation.js</automated>
    <automated>npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/game/renderers/SvgConstellation.js` exists (new directory) with default export.
    - Renderer props are EXACTLY `{ nodes, edges, layout, highlightedSkillIds, selectedSkillId, yearRange, theme, lang, t, onSelectSkill, onHoverSkill }` (verify by reading the destructure signature).
    - `LIGHT_THEME_STROKES` and `SIZING` constants exist at module level.
    - `computeRadius` and `edgeStrokeWidth` helpers exist at module level.
    - `usePrefersReducedMotion` is defined inline (Josh Comeau pattern).
    - `transformBox: 'fill-box'` AND `transformOrigin: 'center'` appear in the node `<g>` inline style.
    - Pitfall 4 guard `selectedSkillId !== null` appears in the fill-opacity ternary.
    - Pitfall 3 guard for `animationDelay` reduced-motion exists.
    - `src/game/renderers/SvgConstellation.test.js` has 10 tests; all pass.
    - Two commits: RED `test(15-02)`, GREEN `feat(15-02)`.
    - `npm run test:run` exits 0 with 88 passing tests.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>The visual constellation paints. Slice 2 sighted user sees: H1 (Slice 1) + 26 category-colored nodes with √count sizing + weight≥2 edges + reveal animation + light-mode AA-safe strokes + halo path when selectedSkillId is set externally. No keyboard interaction yet — that's Slice 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4 (Wave 2, RED→GREEN): Wire SvgConstellation + useConstellation into GameMode; extend GameMode tests</name>
  <files>src/game/GameMode.js, src/game/GameMode.test.js</files>
  <read_first>
    - src/game/GameMode.js (Slice 1 output — the renderer slot at `data-testid="renderer-slot"` is the wiring target)
    - src/game/GameMode.test.js (Slice 1 tests — extend with renderer integration assertions; preserve all 6 existing tests)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-RESEARCH.md (§Pattern 6 detectCapabilities lines 398-418)
    - .planning/phases/15-accessible-constellation-seo-fallback/15-UI-SPEC.md (§Layout Contract lines 640-687, §Error/Empty State Contract lines 809-828)
  </read_first>
  <behavior>
    - `GameMode.js` imports `SvgConstellation` from `./renderers/SvgConstellation` and `useConstellation` from `./useConstellation`.
    - `GameMode.js` imports the same Phase 14 helpers (already imported in Slice 1) and adds `import { computeLayout } from './constellation.layout'`.
    - Module-level: derive `const { nodes: GRAPH_NODES, edges: GRAPH_EDGES } = buildConstellationGraph(EXPERIENCE, SKILLS)` and `const LAYOUT = computeLayout(GRAPH_NODES)`. (Slice 1 already evaluated `buildConstellationGraph` to compute skillCount; refactor that into a single shared call so the graph builds once at module load and is reused for both `skillCount` and the renderer props.)
    - Inside the component: `const cons = useConstellation(GRAPH_NODES)` returns the selection/hover payload.
    - Capability detection function `detectCapabilities()` defined at file scope per 15-RESEARCH.md Pattern 6 lines 402-413 (`prefersReducedMotion`, `saveData`, `isMobile`, `hasWebGL`). Called once on mount via `useState(() => detectCapabilities())`. Slice 2 stores the result but ONLY uses it to decide renderer == SvgConstellation. Slice 3 may use it for hint-pill-vs-pulse choice. Add a `// Phase 17` comment noting where the WebGL adapter will branch.
    - Renderer slot CONTENT changes: the `<p data-testid="renderer-placeholder">{t.game.empty}</p>` is REPLACED with:
      ```
      <SvgConstellation
        nodes={GRAPH_NODES}
        edges={GRAPH_EDGES}
        layout={LAYOUT}
        highlightedSkillIds={cons.highlightedSkillIds}
        selectedSkillId={cons.selectedSkillId}
        yearRange={cons.yearRange}
        theme={theme}
        lang={lang}
        t={t}
        onSelectSkill={cons.onSelectSkill}
        onHoverSkill={cons.onHoverSkill}
      />
      ```
    - The `data-testid="renderer-slot"` wrapping `<div>` is PRESERVED (still the architectural slot; just now wraps the real renderer).
    - `<ConstellationFallback ... />` STAYS as a sibling AFTER `</ConstellationErrorBoundary>`, outside the boundary (per Slice 1 placement; do not move it).
    - `<ConstellationErrorBoundary>` still wraps ONLY the renderer slot — if `SvgConstellation` throws (e.g., layout returns malformed data), the error fallback shows but the fallback `<section>` still renders.
    - Tests added to `GameMode.test.js` (extend the existing 6, don't replace):
      11. `it('renders <svg> inside renderer-slot after wiring')` — `renderWithProviders(<GameMode />)`; assert `container.querySelector('[data-testid="renderer-slot"] svg')` is non-null.
      12. `it('renders 26 node <g> elements')` — assert `container.querySelectorAll('g.nodes > g').length === 26` against the live graph.
      13. `it('passes theme prop through to SvgConstellation')` — render with theme=dark; assert no light-theme stroke colors on node `<circle>` (`querySelector('g.nodes circle[stroke="#0891b2"]')` should be null). Render with theme=light; assert at least one node has the borderline stroke.
      14. `it('still renders ConstellationFallback after wiring renderer')` — same `getByRole('heading', { name: /Full career experience/ })` as Slice 1 test still passes.
    - Total expected tests after Task 4: 88 + 4 = 92 (or so — count whatever you actually add). The Slice 1 test `it('renders renderer slot with data-testid="renderer-slot"')` continues to pass.
  </behavior>
  <action>
    Per the Slice 1 plug-point and the locked renderer contract:

    1. **RED** — open `src/game/GameMode.test.js`. Add 4 new tests at the end of the existing describe block. Keep all 6 existing tests untouched. The Slice 1 test that asserted `data-testid="renderer-placeholder"` (if you wrote one — re-read Slice 1 spec; it was the renderer-slot test, not a renderer-placeholder test) — that test continues to assert the slot exists. If you DID write a placeholder-specific test in Slice 1, remove or update it now. The 4 new tests are listed in <behavior>.

       Run `npm run test:run`. Expect the 4 new tests to fail because `GameMode.js` is not yet wired to the renderer. Commit: `test(15-02): extend GameMode tests for SvgConstellation wiring + theme propagation + 26-node render`.

    2. **GREEN** — modify `src/game/GameMode.js`:

       (a) Add imports:
       ```
       import { computeLayout } from './constellation.layout'
       import useConstellation from './useConstellation'
       import SvgConstellation from './renderers/SvgConstellation'
       ```

       (b) Refactor the module-level derivation so `buildConstellationGraph` is called exactly once:
       ```
       const { nodes: GRAPH_NODES, edges: GRAPH_EDGES } = buildConstellationGraph(EXPERIENCE, SKILLS)
       const LAYOUT = computeLayout(GRAPH_NODES)
       export const yearsActive = Math.max(...EXPERIENCE.map(e => e.period.end ?? CURRENT_YEAR)) - Math.min(...EXPERIENCE.map(e => e.period.start))
       export const skillCount = GRAPH_NODES.length
       ```
       (This replaces the Slice 1 derivation that called `buildConstellationGraph` separately. The exported constants and their values are unchanged; the test count stays 19 / 26.)

       (c) Add the capability detection function at file scope:
       ```
       function detectCapabilities() {
         if (typeof window === 'undefined') return { prefersReducedMotion: true, saveData: false, isMobile: true, hasWebGL: false }
         const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches
         const saveData = (typeof navigator !== 'undefined' && navigator.connection?.saveData) ?? false
         const isMobile = window.innerWidth < 768
         let hasWebGL = false
         try {
           const c = document.createElement('canvas')
           hasWebGL = !!(c.getContext('webgl') || c.getContext('experimental-webgl'))
         } catch (e) { hasWebGL = false }
         return { prefersReducedMotion, saveData, isMobile, hasWebGL }
       }
       ```
       Wrap the matchMedia and innerWidth accesses in `if (typeof window === 'undefined')` guards per the existing ViewModeContext.js SSR-safety convention.

       (d) Inside `GameMode()`:
       ```
       const [capabilities] = useState(() => detectCapabilities())
       const cons = useConstellation(GRAPH_NODES)
       // Phase 17: branch on capabilities for WebGL adapter; Phase 15 always SVG.
       ```
       (Remove the unused-var lint flag for `capabilities` by passing it down or by adding a `// eslint-disable-next-line no-unused-vars` comment with a TODO; the simpler path is to consume it: pass `prefersReducedMotion={capabilities.prefersReducedMotion}` as an additional prop. BUT — the renderer contract is locked and does NOT include `prefersReducedMotion` (the renderer detects via the hook). Instead, log it once for debugging removed before commit, OR thread it through `data-prefers-reduced-motion` on the renderer-slot div for inspection — preferable. Use: `<div data-testid="renderer-slot" data-theme={theme} data-prefers-reduced-motion={capabilities.prefersReducedMotion ? 'true' : 'false'}>`. The data-attribute satisfies the linter and gives Slice 3 a hook for the hint-pill vs pulse decision.)

       (e) Replace the `<p data-testid="renderer-placeholder">{t.game.empty}</p>` body inside `<ConstellationErrorBoundary>` with the `<SvgConstellation ... />` JSX from <behavior>.

       Run `npm run test:run` — expect total 92 (or actual count after Task 4) passing. Run `npm run lint` — must exit 0.

       Commit: `feat(15-02): wire SvgConstellation + useConstellation + capability detection into GameMode renderer slot`.

    3. Optional manual smoke at the end (not gating): `npm start`, open browser at `/`, default landing should show the constellation animating in. Toggle dev/game pill — game branch still mounts. Toggle theme to light — borderline category nodes get the darker stroke. Toggle to ES — H1 reads "19 años. 26 skills. Una constelación.".
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | tee /tmp/15-02-task4.log && grep -E "Tests\s+(9[0-9]|1[0-9]{2})\s+passed" /tmp/15-02-task4.log</automated>
    <automated>rg -n "import SvgConstellation" src/game/GameMode.js</automated>
    <automated>rg -n "import useConstellation" src/game/GameMode.js</automated>
    <automated>rg -n "import \{ computeLayout \}" src/game/GameMode.js</automated>
    <automated>rg -n "detectCapabilities" src/game/GameMode.js | wc -l | awk '$1 >= 2 {print "OK"; exit 0} {exit 1}'</automated>
    <automated>rg -n "renderer-placeholder" src/game/GameMode.js | grep -v '^#' || echo "placeholder removed OK"</automated>
    <automated>npm run build 2>&1 | tail -8 | grep -E "built in|✓"</automated>
    <automated>npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/game/GameMode.js` imports `SvgConstellation`, `useConstellation`, and `computeLayout`.
    - Module-level `GRAPH_NODES`, `GRAPH_EDGES`, `LAYOUT` constants exist; `buildConstellationGraph` is called exactly once at module load.
    - `detectCapabilities` function is at file scope with the `typeof window === 'undefined'` guard.
    - The renderer slot `<div data-testid="renderer-slot">` no longer wraps a `data-testid="renderer-placeholder"` element; it wraps `<SvgConstellation ... />` inside `<ConstellationErrorBoundary>`.
    - `<ConstellationFallback>` is still a sibling of the slot, outside the boundary (DOM order preserved).
    - All 6 Slice 1 GameMode tests still pass; 4 new Task 4 tests pass.
    - Total Vitest test count is 92 or higher passing (88 from Tasks 1-3 of Slice 2 + 4 new GameMode tests). Two commits: RED `test(15-02)`, GREEN `feat(15-02)`.
    - `npm run build` exits 0; the game-mode lazy chunk still splits cleanly.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Slice 2 vertical complete. Default landing visitor sees the full constellation paint in with reveal animation; reduced-motion users see the constellation paint instantly; light/dark themes both hold AA contrast. Slice 3 plugs the keyboard/aria-live/role=application layer on top of the now-visible renderer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client React tree | Pure render; no network/auth/PII. SVG is inline JSX — no `srcdoc`, no external SVG fetch. |
| Style/inline boundary | Inline `style={{ filter: '...' }}` on circles for halo; CSS-var values are repo-controlled. |
| matchMedia / navigator boundary | `window.matchMedia`, `navigator.connection.saveData`, `document.createElement('canvas').getContext('webgl')` — all standard browser APIs, guarded with `typeof window !== 'undefined'`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-T | Tampering | SVG node fill colors `SKILL_CATEGORY_COLORS` (data values, repo-controlled) | accept | Source-controlled; same trust level as `experience.js`. |
| T-15-02-I | Info Disclosure | Capability detection (saveData, hasWebGL) | accept | These are user-agent fingerprinting signals, but: (a) not transmitted off-device, (b) already exposed to any script on any page. No new disclosure surface. |
| T-15-02-D | DoS | reveal animation stagger across 26 nodes | mitigate | Stagger collapses to 0 under reduced-motion; CSS keyframes are GPU-composited (`transform: scale`, `opacity`). Edge reveal is opacity-only (not stroke-dashoffset), so it's also GPU-composited. Lighthouse impact verified at Phase 17 gate. |
| T-15-02-CSP | CSP | No inline `<script>`. Inline `style={{ filter: 'drop-shadow(...)' }}` requires `style-src 'unsafe-inline'` if a strict CSP is set. | accept | Project does not yet enforce CSP headers (deferred from Phase 11). Inline styles are CSS values, not JS. When CSP is added later, switch the halo to a CSS class `motion-safe:halo-active` reading `var(--color-constellation-halo)`. |
| T-15-02-Layout | DoS via layout thrash | SVG `<line>`/`<circle>` count = 26 + ~edge_count (typically < 100 edges from graph data) | accept | Element count is bounded by data; no user input drives node/edge creation. |
| T-15-02-SC | Tampering (supply chain) | npm installs | mitigate | NO new npm packages introduced in Slice 2 (per RESEARCH.md Package Legitimacy Audit lines 148-162). Existing dependencies only. |
</threat_model>

<verification>
- Total Vitest test count progression: Slice 1 ended at 68 → Slice 2 Task 2 adds 10 (useConstellation) → Task 3 adds 10 (SvgConstellation) → Task 4 adds 4 (GameMode wiring) = **92 expected**.
- `npm run build` exits 0; `dist/assets/index-*.css` contains `--color-constellation-edge` and `--color-hint-pill-bg`; `dist/assets/GameMode-*.js` (lazy chunk) contains the renderer code.
- `npm run lint` exits 0 (airbnb + react/recommended).
- Manual smoke (not gating): default landing → bilingual H1 visible immediately → constellation reveals in waves (~1.2s total) → light theme toggle shows darker strokes on borderline categories → ES toggle re-renders H1 in Spanish.
- File count check: `fd -e jsx src/` returns nothing (JSX-in-.js convention upheld).
</verification>

<success_criteria>
1. `SvgConstellation.js` renders the locked renderer-contract props with NO additional/missing props; visual output matches the D-15-VIS-* spec: √count node sizing, weight-gated edges, category colors, light-mode strokes, halo on selected, reduced-motion path.
2. `useConstellation` hook owns selection + hover state; the `selectedSkillId` toggle behavior matches Phase 16 expectations (re-click clears).
3. `GameMode` plugs the renderer + hook into the renderer slot from Slice 1; capability detection runs once on mount; `data-prefers-reduced-motion` is exposed on the slot div for Slice 3's hint-pill-vs-pulse decision.
4. Total Vitest count ≥ 92 passing; airbnb-eslint clean; `npm run build` succeeds.
5. All 5 new CSS custom properties exist in both dark (`:root`) and light (`[data-theme="light"]`) blocks; all 3 new keyframes + animations are in `tailwind.config.js`.
6. No new runtime dependencies (per RESEARCH.md §Standard Stack confirmation).
7. Pitfall guards present in renderer: `transformBox: 'fill-box'` + `transformOrigin: 'center'` (Pitfall 1), `animationDelay` reduced-motion guard (Pitfall 3), `selectedSkillId !== null` pre-dim guard (Pitfall 4), opacity-only edge reveal NOT `stroke-dashoffset` (Pitfall 5).
</success_criteria>

<output>
Create `.planning/phases/15-accessible-constellation-seo-fallback/15-02-SUMMARY.md` when the plan completes. Key fields:
- `provides`: `SvgConstellation renderer (locked props contract)`, `useConstellation hook (selection/hover/Phase16 placeholders)`, `constellation CSS tokens (5 vars × 2 themes)`, `3 keyframes (nodeReveal, edgeReveal, hintFadeOut)`, `GameMode wiring (renderer + capability detection)`.
- `affects`: list the 8 `files_modified` exactly.
- `note`: Slice 3 layers role="application" wrapper + per-node `<g role="button">` with roving tabindex + spatial arrow nav + Enter/Space/Esc + aria-live announcements ON TOP of the existing renderer. The renderer contract does NOT change; Slice 3 enriches the `<g>` elements with interaction attrs and the SVG container with the application wrapper.
</output>
