---
phase: 24-hero
plan: 04
type: execute
wave: 3
depends_on: [24-02, 24-03]
files_modified:
  - src/pages/en/index.astro
  - src/pages/es/index.astro
  - src/components/Hero.jsx
  - src/components/Hero.test.jsx
autonomous: true
requirements: [STATIC-02]

must_haves:
  truths:
    - "Hero.astro is mounted as the first child of <main> (before <About>) on BOTH /en and /es, passing locale={locale} — matching the existing per-section mount convention (this is a pure addition; there is NO <h1>Carlos Montoya</h1> placeholder to remove, per 24-PATTERNS.md)"
    - "D-08: the built /en and /es pages ship the hero photo as the server-rendered LCP element with zero client-JS for Hero markup — no astro-island element is emitted for Hero (confirmed absent client:* directive)"
    - "The deprecated src/components/Hero.jsx and src/components/Hero.test.jsx are deleted now that Hero.astro + its Container API test are mounted (State of the Art: fully replaced)"
    - "A production build emits dist/en/index.html and dist/es/index.html each containing the hero <img src=\"/me-800.webp\">, both count-up.js and details-dismiss.js as small deferred module chunks (not render-blocking, not is:inline)"
  artifacts:
    - path: "src/pages/en/index.astro"
      provides: "Hero mounted first in /en"
      contains: "Hero"
    - path: "src/pages/es/index.astro"
      provides: "Hero mounted first in /es"
      contains: "Hero"
  key_links:
    - from: "src/pages/en/index.astro"
      to: "src/components/astro/Hero.astro"
      via: "import + <Hero locale={locale} /> as first <main> child"
      pattern: "components/astro/Hero.astro"
    - from: "src/pages/es/index.astro"
      to: "src/components/astro/Hero.astro"
      via: "import + <Hero locale={locale} /> as first <main> child"
      pattern: "components/astro/Hero.astro"
---

<objective>
Mount `Hero.astro` as the first section on both locale pages, delete the deprecated React `Hero.jsx`/`Hero.test.jsx`, and verify the production build ships Hero as zero-client-JS static HTML with the photo as LCP candidate and both enhancer scripts as small deferred chunks.

Purpose: Make the migrated Hero live on both routes and prove no LCP regression / no accidental island before the manual cross-browser QA gate (Plan 24-05).
Output: Both pages updated, old React files removed, build-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-hero/24-RESEARCH.md
@.planning/phases/24-hero/24-PATTERNS.md
@.planning/phases/24-hero/24-CONTEXT.md

<interfaces>
<!-- Current en/index.astro + es/index.astro import + mount order (both files, confirmed): -->
<!--   import About/Skill/Projects/Claude/Footer; <main> mounts About, Skill, Projects, Claude, Footer in that order -->
<!--   const locale = Astro.currentLocale ?? 'en'|'es' -->
<!-- Add: `import Hero from '../../components/astro/Hero.astro'` (place immediately before the About import) -->
<!--       `<Hero locale={locale} />` as the FIRST child inside <main>, immediately before <About locale={locale} /> -->
<!-- Both files are byte-for-byte parallel except the locale fallback — apply the IDENTICAL edit to both. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount Hero.astro on /en and /es</name>
  <files>src/pages/en/index.astro, src/pages/es/index.astro</files>
  <read_first>
    - src/pages/en/index.astro (FULL — current import list + mount order)
    - src/pages/es/index.astro (FULL — confirm parallel structure)
    - .planning/phases/24-hero/24-PATTERNS.md (pages MODIFIED section, lines 279-289 — pure addition, no placeholder removal)
    - src/components/astro/Hero.astro (confirm it accepts a `locale` prop)
  </read_first>
  <action>Edit both src/pages/en/index.astro and src/pages/es/index.astro identically: add `import Hero from '../../components/astro/Hero.astro'` immediately before the existing About import, and add `<Hero locale={locale} />` as the FIRST child inside <main>, immediately before `<About locale={locale} />`. This is a pure addition — do NOT search for or attempt to remove any `<h1>Carlos Montoya</h1>` placeholder (none exists in either file; Phase 23 already replaced it). Keep both files structurally in sync.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds
    - `grep -c "components/astro/Hero.astro" src/pages/en/index.astro` returns 1 AND same for es/index.astro
    - `grep -c "<Hero locale={locale}" src/pages/en/index.astro` returns 1 AND same for es
    - In both files, the Hero mount line precedes the About mount line (verify Hero import + mount ordering)
  </acceptance_criteria>
  <done>Hero mounts first on both /en and /es, build clean, files in sync.</done>
</task>

<task type="auto">
  <name>Task 2: Delete deprecated React Hero + build zero-JS/LCP verify</name>
  <files>src/components/Hero.jsx, src/components/Hero.test.jsx</files>
  <read_first>
    - .planning/phases/24-hero/24-RESEARCH.md (State of the Art / Deprecated section, lines 330-337 — Hero.jsx + Hero.test.jsx to be deleted once Hero.astro is mounted)
    - src/components/Hero.jsx (confirm nothing else imports it beyond the pages — the pages now import the .astro version)
  </read_first>
  <action>Confirm no remaining import of src/components/Hero.jsx anywhere (rg "components/Hero" src/ — expect only the new astro path references). Delete src/components/Hero.jsx and src/components/Hero.test.jsx. Run the full production build and the full test suite. Then verify the built output: dist/en/index.html and dist/es/index.html each contain `src="/me-800.webp"`; neither built page emits a Hero astro-island element (Hero ships zero client JS — Nav's island legitimately emits astro-island elsewhere on the page, so the check MUST be scoped to Hero and must not false-positive on Nav); both count-up.js and details-dismiss.js appear as bundled `type="module"` script references (deferred, not in a render-blocking <head> position and not is:inline). Record LCP-relevant observations (photo is the first large paint element, scripts deferred) in the SUMMARY for the Plan 24-05 QA gate.</action>
  <verify>
    <automated>rg -q "components/Hero\.jsx" src/ && echo "STILL REFERENCED" || npm run build && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/components/Hero.jsx && test ! -f src/components/Hero.test.jsx` (both deleted)
    - `rg "components/Hero(\.jsx|')" src/` returns no matches (no dangling imports)
    - `npm run build` succeeds and `npx vitest run` is fully GREEN (baseline suites + Hero.test.ts + count-up/details-dismiss suites, no regressions)
    - `grep -c 'me-800.webp' dist/en/index.html` ≥1 AND `grep -c 'me-800.webp' dist/es/index.html` ≥1
    - `rg -c 'astro-island[^>]*Hero' dist/en/index.html` returns 0 AND `rg -c 'astro-island[^>]*Hero' dist/es/index.html` returns 0 (Hero ships NO island — scoped to Hero so Nav's legitimate island does not false-positive)
    - `grep -c 'count-up' dist/en/index.html` ≥1 AND `grep -c 'details-dismiss' dist/en/index.html` ≥1 (both enhancer scripts present as deferred module bundles)
  </acceptance_criteria>
  <done>Deprecated React Hero deleted, no dangling refs, build + full suite GREEN, built pages ship static Hero with photo LCP + deferred scripts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static build output → CDN | dist/*.html are static assets; no server runtime, no user input on these routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-01 | Denial of Service (perf) | Hero scripts on LCP page | mitigate | Verify in built output that count-up.js/details-dismiss.js are deferred module chunks, not render-blocking; hard LCP timing confirmed at the Lighthouse gate (Phase 27) and spot-checked in Plan 24-05 |
| T-24-04-02 | Tampering | leftover dead React Hero | mitigate | Delete Hero.jsx/Hero.test.jsx so no orphaned CSR entry point survives (aligns with DEPLOY-04 cleanup intent) |
| T-24-04-SC | Tampering | npm installs | accept | Zero packages installed this plan |
</threat_model>

<verification>
- `npm run build` + `npx vitest run` both GREEN
- Built dist/en + dist/es carry static Hero (photo present, no island)
- No dangling Hero.jsx references
</verification>

<success_criteria>
- Hero live and first on both /en and /es
- Deprecated React Hero removed
- Zero-client-JS Hero confirmed in built output; photo is LCP candidate; scripts deferred
</success_criteria>

<output>
Create `.planning/phases/24-hero/24-04-SUMMARY.md` when done
</output>
