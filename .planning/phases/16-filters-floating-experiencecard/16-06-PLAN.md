---
phase: 16-filters-floating-experiencecard
plan: 06
type: execute
wave: 5
depends_on: ["16-03", "16-04", "16-05"]
files_modified:
  - src/game/GameMode.js
  - src/components/Nav.js
autonomous: true
requirements: [GAME-03, GAME-04]
tags: [phase-16, orchestrator, wiring, bundle-budget]
must_haves:
  truths:
    - "GameMode renders <SkillFilters /> below the H1 block — desktop persistent bar, mobile compact toolbar (D-16-FLAYOUT-DESKTOP + D-16-FLAYOUT-MOBILE)"
    - "GameMode conditionally renders <ExperienceCard /> when cons.selectedSkillId !== null; the card receives jobs derived from composeFilters intersecting selectedSkillId + selectedSkills + yearRange + category"
    - "Card onClose calls cons.onSelectSkill(cons.selectedSkillId) — toggles selectedSkillId to null via the Phase 15 hook idempotent behavior (D-16-FLOW-CLOSE-EMPTY); SvgConstellation's existing Esc handler then focuses rovingEntryRef (Phase 15 contract — no separate return-focus logic needed)"
    - "Nav.js <header> element receives data-game-interactive attribute so clicking theme/lang toggles while card is open does NOT close the card (Pitfall 6)"
    - "GameMode renderer-slot wrapper div (the existing `<div data-testid=\"renderer-slot\">` around <SvgConstellation />) receives data-game-interactive attribute (checker BLOCKER 1 fix) — this brings the SVG background into the click-outside allow-list so clicking empty space between nodes does NOT close the card, but clicking a different node still triggers SvgConstellation's onSelectSkill which swaps the card via the hook"
    - "GameMode passes `justFilteredId={cons.justFilteredId}` to <SvgConstellation /> (checker BLOCKER 2 — wires the chip-flash trigger from useConstellation through to the renderer)"
    - "GameMode passes `position={LAYOUT[cons.selectedSkillId]}` to <ExperienceCard /> when selectedSkillId !== null (SUGGESTION 8 — node-anchored desktop popover; LAYOUT is the deterministic radial layout map already computed at module level from Phase 14 buildConstellationGraph output)"
    - "All Phase 15 behavior is preserved — H1 derived copy, ErrorBoundary, ConstellationFallback sr-only list, sub-copy hint pill (none of these regress)"
    - "Final game-mode lazy chunk passes the tightened bundle gate (WARNING 4): HARD FAIL > 16 kB gz, WARN 14–16 kB gz, PASS ≤ 14 kB gz (current Phase 15 baseline 5.09 kB + Phase 16 budget 8-9 kB + chip-flash/position add ~0.5 kB)"
    - "All 6 Phase 16 component test files are GREEN: filters, useConstellation (extended), SkillFilters, ExperienceCard, SvgConstellation (extended), GameMode (extended)"
    - "Full Vitest suite is GREEN — no regression on Phase 14/15 tests"
  artifacts:
    - path: "src/game/GameMode.js"
      provides: "Game-mode orchestrator wiring SkillFilters + ExperienceCard alongside Phase 15 renderer slot"
      contains: "SkillFilters"
    - path: "src/components/Nav.js"
      provides: "Header carrying data-game-interactive for click-outside allow-list"
      contains: "data-game-interactive"
  key_links:
    - from: "src/game/GameMode.js"
      to: "cons.onSelectSkill via ExperienceCard.onClose"
      via: "onClose={() => cons.onSelectSkill(cons.selectedSkillId)} — toggle-off via Phase 15 hook"
      pattern: "cons.onSelectSkill\\(cons.selectedSkillId\\)"
    - from: "src/game/GameMode.js"
      to: "composeFilters in src/game/filters.js"
      via: "jobs filtered by intersection of selectedSkillId + filter state"
      pattern: "composeFilters"
---

<objective>
Wire the Phase 16 surfaces (SkillFilters + ExperienceCard) into GameMode and add the data-game-interactive attribute to the Nav header so click-outside doesn't fire on theme/lang toggle. After this plan, the full Phase 16 user flow works end to end: visitor sees filter bar → applies filters → constellation dims accordingly → clicks node → card opens with intersected jobs → clicks tech chip → filter updates → constellation re-dims.

Purpose: This is the integration plan. It ships the user-visible value of GAME-03 + GAME-04 and verifies the final Lighthouse-budget chunk size. No new pure logic, no new UI surfaces — only orchestration and the one-line Nav edit.

Output: 2 extended files. GameMode.test.js RED → GREEN. End-to-end manual UAT items become testable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md
@.planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md
@.planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md
@.planning/phases/16-filters-floating-experiencecard/16-UI-SPEC.md
@src/game/GameMode.js
@src/game/GameMode.test.js
@src/game/SkillFilters.js
@src/game/ExperienceCard.js
@src/game/useConstellation.js
@src/game/filters.js
@src/components/Nav.js
@src/data/experience.js
@src/data/skills.js

<interfaces>
<!-- GameMode current shape (Phase 15) — extend, do NOT replace -->
const cons = useConstellation(GRAPH_NODES)
// Renders: <section><h1 /><ConstellationErrorBoundary><SvgConstellation /></ConstellationErrorBoundary><ConstellationFallback /></section>

<!-- Phase 16 additions inside <section> -->
- <SkillFilters /> after H1 block (D-16-FLAYOUT layout via CSS, not separate components)
- {cons.selectedSkillId !== null && <ExperienceCard /> }
- SvgConstellation already receives highlightedSkillIds + yearRange from Plan 03 hook extension — props are populated, not added
- SvgConstellation NOW also receives `justFilteredId={cons.justFilteredId}` (checker BLOCKER 2 — chip-flash prop pass-through)
- ExperienceCard NOW receives `position={LAYOUT[cons.selectedSkillId]}` for desktop node-anchored popover (SUGGESTION 8 — uses the existing module-level LAYOUT map from Phase 14)
- Renderer-slot wrapper div NOW carries `data-game-interactive` attribute (checker BLOCKER 1 fix — opts constellation root into click-outside allow-list without touching the renderer's internals)

<!-- ExperienceCard's jobs prop must intersect selectedSkillId + filter state -->
const cardJobs = composeFilters(EXPERIENCE, {
  skillIds: [cons.selectedSkillId, ...cons.selectedSkills.filter((s) => s !== cons.selectedSkillId)],
  yearRange: cons.yearRange,
  category: cons.category,
}, SKILLS)

<!-- Nav.js: header gets data-game-interactive (single-line edit) -->
<header className="..."> → <header data-game-interactive className="...">
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire SkillFilters + ExperienceCard into GameMode.js</name>
  <files>src/game/GameMode.js</files>
  <read_first>
    - src/game/GameMode.js (current Phase 15 structure — destructure of `const cons = useConstellation(GRAPH_NODES)` line 72; section render lines 89-123; ErrorBoundary + ConstellationFallback usage; H1 derivation lines 13-16 + 91-96; module-level `const LAYOUT = computeLayout(GRAPH_NODES)` line 20 — the {nodeId: {x, y}} map used for the ExperienceCard position prop per SUGGESTION 8)
    - src/game/GameMode.test.js (RED tests from Plan 01 — must turn GREEN: SkillFilters group renders below H1; no dialog at init; dialog appears on node click)
    - src/game/SkillFilters.js (delivered Plan 04 — props contract)
    - src/game/ExperienceCard.js (delivered Plan 05 — props contract)
    - src/game/useConstellation.js (delivered Plan 03 — full filter state surface)
    - src/game/filters.js (delivered Plan 02 — composeFilters signature)
    - src/data/experience.js + src/data/skills.js (for composeFilters call)
    - .planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md (D-16-FLAYOUT-DESKTOP/MOBILE layout; D-16-FLOW-OPEN one-click; D-16-FLOW-CLOSE-EMPTY toggle-off pattern)
    - .planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md Wave 5 GameMode.js section (lines 1069-1147)
    - .planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md §"Architectural Responsibility Map" (orchestrator responsibilities)
  </read_first>
  <behavior>
    - Import SkillFilters and ExperienceCard at top of GameMode.js (alongside existing imports)
    - Import composeFilters from './filters.js' and SKILLS from '../data/skills.js' (for cardJobs derivation)
    - Destructure new fields from cons: selectedSkills, yearRange, category, isFilterActive, yearBounds, toggleSkill, setYearRange, setCategory, resetFilters (in addition to Phase 15 fields)
    - Inside the <section> after the H1 block, render <SkillFilters /> with all 11 props wired from cons
    - The SkillFilters wrapper element handles desktop bottom vs mobile top via Tailwind responsive classes already baked into SkillFilters (Plan 04); GameMode just renders it once, BEFORE the renderer-slot div — D-16-FLAYOUT-MOBILE says mobile shows the filter bar BELOW the H1 ABOVE the constellation, and desktop CSS can re-order via flex order if needed (UI-SPEC §Layout decides; default before-renderer is fine for both)
    - After the existing <ConstellationErrorBoundary> wrapping <SvgConstellation />, add the conditional ExperienceCard render: `{cons.selectedSkillId !== null && <ExperienceCard ... />}` — props: selectedNode from GRAPH_NODES.find by id, jobs from composeFilters intersecting [selectedSkillId, ...selectedSkills filtered to avoid duplicate of selectedSkillId] + yearRange + category, selectedSkills array, lang, t, onClose = () => cons.onSelectSkill(cons.selectedSkillId) (Phase 15 idempotent toggle), onToggleSkill = cons.toggleSkill
    - SvgConstellation prop changes: pass highlightedSkillIds={cons.highlightedSkillIds} (Phase 15 already passed it; Plan 03 hook now populates it), yearRange={cons.yearRange}, AND `justFilteredId={cons.justFilteredId}` (checker BLOCKER 2 — wires chip-flash trigger through to renderer). The render block lines 105-118 stays structurally identical except the new prop addition
    - Renderer-slot wrapper div (the existing `<div data-testid="renderer-slot" ...>` line ~107) gains `data-game-interactive` attribute (checker BLOCKER 1) — this brings the SVG background into the click-outside allow-list. SvgConstellation's existing `onSelectSkill` callback fires on node clicks and the useConstellation hook toggles selectedSkillId, so different-node clicks still SWAP the card (Phase 15 behavior preserved); only background clicks (between nodes, on the SVG root or padding) are now safe
    - ExperienceCard position prop (SUGGESTION 8): pass `position={LAYOUT[cons.selectedSkillId]}` — LAYOUT is the module-level deterministic radial layout map (line 20 of current GameMode.js). The map shape is `{ [nodeId]: { x, y } }`. ExperienceCard internally decides desktop-vs-mobile and applies/ignores. Pass `null` only if LAYOUT does not contain the selectedSkillId (defensive)
    - ConstellationFallback + sub-copy / hint pill from Phase 15 remain unchanged
    - Add safety-net body overflow restore in the existing ConstellationErrorBoundary componentDidCatch (Pitfall 5) — IF the error boundary file is accessible; otherwise skip and document in summary
  </behavior>
  <action>EDIT src/game/GameMode.js. Add imports: `import SkillFilters from './SkillFilters'`, `import ExperienceCard from './ExperienceCard'`, `import { composeFilters } from './filters'`, `import { SKILLS } from '../data/skills'`. Update the destructure of `const cons = useConstellation(GRAPH_NODES)` — or use cons.X access pattern throughout — to consume the new fields. Inside the <section>, AFTER the existing H1 block (lines 91-96 currently), INSERT `<SkillFilters nodes={GRAPH_NODES} selectedSkills={cons.selectedSkills} yearRange={cons.yearRange} yearBounds={cons.yearBounds} category={cons.category} isFilterActive={cons.isFilterActive} onToggleSkill={cons.toggleSkill} onYearRangeChange={cons.setYearRange} onCategoryChange={cons.setCategory} onReset={cons.resetFilters} lang={lang} t={t} />`. Preserve the existing <ConstellationErrorBoundary> + <SvgConstellation /> + <ConstellationFallback /> block — confirm the highlightedSkillIds and yearRange props on SvgConstellation are still `cons.highlightedSkillIds` and `cons.yearRange` (now populated by Plan 03 hook), AND ADD `justFilteredId={cons.justFilteredId}` to the SvgConstellation prop list (checker BLOCKER 2 — chip-flash wire-through). ADD `data-game-interactive` attribute to the renderer-slot wrapper div (the existing `<div data-testid="renderer-slot" ...>` around the SvgConstellation — checker BLOCKER 1 fix). Placement: `<div data-game-interactive data-testid="renderer-slot" className="w-full max-w-3xl relative" ...>` — single attribute add, no other changes to that wrapper. AFTER the ErrorBoundary block (before the closing </section>), add the conditional ExperienceCard render per PATTERNS.md lines 1125-1139: only when `cons.selectedSkillId !== null`. Compute cardJobs inline or via a hooked useMemo: `composeFilters(EXPERIENCE, { skillIds: [cons.selectedSkillId, ...cons.selectedSkills.filter((s) => s !== cons.selectedSkillId)], yearRange: cons.yearRange, category: cons.category }, SKILLS)`. Pass selectedNode={GRAPH_NODES.find((n) => n.id === cons.selectedSkillId)}, jobs={cardJobs}, selectedSkills={[cons.selectedSkillId, ...cons.selectedSkills]} (includes the locked one), lang, t, onClose={() => cons.onSelectSkill(cons.selectedSkillId)} (Phase 15 idempotent toggle-off), onToggleSkill={cons.toggleSkill}, AND `position={LAYOUT[cons.selectedSkillId] ?? null}` (SUGGESTION 8 — node-anchored desktop popover; LAYOUT already exists at module scope per current GameMode.js line 20). Wrap cardJobs in useMemo with deps [cons.selectedSkillId, cons.selectedSkills, cons.yearRange, cons.category] to avoid recomputing on every render — Pitfall 7 guard. No semicolons.</action>
  <verify>
    <automated>npx vitest run src/game/GameMode.test.js 2>&amp;1 | tee /tmp/wave5-game.log; grep -E "passed|failed" /tmp/wave5-game.log | tail -3; npx vitest run 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/game/GameMode.js imports SkillFilters from './SkillFilters' and ExperienceCard from './ExperienceCard'
    - src/game/GameMode.js imports composeFilters from './filters' and SKILLS from '../data/skills'
    - <SkillFilters /> JSX element present with all 11 props wired from cons.* — `grep -c "<SkillFilters" src/game/GameMode.js` returns ≥1
    - <ExperienceCard /> JSX element rendered inside a conditional `cons.selectedSkillId !== null && ...` — `grep -c "selectedSkillId !== null" src/game/GameMode.js` returns ≥1
    - cardJobs (or inline) uses composeFilters with skillIds: [cons.selectedSkillId, ...cons.selectedSkills filter] — D-16-INTERSECT-AND across selectedSkillId + selectedSkills
    - ExperienceCard onClose prop is `() => cons.onSelectSkill(cons.selectedSkillId)` (Phase 15 idempotent toggle per D-16-FLOW-CLOSE-EMPTY)
    - SvgConstellation still receives highlightedSkillIds={cons.highlightedSkillIds} and yearRange={cons.yearRange} (Phase 15 props now populated by Plan 03 hook)
    - SvgConstellation NOW also receives `justFilteredId={cons.justFilteredId}` — `grep -c "justFilteredId={cons.justFilteredId}\|justFilteredId={cons\\.justFilteredId}" src/game/GameMode.js` returns ≥1 (checker BLOCKER 2 — chip-flash prop pass-through)
    - Renderer-slot wrapper div carries `data-game-interactive` attribute — `grep -E 'data-game-interactive[^>]*data-testid="renderer-slot"|data-testid="renderer-slot"[^>]*data-game-interactive' src/game/GameMode.js | wc -l` returns ≥1 (checker BLOCKER 1 fix)
    - ExperienceCard receives `position={LAYOUT[cons.selectedSkillId] ?? null}` — `grep -c "position={LAYOUT" src/game/GameMode.js` returns ≥1 (SUGGESTION 8 node-anchored popover)
    - Phase 15 elements preserved: <ConstellationErrorBoundary>, <ConstellationFallback />, H1 block — `grep -c "ConstellationFallback\\|ConstellationErrorBoundary" src/game/GameMode.js` returns ≥2
    - `npx vitest run src/game/GameMode.test.js` exits 0 with all 12+ tests PASSING (Phase 15 9 + Phase 16 3)
    - `npx vitest run` shows the full suite GREEN — no test in any of filters.test.js, useConstellation.test.js, SkillFilters.test.js, ExperienceCard.test.js, SvgConstellation.test.js, GameMode.test.js fails
    - No semicolons in edits; JSX in .js
  </acceptance_criteria>
  <done>GameMode.js wires filters + card. All Phase 16 component tests GREEN; full suite GREEN. Commit: `feat(16): wire SkillFilters + ExperienceCard into GameMode`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add data-game-interactive to Nav header + build/bundle-budget verification</name>
  <files>src/components/Nav.js</files>
  <read_first>
    - src/components/Nav.js (line 26 — the <header> opening tag — single attribute add per Pitfall 6)
    - .planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md §"Pitfall 6" (data-game-interactive allow-list misses Nav header)
    - .planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md (D-16-PERSIST-CROSS-TOGGLE: language/theme toggle preserves in-memory state — toggle clicks must NOT close card)
    - .planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md §10 (bundle budget: Phase 16 add target ≤ 10 kB gz; total game-mode chunk ≤ 14 kB gz)
    - .planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md Wave 5 Nav.js one-line edit section (lines 1151-1160)
  </read_first>
  <behavior>
    - Single one-line edit: add `data-game-interactive` attribute to the existing <header> element at line 26 of src/components/Nav.js — placement before the existing className attribute
    - The attribute is a marker for the click-outside allow-list in ExperienceCard (Plan 05) — Nav itself doesn't read it
    - No other Nav changes; no className changes; no structural changes
    - After the attribute add, run `npm run build` and capture the gzip size of dist/assets/GameMode-*.js via a small node helper or `gzip-size-cli` invocation (non-interactive, CI-friendly)
    - Apply the tightened bundle gate (checker WARNING 4):
      - HARD FAIL if gz size > 16 kB → block plan close (exit non-zero, surface as failure)
      - WARN if gz size in 14–16 kB → log value to /tmp/wave5-bundle.log + cite in Wave 5 summary, proceed (do NOT block)
      - PASS (silent) if gz size ≤ 14 kB
    - The bundle audit is a separate scripted check (small inline node script reading dist/assets/GameMode-*.js with zlib + statSync, OR `npx gzip-size-cli dist/assets/GameMode-*.js`) — do NOT rely on visual inspection of Vite's build log
    - If the chunk file path is non-deterministic due to content hash, use a glob expansion: `for f in dist/assets/GameMode-*.js; do gzip -c "$f" | wc -c; done` and take the first match
  </behavior>
  <action>EDIT src/components/Nav.js. Locate the <header> element (line 26 — `<header className="sticky top-0 z-50 backdrop-blur-md bg-ink-900/70 border-b border-ink-600">`). Add the `data-game-interactive` attribute before the className attribute: `<header data-game-interactive className="..."`. Do not change any other attribute or class. Run `npm run build`. After build success, compute the actual gzip size of `dist/assets/GameMode-*.js` programmatically — preferred path: a small inline node helper `node -e "const fs=require('fs'),zlib=require('zlib'),g=require('glob');const f=g.sync('dist/assets/GameMode-*.js')[0];const sz=zlib.gzipSync(fs.readFileSync(f)).length;console.log('GameMode gz='+sz+' bytes ('+(sz/1024).toFixed(2)+' kB)');process.exit(sz>16384?1:0)"` (or substitute `npx gzip-size-cli dist/assets/GameMode-*.js` if `gzip-size-cli` is installed/preferred; both are equivalent and non-interactive). Apply the tightened gate per WARNING 4: HARD FAIL > 16 kB gz (script exits non-zero, plan close blocks), WARN 14–16 kB gz (log value to /tmp/wave5-bundle.log + cite in summary, proceed), PASS ≤ 14 kB gz (silent). Always log the actual gz size + kB value to /tmp/wave5-bundle.log for the summary regardless of band. Verify via curl or grep that the attribute landed: `grep -c "data-game-interactive" src/components/Nav.js`. Run the full Vitest suite one final time as a regression check.</action>
  <verify>
    <automated>grep -c "data-game-interactive" src/components/Nav.js; npm run build 2>&amp;1 | tee /tmp/wave5-build.log; node -e "const fs=require('fs'),zlib=require('zlib'),g=require('glob');const files=g.sync('dist/assets/GameMode-*.js');if(!files.length){console.log('GameMode chunk MISSING');process.exit(1)}const sz=zlib.gzipSync(fs.readFileSync(files[0])).length;const kb=(sz/1024).toFixed(2);console.log('GameMode gz='+sz+' bytes ('+kb+' kB)');require('fs').appendFileSync('/tmp/wave5-bundle.log','GameMode gz='+sz+' bytes ('+kb+' kB)\n');if(sz>16384){console.error('HARD FAIL: gz size '+kb+' kB > 16 kB');process.exit(1)}else if(sz>14336){console.warn('WARN: gz size '+kb+' kB in 14-16 kB band — proceeding')}else{console.log('PASS: gz size '+kb+' kB ≤ 14 kB')}"; npx vitest run 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "data-game-interactive" src/components/Nav.js` returns exactly 1 (the new header attribute)
    - The Nav.js header retains its existing className verbatim — no styling change
    - `npm run build` exits 0
    - The dist/assets/GameMode-*.js chunk gz size (measured programmatically via node + zlib + glob, NOT Vite's build-log visual) is logged to /tmp/wave5-bundle.log
    - Tightened bundle gate (WARNING 4):
      - if size > 16 kB gz: the verify script exits non-zero → task FAILS → plan close blocked (HARD FAIL)
      - if size in 14–16 kB gz: the verify script warns to stderr but exits 0 → proceed, cite the value in Wave 5 summary as a Phase 17 budget concern (WARN)
      - if size ≤ 14 kB gz: silent pass (PASS)
    - Expected outcome with chip-flash state (+0.3 kB) + position prop (+0.2 kB) + Plan 06 wiring (≈0 kB): total ≤ 14 kB gz (PASS band)
    - `npx vitest run` exits 0 — the FULL Vitest suite (Phase 14 + 15 + 16 tests) is GREEN
    - No regression: ConstellationFallback.test.js, Nav-related tests if any, and all Phase 14 pure-logic tests still pass
    - No semicolons added to the JSX edit
  </acceptance_criteria>
  <done>Nav header attribute added; build succeeds; chunk size logged. Full Phase 16 user flow live and tested. Commit: `feat(16): add data-game-interactive to Nav header + finalize bundle`. The phase is now executable end-to-end.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — entire repo Vitest suite GREEN (Phase 14 + 15 + 16 all passing)
- `npm run build` — exits 0; dist/assets/GameMode-*.js size logged for /gsd:verify-work
- Manual UAT (browser, conducted in /gsd:verify-work):
  - Visitor sees filter bar with skill chips + year slider + category chips + Reset
  - Click skill chip → corresponding nodes brighten, others dim
  - Drag year slider thumb (or keyboard ArrowRight) → matching nodes dim/brighten
  - Click category chip → category nodes brighten
  - Reset button enabled state, click clears all filters
  - Click node → ExperienceCard opens with role=dialog announced
  - Tab through card cycles within (heading → tech chips → close → CV CTA → wrap)
  - Esc closes card, focus returns to node
  - Click outside card (but NOT on Nav theme toggle) closes card
  - Click theme toggle in Nav while card open → theme switches, card stays open
  - Click another skill node while open → card swaps content (cross-fade)
  - Click tech chip inside card → adds skill to filter (intersection); chip flash on constellation
  - CV CTA download triggers .docx file for active lang
  - Bilingual: switch lang while card open → all card content + filter labels switch language
  - Reduced-motion: animations are instant
  - Mobile: bottom-sheet variant slides up; drag-handle visible but not draggable (v1)
</verification>

<success_criteria>
- GameMode.js wires SkillFilters + ExperienceCard with full filter state from cons
- Nav.js header carries data-game-interactive for click-outside allow-list
- ALL 6 Phase 16 component test files are GREEN
- FULL Vitest suite is GREEN (no regression)
- Bundle (tightened gate per WARNING 4): game-mode chunk in PASS band (≤ 14 kB gz) OR WARN band (14–16 kB gz, cited in summary); HARD FAIL band (> 16 kB gz) blocks plan close
- Phase 16 end-to-end user flow works: filter → highlight → click → card → tech chip → re-filter → close → return-focus
- 2 atomic commits, one per task
</success_criteria>

<output>
Create `.planning/phases/16-filters-floating-experiencecard/16-06-SUMMARY.md` — list the wiring changes in GameMode.js (props passed to SkillFilters; conditional ExperienceCard render; cardJobs derivation; onClose toggle-off pattern), the Nav.js one-line edit, the final gzip chunk size from `npm run build`, and confirm all 6 Phase 16 acceptance criteria from CONTEXT.md are end-to-end testable.
</output>
