---
phase: 14-foundation-data-layer-viewmode-toggle-test-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - src/test/setup.js
  - src/test/setup.test.js
  - src/data/skills.js
  - src/data/skills.test.js
  - src/data/experience.js
  - src/game/constellation.graph.js
  - src/game/constellation.layout.js
  - src/game/constellation.graph.test.js
  - src/game/constellation.layout.test.js
autonomous: true
requirements: [GAME-02, TEST-01]
must_haves:
  truths:
    - "`npm run test:run` exits 0 with a passing Vitest suite running under jsdom"
    - "The derived skill graph exposes one node per canonical skill (GCP folded into Google Cloud, GKE separate), each with category, color, count, year range and experience indices"
    - "The derived graph exposes a co-occurrence edge for every pair of skills sharing a job, weight = number of shared jobs"
    - "Every one of the 12 experience entries carries a numeric period { start, end|null } without losing its display date string"
    - "Layout produces deterministic baked positions (same input → identical coordinates) clustered by category, with no d3-force shipped in the client bundle"
    - "Pure graph + layout + normalization logic is unit-tested near-100%"
  artifacts:
    - path: "src/data/skills.js"
      provides: "SKILL_CATEGORIES (8 categories with en/es/color) + canonical skill→category+aliases catalog"
      contains: "SKILL_CATEGORIES"
    - path: "src/game/constellation.graph.js"
      provides: "Pure derivation of skill nodes + co-occurrence edges from experience.js + skills.js"
      exports: ["buildConstellationGraph"]
    - path: "src/game/constellation.layout.js"
      provides: "Deterministic baked node positions clustered by category (build-time, fixed seed)"
      exports: ["computeLayout"]
    - path: "src/data/experience.js"
      provides: "12 entries each extended with numeric period { start, end|null }"
      contains: "period:"
    - path: "vite.config.js"
      provides: "Vitest test block (jsdom env, globals, setupFiles) inside the existing Vite config"
      contains: "test:"
    - path: "src/test/setup.js"
      provides: "RTL/jest-dom matcher registration for the suite"
      contains: "@testing-library/jest-dom"
  key_links:
    - from: "src/game/constellation.graph.js"
      to: "src/data/experience.js + src/data/skills.js"
      via: "import EXPERIENCE + SKILLS/SKILL_CATEGORIES, derive nodes/edges"
      pattern: "import.*experience|import.*skills"
    - from: "src/game/constellation.layout.js"
      to: "src/game/constellation.graph.js"
      via: "consumes derived nodes to place them"
      pattern: "constellation\\.graph"
    - from: "vite.config.js"
      to: "src/test/setup.js"
      via: "test.setupFiles reference"
      pattern: "setupFiles"
---

<objective>
Stand up the project's first test infrastructure (Vitest + React Testing Library) and build the pure, fully-tested data foundation for game mode: a curated skill catalog, a derived skill graph (nodes + co-occurrence edges), numeric experience periods, and deterministic baked layout positions.

Purpose: This is the data + test slice the rest of v3.8 stands on. After it, `npm test` runs and any other module can import a queryable, verified skill graph — without any rendering. It pays down the test-infra debt deferred across 9 milestones (D8 / TEST-01) and implements the design spec Data Model section (GAME-02).

Output: `vite.config.js` test block, `src/test/setup.js`, `src/data/skills.js`, `src/game/constellation.graph.js`, `src/game/constellation.layout.js`, `period` added to all 12 entries in `src/data/experience.js`, and near-100% unit tests for graph + layout + normalization.
</objective>

<phase_goal>
**As a** recruiter visiting the portfolio, **I want to** have my career data presented as an explorable skill graph, **so that** I can later navigate Carlos's experience by skill instead of scrolling a linear list.

(Phase 14 ships the tested data + state foundation only; this plan delivers the data + graph + test runner. No visual rendering — that is Phase 15.)
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-foundation-data-layer-viewmode-toggle-test-infra/14-CONTEXT.md
@.planning/phases/14-foundation-data-layer-viewmode-toggle-test-infra/14-PATTERNS.md
@docs/superpowers/specs/2026-05-29-game-mode-design.md

# Source files this plan reads/extends
@src/data/experience.js
@src/data/projects.js
@vite.config.js
@package.json

<critical_conventions>
- JSX lives in `.js` files (NOT `.jsx`). `vite.config.js` sets `esbuild.loader:'jsx'` with `include:/src\/.*\.js$/`. ALL new files use `.js`: `src/data/skills.js`, `src/game/constellation.graph.js`, `src/game/constellation.layout.js`, and `.test.js` files. Do NOT create `.jsx`.
- Vitest config goes INSIDE the existing `vite.config.js` (a `test:` block), NOT a separate `vitest.config.js` — a separate config would lose the `.js`-as-JSX esbuild loader and break parsing of `.js` test/component files. `defineConfig` from `vite` does not type-check the `test` key but Vitest reads it at runtime; the project already uses Vitest ≥2 conventions for Vite 6.
- `src/data/*.js` are pure data modules with zero framework imports. `src/game/*.js` derivation/layout modules MUST be pure (no React, no DOM) so they unit-test trivially.
- Testing rules (project): deterministic, fixed seeds, no random data; pure logic near-100% coverage; test names describe behavior.
</critical_conventions>

<interfaces>
<!-- Extracted from the live codebase. Use directly — no exploration needed. -->

experience.js entry shape (src/data/experience.js, exported `default EXPERIENCE`, 12 entries). Each entry:
  { date: { en, es }, title: { en, es }, company: string, location: { en, es },
    bullets: { en: [], es: [] }, tech: [string], featured?: boolean }
ADD to every entry: period: { start: <number>, end: <number|null> }  // null = Present

The 12 entries' tech[] arrays (index : company : tech) — THE source of nodes + edges:
  0 : Coderio (Jan 2026 — Present) : ['Java','Spring Boot','Claude Code','GitHub Copilot','JetBrains Junie']  → period {start:2026, end:null}
  1 : F2X SAS (Sep 2024 — Dec 2025) : ['Java','Spring Boot','Microservices']                                   → {start:2024, end:2025}
  2 : Blerify (Jan 2024 — Jan 2025) : ['Java','Keycloak','KrakenD','Kubernetes','GKE','Google Cloud']          → {start:2024, end:2025}
  3 : KLEVER SAS (Aug 2020 — Feb 2022) : ['Java','Spring Boot','AWS','Keycloak','GCP']                          → {start:2020, end:2022}
  4 : Tata (Aug 2013 — Sep 2015) : ['Java','Oracle Service Bus','WebSphere','Jenkins']                          → {start:2013, end:2015}
  5 : SM TECH SAS (Aug 2023 — Mar 2024) : ['Java','Spring Boot','Microservices']                                → {start:2023, end:2024}
  6 : TUL SAS (Apr 2022 — Jun 2023) : ['Java','Spring Boot','Spring Security','Microservices']                  → {start:2022, end:2023}
  7 : Linked (May 2021 — Feb 2022) : ['Jenkins','SonarQube','Nexus','Docker','Microservices']                   → {start:2021, end:2022}
  8 : PATH — INNKUA (Nov 2009 — Jan 2021) : ['Java','AWS','IoT','Spring Boot']                                  → {start:2009, end:2021}
  9 : Grupo Nethexa (Jun 2012 — Aug 2013) : ['Java','Asterisk','Raspberry Pi']                                  → {start:2012, end:2013}
  10 : Ceiba (Jun 2009 — Jun 2010) : ['Java','JEE 5','Oracle SQL']                                               → {start:2009, end:2010}
  11 : Mercurio SAS (Apr 2007 — Jun 2009) : ['Java','SQL Server','MySQL']                                        → {start:2007, end:2009}

SKILL_CATEGORIES (verbatim from design spec — 8 categories, each { en, es, color }):
  lang     #3b82f6  (Languages & Frameworks / Lenguajes & Frameworks)
  ai       #a855f7  (AI Tooling / Herramientas IA)
  arch     #06b6d4  (Architecture & Integration / Arquitectura & Integración)
  cloud    #10b981  (Cloud / Cloud)
  devops   #f59e0b  (DevOps & Infra / DevOps & Infra)
  security #ef4444  (Security / Seguridad)
  data     #8b5cf6  (Data / Datos)
  hardware #ec4899  (IoT & Hardware / IoT & Hardware)

Skill → category mapping (single category per skill):
  lang:     Java, Spring Boot, JEE 5
  ai:       Claude Code, GitHub Copilot, JetBrains Junie
  arch:     Microservices, Oracle Service Bus, WebSphere, KrakenD
  cloud:    AWS, Google Cloud (canonical; alias 'GCP'), GKE
  devops:   Kubernetes, Docker, Jenkins, SonarQube, Nexus
  security: Keycloak, Spring Security
  data:     Oracle SQL, SQL Server, MySQL
  hardware: IoT, Asterisk, Raspberry Pi

Node shape (design spec line 136): { id, label, category, color, count, years: [min, max], experienceIdx: [] }
Edge shape (design spec line 138): { source, target, weight }   // weight = # shared jobs

projects.js convention (pure data module): module-scope `const NAME = [...]`, bilingual `{ en, es }` per text field, `export default`, no framework imports.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vitest + RTL test infrastructure</name>
  <files>package.json, vite.config.js, src/test/setup.js, src/test/setup.test.js</files>
  <read_first>
    - package.json (scripts at lines 12-24, devDependencies at lines 25-41)
    - vite.config.js (the esbuild.loader:'jsx' + include:/src\/.*\.js$/ block at lines 20-24 — MUST be preserved)
    - 14-PATTERNS.md "No Analog Found" row for Vitest (lines 362) — the authoritative scaffold guidance
  </read_first>
  <action>
    Add the project's first test infrastructure (TEST-01 / D8). Add devDependencies: `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom`, `@vitest/coverage-v8` — install pinned versions compatible with Vite 6 + React 18 (Vitest 2.x line) via `npm install -D`. Add `scripts`: `"test": "vitest"`, `"test:run": "vitest run"`, `"coverage": "vitest run --coverage"`. Add a `test` block INSIDE the existing `vite.config.js` defineConfig object (NOT a new vitest.config.js — that would lose the `.js`-as-JSX esbuild loader and break test parsing): `environment: 'jsdom'`, `globals: true`, `setupFiles: ['./src/test/setup.js']`, and a `coverage` provider `'v8'`. Create `src/test/setup.js` that imports `@testing-library/jest-dom/vitest`. Keep the existing react plugin, visualizer, esbuild, resolve and optimizeDeps blocks untouched. Add a trivial smoke test `src/test/setup.test.js` (e.g. `expect(true).toBe(true)`) so the runner has something to run green on first invocation; the real tests come in Tasks 2-3.
  </action>
  <verify>
    <automated>npm run test:run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run` exits 0 and reports the jsdom environment in use
    - `vite.config.js` still contains the `esbuild` block with `loader: 'jsx'` and `include: /src\/.*\.js$/` (grep: `rg -n "loader: 'jsx'" vite.config.js` returns a match) AND now also contains a `test:` block referencing `./src/test/setup.js`
    - No `vitest.config.js` / `vitest.config.ts` file was created (`fd vitest.config` returns nothing)
    - `package.json` has `test`, `test:run`, `coverage` scripts and the 6 listed devDeps
  </acceptance_criteria>
  <done>`npm run test:run` runs Vitest under jsdom and the smoke test passes; Vitest config lives inside vite.config.js and the JSX esbuild loader is intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Skill catalog + numeric experience periods (GAME-02 data)</name>
  <files>src/data/skills.js, src/data/skills.test.js, src/data/experience.js</files>
  <read_first>
    - src/data/experience.js (full — all 12 entries; the tech[] arrays + date strings are the source of truth)
    - src/data/projects.js (pure-data module convention: const + bilingual {en,es} + export default)
    - 14-PATTERNS.md sections "src/data/skills.js" (lines 230-256) and "experience.js period field" (lines 259-281)
    - design spec Data Model (lines 98-145) — SKILL_CATEGORIES verbatim + skill→category table + period shape
  </read_first>
  <behavior>
    - Test: SKILL_CATEGORIES has exactly the 8 keys lang/ai/arch/cloud/devops/security/data/hardware, each with `en`, `es`, and the exact color from the interfaces block.
    - Test: every skill string appearing in any experience.js tech[] resolves to a category via the catalog (no orphan skill) — after alias normalization.
    - Test: 'GCP' and 'Google Cloud' resolve to the SAME canonical skill id; 'GKE' resolves to a DIFFERENT skill id (stays separate).
    - Test: 'Spring Security' resolves to category `security` (single-category, per D-locked decision).
    - Test: all 12 experience entries expose `period.start` (number) and `period.end` (number or null); the Coderio entry (index 0) has `period.end === null`; the display `date` strings are unchanged.
  </behavior>
  <action>
    Create `src/data/skills.js` per GAME-02 and the locked CONTEXT decisions. Export `SKILL_CATEGORIES` (named) — the 8-category object copied verbatim from the interfaces block (en/es/color). Export a canonical skill catalog (default export, module-scope UPPER_SNAKE const like `experience.js`'s `EXPERIENCE` and `projects.js`'s `PROJECTS`) mapping each canonical skill label → `{ category, aliases: [] }`. Use `aliases` to collapse `GCP` into the canonical `Google Cloud` node; keep `GKE` as its own node. Assign `Spring Security` to `security` (single category per locked decision). The catalog adds category + color + aliases ONLY — it MUST NOT duplicate edges (edges derive from tech[] in Task 3). Then MODIFY `src/data/experience.js`: add a numeric `period: { start, end|null }` to EVERY one of the 12 entries using the start/end year mapping in the interfaces block; null = Present (Coderio). Do NOT regex-parse the localized `date` string; do NOT change any existing `date`/`title`/`bullets`/`tech` field. Write `src/data/skills.test.js` first covering the behaviors above (RED), then implement to green.
  </action>
  <verify>
    <automated>npm run test:run -- skills</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- skills` passes all behavior tests above
    - `rg -c "period:" src/data/experience.js` returns 12
    - `rg -n "SKILL_CATEGORIES" src/data/skills.js` shows a named export; the 8 colors match the interfaces block exactly
    - No edge/co-occurrence data is authored in skills.js (it only annotates skills)
  </acceptance_criteria>
  <done>skills.js exposes the 8 categories + a canonical skill catalog with GCP→Google Cloud aliasing and GKE separate; all 12 experiences carry numeric period; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pure skill-graph derivation + deterministic baked layout (GAME-02 + TEST-01)</name>
  <files>src/game/constellation.graph.js, src/game/constellation.layout.js, src/game/constellation.graph.test.js, src/game/constellation.layout.test.js</files>
  <read_first>
    - src/data/skills.js + src/data/experience.js (as produced by Task 2 — the inputs)
    - 14-PATTERNS.md "No Analog Found" rows for constellation.graph.js and constellation.layout.js (lines 360-361) — pure-function guidance, node/edge shapes, determinism requirement
    - design spec Data Model "Graph" (lines 135-140) and "Layout" (lines 147-151)
  </read_first>
  <behavior>
    - graph Test: derives one node per canonical skill; 'GCP' (KLEVER) and 'Google Cloud' (Blerify) collapse into a single node whose `count` reflects both jobs and whose `experienceIdx` includes both indices.
    - graph Test: 'GKE' is its own node, separate from 'Google Cloud'.
    - graph Test: node `count` equals the number of distinct experiences using that skill; node `years` = [min start, max end-or-current] across those experiences (Present resolves to a fixed current year constant, e.g. 2026, derived from period.end===null — no `new Date()` in the pure function so the test stays deterministic).
    - graph Test: an edge exists for each unordered pair of skills sharing a job; edge `weight` = number of shared jobs. e.g. Java+Spring Boot weight = count of jobs containing both; a pair appearing together in exactly one job has weight 1.
    - graph Test: edges are de-duplicated (no source/target pair appears twice, no self-edges).
    - layout Test: `computeLayout(nodes)` returns one position { x, y } per node id; calling it twice with identical input yields byte-identical coordinates (deterministic / fixed seed).
    - layout Test: nodes of the same category are placed closer to that category's centroid than to a different category's centroid (clustering holds).
  </behavior>
  <action>
    Create `src/game/constellation.graph.js` exporting a pure `buildConstellationGraph(experience, skills)` (no React/DOM imports) that returns `{ nodes, edges }`. Node `{ id, label, category, color, count, years: [min, max], experienceIdx: [] }`; resolve aliases (GCP→Google Cloud) BEFORE counting so duplicates merge; color comes from SKILL_CATEGORIES[category].color. For `years`, map period.start/end with `end===null` → a module-level CURRENT_YEAR constant (2026) so output is deterministic and testable (do NOT call `new Date()`). Edge `{ source, target, weight }` for every unordered skill pair co-occurring in a job, weight = shared-job count, de-duplicated, no self-edges. Create `src/game/constellation.layout.js` exporting a pure `computeLayout(nodes, options?)` that returns deterministic baked positions clustered by category centroid — d3-force is allowed ONLY at build/derivation time inside this module run deterministically (fixed seed / fixed iteration count and a seeded RNG), but the chosen approach must NOT pull d3-force into the client render path of later phases (per GAME-07 / locked layout decision: positions are data). Simplest deterministic approach is acceptable (CONTEXT discretion): a seeded radial/centroid placement per category requires no d3-force dependency at all — prefer that if it satisfies the clustering test, to keep the client bundle free of d3-force. Write both `.test.js` files first (RED) covering the behaviors above, then implement to green; aim near-100% line coverage on both modules.
  </action>
  <verify>
    <automated>npm run test:run -- constellation</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- constellation` passes all graph + layout behaviors
    - `npm run coverage -- constellation` reports near-100% line coverage for constellation.graph.js and constellation.layout.js
    - Both modules have zero `import 'react'` / DOM references (grep: `rg -n "from 'react'|document\.|window\." src/game/constellation.graph.js src/game/constellation.layout.js` returns nothing)
    - Layout determinism test (same input twice → identical output) passes
  </acceptance_criteria>
  <done>The skill graph derives correct merged nodes + weighted de-duplicated edges purely from data, and layout bakes deterministic category-clustered positions; both are unit-tested near-100% and ship no d3-force into the client path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/test toolchain → repo | New devDependencies (vitest, RTL, jsdom) enter the supply chain |
| static data modules → derivation | All inputs are first-party committed data — no untrusted runtime input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-SC | Tampering | npm devDep installs (vitest, @testing-library/*, jsdom, @vitest/coverage-v8) | mitigate | All are mainstream, high-download, first-party-maintained packages (vitest = Vitest core team; @testing-library/* = Testing Library org). Treated as `[ASSUMED]` legitimate per fallback policy (no RESEARCH.md audit table this phase); executor verifies each on npmjs.com before install and pins exact versions. No transitive postinstall scripts beyond standard. |
| T-14-01 | Tampering | constellation.graph.js / layout.js | accept | Inputs are static committed first-party data; functions are pure with no runtime external input. No injection surface. Low value, no PII. |
| T-14-02 | Information Disclosure | experience.js period data | accept | Career years are already public on the live site; adding numeric period discloses nothing new. |

Overall surface for this plan is minimal (client-only static SPA, pure data + local test runner, no backend, no runtime user input). Block only on a high-severity supply-chain signal (none expected for these mainstream packages).
</threat_model>

<verification>
- `npm run test:run` exits 0 with the full suite green under jsdom.
- `npm run coverage` shows near-100% on `src/game/constellation.graph.js` and `src/game/constellation.layout.js`.
- `npm run build` still succeeds (Vitest block in vite.config.js does not break the production build).
- `rg -c "period:" src/data/experience.js` returns 12; GCP/Google Cloud merge + GKE-separate + Spring Security→security all assert green.
- No `.jsx` files created; no separate `vitest.config.*`; esbuild JSX loader intact.
</verification>

<success_criteria>
- ROADMAP Phase 14 criteria #1, #2, #3, and the unit-test half of #5 are satisfied: skills.js catalog with GCP normalization + GKE separate (#1), graph derivation with nodes/edges/year ranges + numeric period (#2), deterministic baked layout with no client d3-force (#3), and `npm test` running near-100% unit-tested pure logic (#5 unit portion).
- GAME-02 (derivation) and TEST-01 (setup + pure-logic) are delivered for this phase.
</success_criteria>

<output>
Create `.planning/phases/14-foundation-data-layer-viewmode-toggle-test-infra/14-01-SUMMARY.md` when done. Note for Plan 14-02: the Vitest config (jsdom + setupFiles) is now in `vite.config.js` — the toggle component test reuses it, no re-scaffolding needed.
</output>
