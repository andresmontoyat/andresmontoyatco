---
phase: 24-hero
plan: 03
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - src/components/astro/About.astro
  - src/components/astro/About.test.ts
autonomous: true
requirements: [STATIC-02]

must_haves:
  truths:
    - "D-03: About.astro re-adds count-up (closing Phase 23's D-04 deferral) by adding data-count-up/data-count-up-template attributes to the quick-facts value span for any fact whose picked value contains a number, and referencing the SAME shared src/scripts/count-up.js via one <script src> tag"
    - "D-02: About consumes the single shared count-up.js module (not a duplicated inline script), proving the consolidation success criterion across two components"
    - "Facts without an embedded number (Location, Current role, Languages, Work mode) render unchanged — no data-count-up attribute added to them"
    - "The edit is surgical — the existing .map() loop, pick() helper, and about.json data shape are untouched; About.astro remains a zero-JS section aside from the additive count-up <script>"
  artifacts:
    - path: "src/components/astro/About.astro"
      provides: "Count-up-enhanced quick-facts (Experience fact only) + shared script reference"
      contains: "scripts/count-up.js"
    - path: "src/components/astro/About.test.ts"
      provides: "Added count-up attribute + script-tag assertions; stale D-04 comment corrected"
  key_links:
    - from: "src/components/astro/About.astro"
      to: "src/scripts/count-up.js"
      via: "<script src=\"../../scripts/count-up.js\">"
      pattern: "scripts/count-up.js"
    - from: "About.astro exp fact value span"
      to: "count-up.js"
      via: "data-count-up attribute selector"
      pattern: "data-count-up"
---

<objective>
Retrofit `src/components/astro/About.astro` (built zero-JS in Phase 23, count-up dropped per that phase's D-04) to re-add count-up via the shared `src/scripts/count-up.js` (D-03) — the one file outside Hero/scripts this phase touches. Extend `About.test.ts` with count-up assertions and correct the now-stale D-04 comment.

Purpose: Close Phase 23's explicit deferral and prove the count-up consolidation (D-02) works across two different `.astro` components sharing one module.
Output: Surgical edit to About.astro + extended About.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-hero/24-RESEARCH.md
@.planning/phases/24-hero/24-PATTERNS.md
@.planning/phases/24-hero/24-CONTEXT.md

<interfaces>
<!-- About.astro is at src/components/astro/ so the shared-script relative path is IDENTICAL to Hero's: ../../scripts/count-up.js -->
<!-- about.json facts (confirmed): only the "exp" fact carries a number — value.en "18+ years", value.es "+18 años". The other 4 (location/role/lang/work) have no digit. -->
<!-- statParts regex (same as Hero.astro, from Hero.jsx lines 66-69): value.match(/\d+/) → target; value.replace(/\d+/, '{n}') → template. Applied to the picked (locale-resolved) value string. -->
<!-- Value span to enhance is About.astro lines 45-47 (the right-aligned <span> rendering pick(f.value, locale)). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: About.astro count-up retrofit (surgical)</name>
  <files>src/components/astro/About.astro</files>
  <read_first>
    - src/components/astro/About.astro (FULL — 54 lines; only the value <span> at lines 45-47 and a script tag near </aside> change)
    - .planning/phases/24-hero/24-PATTERNS.md (About.astro MODIFIED section, lines 247-265 — surgical-edit constraints)
    - src/data/about.json (confirm only the exp fact value has a digit)
    - src/components/astro/Hero.astro (reuse the statParts helper shape for consistency — do NOT re-invent)
  </read_first>
  <action>Edit src/components/astro/About.astro additively only. In the frontmatter add a local statParts(value) helper (value.match(/\d+/) → { target: Number(match[0]), template: value.replace(/\d+/, '{n}') }; returns null-ish when no match) mirroring Hero.astro's. In the facts .map() value <span> (currently rendering {pick(f.value, locale)}), compute the picked value once, and when it contains a digit, add data-count-up={parts.target} and data-count-up-template={parts.template} to that span while keeping the settled picked string as the visible fallback text; when it has no digit, render the span exactly as today (no new attributes). Add exactly one <script src="../../scripts/count-up.js"></script> once, after the closing </aside> (or inside it), matching Hero.astro's tag verbatim. Do NOT restructure the .map() loop, the pick() helper, the JSON shape, or any other markup — attribute + script additions only.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds
    - `grep -c "scripts/count-up.js" src/components/astro/About.astro` returns 1
    - `grep -c "data-count-up" src/components/astro/About.astro` is ≥1 (the exp fact; template + value attrs may match twice — acceptable)
    - `grep -c "client:" src/components/astro/About.astro` returns 0 (still zero-island)
    - `git diff --stat src/components/astro/About.astro` shows a small additive diff (no wholesale rewrite)
  </acceptance_criteria>
  <done>About.astro adds count-up to the numeric quick-fact via the shared script; all other facts and structure unchanged; builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: About.test.ts count-up assertions + stale-comment fix</name>
  <files>src/components/astro/About.test.ts</files>
  <read_first>
    - src/components/astro/About.test.ts (FULL — 89 lines; extend, do not replace; note the stale D-04 comment at lines 43-44)
    - .planning/phases/24-hero/24-PATTERNS.md (About.test.ts MODIFIED section, lines 269-275)
  </read_first>
  <action>Edit src/components/astro/About.test.ts additively. Update/remove the stale comment at lines 43-44 that says "no count-up on mount per D-04" — it is outdated now that D-03 re-adds count-up (the value still renders as the settled static string in renderToString() output since no client JS runs during Container API render, but the D-04-deferral reasoning is wrong). Keep the existing `expect(result).toMatch(/\d+\+ years/)` assertion (Container API output is still the settled string). ADD new assertions mirroring Hero.test.ts: the Experience fact value span carries data-count-up and data-count-up-template; a `<script src="../../scripts/count-up.js">` tag appears exactly once in the rendered output. Keep the `// @vitest-environment node` pragma at line 1.</action>
  <verify>
    <automated>npx vitest run src/components/astro/About.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/astro/About.test.ts` is GREEN
    - `grep -c "no count-up on mount per D-04" src/components/astro/About.test.ts` returns 0 (stale comment removed/corrected)
    - Suite asserts data-count-up attribute presence and the single count-up.js script tag
  </acceptance_criteria>
  <done>About.test.ts GREEN with count-up coverage added and the stale D-04 comment corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| about.json → static HTML | quick-facts values are build-time JSON constants; statParts operates on those strings only, never runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-01 | Tampering | About.astro count-up attrs | accept | data-count-up target derived from a build-time JSON digit via regex; consumed only by count-up.js textContent path (no innerHTML) |
| T-24-03-SC | Tampering | npm installs | accept | Zero packages installed this plan |
</threat_model>

<verification>
- `npm run build` succeeds
- `npx vitest run` GREEN (About.test.ts extended, no regressions across the suite)
</verification>

<success_criteria>
- About.astro shares the single count-up.js module (D-02/D-03), enhancing only the numeric fact
- Non-numeric facts unchanged; edit is surgical
- About.test.ts GREEN; stale D-04 comment corrected
</success_criteria>

<output>
Create `.planning/phases/24-hero/24-03-SUMMARY.md` when done
</output>
