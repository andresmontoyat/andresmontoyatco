---
phase: 24-hero
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - src/components/astro/Hero.astro
  - src/components/astro/Hero.test.ts
  - src/index.css
autonomous: true
requirements: [STATIC-02]

must_haves:
  truths:
    - "D-01: Hero's char-reveal H1 middle line renders via a pure CSS steps() + ch-unit width animation (.hero-reveal, --reveal-chars custom property), zero JS, gated to full-text-immediately under prefers-reduced-motion via the repo's global src/index.css override"
    - "D-05: the CV download control is a native <details>/<summary> (zero JS for open/close/keyboard-focus) with role=\"menu\"/role=\"menuitem\" DELIBERATELY dropped — native disclosure semantics only, two plain <a> download links"
    - "D-08: the hero <img> (me-800.webp + srcset) ports directly into Hero.astro as the server-rendered LCP element — the old #hero-static duplicate-DOM workaround is NOT reintroduced (BaseLayout only carries the preload hint)"
    - "D-02: Hero's 4 stat spans carry data-count-up/data-count-up-template attributes (from build-time statParts regex) with the settled value as zero-JS fallback text, and the page references src/scripts/count-up.js exactly once via <script src>"
    - "D-06: Hero's <details> carries class \"details-dismiss\" and the page references src/scripts/details-dismiss.js via <script src> for Escape + outside-click close"
    - "Hero renders correct bilingual copy on /en and /es from translations[locale].hero and .stats — no LanguageContext, no client:* directive anywhere"
  artifacts:
    - path: "src/components/astro/Hero.astro"
      provides: "Zero-island static Hero section (photo LCP, CSS typewriter, native <details> CV dropdown, count-up stats)"
      min_lines: 60
      contains: "details-dismiss"
    - path: "src/components/astro/Hero.test.ts"
      provides: "Astro Container API coverage ported from Hero.test.jsx + new count-up/script/dropped-role assertions"
    - path: "src/index.css"
      provides: ".hero-reveal keyframes + summary marker-removal rules"
      contains: "hero-typewriter"
  key_links:
    - from: "src/components/astro/Hero.astro"
      to: "src/scripts/count-up.js"
      via: "<script src=\"../../scripts/count-up.js\">"
      pattern: "scripts/count-up.js"
    - from: "src/components/astro/Hero.astro"
      to: "src/scripts/details-dismiss.js"
      via: "<script src=\"../../scripts/details-dismiss.js\">"
      pattern: "scripts/details-dismiss.js"
    - from: "src/components/astro/Hero.astro"
      to: "translations[locale].hero"
      via: "build-time frontmatter resolution"
      pattern: "translations\\[locale\\]"
---

<objective>
Port `src/components/Hero.jsx` to a zero-island `src/components/astro/Hero.astro`: markup/Tailwind near-verbatim, char-reveal → CSS `steps()` (D-01), CV dropdown → native `<details>` with dropped ARIA menu roles (D-05), stats → `data-count-up` spans wired to the shared script (D-02), hero photo direct with no `#hero-static` duplication (D-08). Add the required `.hero-reveal` + `<summary>` marker CSS to `src/index.css`. Author the Container API test.

Purpose: Hero is the LCP-critical, highest-value recruiter moment — it must paint instantly as static HTML with no hydration and no LCP-blocking JS.
Output: Hero.astro + Hero.test.ts + index.css additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-hero/24-RESEARCH.md
@.planning/phases/24-hero/24-PATTERNS.md
@.planning/phases/24-hero/24-CONTEXT.md

<interfaces>
<!-- Frontmatter idiom (exact analog: src/components/astro/Footer.astro lines 8-11): -->
<!--   import translations from '../../i18n/translations.js'; const { locale } = Astro.props; const t = translations[locale] ?? translations.en -->

<!-- translations[locale].hero keys (confirmed src/i18n/translations.js): status, h1a, h1b, h1c, lead, cta1, cv, cvEn, cvEs -->
<!-- translations[locale].stats keys: years, team, companies, industries -->
<!-- EN h1b = "Senior Backend" (14 chars) · ES h1b = "e Ingeniero" — charCount MUST be computed per-locale from t.hero.h1b.length -->
<!-- Stats source values (Hero.jsx lines 249-252): "18+", "45+", "15+", "8" -->

<!-- Container API test skeleton (exact analog: src/components/astro/Footer.test.ts lines 1-16) — MUST keep the `// @vitest-environment node` pragma as line 1: -->
<!--   import { experimental_AstroContainer as AstroContainer } from 'astro/container'; renderToString(Hero, { props: { locale } }) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hero.astro + index.css CSS additions</name>
  <files>src/components/astro/Hero.astro, src/index.css</files>
  <read_first>
    - src/components/Hero.jsx (FULL file — markup/Tailwind to port; note lines 43-80 Stat/useCountUp, 82-135 CvDownload, 137-257 the section JSX)
    - src/components/astro/Footer.astro (lines 1-12 — the translations[locale] frontmatter idiom, the correct structural analog since Hero has no JSON data file)
    - .planning/phases/24-hero/24-RESEARCH.md (Patterns 1-3 — verified CSS typewriter, count-up markup, native <details> markup + marker-removal CSS)
    - .planning/phases/24-hero/24-PATTERNS.md (Hero.astro section lines 21-106 — exact port map with line references)
    - src/index.css (lines 242-244 .font-pixel utility, lines 314-323 the global prefers-reduced-motion override — add new rules alongside these, do not duplicate the RM guard)
    - src/i18n/translations.js (lines 33-48 EN hero/stats, 86-101 ES hero/stats)
  </read_first>
  <behavior>
    - Rendering with locale="en" produces the H1 aria-label "Solutions Architect & Senior Backend Engineer." (& becomes &amp; in HTML output) and lead paragraph from t.hero.lead
    - Rendering with locale="es" produces the ES h1a/h1b/h1c and ES lead
    - The middle H1 line is wrapped in <span class="hero-reveal ..."> carrying style="--reveal-chars: N" where N = t.hero.h1b.length for that locale, with NO blinking-cursor element and NO JS
    - The CV control renders <details class="details-dismiss relative group"> with a <summary> and two <a download> links to /CarlosMontoya_CV_EN.pdf and /CarlosMontoya_CV_ES.pdf, and contains NO role="menu" or role="menuitem"
    - All 4 stat spans carry data-count-up (integer) + data-count-up-template, with the settled string (e.g. "18+") as inner text fallback
    - Exactly one <script src="../../scripts/count-up.js"> and one <script src="../../scripts/details-dismiss.js"> tag are present; neither uses is:inline
    - The hero <img src="/me-800.webp"> with srcSet exists inside the aria-hidden photo layer; NO element with id="hero-static" is present
  </behavior>
  <action>Create src/components/astro/Hero.astro. Frontmatter: import translations from '../../i18n/translations.js'; const { locale } = Astro.props; const t = translations[locale] ?? translations.en; const midLine = t.hero.h1b; const charCount = midLine.length; a local statParts(num) helper (num.match(/\d+/) → target Number + template num.replace(/\d+/, '{n}')) applied to '18+','45+','15+','8'. Port the section markup from Hero.jsx near-verbatim (className→class, style objects→style strings), keeping id="hero", the photo layer (D-08: direct <img> with srcset/sizes/objectPosition/hero-photo-filter, overlay div — NO #hero-static duplicate), status badge, the three-line H1 with the middle line replaced by <span class="hero-reveal inline-block overflow-hidden whitespace-nowrap align-bottom bg-brand-gradient bg-clip-text text-transparent" style={`--reveal-chars: ${charCount}`}>{midLine}</span> (D-01 — drop the useCharReveal cursor entirely), lead paragraph, CTA row (contact link + native <details> CV dropdown per 24-RESEARCH.md Pattern 3 with class "details-dismiss relative group", summary marker hidden via `list-none [&::-webkit-details-marker]:hidden`, chevron `group-open:rotate-180`, and role="menu"/role="menuitem" DROPPED per D-05 — flag this dropped-role choice in an Astro comment as deliberate, not oversight), LinkedIn/GitHub SVG links with their aria-labels, and the stats grid using <span data-count-up={parts.target} data-count-up-template={parts.template}> per stat (D-02). Add exactly one <script src="../../scripts/count-up.js"></script> and one <script src="../../scripts/details-dismiss.js"></script> (NOT is:inline — Pitfall 4 LCP). Then in src/index.css add near .font-pixel: `.hero-reveal { width: 0; vertical-align: bottom; }`, `@media (prefers-reduced-motion: no-preference) { .hero-reveal { animation: hero-typewriter 1.2s steps(var(--reveal-chars), end) 300ms forwards; } }`, `@keyframes hero-typewriter { from { width: 0; } to { width: calc(var(--reveal-chars) * 1ch); } }`, plus `summary { list-style: none; }` and `summary::-webkit-details-marker { display: none; }`. CSS steps() duration is Claude's discretion (approximate the old ~40ms/char cadence, not pixel-identical).</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds (the <script src> paths resolve; no missing-module error)
    - `grep -c "role=\"menu\"\|role=\"menuitem\"" src/components/astro/Hero.astro` returns 0 (D-05)
    - `grep -c "hero-static\|#hero-static" src/components/astro/Hero.astro` returns 0 (D-08)
    - `grep -c "client:" src/components/astro/Hero.astro` returns 0 (no island)
    - `grep -c "scripts/count-up.js" src/components/astro/Hero.astro` returns 1 AND `grep -c "scripts/details-dismiss.js" src/components/astro/Hero.astro` returns 1
    - `grep -c "is:inline" src/components/astro/Hero.astro` returns 0 (Pitfall 4 — neither of Hero's two <script> tags may use is:inline; both must stay Astro-bundled deferred module scripts)
    - `grep -v '^#' src/index.css | grep -c "hero-typewriter"` is ≥1 AND `grep -c "webkit-details-marker" src/index.css` is ≥1
    - `grep -c "data-count-up" src/components/astro/Hero.astro` is ≥4 (all four stats)
  </acceptance_criteria>
  <done>Hero.astro builds, renders bilingual copy, uses CSS typewriter + native <details> (roles dropped) + direct photo + count-up spans + both script tags; index.css carries the new rules.</done>
</task>

<task type="auto">
  <name>Task 2: Hero.test.ts (Container API)</name>
  <files>src/components/astro/Hero.test.ts</files>
  <read_first>
    - src/components/astro/Footer.test.ts (FULL — the Container API skeleton + `// @vitest-environment node` pragma to copy exactly)
    - src/components/astro/About.test.ts (lines 61-64 — the negative-assertion "no raw markup" style to mirror for the dropped-role guard)
    - src/components/Hero.test.jsx (RTL baseline — port each it-block to string-matching; see 24-PATTERNS.md Hero.test.ts section lines 110-137 for the exact assertion mapping)
    - .planning/phases/24-hero/24-PATTERNS.md (Hero.test.ts section, lines 110-137)
  </read_first>
  <action>Create src/components/astro/Hero.test.ts with `// @vitest-environment node` as line 1, importing experimental_AstroContainer and Hero.astro, with a renderHero(locale='en') helper. Port assertions from Hero.test.jsx adapted to renderToString() output: H1 aria-label EN (assert contains 'Solutions Architect &amp; Senior Backend Engineer.') and ES equivalent; lead paragraph EN + ES; primary CTA href="#contact"; the <details> element is present and BOTH /CarlosMontoya_CV_EN.pdf and /CarlosMontoya_CV_ES.pdf download links are in the rendered string (native <details> always includes panel markup); LinkedIn/GitHub aria-label + rel + target; no .docx links present. Add NEW assertions this phase requires: all 4 stat spans carry data-count-up + data-count-up-template; `<script src="../../scripts/count-up.js">` and `<script src="../../scripts/details-dismiss.js">` each appear exactly once; NO 'role="menu"' and NO 'role="menuitem"' present (D-05 regression guard); no 'hero-static' string present (D-08 regression guard); img src="/me-800.webp" present.</action>
  <verify>
    <automated>npx vitest run src/components/astro/Hero.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/astro/Hero.test.ts` is GREEN
    - Test file line 1 is `// @vitest-environment node`
    - Suite includes explicit negative assertions for role="menu"/role="menuitem" and hero-static
    - Suite asserts both CV PDF links and both script-src tags
  </acceptance_criteria>
  <done>Hero.test.ts GREEN with ported RTL coverage plus count-up/script/dropped-role/no-duplicate-photo guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time translations → static HTML | t.hero.*/t.stats.* resolved once at build; no runtime user input |
| static PDF asset links | /CarlosMontoya_CV_{EN,ES}.pdf are same-origin static assets from public/, unchanged by this migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Tampering | Hero.astro rendered markup | accept | Zero user input, zero innerHTML/dangerouslySetInnerHTML; all copy is build-time constants (24-RESEARCH.md V5 Input Validation: N/A) |
| T-24-02-02 | Information Disclosure | external LinkedIn/GitHub links | mitigate | Keep target="_blank" rel="noopener noreferrer" on both social anchors (ported verbatim from Hero.jsx) |
| T-24-02-03 | Denial of Service (perf) | added <script> tags near LCP image | mitigate | Both scripts use default Astro module processing (deferred, non-blocking) — NOT is:inline (Pitfall 4); LCP validated in Plan 24-04/24-05 |
| T-24-02-SC | Tampering | npm installs | accept | Zero packages installed this plan |
</threat_model>

<verification>
- `npm run build` succeeds
- `npx vitest run` GREEN (Hero.test.ts added, no regressions)
- Manual grep gates in acceptance_criteria all pass
</verification>

<success_criteria>
- Hero.astro is a zero-island static section (no client:*), rendering correct bilingual copy
- D-01 CSS typewriter, D-05 native <details> with dropped roles, D-08 direct photo (no duplication), D-02 count-up spans + shared script, D-06 details-dismiss wired — all present
- Hero.test.ts GREEN with full ported + new coverage
</success_criteria>

<output>
Create `.planning/phases/24-hero/24-02-SUMMARY.md` when done
</output>
