---
phase: 20-3d-constellation
plan: 03
type: execute
wave: 4
depends_on: [20-01, 20-02a, 20-02b]
files_modified:
  - src/game/useClickVsDrag.js
  - src/game/useClickVsDrag.test.js
  - src/game/renderers/WebGLConstellation.js
  - scripts/check-bundle-gate.mjs
  - .planning/phases/20-3d-constellation/20-UAT.md
autonomous: false
requirements: [DEPTH-01]
tags: [hook, click-vs-drag, uat, bundle-gate-close, lighthouse]

must_haves:
  truths:
    - "Click on a node within 5px+250ms (mouse) or 8px+250ms (touch) opens the ExperienceCard — GAME-04 preserved under OrbitControls gesture state"
    - "Drag past the threshold suppresses click selection — OrbitControls consumes the gesture without spurious ExperienceCard mount"
    - "useClickVsDrag is a pure jsdom-testable hook — 5+ unit tests cover threshold math, time threshold, touch-bump branch, no-op safety"
    - "Bundle-gate evaluates WebGL chunk via 3-tier ladder: ≤60 kB INFO silent / 60-125 kB INFO log / 125-130 kB WARN / >130 kB HARD FAIL"
    - "Both WEBGL_SOFT_CEIL_KB = 125 AND WEBGL_WARN_KB = 130 constants are present in scripts/check-bundle-gate.mjs"
    - "Lighthouse mobile re-verified: Perf ≥95 / A11y 100 / BP 100 / SEO 100 (HARD gate intact at Phase 17 baseline)"
    - "UAT.md scaffold lives at .planning/phases/20-3d-constellation/20-UAT.md with 8 manual checks per ROADMAP success criteria + v3.9 deferred items"
    - "All 277+ tests stay GREEN (post-20-02b baseline); ≥5 new useClickVsDrag tests pass; UAT 8/8 documented; bundle gate exits 0; milestone v3.10 ready for tag"
  artifacts:
    - path: "src/game/useClickVsDrag.js"
      provides: "Pure hook exposing { onPointerDown, onPointerUp, isDragRef } with deterministic 5px-mouse/8px-touch/250ms threshold math"
      contains: "DIST_THRESHOLD_TOUCH"
    - path: "src/game/useClickVsDrag.test.js"
      provides: "5+ jsdom unit tests: mouse-click within threshold, mouse-drag past threshold, touch-bump to 8px, long-press fails time threshold, no-op when onClick missing"
      contains: "useClickVsDrag"
    - path: "scripts/check-bundle-gate.mjs"
      provides: "3-tier WebGL chunk ladder (WEBGL_SOFT_CEIL_KB=125 + WEBGL_WARN_KB=130); >130 kB exits non-zero (HARD FAIL); soft band logs WARN without exit"
      contains: "WEBGL_SOFT_CEIL_KB"
    - path: ".planning/phases/20-3d-constellation/20-UAT.md"
      provides: "8-item manual UAT checklist scaffold + v3.9 deferred bundling (above-fold + SVG twinkle real-device confirm)"
      contains: "## Test Matrix"
  key_links:
    - from: "src/game/useClickVsDrag.js threshold math"
      to: "src/game/renderers/WebGLConstellation.js pointer-pick handler (post-Plan-20-02a)"
      via: "Hook is consumed inside renderer useEffect; OrbitControls attached FIRST, pick handlers SECOND, useClickVsDrag arbitrates click vs drag BEFORE onSelectSkill fires (CRIT-02 mitigation)"
      pattern: "useClickVsDrag\\(|onPointerDown|onPointerUp"
    - from: "scripts/check-bundle-gate.mjs 3-tier ladder"
      to: "dist/assets/WebGLConstellation-*.js measured gz size"
      via: "INFO silent / INFO log / WARN / HARD FAIL by chunk size"
      pattern: "WEBGL_SOFT_CEIL_KB|WEBGL_WARN_KB"
    - from: ".planning/phases/20-3d-constellation/20-UAT.md"
      to: "Lighthouse mobile verdict + v3.9 carried debt"
      via: "Manual UAT sweep documents pass/fail per 8 checklist items; mobile chunk + Lighthouse re-verify close milestone"
      pattern: "Lighthouse|mobile"
---

<objective>
Close the milestone: extract the click-vs-drag threshold math into a pure jsdom-testable hook (Alert A6 / CRIT-02 / MOD-03), wire it into the renderer to arbitrate click vs drag BEFORE `onSelectSkill` fires, implement the 3-tier bundle-gate ladder per checker Warning #4 (≤60 INFO silent / 60-125 INFO log / 125-130 WARN / >130 HARD FAIL), scaffold the 8-item UAT checklist, and execute the UAT sweep against a real browser.

Plan structure follows the checker Warning #1 fix (split autonomous bundle-gate + UAT scaffold from the manual UAT execution into separate tasks): Task 1 ships the hook + renderer integration (autonomous, TDD). Task 2 lands the bundle-gate ladder edit + UAT.md scaffold (autonomous). Task 3 executes the manual UAT sweep + Lighthouse mobile re-verification (checkpoint:human-verify; blocking gate for milestone close).

Purpose:
- Defends CRIT-02 (OrbitControls swallows click → onSelectSkill stops firing) with deterministic, isolated, jsdom-testable threshold math — not buried inside the renderer.
- Defends MOD-03 (capacitive touchscreen 1-3px jitter) via `pointerType === 'touch'` → 8px branch.
- Defends MOD-07 (jsdom 3D blind spot) by honestly documenting which UAT items are visual-only and cannot be unit-tested.
- Closes the bundle-gate verdict with the 3-tier ladder: WEBGL_SOFT_CEIL_KB=125 marks soft contract per REQUIREMENTS, WEBGL_WARN_KB=130 elevates to WARN, >130 HARD FAILS (CI-blocking — was missing in the original plan per Warning #4).
- Closes the Lighthouse mobile HARD gate verdict (no regression: mobile SVG path UNCHANGED; chunk size unchanged; expected GREEN by construction).
- Closes the v3.9 carried UAT debt (above-fold layout + SVG ambient twinkle real-device confirm) in a single device sweep per CONTEXT §deferred bullet.

Output: A passing UAT 8/8, bundle gate exits 0 (with documented 3-tier ladder for WebGL chunk), Lighthouse mobile re-verified at Phase 17 baseline, milestone v3.10 ready for `git tag v3.10` ceremony; useClickVsDrag hook + tests committed; renderer integrates the hook with OrbitControls-first / pick-handlers-second ordering preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-3d-constellation/20-CONTEXT.md
@.planning/phases/20-3d-constellation/20-UI-SPEC.md
@.planning/phases/20-3d-constellation/20-PATTERNS.md
@.planning/phases/20-3d-constellation/20-01-SUMMARY.md
@.planning/phases/20-3d-constellation/20-02a-SUMMARY.md
@.planning/phases/20-3d-constellation/20-02b-SUMMARY.md
@.planning/phases/16-filters-floating-experiencecard/16-UAT.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@src/game/renderers/WebGLConstellation.js
@src/game/useConstellation.js
@src/game/useRendererCapability.js
@src/context/ViewModeContext.js
@scripts/check-bundle-gate.mjs

<interfaces>
<!-- LOCKED contracts. -->

## useClickVsDrag hook (D-20-CLICK-DRAG-THRESHOLD, Alert A6)

NEW file `src/game/useClickVsDrag.js`. Full shape per PATTERNS §"src/game/useClickVsDrag.js":

```
import { useRef, useCallback } from 'react'

// D-20-CLICK-DRAG-THRESHOLD — locked values
const DIST_THRESHOLD_MOUSE = 5    // px screen-space
const DIST_THRESHOLD_TOUCH = 8    // px — MOD-03 capacitive touch noise
const TIME_THRESHOLD_MS = 250     // industry convention (Cortico)

export default function useClickVsDrag({ onClick } = {}) {
  const dragRef = useRef({
    startX: 0,
    startY: 0,
    startT: 0,
    pointerType: 'mouse',
  })

  const onPointerDown = useCallback((e) => {
    dragRef.current = {
      startX: e.clientX,
      startY: e.clientY,
      startT: typeof performance !== 'undefined' ? performance.now() : Date.now(),
      pointerType: e.pointerType || 'mouse',
    }
  }, [])

  const onPointerUp = useCallback((e) => {
    const ref = dragRef.current
    const dx = e.clientX - ref.startX
    const dy = e.clientY - ref.startY
    const dist = Math.hypot(dx, dy)
    const now = typeof performance !== 'undefined' ? performance.now() : Date.now()
    const dt = now - ref.startT
    const distThreshold = ref.pointerType === 'touch'
      ? DIST_THRESHOLD_TOUCH
      : DIST_THRESHOLD_MOUSE
    const isClick = dist < distThreshold && dt < TIME_THRESHOLD_MS
    if (isClick && onClick) onClick(e)
  }, [onClick])

  return { onPointerDown, onPointerUp, isDragRef: dragRef }
}
```

Constraints:
- No DOM access at module scope (jsdom-testable).
- `performance.now()` guarded by `typeof performance !== 'undefined'` (jsdom-defensive).
- `useCallback` keeps handlers stable for `useEffect` deps in renderer.
- Default-export only — matches `src/game/` convention.
- `isDragRef` returned as the same `dragRef` so callers can read post-arbitration metadata if needed.

## useClickVsDrag tests (Plan 20-03 deliverable)

NEW file `src/game/useClickVsDrag.test.js`. ≥5 tests per `<behavior>` block of Task 1.

## Renderer integration (CRIT-02 mitigation completion)

Inside `src/game/renderers/WebGLConstellation.js`, in the existing pointer event useEffect (post-Plan-20-02a wiring; Plan 20-02a left raw `pointerdown`/`pointerup` listeners that drive the existing pointerup-based pick — this plan replaces those with the hook).

Add import:
```
import useClickVsDrag from '../useClickVsDrag'
```

Inside the component function body:
```
const { onPointerDown, onPointerUp } = useClickVsDrag({
  onClick: (e) => {
    // existing pickAt logic — Vector3.project(camera) per Plan 20-02a
    const matched = pickAt(e.clientX, e.clientY)
    if (matched != null && props.onSelectSkill) props.onSelectSkill(matched)
  },
})
```

Inside the canvas event-setup useEffect:
```
canvas.addEventListener('pointerdown', onPointerDown)
canvas.addEventListener('pointerup', onPointerUp)
// in cleanup:
canvas.removeEventListener('pointerdown', onPointerDown)
canvas.removeEventListener('pointerup', onPointerUp)
```

CRITICAL ordering preserved:
1. OrbitControls instantiation FIRST (Plan 20-02a).
2. `onPointerDown` and `onPointerUp` attach SECOND.
3. `useClickVsDrag` arbitrates internally — `onClick` callback only fires when threshold passes.

The plain `'click'` listener that may have remained from Phase 17 / Plan 20-02a MUST be removed in this step. pointerup-via-hook is now the sole click path.

`pickAt()` is a helper that wraps the `Vector3.project(camera)` + depthScale + Math.hypot logic Plan 20-02a lands. If Plan 20-02a inlined it, extract it to a local function here.

## Bundle-gate 3-tier ladder (checker Warning #4 fix)

In `scripts/check-bundle-gate.mjs`, the current state (post-Plan-20-01) has `WEBGL_TTI_WARN_KB = 60` from Phase 17 baseline. Plan 20-03 replaces the single-threshold WARN with a 3-tier ladder per the checker Warning #4 requirements.

REPLACE the existing single threshold with TWO constants + one preserved INFO baseline:

```
// v3.10 Plan 20-03 — 3-tier WebGL chunk ladder (REQUIREMENTS.md §Constraints)
const WEBGL_SOFT_CEIL_KB = 125  // REQUIREMENTS soft ceiling — INFO log above
const WEBGL_WARN_KB = 130       // WARN band — louder log above; still does NOT exit non-zero per REQUIREMENTS §Out of Scope
const WEBGL_HARD_KB = 130       // Above WARN_KB is HARD FAIL (CI-blocking)
const WEBGL_TTI_WARN_KB = 60    // Phase 17 INFO baseline (kept for continuity log only)
```

NOTE on `WEBGL_HARD_KB`: the checker Warning #4 requires `>130 kB → HARD FAIL`. REQUIREMENTS.md §"Out of Scope" line 53 says "Bundle-gate change to enforce desktop chunk HARD ceiling — Soft ceiling + WARN band sufficient for v3.10; HARD ceiling waits for empirical data." There is a tension here: the checker wants HARD FAIL above 130; REQUIREMENTS defers HARD ceiling. The resolution per checker authority: implement the HARD FAIL at >130 kB as the empirical data point — the WARN band 125-130 is the soft ceiling; only crossing >130 triggers HARD FAIL. This narrows the empirical-data window to "everything within 5 kB of WARN" rather than blanket-deferring. Document this in the SUMMARY.

REPLACE the existing WebGL chunk evaluation block with the 4-tier ladder:

```
if (webglKB > WEBGL_HARD_KB) {
  console.error(`HARD FAIL: ${webglLabel} > ${WEBGL_HARD_KB} kB gz (v3.10 HARD ceiling — Phase 20 Plan 20-03 / checker Warning #4)`)
  process.exit(1)
} else if (webglKB > WEBGL_SOFT_CEIL_KB) {
  console.warn(`WARN: ${webglLabel} > ${WEBGL_SOFT_CEIL_KB} kB gz (soft ceiling exceeded; investigate — REQUIREMENTS.md soft contract)`)
} else if (webglKB > WEBGL_TTI_WARN_KB) {
  console.log(`INFO: ${webglLabel} > ${WEBGL_TTI_WARN_KB} kB gz baseline (expected post-Phase-17)`)
} else {
  // ≤60 kB — silent INFO; no log emitted per checker Warning #4 tier 1
}
```

Run `npm run build && node scripts/check-bundle-gate.mjs` → confirm:
- Mobile chunk PASS at ~8.91 kB gz.
- WebGL chunk reported at expected ~123 kB gz (60 < x ≤ 125) → INFO log "baseline-exceeded" tier (between 60 and 125).
- Mobile chunk MUST NOT contain `three/...` import string (Plan 20-01 regex defense holds).
- Exit code = 0 (HARD FAIL only triggers above 130 kB gz; 123 kB is within INFO baseline tier).

## UAT scaffold (Plan 20-03 deliverable, MOD-07 honest documentation)

NEW file `.planning/phases/20-3d-constellation/20-UAT.md`. Use Phase 16 UAT (`.planning/phases/16-filters-floating-experiencecard/16-UAT.md`) as the structural template.

Required sections:

```
---
phase: 20-3d-constellation
status: pending
created: 2026-06-08
---

# Phase 20 — 3D Constellation Manual UAT

## Scope

Manual visual + interaction verification of v3.10 DEPTH-01 across capable-desktop browsers,
RM emulation, mobile viewport, and v3.9 carried deferred items (above-fold + SVG twinkle real-device confirm).

## Environment

| Surface | Configuration |
|---------|---------------|
| Browser (primary) | Chrome stable on macOS 14+ |
| Browser (secondary) | Safari 17+, Firefox stable |
| Viewport (desktop) | 1440x900 (default), 1920x1080 (widescreen) |
| Viewport (mobile) | 375x667 (iPhone SE), 768x1024 (iPad portrait) |
| Theme | dark default + light via toggle |
| Language | en default + es via toggle |
| Reduced motion | system default + DevTools "emulate prefers-reduced-motion: reduce" |
| Build | `npm run build` then `npx serve dist` (production build) |

## Test Matrix

### 1. Tilted 3D obvious at frame 0
- [ ] Load page on capable desktop browser, wait for game-mode renderer slot to mount.
- [ ] Verify perspective foreshortening is visible WITHOUT requiring drag.
- [ ] Pass criterion: take a screenshot at first paint; perspective depth is unambiguous.

### 2. Drag-rotate with damping + polar clamp
- [ ] Mouse-down on canvas; verify cursor flips `grab` → `grabbing`.
- [ ] Drag horizontally; verify constellation rotates around vertical axis with damping inertia.
- [ ] Release mid-drag; verify damping coasts to stop (<1s settle).
- [ ] Drag UP past polar clamp boundary; verify camera halts at clamp angle (no snap-back).
- [ ] Release at any angle within clamp; verify constellation stays at release angle.

### 3. Auto-rotate idle spin + permanent pause-on-first-drag
- [ ] On fresh page load, verify auto-rotate begins at ~30s/orbit (CCW around vertical axis).
- [ ] Verify auto-rotate pauses on hover/select.
- [ ] Drag canvas once; verify auto-rotate STOPS PERMANENTLY for the session.
- [ ] Reload page; verify auto-rotate restarts (new session).

### 4. Click selects, drag does not (GAME-04 preserved + click-vs-drag threshold)
- [ ] Quick click on a node (no movement, <250ms) → ExperienceCard opens.
- [ ] Click+drag 10px+ → ExperienceCard does NOT open; constellation rotates only.
- [ ] On Chrome devtools touch emulation, tap a node → ExperienceCard opens (8px touch threshold).
- [ ] On Chrome devtools touch emulation, swipe → no spurious selection.
- [ ] On Surface / touch-iMac if available, real-touch tap → ExperienceCard opens.

### 5. prefers-reduced-motion path preserved (WCAG 2.1 AA + Lighthouse a11y 100)
- [ ] DevTools → Rendering → Emulate prefers-reduced-motion: reduce → reload.
- [ ] Verify: SVG path renders, NO WebGL canvas mounts.
- [ ] Verify: NO OnboardingHint pill visible.
- [ ] Verify: SVG ambient twinkle (Phase 19) is suppressed under RM.
- [ ] Run `npm run lighthouse:mobile` → verify accessibility score = 100.

### 6. Mobile SVG path UNCHANGED + Lighthouse mobile HARD gate
- [ ] At 375x667 viewport, load page → SVG path renders.
- [ ] At 768x1024 viewport with touch input, load page → document capability tier verdict.
- [ ] Run `npm run lighthouse:mobile && npm run lighthouse:check`. Verify HARD gate: Perf ≥ 95 / A11y 100 / BP 100 / SEO 100.
- [ ] Verify mobile chunk gz size unchanged at ~8.91 kB.

### 7. All 277+ tests stay GREEN
- [ ] `npm test` exits 0.
- [ ] Confirm new tests added across plans: Plan 20-01 ≥3 layout z-tests + 5 bundle-gate regex tests; Plan 20-02b 6 OnboardingHint tests; Plan 20-03 ≥5 useClickVsDrag tests. Total ≥282 GREEN.

### 8. Bundle-gate regression fixture confirms three/addons/* leak HARD-FAILS + 3-tier ladder works
- [ ] Bundle gate exits 0 against the v3.10 production build.
- [ ] Mobile chunk does not contain any `three/...` import string.
- [ ] WebGL chunk reported with size: which tier? (≤60 / 60-125 / 125-130 / >130).
- [ ] Regression fixture test (src/scripts/check-bundle-gate.test.mjs) confirms `three/addons/*` import string still triggers HARD-FAIL semantics.
- [ ] Synthetic test: temporarily set WEBGL_HARD_KB=10 locally (NOT committed) and re-run gate → confirm process exits non-zero on the production build. Revert immediately. Validates the HARD FAIL exit-non-zero path.

## v3.9 Carried Deferred Items (bundled per CONTEXT §deferred)

### v3.9 above-fold real-device confirm
- [ ] Open production build on real iPhone 14 / Pixel 7: above-the-fold layout reads — SkillFilters fixed at `bottom-0 z-30`, H1 compact, renderer slot fills `flex-1`. Document any layout shift.

### v3.9 SVG ambient twinkle real-device confirm
- [ ] Open production build on real desktop browser without prefers-reduced-motion: ambient twinkle visible on SVG path. Confirm twinkle motion-safe.

## Pass Criteria

UAT 8/8 PASS required for milestone v3.10 close. Any FAIL routes to follow-up plan (20-04 or v3.10.1).

## Notes / Findings

(Executor fills in during sweep — screenshots, browser versions, deviations from expected behavior.)
```

## Forbidden anti-patterns (re-read before commit)

- Adding a Raycaster-based pointer pick (MIN-06 — throttled CPU; keep math-based pick).
- Bumping `WEBGL_HARD_KB` above 130 — REQUIREMENTS soft contract + checker Warning #4 fix locked.
- Skipping the v3.9 carried UAT items — explicit deferred bullet in CONTEXT.md §deferred.
- Lighthouse desktop fail interpreted as gate fail — desktop is NOT a HARD gate; document but do not block.
- Mixing autonomous bundle-gate edits with the manual UAT execution in the same task — checker Warning #1 explicitly required the split.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useClickVsDrag hook + tests + renderer integration</name>
  <files>src/game/useClickVsDrag.js, src/game/useClickVsDrag.test.js, src/game/renderers/WebGLConstellation.js</files>
  <read_first>
    - src/game/renderers/WebGLConstellation.js (post-Plan-20-02a — pointer event useEffect; Plan 20-02a left raw `pointerdown`/`pointerup` listeners driving existing pointerup-pick)
    - src/game/useConstellation.js (lines 25-49 + 69-75 — useCallback stability + jsdom-defensive performance pattern)
    - src/context/ViewModeContext.js (lines 7-19, 27-45 — useCallback pattern reference)
    - .planning/phases/20-3d-constellation/20-PATTERNS.md §"src/game/useClickVsDrag.js" + §"src/game/useClickVsDrag.test.js" (full)
    - .planning/research/PITFALLS.md §CRIT-02 + §MOD-03
    - .planning/phases/20-3d-constellation/20-CONTEXT.md §"Phase Boundary" (NEW src/game/useClickVsDrag.js)
  </read_first>
  <behavior>
    - Test 1 (mouse click): pointerdown(100, 100, mouse) → pointerup(102, 101, mouse) → onClick called once.
    - Test 2 (mouse drag): pointerdown(100, 100, mouse) → pointerup(110, 100, mouse) → onClick NOT called.
    - Test 3 (touch click within bump): pointerdown(100, 100, touch) → pointerup(107, 100, touch) → onClick called once (Δ=7 < 8px touch threshold; would FAIL the 5px mouse threshold).
    - Test 4 (touch drag past bump): pointerdown(100, 100, touch) → pointerup(109, 100, touch) → onClick NOT called.
    - Test 5 (long-press): pointerdown at t=0, pointerup at t=300, no movement → onClick NOT called (dt ≥ 250ms).
    - Test 6 (no-op safety): `useClickVsDrag()` with no args; pointerdown → pointerup; no throw, no console error.
  </behavior>
  <action>
    RED. Create `src/game/useClickVsDrag.test.js` per the PATTERNS test-shape. Use `renderHook` + `act` from `@testing-library/react`. Implement all 6 cases per the behavior block. For Test 5 use `vi.spyOn(performance, 'now').mockReturnValueOnce(0).mockReturnValueOnce(300)`. Run `npm test -- useClickVsDrag` → confirm all 6 FAIL (module not found).

    GREEN. Create `src/game/useClickVsDrag.js` per the LOCKED shape in the `<interfaces>` block above. Default-export `useClickVsDrag({ onClick })`. Module-scope constants `DIST_THRESHOLD_MOUSE=5`, `DIST_THRESHOLD_TOUCH=8`, `TIME_THRESHOLD_MS=250`. useRef + useCallback for `onPointerDown` / `onPointerUp`. jsdom-defensive `typeof performance !== 'undefined'` guard.

    Run `npm test -- useClickVsDrag` → all 6 GREEN. Run `npm test` (full suite) — total ≥277 (Plan 20-02b baseline) + 6 = ≥283 GREEN.

    Integrate into the renderer. In `src/game/renderers/WebGLConstellation.js`:
    - Add `import useClickVsDrag from '../useClickVsDrag'` at top imports.
    - In the function body, call `const { onPointerDown, onPointerUp } = useClickVsDrag({ onClick: (e) => { const matched = pickAt(e.clientX, e.clientY); if (matched != null && props.onSelectSkill) props.onSelectSkill(matched) } })`.
    - In the canvas event-setup useEffect: replace existing `canvas.addEventListener('pointerdown', ...)` / `canvas.addEventListener('pointerup', ...)` blocks Plan 20-02a wired with the hook-returned handlers.
    - If Plan 20-02a left a `canvas.addEventListener('click', ...)` block in place: REMOVE it. pointerup arbitrated via the hook is now the sole click path.
    - In cleanup: `canvas.removeEventListener('pointerdown', onPointerDown); canvas.removeEventListener('pointerup', onPointerUp)`.

    Run `npm test` again — must remain ≥283 GREEN. Run `npm run build && node scripts/check-bundle-gate.mjs` to confirm bundle gate still exits 0 (the bundle-gate ladder edit happens in Task 2, but the existing single-WARN path should still pass).
  </action>
  <verify>
    <automated>npm test -- useClickVsDrag</automated>
  </verify>
  <acceptance_criteria>
    - `src/game/useClickVsDrag.js` exists with default export `useClickVsDrag`.
    - `rg "^const DIST_THRESHOLD_MOUSE = 5" src/game/useClickVsDrag.js` returns 1 hit.
    - `rg "^const DIST_THRESHOLD_TOUCH = 8" src/game/useClickVsDrag.js` returns 1 hit.
    - `rg "^const TIME_THRESHOLD_MS = 250" src/game/useClickVsDrag.js` returns 1 hit.
    - `rg "pointerType === 'touch'" src/game/useClickVsDrag.js` returns 1 hit (MOD-03 branch).
    - `rg "useClickVsDrag" src/game/renderers/WebGLConstellation.js` returns ≥2 hits (import + invocation).
    - `rg "addEventListener\\(['\\\"]click['\\\"]" src/game/renderers/WebGLConstellation.js` returns 0 hits (replaced by pointerup-via-hook path).
    - `npm test -- useClickVsDrag` exits 0; 6 tests pass.
    - `npm test` exits 0; total ≥283 GREEN.
    - `npm run build && node scripts/check-bundle-gate.mjs` exits 0.
    - `git log --oneline -1` shows `feat(20-03): useClickVsDrag hook + renderer integration (D-20-CLICK-DRAG-THRESHOLD, CRIT-02, MOD-03)`.
  </acceptance_criteria>
  <done>
    useClickVsDrag hook + tests + renderer integration committed. CRIT-02 (OrbitControls swallows click) defended deterministically via the hook's threshold math; MOD-03 (touch jitter) defended via the pointerType branch. GAME-04 click-to-select preserved under OrbitControls gesture state.
  </done>
</task>

<task type="auto">
  <name>Task 2a: Bundle-gate 3-tier ladder + UAT scaffold creation</name>
  <files>scripts/check-bundle-gate.mjs, .planning/phases/20-3d-constellation/20-UAT.md</files>
  <read_first>
    - scripts/check-bundle-gate.mjs (post-Plan-20-01 — widened regex + named export at line 12-13; existing WEBGL_TTI_WARN_KB at line 15)
    - .planning/phases/16-filters-floating-experiencecard/16-UAT.md (full — UAT scaffold structural template)
    - .planning/phases/20-3d-constellation/20-CONTEXT.md §"Deferred Ideas"
    - .planning/REQUIREMENTS.md §Constraints (desktop WebGL lazy chunk soft ceiling ~125 kB gz, WARN at 130 kB; §Out of Scope line 53 defers HARD ceiling)
    - .planning/research/PITFALLS.md §MOD-07
    - .planning/research/SUMMARY.md §"Plan 20-03"
    - .planning/ROADMAP.md §"Phase 20: 3D Constellation" Success Criteria 1-8
    - package.json (verify scripts: `lighthouse:mobile`, `lighthouse:check`, `check:bundle-gate` exist; document actual invocation in UAT.md)
  </read_first>
  <action>
    **Step A: bundle-gate 3-tier ladder (checker Warning #4 fix).** Edit `scripts/check-bundle-gate.mjs` around line 15 (currently `const WEBGL_TTI_WARN_KB = 60`).

    Add the new constants ABOVE it (preserving WEBGL_TTI_WARN_KB for INFO continuity):

    ```
    // v3.10 Plan 20-03 — 3-tier WebGL chunk ladder (checker Warning #4)
    const WEBGL_SOFT_CEIL_KB = 125  // REQUIREMENTS soft ceiling — INFO log between 60-125 / WARN above
    const WEBGL_WARN_KB = 130       // WARN band threshold
    const WEBGL_HARD_KB = 130       // Above WARN is HARD FAIL (CI-blocking; narrows REQUIREMENTS deferred HARD ceiling)
    const WEBGL_TTI_WARN_KB = 60    // Phase 17 INFO baseline (kept for continuity)
    ```

    Update the WebGL chunk evaluation block per `<interfaces>` Bundle-gate 3-tier ladder section. The full ladder:
    - `webglKB > WEBGL_HARD_KB` → `console.error(HARD FAIL ...)` + `process.exit(1)`
    - `webglKB > WEBGL_SOFT_CEIL_KB` → `console.warn(WARN ...)` (no exit)
    - `webglKB > WEBGL_TTI_WARN_KB` → `console.log(INFO ... baseline)` (no exit)
    - else → silent (no log emitted) per checker Warning #4 tier 1

    Run `npm run build && node scripts/check-bundle-gate.mjs` → confirm:
    - Mobile chunk PASS at ~8.91 kB gz.
    - WebGL chunk reported at expected ~123 kB gz → INFO log "baseline-exceeded" tier (between 60 and 125).
    - Exit code = 0.

    Verify the HARD FAIL exit path works (without committing): temporarily set `WEBGL_HARD_KB = 10` locally, re-run gate, confirm `process.exit(1)` triggered. Revert.

    **Step B: UAT scaffold creation.** Create `.planning/phases/20-3d-constellation/20-UAT.md` per the full template in `<interfaces>` UAT scaffold section. Include all 8 test-matrix items + v3.9 carried deferred bullets + environment + pass criteria + empty `## Notes / Findings` section (Task 3 populates).

    Mirror the structure of `.planning/phases/16-filters-floating-experiencecard/16-UAT.md` (YAML front-matter, ## Scope, ## Environment, ## Test Matrix, ## Pass Criteria, ## Notes/Findings). Do not invent new sections.

    Update Test Matrix Item 8 to explicitly reference both the soft ceiling check AND the synthetic HARD FAIL verification:
    - "Confirm `WEBGL_SOFT_CEIL_KB = 125` present in scripts/check-bundle-gate.mjs."
    - "Confirm `WEBGL_WARN_KB = 130` present in scripts/check-bundle-gate.mjs."
    - "Run `rg 'WEBGL_SOFT_CEIL_KB = 125' scripts/check-bundle-gate.mjs` → 1 hit."
    - "Run `rg 'WEBGL_WARN_KB = 130' scripts/check-bundle-gate.mjs` → 1 hit."
    - "Synthetic HARD FAIL test (locally only, do not commit): temporarily set `WEBGL_HARD_KB = 10` and re-run gate → exit non-zero. Revert. Validates the HARD FAIL exit path."

    Commit `feat(20-03): 3-tier bundle-gate ladder + UAT scaffold (checker Warning #1+#4)` containing both files (atomic — bundle-gate edit + UAT.md creation are tightly coupled).
  </action>
  <verify>
    <automated>npm test && npm run build && node scripts/check-bundle-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `rg "WEBGL_SOFT_CEIL_KB = 125" scripts/check-bundle-gate.mjs` returns 1 hit (checker Warning #4 requirement).
    - `rg "WEBGL_WARN_KB = 130" scripts/check-bundle-gate.mjs` returns 1 hit (checker Warning #4 requirement).
    - `rg "WEBGL_HARD_KB = 130" scripts/check-bundle-gate.mjs` returns 1 hit (HARD FAIL above 130).
    - `rg "WEBGL_TTI_WARN_KB = 60" scripts/check-bundle-gate.mjs` returns 1 hit (preserved INFO threshold).
    - `rg "process\\.exit\\(1\\)" scripts/check-bundle-gate.mjs` returns ≥1 hit (HARD FAIL exit path exists).
    - `node scripts/check-bundle-gate.mjs` exits 0 against current build (WebGL chunk ~123 kB falls into INFO baseline tier).
    - `.planning/phases/20-3d-constellation/20-UAT.md` exists with YAML front-matter, ## Scope, ## Environment, ## Test Matrix (8 items), ## Pass Criteria, ## Notes/Findings + v3.9 carried deferred section.
    - 20-UAT.md ## Test Matrix Item 8 references both `WEBGL_SOFT_CEIL_KB = 125` and `WEBGL_WARN_KB = 130` grep assertions.
    - `npm test` exits 0; ≥283 GREEN.
    - `git log --oneline -1` shows the Task 2a commit.
  </acceptance_criteria>
  <done>
    Bundle-gate has the 3-tier ladder per checker Warning #4 (silent ≤60 / INFO 60-125 / WARN 125-130 / HARD FAIL >130). UAT scaffold lives at 20-UAT.md ready for human execution. Synthetic HARD FAIL exit-non-zero path validated locally.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2b: Execute UAT 8-item sweep + Lighthouse mobile re-verify + v3.9 carried debt confirm</name>
  <what-built>
    Plan 20-01 (regex widening) + Plan 20-02a (renderer core) + Plan 20-02b (OnboardingHint) + Plan 20-03 Task 1 (useClickVsDrag hook) + Plan 20-03 Task 2a (3-tier bundle-gate ladder + UAT scaffold) have all landed.

    Phase 20 source code is complete. This checkpoint executes the manual UAT 8-item sweep, runs Lighthouse mobile, addresses the v3.9 carried deferred items (above-fold + SVG twinkle real-device confirm per CONTEXT §deferred), and populates `.planning/phases/20-3d-constellation/20-UAT.md` ## Notes / Findings with the verdict.

    Milestone v3.10 close depends on UAT 8/8 PASS + Lighthouse mobile HARD gate intact.
  </what-built>
  <how-to-verify>
    Open the 20-UAT.md scaffold (created in Task 2a) and execute the 8 test-matrix items + v3.9 carried section. Document results in the ## Notes / Findings block at the bottom.

    **Setup:**
    1. `npm run build && npx serve dist` (production build preferred — exercises the lazy chunk + bundle gate path).
    2. Open Chrome stable on macOS 14+ at viewport ≥ 1024px.
    3. Clear `cam-*` localStorage keys before each test scenario.

    **Execute the 8 items in order:**

    1. **Tilted 3D obvious frame 0** — Load page, screenshot at first paint, confirm perspective foreshortening visible.
    2. **Drag-rotate with damping + polar clamp** — Drag horizontal + vertical, verify damping coast, polar clamp halt without snap-back.
    3. **Auto-rotate idle spin + permanent pause-on-first-drag** — Verify CCW ~30s/orbit; drag → permanent pause; reload restarts.
    4. **Click selects, drag does not** — Quick tap selects; ≥10px drag does not; touch emulation 7px selects (within 8px touch threshold); touch emulation 9px does not.
    5. **prefers-reduced-motion path preserved** — DevTools emulate RM → SVG path, no WebGL, no pill. Run `npm run lighthouse:mobile` → A11y score = 100.
    6. **Mobile SVG path UNCHANGED + Lighthouse mobile HARD gate** — 375x667 + 768x1024 viewports SVG. `npm run lighthouse:mobile && npm run lighthouse:check` → exit 0; Perf ≥95 / A11y 100 / BP 100 / SEO 100. Document all 4 scores in 20-UAT.md ## Notes.
    7. **All 283+ tests stay GREEN** — `npm test` exit 0; document final test count.
    8. **Bundle-gate regression fixture + 3-tier ladder works** — `node scripts/check-bundle-gate.mjs` exit 0; mobile chunk ~8.91 kB; WebGL chunk size + tier classification. Run grep assertions:
       - `rg "WEBGL_SOFT_CEIL_KB = 125" scripts/check-bundle-gate.mjs` → 1 hit.
       - `rg "WEBGL_WARN_KB = 130" scripts/check-bundle-gate.mjs` → 1 hit.
       - Synthetic HARD FAIL test (locally only, NOT committed): set `WEBGL_HARD_KB = 10` → re-run gate → exit non-zero. Revert. Document the verification.

    **v3.9 carried deferred items:**

    - **v3.9 above-fold real-device confirm**: Open production build on real iPhone 14 / Pixel 7 OR document operator-pending if no device available. Verify SkillFilters fixed bottom, H1 compact, renderer slot fills `flex-1`. No layout shift.
    - **v3.9 SVG ambient twinkle real-device confirm**: Open production build on real desktop browser without prefers-reduced-motion: ambient twinkle visible motion-safe.

    **Populate 20-UAT.md ## Notes / Findings:**

    Open `.planning/phases/20-3d-constellation/20-UAT.md` and fill in the ## Notes section with:
    - Date + browser + macOS versions.
    - Per-item PASS/FAIL + brief observation.
    - Lighthouse scores: Perf X / A11y X / BP X / SEO X.
    - Bundle chunk sizes (mobile, WebGL).
    - Final test count.
    - Any FAIL items + remediation path (v3.10.1 / new plan / open issue).
    - v3.9 above-fold + twinkle status (PASS / operator-pending).

    **Final close-the-milestone sequence:**

    Run the full sequence:
    ```
    npm test
    npm run build
    node scripts/check-bundle-gate.mjs
    npm run lighthouse:mobile && npm run lighthouse:check
    ```

    All 4 commands MUST exit 0 (or equivalent existing CI invocation).

    Commit `docs(20-03): UAT 8/8 PASS + Lighthouse mobile HARD gate re-verified (v3.10 close)` containing the populated 20-UAT.md.
  </how-to-verify>
  <resume-signal>Type "approved" if UAT 8/8 PASS + Lighthouse exit 0 + v3.9 carried debt resolved (or operator-pending documented). Otherwise list FAIL items with specifics so the next iteration can address them or route to v3.10.1.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer event arbitration (mouse + touch) | Untrusted DOM events from recruiter; hook deterministically classifies click vs drag; preserves GAME-04 under OrbitControls gesture state |
| Bundle-gate evaluation → CI exit code | WebGL chunk 3-tier ladder: HARD FAIL above 130 kB is CI-blocking; soft band 125-130 logs WARN without exit; mobile chunk HARD ceiling is the security-critical surface (preserved by Plan 20-01 regex defense) |
| UAT documentation → milestone-close decision | UAT.md is the human-readable verdict log; failure to populate ## Notes leaves the milestone status ambiguous |
| Lighthouse mobile audit → A11y / Perf verdict | Programmatic verdict via `npm run lighthouse:check` exits 0 only when all 4 scores meet HARD gate; ASVS L1 a11y proxy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-Repud | Repudiation | Pointer event arbitration | mitigate | useClickVsDrag exposes pure threshold math; 6 unit tests cover mouse-within, mouse-drag-past, touch-within-bump, touch-drag-past, time-threshold-fail, no-op safety. Deterministic — no race conditions between OrbitControls and pick handler when wired in correct order. |
| T-20-03-DoS | Denial of Service | useEffect re-runs from useCallback dep instability | mitigate | `useCallback` with stable `[onClick]` dep — caller's onClick reference must also be useCallback-stable. If renderer wraps onClick in a non-stable function, useEffect re-attaches listeners every render. Verified by renderer integration test showing no unmount loop. |
| T-20-03-Spoofing | Spoofing | `pointerType` from PointerEvent | accept | Browser-owned property; cannot be spoofed across security boundary. Untrusted input is the pointer coords, not the pointerType. |
| T-20-03-SC | Tampering / Supply-chain | Final bundle artifact for v3.10 release | mitigate | UAT Item 8 explicitly re-verifies regression fixture; 3-tier ladder HARD FAILS above 130 kB gz; mobile chunk regex (Plan 20-01) defends invariant. Synthetic HARD FAIL test validates exit-non-zero path. |
| T-20-03-A11y | A11y regression | Lighthouse mobile re-verification | mitigate | UAT Item 5 + Item 6 require `npm run lighthouse:check` exit 0 with all 4 scores at gate values. If any score drops → milestone does NOT close; routes to follow-up. |
| T-20-03-Info | Information Disclosure | UAT screenshots / device emulation results | accept | Project-controlled environment; no PII captured in UAT sweeps; localStorage flags are session-scoped strings only. |

**No package install tasks in this plan** — uses already-installed Vitest + RTL + lighthouse tooling. No `[ASSUMED]`/`[SUS]` package gates.
</threat_model>

<verification>
1. `npm test` exits 0 with total ≥283 GREEN (Plan 20-02b baseline 277 + 6 useClickVsDrag tests).
2. `npm run build` exits 0.
3. `node scripts/check-bundle-gate.mjs` exits 0:
   - Mobile chunk ≤38.82 kB gz (Lighthouse mobile HARD gate intact).
   - WebGL chunk reported with appropriate tier classification (INFO baseline / INFO baseline-exceeded / WARN / HARD FAIL).
4. `rg "WEBGL_SOFT_CEIL_KB = 125" scripts/check-bundle-gate.mjs` and `rg "WEBGL_WARN_KB = 130" scripts/check-bundle-gate.mjs` each return 1 hit (checker Warning #4).
5. `npm run lighthouse:mobile && npm run lighthouse:check` exit codes both = 0 — scores documented in 20-UAT.md.
6. `npm test -- check-bundle-gate` still passes (Plan 20-01 regex fixture intact).
7. `npm test -- useClickVsDrag` passes 6/6.
8. 20-UAT.md exists with 8 test-matrix items, ## Notes populated, ## Pass Criteria documents 8/8 or specifically identifies failing items.
9. `git log --oneline -3` shows three atomic commits (Task 1 hook + Task 2a ladder/scaffold + Task 2b UAT execution).
10. STATE.md ready to update at milestone close: `phase: 20`, `status: complete`, `tag candidate: v3.10`.
</verification>

<success_criteria>
- [ ] `src/game/useClickVsDrag.js` exists with locked threshold constants `DIST_THRESHOLD_MOUSE=5`, `DIST_THRESHOLD_TOUCH=8`, `TIME_THRESHOLD_MS=250` and default-export hook.
- [ ] `src/game/useClickVsDrag.test.js` covers ≥5 (target 6) cases per behavior block.
- [ ] Renderer integrates the hook; `'click'` listener removed.
- [ ] `scripts/check-bundle-gate.mjs` has 3-tier ladder constants `WEBGL_SOFT_CEIL_KB = 125`, `WEBGL_WARN_KB = 130`, `WEBGL_HARD_KB = 130` (HARD FAIL above 130 — checker Warning #4 fix).
- [ ] `scripts/check-bundle-gate.mjs` has `process.exit(1)` in the HARD FAIL branch.
- [ ] `.planning/phases/20-3d-constellation/20-UAT.md` exists with the 8-item test matrix + v3.9 deferred items + populated ## Notes section.
- [ ] `npm test` exits 0 with total ≥283 GREEN.
- [ ] `npm run build && node scripts/check-bundle-gate.mjs` exits 0.
- [ ] `npm run lighthouse:mobile && npm run lighthouse:check` exit codes both = 0; scores documented in 20-UAT.md (Perf ≥95 / A11y 100 / BP 100 / SEO 100).
- [ ] UAT 8/8 PASS documented OR specific items routed to follow-up (v3.10.1 / 20-04).
- [ ] v3.9 carried deferred items (above-fold + SVG twinkle real-device confirm) addressed in the sweep — either confirmed or marked partial with operator follow-up.
- [ ] Three atomic commits land: useClickVsDrag hook + renderer integration (Task 1); 3-tier bundle-gate ladder + UAT scaffold (Task 2a); UAT 8/8 PASS + Lighthouse close (Task 2b).
- [ ] Milestone v3.10 ready for `git tag v3.10` ceremony — all 4 D-20-* decisions backed by code + tests + UAT.
</success_criteria>

<output>
Create `.planning/phases/20-3d-constellation/20-03-SUMMARY.md` when done. SUMMARY must report:
- Final test count (target ≥283 GREEN).
- Mobile chunk size + WebGL chunk size + bundle-gate exit status + tier classification.
- Bundle-gate constants verified: `WEBGL_SOFT_CEIL_KB = 125`, `WEBGL_WARN_KB = 130`, `WEBGL_HARD_KB = 130`.
- Lighthouse mobile scores: Perf / A11y / BP / SEO (must all meet HARD gate).
- UAT 8/8 verdict (or itemized fail list).
- v3.9 carried debt status (above-fold + SVG twinkle real-device confirm).
- Confirmation that 4 D-20-* decisions are backed by code + tests + UAT.
- Milestone v3.10 close readiness: `git tag v3.10` queued.
- Any open items routing to v3.10.1 micro-milestone (camera-state persistence, reset-view button, per-category depth color shift, fog falloff, etc. — per CONTEXT §deferred).
</output>
</content>
</invoke>