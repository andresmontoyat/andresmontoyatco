# Phase 26 · Plan 26-01 — Experience island: close test debt + document divergence

**Requirements:** ISLAND-03, STATIC-02 (Experience half — gate-deferred per D-26)
**Decision:** D-26-MEASURE-FIRST (see CONTEXT.md) — accept React island, no speculative `<details>` refactor.

## Goal

Close the untested-island gap and record the spec divergence, so Phase 27's Lighthouse
gate is the only remaining arbiter of STATIC-02's Experience half.

## Tasks

### T1 — Experience island characterization suite (`src/components/react/Experience.test.jsx`)

New RTL suite rendering `<Experience locale=… />` directly (props-only, no LanguageProvider).
`./CareerGame.jsx` mocked to a lightweight stub (its own suite covers the game island).
Assert the island contract:

- Renders all 11 visible entries; respects `visible !== false`.
- Bilingual: `locale="es"` renders Spanish label/hint/clear copy.
- Filter chip toggle → target card `data-dim="false"`, non-matching `data-dim="true"`, chip `aria-pressed="true"`.
- Live match count region (`role="status"`) shows count only when a filter is active.
- Clear button appears with an active filter and resets all dimming.
- Expand toggle → `aria-expanded` flips, bullets appear/disappear.
- Featured entries render `data-variant="featured"`, compact render `data-variant="compact"`.
- Active role (`— Present`) renders the Active badge.
- CareerGame entry mounted (stub marker present) — accessible timeline remains default.

**Gate:** `npx vitest run` GREEN, no regressions (185 baseline + new specs).

### T2 — Document divergence in REQUIREMENTS.md

- ISLAND-03 → `[x]` Done (client:visible island satisfies "not client:load").
- STATIC-02 → keep unchecked; append gate-deferred disposition note referencing D-26.

### T3 — Update ROADMAP.md + STATE.md

- ROADMAP Phase 26 checkbox `[x]` with a one-line D-26 note (island accepted, gate judges).
- STATE.md: Phase 26 complete, next = Phase 27; record D-26.

## Verification

- `npx vitest run` GREEN.
- `npm run build` succeeds (no import breakage).
- Divergence documented, not silent (CONTEXT + REQUIREMENTS + ROADMAP + STATE).

## Out of scope

Native `<details>` refactor (Phase 27 gate-conditional); Phase 27 Lighthouse/cleanup/merge.
