---
phase: 07-tailwind-css-var-refactor
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/index.css
  - tailwind.config.js
  - src/components/Hero.js
  - src/components/Experience.js
  - scripts/og-template.html
  - public/og-image.png
autonomous: false
requirements: [COLOR-01]
gap_closure: false
commit_strategy: atomic-per-task
task_count: 9

must_haves:
  truths:
    - "Dark-mode :root declares the new blue (#3B82F6) + emerald (#10B981) palette with full variant set"
    - "[data-theme=\"light\"] declares the WCAG-AA-darkened blue (#2563EB) + emerald (#047857) palette with full variant set"
    - "Gradients (hero-gradient, brand-gradient, card-gradient) use the new palette while preserving 135deg direction"
    - "boxShadow.brand + boxShadow.brand-lg use rgba(59,130,246,*) with current opacities preserved (0.35 / 0.55)"
    - "Hero.js inline shadow uses var(--color-brand) (theme-aware) instead of any hardcoded hex"
    - "Experience.js timeline-dot shadow uses var(--color-ink-950) + var(--color-brand-muted) (theme-aware) instead of hardcoded hex/rgba"
    - "scripts/og-template.html has zero references to #6C63FF or rgba(108,99,255,*); all 4 hex + 3 rgba refs swapped to blue-500 palette"
    - "public/og-image.png is regenerated with the new palette and committed"
    - "Final grep sweep finds zero #6C63FF or #FF6B6B references across src/ scripts/ tailwind.config.js (PNG binary excluded)"
  artifacts:
    - path: "src/index.css"
      provides: "Blue + emerald palette in both :root and [data-theme=\"light\"]"
      contains: "#3B82F6, #10B981, #2563EB, #047857"
    - path: "tailwind.config.js"
      provides: "Gradients + shadows for new palette"
      contains: "rgba(59,130,246, #3B82F6, #10B981"
    - path: "src/components/Hero.js"
      provides: "Theme-aware inline shadow"
      contains: "shadow-[0_0_12px_var(--color-brand)]"
    - path: "src/components/Experience.js"
      provides: "Theme-aware timeline-dot ring + halo"
      contains: "shadow-[0_0_0_3px_var(--color-ink-950),0_0_8px_var(--color-brand-muted)]"
    - path: "scripts/og-template.html"
      provides: "OG template using new brand hex + rgba"
      contains: "#3B82F6, rgba(59,130,246"
    - path: "public/og-image.png"
      provides: "Regenerated OG card with new palette"
  key_links:
    - from: "src/components/Hero.js shadow-[0_0_12px_var(--color-brand)]"
      to: "src/index.css --color-brand declaration"
      via: "CSS var consumption"
      pattern: "shadow-\\[0_0_12px_var\\(--color-brand\\)\\]"
    - from: "src/components/Experience.js shadow-[0_0_0_3px_var(--color-ink-950),0_0_8px_var(--color-brand-muted)]"
      to: "src/index.css --color-ink-950 + --color-brand-muted declarations"
      via: "CSS var consumption (theme-aware ring + halo)"
      pattern: "shadow-\\[0_0_0_3px_var\\(--color-ink-950\\),0_0_8px_var\\(--color-brand-muted\\)\\]"
    - from: "scripts/og-template.html"
      to: "public/og-image.png"
      via: "npm run og:gen (Playwright screenshot)"
      pattern: "og:gen"
---

<objective>
Swap brand identity from indigo + coral to blue-500 + emerald-500 across CSS-var tokens, Tailwind gradients/shadows, the Hero inline shadow, the Experience timeline-dot inline shadow, the OG card template, and the regenerated og-image.png.

Purpose: Deliver COLOR-01. Because Plan 07-01 already inverted the dependency direction (Tailwind reads from CSS vars; CSS var blocks own values), this is now a trivial hex-value swap — no component file changes are required beyond Hero.js + Experience.js (the two inline arbitrary-value shadows) and og-template.html (a static HTML file outside the Tailwind pipeline).

Output: New blue/emerald brand visible across the entire site in both dark and light themes, with WCAG-AA-compliant darker variants applied in light mode; new og-image.png surfaced on social shares.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-tailwind-css-var-refactor/07-CONTEXT.md
@.planning/phases/07-tailwind-css-var-refactor/07-01-SUMMARY.md

@src/index.css
@tailwind.config.js
@src/components/Hero.js
@src/components/Experience.js
@scripts/og-template.html

<interfaces>
<!-- Locked palette per CONTEXT decision-b -->

Dark-mode (:root) palette:
- --color-brand:        #3B82F6   (blue-500)
- --color-brand-light:  #60A5FA   (blue-400)
- --color-brand-dark:   #2563EB   (blue-600)
- --color-brand-muted:  rgba(59, 130, 246, 0.12)
- --color-accent:       #10B981   (emerald-500)
- --color-accent-light: #34D399   (emerald-400)
- --color-accent-dark:  #059669   (emerald-600)
- --color-accent-muted: rgba(16, 185, 129, 0.12)

Light-mode ([data-theme="light"]) palette (darkened per WCAG AA on white text/border surfaces):
- --color-brand:        #2563EB   (blue-600 — 4.97:1 on #FFFFFF)
- --color-brand-light:  #3B82F6   (blue-500)
- --color-brand-dark:   #1D4ED8   (blue-700)
- --color-brand-muted:  rgba(37, 99, 235, 0.12)
- --color-accent:       #047857   (emerald-700 — 5.05:1 on #FFFFFF)
- --color-accent-light: #10B981   (emerald-500)
- --color-accent-dark:  #065F46   (emerald-800)
- --color-accent-muted: rgba(4, 120, 87, 0.12)

Gradient specs (decision-e — keep 135deg direction + 2-stop structure):
- hero-gradient:  radial-gradient(ellipse 80% 50% at 50% -20%, rgba(59,130,246,0.15), transparent), linear-gradient(180deg, #12121F 0%, #0D0D1A 100%)
- brand-gradient: linear-gradient(135deg, #3B82F6 0%, #10B981 100%)
- card-gradient:  linear-gradient(145deg, rgba(59,130,246,0.08), rgba(16,185,129,0.04))

Shadow specs (decision-g — keep opacities 0.35 / 0.55):
- boxShadow.brand:    '0 20px 40px -20px rgba(59,130,246,0.35)'
- boxShadow.brand-lg: '0 25px 50px -20px rgba(59,130,246,0.55)'

Hero.js inline shadow (decision-c — theme-aware via var):
- shadow-[0_0_12px_var(--color-brand)]

Experience.js inline shadow (decision-c — theme-aware via vars; ring + halo both flip with theme):
- shadow-[0_0_0_3px_var(--color-ink-950),0_0_8px_var(--color-brand-muted)]

OG template refs to swap:
- 4 × #6C63FF → #3B82F6 (lines 72, 87, 92, 106)
- 3 × rgba(108,99,255,X) → rgba(59,130,246,X) preserving opacities:
  - line 58 grid: rgba(108,99,255,0.06) → rgba(59,130,246,0.06)
  - line 117 photo border/ring: rgba(108,99,255,0.4) → rgba(59,130,246,0.4)
  - line 118 photo box-shadow: rgba(108,99,255,0.2) → rgba(59,130,246,0.2)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap :root brand + accent hex values to dark-mode blue/emerald palette</name>
  <files>src/index.css</files>
  <action>
    In `src/index.css` `:root` block (already expanded in Plan 07-01), replace ONLY the 8 brand + accent hex/rgba values with the dark-mode palette from CONTEXT decision-b. Do NOT touch the ink scale, surface aliases, or text variables — those are theme-neutral or do not change in this milestone.

    Required new values in `:root`:
    - `--color-brand:        #3B82F6;`        (was #6C63FF)
    - `--color-brand-light:  #60A5FA;`        (was #8B85FF)
    - `--color-brand-dark:   #2563EB;`        (was #4A42E8)
    - `--color-brand-muted:  rgba(59, 130, 246, 0.12);`   (was rgba(108,99,255,0.15))
    - `--color-accent:        #10B981;`       (was #FF6B6B)
    - `--color-accent-light:  #34D399;`       (was #FF8E8E)
    - `--color-accent-dark:   #059669;`       (was #E64444)
    - `--color-accent-muted:  rgba(16, 185, 129, 0.12);`  (was rgba(255,107,107,0.15))

    Update inline comments to reflect the new palette (e.g., `/* blue-500 */`, `/* emerald-500 */`) so future readers immediately know which Tailwind reference color is in play.

    Also update `.bg-grid-subtle` rule in the `@layer utilities` block — it currently uses `rgba(108, 99, 255, 0.06)` (indigo). Swap to `rgba(59, 130, 246, 0.06)` (blue-500) so the dot-grid overlay carries the new brand.

    Conventional Commit message: `feat(07-02): swap :root brand+accent to blue-500/emerald-500 palette`
  </action>
  <verify>
    <automated>awk '/^\s*:root\s*\{/,/^\s*\}\s*$/' src/index.css > /tmp/root-block.txt && grep -q "color-brand:\s*#3B82F6" /tmp/root-block.txt && grep -q "color-brand-light:\s*#60A5FA" /tmp/root-block.txt && grep -q "color-brand-dark:\s*#2563EB" /tmp/root-block.txt && grep -q "color-brand-muted:\s*rgba(59,\s*130,\s*246" /tmp/root-block.txt && grep -q "color-accent:\s*#10B981" /tmp/root-block.txt && grep -q "color-accent-light:\s*#34D399" /tmp/root-block.txt && grep -q "color-accent-dark:\s*#059669" /tmp/root-block.txt && grep -q "color-accent-muted:\s*rgba(16,\s*185,\s*129" /tmp/root-block.txt && grep -q "rgba(59,\s*130,\s*246,\s*0.06)" src/index.css</automated>
  </verify>
  <done>
    `:root` brand + accent variables hold the locked dark-mode hex/rgba values. `.bg-grid-subtle` uses blue-500 rgba. No remaining indigo/coral hex in `:root` brand/accent blocks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Swap [data-theme="light"] brand + accent hex values to WCAG-AA-darkened light-mode palette</name>
  <files>src/index.css</files>
  <action>
    In `src/index.css` `[data-theme="light"]` block, replace the 8 brand + accent hex/rgba values with the light-mode palette from CONTEXT decision-b. Darker variants chosen to pass WCAG AA contrast on near-white surfaces.

    Required new values in `[data-theme="light"]`:
    - `--color-brand:        #2563EB;`        (blue-600 — 4.97:1 on #FFFFFF)
    - `--color-brand-light:  #3B82F6;`        (blue-500)
    - `--color-brand-dark:   #1D4ED8;`        (blue-700)
    - `--color-brand-muted:  rgba(37, 99, 235, 0.12);`
    - `--color-accent:        #047857;`       (emerald-700 — 5.05:1 on #FFFFFF)
    - `--color-accent-light:  #10B981;`       (emerald-500)
    - `--color-accent-dark:   #065F46;`       (emerald-800)
    - `--color-accent-muted:  rgba(4, 120, 87, 0.12);`

    Update inline comments to carry the WCAG AA contrast ratios on the line where each "primary" variant is declared (e.g., `/* blue-600 — 4.97:1 on #FFFFFF, AA text */`). These annotations replace the old Phase 5 `#BB3A3A 4.5:1` comment.

    Conventional Commit message: `feat(07-02): swap [data-theme="light"] brand+accent to WCAG-AA blue/emerald palette`
  </action>
  <verify>
    <automated>awk '/\[data-theme="light"\]/,/^\s*\}\s*$/' src/index.css > /tmp/light-block.txt && grep -q "color-brand:\s*#2563EB" /tmp/light-block.txt && grep -q "color-brand-light:\s*#3B82F6" /tmp/light-block.txt && grep -q "color-brand-dark:\s*#1D4ED8" /tmp/light-block.txt && grep -q "color-brand-muted:\s*rgba(37,\s*99,\s*235" /tmp/light-block.txt && grep -q "color-accent:\s*#047857" /tmp/light-block.txt && grep -q "color-accent-light:\s*#10B981" /tmp/light-block.txt && grep -q "color-accent-dark:\s*#065F46" /tmp/light-block.txt && grep -q "color-accent-muted:\s*rgba(4,\s*120,\s*87" /tmp/light-block.txt</automated>
  </verify>
  <done>
    `[data-theme="light"]` brand + accent variables hold the locked WCAG-AA-darkened light-mode hex/rgba values. Inline comments annotate the contrast ratios for the primary variants.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update tailwind.config.js gradients (hero-gradient, brand-gradient, card-gradient) to blue/emerald palette</name>
  <files>tailwind.config.js</files>
  <action>
    In `theme.extend.backgroundImage`, swap the hex/rgba values to the new palette per CONTEXT decision-e. Keep direction `135deg` for `brand-gradient`, `145deg` for `card-gradient`, and the existing 2-stop structure across all three. Do NOT change the ink-stops in `hero-gradient` (the dark linear background is theme-neutral).

    Required new values:

    ```js
    backgroundImage: {
      'hero-gradient': 'radial-gradient(ellipse 80% 50% at 50% -20%, rgba(59,130,246,0.15), transparent), linear-gradient(180deg, #12121F 0%, #0D0D1A 100%)',
      'brand-gradient': 'linear-gradient(135deg, #3B82F6 0%, #10B981 100%)',
      'card-gradient': 'linear-gradient(145deg, rgba(59,130,246,0.08), rgba(16,185,129,0.04))',
    },
    ```

    Conventional Commit message: `feat(07-02): update gradients to blue-500/emerald-500 palette`
  </action>
  <verify>
    <automated>grep -q "'hero-gradient':.*rgba(59,130,246,0.15)" tailwind.config.js && grep -q "'brand-gradient':.*linear-gradient(135deg, #3B82F6 0%, #10B981 100%)" tailwind.config.js && grep -q "'card-gradient':.*linear-gradient(145deg, rgba(59,130,246,0.08), rgba(16,185,129,0.04))" tailwind.config.js && ! grep -E "(rgba\(108,\s*99,\s*255|#6C63FF|#FF6B6B|rgba\(255,\s*107,\s*107)" tailwind.config.js</automated>
  </verify>
  <done>
    All three gradients reference the new blue/emerald palette. No remaining indigo/coral hex/rgba inside `backgroundImage`. Directions (135deg, 145deg) and 2-stop structure preserved.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update tailwind.config.js boxShadow.brand + boxShadow.brand-lg rgba values to new brand RGB</name>
  <files>tailwind.config.js</files>
  <action>
    In `theme.extend.boxShadow`, swap the RGB tuple of `brand` and `brand-lg` to the new blue-500 base, keeping the offset/blur/spread and opacity values identical to v3.4/v3.5 (decision-g: 0.35 / 0.55 already tuned for dark surfaces, no re-tune needed).

    Required new values:

    ```js
    boxShadow: {
      brand:     '0 20px 40px -20px rgba(59,130,246,0.35)',
      'brand-lg': '0 25px 50px -20px rgba(59,130,246,0.55)',
      card:      '0 4px 24px rgba(13,13,26,0.6)',          // unchanged
    },
    ```

    Leave `boxShadow.card` unchanged (it uses ink-950 rgba, theme-neutral).

    Conventional Commit message: `feat(07-02): shadows.brand+brand-lg use rgba(59,130,246,*) with preserved opacities`
  </action>
  <verify>
    <automated>grep -q "brand:\s*'0 20px 40px -20px rgba(59,130,246,0.35)'" tailwind.config.js && grep -q "'brand-lg':\s*'0 25px 50px -20px rgba(59,130,246,0.55)'" tailwind.config.js && grep -q "card:\s*'0 4px 24px rgba(13,13,26,0.6)'" tailwind.config.js</automated>
  </verify>
  <done>
    `boxShadow.brand` and `boxShadow.brand-lg` use `rgba(59,130,246,*)` at the same opacities as v3.4/v3.5. `boxShadow.card` is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 5: Replace Hero.js inline shadow hardcoded hex with theme-aware var(--color-brand)</name>
  <files>src/components/Hero.js</files>
  <action>
    In `src/components/Hero.js` line 67 (the status-badge pulsing dot), replace the arbitrary Tailwind class:

    Before:
    ```jsx
    <span className="w-2 h-2 rounded-full bg-brand motion-safe:animate-pulse2 shadow-[0_0_12px_#6C63FF]" />
    ```

    After:
    ```jsx
    <span className="w-2 h-2 rounded-full bg-brand motion-safe:animate-pulse2 shadow-[0_0_12px_var(--color-brand)]" />
    ```

    Per CONTEXT decision-c: this is theme-aware automatically (the CSS var resolves to whichever value `:root` or `[data-theme="light"]` is currently providing), and avoids perpetuating a hardcoded hex inside a JSX component. No new `boxShadow` token is added to tailwind.config.js for this one-off.

    Do not change any other JSX. Do not touch the `bg-brand` class (that already flows through the Tailwind token → CSS var pipeline from Plan 07-01).

    Conventional Commit message: `feat(07-02): Hero pulsing-dot shadow uses var(--color-brand) — theme-aware`
  </action>
  <verify>
    <automated>grep -q 'shadow-\[0_0_12px_var(--color-brand)\]' src/components/Hero.js && ! grep -q '#6C63FF' src/components/Hero.js</automated>
  </verify>
  <done>
    Hero.js line 67 uses `shadow-[0_0_12px_var(--color-brand)]`. No remaining `#6C63FF` reference in the file.
  </done>
</task>

<task type="auto">
  <name>Task 5b: Replace Experience.js timeline-dot inline shadow with theme-aware vars (ring + halo)</name>
  <files>src/components/Experience.js</files>
  <action>
    In `src/components/Experience.js` line 56 (the timeline-dot ring + halo on each Experience entry), replace the arbitrary Tailwind class:

    Before:
    ```jsx
    shadow-[0_0_0_3px_#0D0D1A,0_0_8px_rgba(108,99,255,0.4)]
    ```

    After:
    ```jsx
    shadow-[0_0_0_3px_var(--color-ink-950),0_0_8px_var(--color-brand-muted)]
    ```

    Why both layers are tokenized (per CONTEXT decision-c):
    - The `3px` solid ring (`#0D0D1A` → `var(--color-ink-950)`) is the surface backdrop the dot sits against. In light mode `--color-ink-950` resolves to `#FFFFFF`, so the ring inverts correctly and the dot reads as a punched-out circle on the timeline rail in both themes.
    - The `8px` halo (`rgba(108,99,255,0.4)` → `var(--color-brand-muted)`) was the OLD indigo-tinted glow. By pointing it at `--color-brand-muted` (rgba(59,130,246,0.12) in dark / rgba(37,99,235,0.12) in light) it now picks up the new brand color AND flips between themes via the CSS-var pipeline established in Plan 07-01.

    Note on opacity: the original hardcoded value used 0.4 alpha; `--color-brand-muted` uses 0.12 alpha (the canonical token). This is a deliberate design alignment with the rest of the brand-muted system — the halo will be more subtle than the legacy version, consistent with how `--color-brand-muted` is used elsewhere. If during Task 9 visual review the halo reads as TOO subtle, the fix is to introduce a new `--color-brand-glow` token in src/index.css with higher alpha rather than re-hardcoding the rgba; record any such follow-up in SUMMARY.md as a Phase 10 polish item.

    Do not change any other JSX in Experience.js. Do not touch unrelated classes on the same element.

    Conventional Commit message: `feat(07-02): Experience timeline-dot shadow uses theme-aware CSS vars`
  </action>
  <verify>
    <automated>[ "$(grep -c 'shadow-\[0_0_0_3px_var(--color-ink-950),0_0_8px_var(--color-brand-muted)\]' src/components/Experience.js)" -eq 1 ] && ! grep -E "0D0D1A|rgba\(108" src/components/Experience.js</automated>
  </verify>
  <done>
    Experience timeline dot ring and halo are now theme-aware (will flip with theme toggle and pick up the new brand color via CSS vars). Zero hits for `#0D0D1A` or `rgba(108,*)` in Experience.js.
  </done>
</task>

<task type="auto">
  <name>Task 6: Swap all indigo refs in scripts/og-template.html (4 hex + 3 rgba) to blue-500 palette</name>
  <files>scripts/og-template.html</files>
  <action>
    In `scripts/og-template.html`, replace ALL indigo references with the new blue-500 palette. Two distinct patterns to swap:

    **A) Hex literals (4 occurrences):** `#6C63FF` → `#3B82F6`
    - Line 72 — `.logomark .slash { color: #6C63FF; }`
    - Line 87 — `.role .line2 { font-weight: 800; color: #6C63FF; display: block; }`
    - Line 92 — `background: #6C63FF;`
    - Line 106 — `color: #6C63FF;`

    **B) Rgba refs (3 occurrences) — preserve opacities exactly:**
    - Line 58 (grid overlay): `rgba(108,99,255,0.06)` → `rgba(59,130,246,0.06)`  ← preserve 0.06
    - Line 117 (photo border/ring): `rgba(108,99,255,0.4)` → `rgba(59,130,246,0.4)`  ← preserve 0.4
    - Line 118 (photo box-shadow): `rgba(108,99,255,0.2)` → `rgba(59,130,246,0.2)`  ← preserve 0.2

    All three opacities (0.06, 0.4, 0.2) are visually load-bearing — the grid is a subtle backdrop, the photo border is a prominent accent, and the box-shadow is a soft glow. Preserve them byte-for-byte; do NOT round, normalize, or "harmonize" them with the CSS-var token alphas. The OG template is a standalone HTML file that runs through Playwright, not through Tailwind — token alignment is not a concern here.

    A blanket find-and-replace for each pattern is acceptable, but verify the post-swap counts before committing.

    Do NOT touch any other styling in og-template.html (font-face declarations, layout CSS, the @font-face self-host blocks from DEBT-02). Only the brand color literals.

    Conventional Commit message: `chore(07-02): og-template indigo→blue-500 (4 hex + 3 rgba) for COLOR-01`
  </action>
  <verify>
    <automated>! grep -E "(#6C63FF|rgba\(108,?\s*99,?\s*255)" scripts/og-template.html && [ "$(grep -c '#3B82F6' scripts/og-template.html)" -ge 4 ] && [ "$(grep -c 'rgba(59,\s*130,\s*246' scripts/og-template.html)" -ge 3 ]</automated>
  </verify>
  <done>
    og-template.html: 4 hex literals + 3 rgba color refs all swapped to blue-500 palette; ready for og-image regeneration in next task.
  </done>
</task>

<task type="auto">
  <name>Task 7: Regenerate public/og-image.png via npm run og:gen (with Playwright fallback)</name>
  <files>public/og-image.png</files>
  <action>
    Regenerate the Open Graph card image so the new blue brand replaces the indigo on social previews.

    Steps:
    1. Run `npm run og:gen` from the repo root.
    2. If the run fails with a Playwright "browser not installed" / "executable doesn't exist" error, run `npx playwright install chromium` (or `npx playwright install` for full installation) and retry `npm run og:gen`. Note this in the commit body.
    3. Verify the output:
       - `public/og-image.png` exists and is non-zero size.
       - Size sanity check: between roughly 50 KB and 500 KB (typical PNG range for 1200×630 OG cards; the prior indigo-coral version is in this range). The CONTEXT brief specified 150–250 KB as expected — a value outside that range warrants a quick visual inspection but is not automatically a failure.
       - Visual sanity check: open the PNG in Preview / a browser; confirm the logomark slash, role line2, badge background, grid overlay, photo border/ring, and photo box-shadow all render in blue (`#3B82F6` / `rgba(59,130,246,*)`), not indigo.
    4. Commit the regenerated PNG. Use `git add -- public/og-image.png` (named-file form per security rule — do NOT use `git add .`).

    Conventional Commit message: `chore(07-02): regenerate og-image.png with blue-500 palette`
    (Include in the body any Playwright install step that was required, so a future re-run on a fresh machine knows the fallback.)
  </action>
  <verify>
    <automated>test -f public/og-image.png && [ "$(stat -f%z public/og-image.png 2>/dev/null || stat -c%s public/og-image.png)" -ge 10000 ] && file public/og-image.png | grep -q "PNG image data"</automated>
  </verify>
  <done>
    `public/og-image.png` is regenerated, present, non-empty, valid PNG; visual inspection confirms blue brand replaces indigo across hex AND rgba surfaces. File is staged for commit. If Playwright install was needed, the commit body documents it.
  </done>
</task>

<task type="auto">
  <name>Task 8: Final grep sweep — zero hits for #6C63FF or #FF6B6B across src/, scripts/, tailwind.config.js</name>
  <files>(no file modified — verification-only sweep)</files>
  <action>
    Run a repo-wide grep to confirm zero remaining references to the legacy indigo (`#6C63FF`) or coral (`#FF6B6B`) hex values in the code surface area for this milestone. The PNG binary is excluded (grep would not match its bytes anyway).

    Run from the repo root:
    ```bash
    grep -rn --color=never -E "(#6C63FF|#FF6B6B|rgba\(108,\s*99,\s*255|rgba\(255,\s*107,\s*107)" \
      src/ scripts/ tailwind.config.js 2>/dev/null
    ```

    Expected: zero output lines.

    If ANY hits surface:
    1. Inspect each hit. The CONTEXT risk register flagged this exact scenario ("some component uses an arbitrary Tailwind class with hardcoded hex e.g., `bg-[#6C63FF]` that grep missed").
    2. For each hit, either:
       - **Tokenize** — replace the hardcoded hex with a CSS-var arbitrary value (e.g., `bg-[var(--color-brand)]` or `bg-brand`) or a Tailwind token class. Commit as a fix-up.
       - **Explicit justification** — if the hex MUST stay hardcoded (e.g., a non-themeable third-party widget), add a one-line comment above the line documenting why, AND note it in the SUMMARY.md "Known Exceptions" section.
    3. Re-run grep until clean.

    If the sweep is clean on the first try, commit a note in SUMMARY.md confirming "Final grep sweep: 0 hits". No code commit is needed for the sweep itself (verification-only task).

    Also extend the sweep to capture `[data-theme="light"]`'s old `#BB3A3A` accent — confirm it is gone (replaced by `#047857`):
    ```bash
    grep -rn "#BB3A3A" src/ scripts/ tailwind.config.js 2>/dev/null
    ```
    Expected: zero output.

    Conventional Commit message: (no commit if sweep is clean — record in SUMMARY.md instead. If fix-ups were needed, batch them as `fix(07-02): tokenize remaining hardcoded brand hex refs found in grep sweep`.)
  </action>
  <verify>
    <automated>! grep -rn -E "(#6C63FF|#FF6B6B|#BB3A3A|rgba\(108,\s*99,\s*255|rgba\(255,\s*107,\s*107))" src/ scripts/ tailwind.config.js 2>/dev/null</automated>
  </verify>
  <done>
    Zero `#6C63FF`, `#FF6B6B`, `#BB3A3A` (Phase 5 transitional accent), `rgba(108,99,255,*)`, and `rgba(255,107,107,*)` matches across `src/`, `scripts/`, and `tailwind.config.js`. Any hits found by the sweep were either tokenized or explicitly justified in SUMMARY.md.
  </done>
</task>

<task type="auto">
  <name>Task 9: Optional fix-up — tokenize any additional hits surfaced by the grep sweep</name>
  <files>(determined dynamically by Task 8 grep output — typically zero files)</files>
  <action>
    This task ONLY runs if Task 8's grep sweep surfaced one or more hits. If Task 8 was clean (zero output), skip this task entirely and proceed to phase verification.

    For each hit discovered by Task 8:
    1. Read the surrounding context (5 lines before/after) to understand intent.
    2. Apply ONE of:
       - **Tokenize** (preferred): replace the hardcoded hex/rgba with an arbitrary CSS-var Tailwind class (e.g., `bg-[var(--color-brand)]`, `text-brand`, `shadow-[0_0_8px_var(--color-brand-muted)]`). Choose the token whose semantic meaning matches the intended use.
       - **Explicit justification** (rare): if the hex MUST stay hardcoded (third-party embed, OG template-style standalone file, etc.), prepend a `/* COLOR-01: justified hardcoded — <reason> */` comment AND record the exception in SUMMARY.md "Known Exceptions".
    3. Re-run the Task 8 sweep to confirm clean.

    Batch all fix-ups into a single commit, even if multiple files are touched.

    Conventional Commit message: `fix(07-02): tokenize remaining hardcoded brand refs found in grep sweep`
    (Include in the body the list of files + lines fixed, and the token chosen for each.)
  </action>
  <verify>
    <automated>! grep -rn -E "(#6C63FF|#FF6B6B|#BB3A3A|rgba\(108,\s*99,\s*255|rgba\(255,\s*107,\s*107))" src/ scripts/ tailwind.config.js 2>/dev/null</automated>
  </verify>
  <done>
    If Task 8 surfaced hits: all of them are either tokenized (via CSS-var Tailwind arbitrary classes) or explicitly justified with an inline comment + SUMMARY.md entry. Re-run sweep is clean. If Task 8 was clean: this task is a no-op and SUMMARY.md notes "Task 9 skipped — sweep was clean on first pass."
  </done>
</task>

</tasks>

<verification>
- `:root` brand + accent variables hold blue-500 + emerald-500 dark-mode hex/rgba per decision-b.
- `[data-theme="light"]` brand + accent variables hold WCAG-AA-darkened blue-600 + emerald-700 light-mode hex/rgba per decision-b.
- `tailwind.config.js`: hero-gradient, brand-gradient, card-gradient updated; boxShadow.brand + brand-lg use rgba(59,130,246,*).
- `src/components/Hero.js` line 67 uses `shadow-[0_0_12px_var(--color-brand)]` (theme-aware).
- `src/components/Experience.js` line 56 uses `shadow-[0_0_0_3px_var(--color-ink-950),0_0_8px_var(--color-brand-muted)]` (theme-aware ring + halo).
- `scripts/og-template.html` has zero `#6C63FF` AND zero `rgba(108,99,255,*)` references; at least 4 `#3B82F6` and at least 3 `rgba(59,130,246,*)` references.
- `public/og-image.png` regenerated, valid PNG, visually confirms blue brand across hex AND rgba surfaces.
- Repo-wide grep sweep for `#6C63FF | #FF6B6B | #BB3A3A | rgba(108,99,255,*) | rgba(255,107,107,*)` returns ZERO matches in `src/`, `scripts/`, `tailwind.config.js`.
- Manual visual check (no separate task): site reads as blue/emerald in both dark and light themes at 1440px, iPad, iPhone 14.
</verification>

<success_criteria>
The portfolio brand identity is now blue-500 (`#3B82F6`) + emerald-500 (`#10B981`) in dark mode, and blue-600 (`#2563EB`) + emerald-700 (`#047857`) in light mode (passing WCAG AA on text/border surfaces). The og-image.png on social shares carries the new palette. No legacy indigo or coral hex literals remain in the code surface area. The Hero pulsing-dot shadow AND the Experience timeline-dot ring+halo are both theme-aware. Phase 7 is complete and ready to hand off to Phase 8 (Hero photo integration).
</success_criteria>

<output>
After completion, create `.planning/phases/07-tailwind-css-var-refactor/07-02-SUMMARY.md` capturing:
- Files modified with line-count deltas
- The 7–8 atomic commits (7 mandatory feat/chore + optional fix-up if grep sweep surfaced hits) — Conventional Commits format
- Embedded thumbnail / link to the regenerated og-image.png for visual confirmation
- Confirmation that the grep sweep returned 0 hits — or, if any hits were found, the list of files and how each was resolved (tokenize / justify)
- Any Playwright install step required for og:gen on this machine
- Brief visual check note: "Site visually reads as blue/emerald in dark + light modes at 1440px, iPad, iPhone 14" — does NOT duplicate the Phase 10 sweep, just a sanity check.
- Explicit note on Experience timeline-dot halo subtlety: confirm whether the new `var(--color-brand-muted)` alpha (0.12) reads acceptable, or flag as Phase 10 polish item.
- Explicit statement: "Phase 7 (THEME-01 + COLOR-01) complete. Unblocks Phase 8 (HERO-01)."
</output>
</content>
</invoke>