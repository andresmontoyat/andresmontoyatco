---
phase: quick-260701-v6a-aseo-jsonld-person
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/seo/jsonld.test.js
autonomous: true
requirements: [ASEO-01]

must_haves:
  truths:
    - "Crawlers reading index.html <head> without executing JS find a valid schema.org Person JSON-LD block"
    - "The JSON-LD is valid, parseable JSON with @context https://schema.org and @type Person"
    - "Person name, contact points, sameAs profiles, and knowsAbout reflect canonical contact data"
    - "A vitest test proves the JSON-LD stays present, valid, and correctly typed"
  artifacts:
    - path: "index.html"
      provides: "Static ld+json Person script in <head> before the GA script"
      contains: "application/ld+json"
    - path: "src/seo/jsonld.test.js"
      provides: "Regression test that reads index.html and validates the Person schema"
      contains: "application/ld+json"
  key_links:
    - from: "src/seo/jsonld.test.js"
      to: "index.html"
      via: "fs.readFileSync at project root"
      pattern: "readFileSync"
---

<objective>
Add schema.org Person structured data to `index.html` as a static `<script type="application/ld+json">` block, so crawlers (Google rich results, LinkedIn, etc.) can read Carlos's identity without executing JavaScript. Mirrors the existing static OG/meta fallback pattern already in `<head>`.

Purpose: Improve SEO/knowledge-graph presence (requirement ASEO-01) with crawler-readable identity data.
Output: A validated JSON-LD Person block in `index.html` plus a vitest regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Canonical data for the Person schema — from src/data/contact.json + the meta description in index.html. Use exactly these values. -->

index.html <head> current static SEO block (lines 17-40): meta description, OG fallbacks,
twitter card, then Critical CSS <style>, then the Google Analytics <script> pair (lines 53-60).
The JSON-LD must go AFTER the SEO/OG meta and BEFORE the GA script (i.e. near line 41, before the Critical CSS/GA block, or right after the twitter meta at line 36). Keep it inside <head>.

Meta description string to reuse verbatim (index.html line 19):
"Carlos Andrés Montoya Tobón — Solutions Architect and Senior Backend Engineer with 18+ years of experience in Java, Spring, microservices and cloud."

Canonical Person fields:
- name: "Carlos Andrés Montoya Tobón"
- jobTitle: "Solutions Architect & Senior Backend Engineer"
- url: "https://andresmontoyat.co/"
- image: "https://andresmontoyat.co/og-image.png"
- email: "mailto:andresmontoyat@gmail.com"
- telephone: "+573244422196"
- address: { "@type": "PostalAddress", "addressLocality": "Medellín", "addressCountry": "CO" }
- sameAs: ["https://github.com/andresmontoyat", "https://www.linkedin.com/in/carlos-andres-montoya-tobon-8b508033"]
- knowsAbout: ["Java", "Spring", "Microservices", "Cloud", "Hexagonal Architecture"]
- description: the meta description string above
</interfaces>

<test_conventions>
<!-- From src/App.test.jsx + vite.config.js -->
- vitest with globals: true (describe/it/expect available without import, but existing tests still import them from 'vitest' — importing is fine)
- No semicolons, 2-space indent, LF endings (EditorConfig)
- Test env is jsdom; vitest runs from project root so process.cwd() resolves to the repo root
- coverage include glob is src/**/*.{js,jsx} — index.html is not covered, which is expected; the test reads it as a fixture only
</test_conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add static Person JSON-LD to index.html head</name>
  <files>index.html</files>
  <action>
Insert a single `<script type="application/ld+json">` block inside `<head>`, placed after the Twitter/OG meta tags and before the Critical CSS `<style>`/Google Analytics script (around line 41). Add a short HTML comment above it consistent with the existing "static fallbacks (crawlers read these...)" comment style.

The script content must be ONE JSON object (not an array), valid JSON, with `"@context": "https://schema.org"` and `"@type": "Person"`. Populate every field using the canonical values in the `<interfaces>` block exactly: name, jobTitle, url, image, email, telephone, address (nested PostalAddress with addressLocality "Medellín" and addressCountry "CO"), sameAs (github + linkedin), knowsAbout array, and description (the verbatim meta description string).

Static markup only — do NOT add any JS injection, build-time generator, or runtime override. Keep the é/ó accented characters as literal UTF-8 (file is UTF-8). Match the 2-space indentation of the surrounding `<head>` markup for the script tag; the JSON inside may be pretty-printed with 2-space indent.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const m=h.match(/<script type=\"application\/ld\+json\">([\s\S]*?)<\/script>/);if(!m)throw new Error('no ld+json block');const o=JSON.parse(m[1]);if(o['@type']!=='Person')throw new Error('not Person');if(o['@context']!=='https://schema.org')throw new Error('bad context');if(o.name!=='Carlos Andrés Montoya Tobón')throw new Error('bad name');if(!o.sameAs.some(s=>s.includes('github.com'))||!o.sameAs.some(s=>s.includes('linkedin.com')))throw new Error('bad sameAs');console.log('ok')"</automated>
  </verify>
  <done>index.html contains exactly one `application/ld+json` script in `<head>` (before the GA script); its content parses as JSON with @context "https://schema.org", @type "Person", correct name, PostalAddress in Medellín/CO, and sameAs containing both github and linkedin URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Add vitest regression test for the JSON-LD</name>
  <files>src/seo/jsonld.test.js</files>
  <action>
Create `src/seo/jsonld.test.js` (plain .js — no JSX). It reads `index.html` from the project root via `fs.readFileSync` using a path resolved from `process.cwd()` (vitest runs from repo root), extracts the `<script type="application/ld+json">…</script>` content with a regex, and `JSON.parse`s it.

Follow existing conventions: import `{ describe, it, expect }` from 'vitest', no semicolons, 2-space indent. Structure with a top-level `describe('index.html Person JSON-LD (ASEO-01)', ...)`.

Assert: (1) the ld+json block exists; (2) JSON.parse does not throw (valid JSON); (3) `@context === 'https://schema.org'`; (4) `@type === 'Person'`; (5) `name === 'Carlos Andrés Montoya Tobón'`; (6) `sameAs` is an array containing a github.com URL and a linkedin.com URL; (7) `address['@type'] === 'PostalAddress'` with `addressCountry === 'CO'`. Keep assertions minimal and behavior-focused (one behavior per `it`).
  </action>
  <verify>
    <automated>npx vitest run src/seo/jsonld.test.js</automated>
  </verify>
  <done>`npx vitest run src/seo/jsonld.test.js` passes; the test reads index.html from disk and validates the Person schema (@type, @context, name, sameAs github+linkedin, PostalAddress CO).</done>
</task>

</tasks>

<verification>
- `npx vitest run src/seo/jsonld.test.js` passes.
- Manual: paste index.html into Google Rich Results Test (https://search.google.com/test/rich-results) shows a valid Person item (optional, post-deploy).
- Full suite unaffected: `npx vitest run` stays green.
</verification>

<success_criteria>
- Static, JS-free Person JSON-LD present in index.html `<head>` before the GA script.
- Valid schema.org Person: @context, @type, name, jobTitle, url, image, email, telephone, PostalAddress (Medellín/CO), sameAs (github+linkedin), knowsAbout, description.
- Regression test in src/seo/jsonld.test.js proves validity and passes.
- No build-time generator or runtime JS injection introduced.
</success_criteria>

<output>
Create `.planning/quick/260701-v6a-aseo-jsonld-person/260701-v6a-SUMMARY.md` when done
</output>
