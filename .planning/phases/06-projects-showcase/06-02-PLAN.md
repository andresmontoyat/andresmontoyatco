---
phase: 06-projects-showcase
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/Nav.js
  - src/App.js
autonomous: true
requirements: [VIS-03]

must_haves:
  truths:
    - "The Projects nav link appears between Experience and Contact in DesktopNav and MobileMenu and uses t.nav.projects"
    - "Clicking the Projects nav link smooth-scrolls to #projects and the scroll-spy highlights it as active"
    - "App.js mounts Projects via React.lazy + Suspense between Experience and Contact"
    - "SkipLink, main#main, ThemeProvider, and LanguageProvider remain wired exactly as before"
    - "Production build emits a separate Projects-*.js lazy chunk under dist/assets/"
  artifacts:
    - path: "src/components/Nav.js"
      provides: "Updated SECTION_IDS array + Projects link in desktop and mobile menus"
      contains: "'projects'"
    - path: "src/App.js"
      provides: "Lazy-loaded Projects section composed between Experience and Contact"
      contains: "Projects"
  key_links:
    - from: "src/components/Nav.js"
      to: "src/_shared/useActiveSection (or equivalent SECTION_IDS consumer)"
      via: "SECTION_IDS includes 'projects'"
      pattern: "'projects'"
    - from: "src/App.js"
      to: "src/components/Projects.js"
      via: "lazy(() => import('./components/Projects'))"
      pattern: "import\\('./components/Projects'\\)"
    - from: "Nav.js DesktopNav + MobileMenu link items"
      to: "translations.js t.nav.projects"
      via: "t.nav.projects label"
      pattern: "t\\.nav\\.projects"
---

<objective>
Wire the Projects section produced by Plan 06-01 into the Nav scroll-spy and into the App composition behind a lazy boundary so it renders between Experience and Contact.

Purpose: Make the Projects section reachable, scroll-spy-highlighted, and code-split — the integration step that turns the Plan 06-01 building blocks into a navigable section.
Output: src/components/Nav.js (UPDATED — Projects link + SECTION_IDS), src/App.js (UPDATED — lazy import + Suspense boundary in section order).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-projects-showcase/06-CONTEXT.md
@.planning/phases/06-projects-showcase/06-01-PLAN.md
@src/components/Nav.js
@src/App.js

<interfaces>
<!-- Existing wiring this plan must preserve. Discovery embedded; do not re-research. -->

App.js composition (already established in Phase 4):
- ThemeProvider wraps LanguageProvider wraps the page tree.
- A SkipLink renders before the main landmark.
- `<main id="main">` contains the section composition in order: Nav, Hero, About, Skill, Experience, Contact, Footer.
- Experience, Contact, and Footer are already lazy-loaded with `React.lazy` + `Suspense` using a shared `SectionFallback` element.

Nav.js scroll-spy contract:
- Module-level `SECTION_IDS` array in document order (e.g. `['hero','about','skill','experience','contact']`) is consumed by the active-section hook to highlight the current link.
- DesktopNav links and MobileMenu links iterate the same source of truth (or matching parallel arrays); both render an anchor `href="#{id}"` with label from `t.nav.{id}`.
- Existing nav surface elements: Logomark, ProgressBar, LangPill, ThemeToggle, Hamburger — none of these may be removed or re-ordered by this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Projects to Nav (SECTION_IDS + DesktopNav + MobileMenu)</name>
  <files>src/components/Nav.js</files>
  <action>
Update `src/components/Nav.js` to expose the Projects section in the scroll-spy and in both the desktop and mobile menus.

Three edits, all minimal:

1. SECTION_IDS array — insert `'projects'` between `'experience'` and `'contact'`. The resulting order MUST be `['hero', 'about', 'skill', 'experience', 'projects', 'contact']` (or the existing equivalent — preserve whatever is already there and only insert the new id between experience and contact).

2. DesktopNav links list — add a Projects link between the Experience and Contact links. Use the same JSX shape as the surrounding links (anchor tag with `href="#projects"`, scroll-spy active class wiring via the existing pattern, label text `{t.nav.projects}`).

3. MobileMenu links list — add the same Projects link in the same position. Use `{t.nav.projects}` for the label and the same href/handler shape as the other mobile links (close-on-click behavior, focus-ring classes, etc.).

Strict preservation requirements:
- Do NOT remove or relocate Logomark, ProgressBar, LangPill, ThemeToggle, or the Hamburger button. The nav surface stays identical apart from the new link.
- Do NOT change the SECTION_IDS export visibility (keep it as the existing module-level constant).
- Do NOT alter any aria-* attributes, focus-trap logic, or motion-safe: animation classes used by the mobile menu.

Per D-12, D-13, D-14: insert section id, add t.nav.projects, preserve full Nav structure.
  </action>
  <verify>
    <automated>[ "$(grep -c "'projects'" src/components/Nav.js)" -ge 1 ] && [ "$(grep -c 't.nav.projects' src/components/Nav.js)" -ge 2 ] && [ "$(grep -c 'ThemeToggle\|LangPill' src/components/Nav.js)" -ge 6 ] && [ "$(grep -c 'href=\"#projects\"' src/components/Nav.js)" -ge 2 ]</automated>
  </verify>
  <done>SECTION_IDS includes 'projects' in correct position; DesktopNav and MobileMenu both render an anchor href="#projects" using t.nav.projects; ThemeToggle, LangPill, Logomark, ProgressBar, Hamburger preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Lazy-load Projects in App.js between Experience and Contact</name>
  <files>src/App.js</files>
  <action>
Update `src/App.js` to mount the new Projects section using the same lazy + Suspense pattern already used for Experience, Contact, and Footer.

Edits:

1. Imports — alongside the existing `lazy(() => import('./components/Experience'))` and similar lines, add:
```js
const Projects = lazy(() => import('./components/Projects'))
```
Place this declaration immediately after the Experience lazy import so the source order matches the on-page render order.

2. Section composition — inside `<main id="main">`, between the existing `<Suspense ...><Experience /></Suspense>` and `<Suspense ...><Contact /></Suspense>` blocks, add:
```jsx
<Suspense fallback={SectionFallback}>
  <Projects />
</Suspense>
```
Use the exact same `fallback` value (the shared SectionFallback element/variable) as the surrounding Suspense boundaries. If the existing code passes a JSX element directly (e.g. `fallback={<div className="..." />}`), mirror that shape.

Strict preservation requirements:
- Do NOT remove or alter SkipLink, ThemeProvider, LanguageProvider, `<main id="main">`, Nav, Hero, About, Skill, Experience, Contact, Footer wiring or order (apart from inserting Projects between Experience and Contact).
- Do NOT change the existing lazy/Suspense shape for Experience / Contact / Footer.

Per D-15, D-16: insert lazy Projects between Experience and Contact, preserve full App tree.
  </action>
  <verify>
    <automated>[ "$(grep -c 'Projects' src/App.js)" -ge 2 ] && [ "$(grep -c 'Suspense' src/App.js)" -ge 4 ] && [ "$(grep -c 'id=\"main\"' src/App.js)" -ge 1 ] && [ "$(grep -c 'SkipLink' src/App.js)" -ge 1 ] && [ "$(grep -c \"import('./components/Projects')\" src/App.js)" -ge 1 ]</automated>
  </verify>
  <done>App.js declares `const Projects = lazy(() => import('./components/Projects'))` and renders `<Suspense ...><Projects /></Suspense>` between Experience and Contact; SkipLink, main#main, and the existing Suspense boundaries for Experience/Contact/Footer remain intact.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0; production build emits a separate Projects-*.js chunk: `ls dist/assets/Projects-*.js` lists at least one file
- `npm run lint` exits with 0 errors
- DOM check (manual via dev server, but build succeeding is the gate): the Nav renders a Projects link between Experience and Contact in both desktop and mobile menus
- Anchor smooth-scroll: clicking the Projects nav link scrolls to `#projects`; scroll-spy highlights Projects when the section is in view
</verification>

<success_criteria>
- Nav.js: SECTION_IDS includes 'projects' between experience and contact; DesktopNav and MobileMenu both contain a `href="#projects"` link rendered with `t.nav.projects`; ThemeToggle / LangPill / Logomark / ProgressBar / Hamburger unchanged
- App.js: Projects is lazy-loaded and rendered inside its own Suspense boundary between Experience and Contact; SkipLink, main#main, and the ThemeProvider+LanguageProvider tree unchanged
- Vite build succeeds and emits a Projects lazy chunk; ESLint reports zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-projects-showcase/06-02-SUMMARY.md` documenting:
- Files modified (Nav.js, App.js)
- Exact placement of the new link / lazy section in source order
- Confirmation that the lazy chunk was emitted (filename under dist/assets/)
- Any deviations from the plan
</output>
