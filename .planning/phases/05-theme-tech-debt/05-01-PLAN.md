---
phase: 05-theme-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/translations.js
  - scripts/og-template.html
  - public/og-image.png
  - index.html
autonomous: true
requirements: [DEBT-01, DEBT-02, DEBT-03]

must_haves:
  truths:
    - "translations.js EN and ES branches no longer contain hero.cta2 or contact.loc keys"
    - "Hero CTA buttons still render with hardcoded 'Download CV (EN)' / 'Descargar CV (ES)' text"
    - "Contact section still renders Email, Phone, LinkedIn, GitHub rows correctly"
    - "scripts/og-template.html no longer makes a fonts.googleapis.com request"
    - "npm run og:gen still produces a public/og-image.png with the same Inter typography"
    - "Google Analytics <script> tag is inside <head> in index.html"
    - "Lighthouse SEO score remains 100 after the GA move"
  artifacts:
    - path: "src/i18n/translations.js"
      provides: "Translation tree with hero.cta2 and contact.loc removed"
      forbids: "hero.cta2 key, contact.loc key"
    - path: "scripts/og-template.html"
      provides: "OG template with self-hosted Inter @font-face declarations"
      forbids: "fonts.googleapis.com URL"
    - path: "public/og-image.png"
      provides: "Regenerated 1200x630 OG image identical to current"
    - path: "index.html"
      provides: "GA script tags inside <head>, body closing tag clean"
      forbids: "googletagmanager.com script tag outside </body>"
  key_links:
    - from: "src/components/Hero.js"
      to: "translations.js"
      via: "no longer reads t.hero.cta2 (hardcoded labels)"
      pattern: "Download CV \\(EN\\)|Descargar CV \\(ES\\)"
    - from: "scripts/og-template.html"
      to: "node_modules/@fontsource/inter"
      via: "@font-face src url() referencing local woff2 file"
      pattern: "@font-face"
    - from: "index.html <head>"
      to: "googletagmanager.com"
      via: "<script async src=...> tag inside head"
      pattern: "G-4TZJGR3MXR"
---

<objective>
Clear the three v3.4 tech-debt items so Phase 5 can ship clean: prune two orphaned translation keys, eliminate the Google Fonts CDN dependency from the OG image generator, and move the Google Analytics script into <head> for HTML spec compliance.

Purpose: These are independent, low-risk cleanups that close DEBT-01/02/03 without touching theme work. Running them in Wave 1 (parallel with theme tokens) keeps the critical path short.
Output: Three modified source files, one regenerated PNG, zero new runtime errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/i18n/translations.js
@src/components/Hero.js
@src/components/Contact.js
@scripts/og-template.html
@index.html
@package.json

<interfaces>
<!-- Confirmed during discovery — embed verbatim, no exploration needed -->

translations.js usage facts:
- t.hero.cta2 is referenced ONLY in old Hero.js (already replaced by hardcoded text at lines 123/130 in current Hero.js)
- t.contact.loc is referenced ONLY in old Contact.js (current Contact.js renders Email/Phone/LinkedIn/GitHub only — no loc)
- t.about.loc IS still used by About.js — DO NOT REMOVE THAT KEY (different namespace: about.loc, not contact.loc)

index.html GA block (current — lines 38-44, AFTER </body>):
```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-4TZJGR3MXR"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-4TZJGR3MXR');
</script>
```

scripts/og-template.html current font import (lines 6-8):
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
```

@fontsource/inter package is installed in node_modules. Locate the woff2 files via:
  ls node_modules/@fontsource/inter/files/inter-latin-*-normal.woff2

Generator command: `npm run og:gen` (defined in package.json scripts — uses Puppeteer to render og-template.html and screenshot to public/og-image.png).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove orphaned hero.cta2 and contact.loc translation keys</name>
  <files>src/i18n/translations.js</files>
  <read_first>
    - src/i18n/translations.js (full file — locate hero block and contact block in BOTH en and es branches)
    - src/components/Hero.js (confirm cta2 not referenced — should see hardcoded "Download CV (EN)" / "Descargar CV (ES)")
    - src/components/Contact.js (confirm contact.loc not referenced — should render Email/Phone/LinkedIn/GitHub rows only)
    - src/components/About.js (confirm t.about.loc IS referenced — must NOT touch this key)
  </read_first>
  <action>
    Remove four lines total from src/i18n/translations.js:
    1. In the `en.hero` block: delete the line containing `cta2: ...` (whatever its current value)
    2. In the `en.contact` block: delete the line containing `loc: ...`
    3. In the `es.hero` block: delete the line containing `cta2: ...`
    4. In the `es.contact` block: delete the line containing `loc: ...`

    Preserve trailing commas on the line ABOVE each removed line if needed for syntax.
    DO NOT touch `en.about.loc` or `es.about.loc` — these are still used by About.js (different namespace).
    DO NOT touch any other key in hero or contact blocks.

    Implements DEBT-01 per phase requirements.
  </action>
  <verify>
    <automated>
      grep -E "cta2|contact\.loc|^\s*loc:" src/i18n/translations.js | grep -v "about" ; \
      test $(grep -c "cta2" src/i18n/translations.js) -eq 0 && \
      npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cta2" src/i18n/translations.js` returns 0
    - `grep -n "loc:" src/i18n/translations.js` returns ONLY the 2 about.loc occurrences (en + es)
    - `npm run build` exits 0 with no warnings about undefined translation keys
    - Manual smoke: open dev server, hero CTA buttons show "Download CV (EN)" and "Descargar CV (ES)" in EN; "Descargar CV (EN)" and "Descargar CV (ES)" in ES (or whatever Hero.js hardcodes)
  </acceptance_criteria>
  <done>translations.js has 0 cta2 keys, 0 contact.loc keys, build passes, no console errors on Hero or Contact section render</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate scripts/og-template.html from Google Fonts CDN to self-hosted @fontsource/inter</name>
  <files>scripts/og-template.html, public/og-image.png</files>
  <read_first>
    - scripts/og-template.html (full file — current font loading at lines 6-8)
    - package.json (locate `og:gen` script command for verification step)
    - node_modules/@fontsource/inter/files/ directory listing (find available weight woff2 paths)
  </read_first>
  <action>
    Step A — Replace lines 6-8 of scripts/og-template.html.

    DELETE these three lines:
    ```html
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    ```

    INSERT in their place a `<style>` block with @font-face declarations referencing `node_modules/@fontsource/inter/files/inter-latin-{weight}-normal.woff2` paths via `file://` absolute URLs OR relative path `../node_modules/@fontsource/inter/files/...`.

    First inspect the available files:
    ```
    ls node_modules/@fontsource/inter/files/inter-latin-{400,500,600,700,800}-normal.woff2
    ```

    Use whichever path scheme Puppeteer can resolve (relative from scripts/ directory works for headless Chrome with `--allow-file-access-from-files` which is the default for puppeteer.launch). Use this exact template, substituting the verified woff2 paths:

    ```html
    <style>
      @font-face {
        font-family: 'Inter';
        font-style: normal;
        font-weight: 400;
        font-display: swap;
        src: url('../node_modules/@fontsource/inter/files/inter-latin-400-normal.woff2') format('woff2');
      }
      @font-face {
        font-family: 'Inter';
        font-style: normal;
        font-weight: 500;
        font-display: swap;
        src: url('../node_modules/@fontsource/inter/files/inter-latin-500-normal.woff2') format('woff2');
      }
      @font-face {
        font-family: 'Inter';
        font-style: normal;
        font-weight: 600;
        font-display: swap;
        src: url('../node_modules/@fontsource/inter/files/inter-latin-600-normal.woff2') format('woff2');
      }
      @font-face {
        font-family: 'Inter';
        font-style: normal;
        font-weight: 700;
        font-display: swap;
        src: url('../node_modules/@fontsource/inter/files/inter-latin-700-normal.woff2') format('woff2');
      }
      @font-face {
        font-family: 'Inter';
        font-style: normal;
        font-weight: 800;
        font-display: swap;
        src: url('../node_modules/@fontsource/inter/files/inter-latin-800-normal.woff2') format('woff2');
      }
    </style>
    ```

    Step B — Regenerate the OG image:
    ```
    npm run og:gen
    ```

    Step C — If npm run og:gen errors out due to file:// access, update scripts/og-gen.{js,mjs} (whichever generator exists) to pass `--allow-file-access-from-files` to puppeteer.launch args. Do not change generation logic otherwise.

    Step D — Verify visual identity: capture file size before/after with `wc -c public/og-image.png`. New size should be within ~10% of previous (pixel-identical not required, but typography must look the same — it's still Inter at the same weights).

    Implements DEBT-02 per phase requirements.
  </action>
  <verify>
    <automated>
      grep -c "fonts.googleapis.com" scripts/og-template.html ; \
      grep -c "@font-face" scripts/og-template.html ; \
      npm run og:gen 2>&1 | tail -10 ; \
      test -f public/og-image.png && wc -c public/og-image.png
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fonts.googleapis.com" scripts/og-template.html` returns 0
    - `grep -c "@font-face" scripts/og-template.html` returns >= 5 (one per weight)
    - `npm run og:gen` exits 0
    - `public/og-image.png` exists, size > 50KB and < 500KB
    - Visual inspection: open public/og-image.png — Inter typography, no font fallback to Times/serif
  </acceptance_criteria>
  <done>og-template.html has zero Google Fonts CDN references, og-image.png regenerated with self-hosted Inter, file size sane, npm run og:gen runs offline-clean</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Move Google Analytics script tags from end of body into head</name>
  <files>index.html</files>
  <read_first>
    - index.html (full file — locate <head> closing tag and the GA <script> block currently at lines 38-44 outside </body>)
  </read_first>
  <action>
    Step A — DELETE these exact lines from index.html (currently positioned AFTER `</body>` closing tag at lines 38-44):

    ```html
    <!-- Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-4TZJGR3MXR"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-4TZJGR3MXR');
    </script>
    ```

    Step B — INSERT the same block immediately BEFORE the `</head>` closing tag in index.html. Indent to match surrounding head content (typically 2 spaces). Preserve the exact GA tracking ID `G-4TZJGR3MXR` and the exact script content verbatim — only the position changes.

    Final index.html shape:
    ```html
    <head>
      ... existing meta/link tags ...
      <!-- Google Analytics -->
      <script async src="https://www.googletagmanager.com/gtag/js?id=G-4TZJGR3MXR"></script>
      <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', 'G-4TZJGR3MXR');
      </script>
    </head>
    <body>
      <div id="root"></div>
      <script type="module" src="/src/index.js"></script>
    </body>
    </html>
    ```

    NOTE: Do NOT touch the React entry `<script type="module">` (must stay at end of body — that's correct per Vite convention).

    Implements DEBT-03 per phase requirements.
  </action>
  <verify>
    <automated>
      awk '/<\/head>/{exit} {print}' index.html | grep -c "G-4TZJGR3MXR" ; \
      awk '/<\/body>/{found=1; next} found' index.html | grep -c "googletagmanager" ; \
      npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - First grep (content before </head>) returns >= 2 (script src + config call)
    - Second grep (content after </body>) returns 0 — no GA references after body close
    - `npm run build` exits 0 with no errors
    - Manual: open built index.html in dist/, confirm GA scripts in head, confirm React script tag still at end of body
  </acceptance_criteria>
  <done>GA script block is inside <head>, content after </body> contains no script tags, build passes, GA still loads in browser DevTools Network tab</done>
</task>

</tasks>

<verification>
  After all 3 tasks complete:
  1. `npm run build` exits 0
  2. `grep -rn "cta2\|contact\.loc" src/i18n/translations.js` returns nothing
  3. `grep -c "fonts.googleapis.com" scripts/og-template.html` returns 0
  4. `grep -c "G-4TZJGR3MXR" index.html` returns >= 2 (in head, not body)
  5. `npm run og:gen` exits 0 and public/og-image.png exists
  6. Manual dev-server smoke: Hero CTAs render, Contact rows render, no console errors
</verification>

<success_criteria>
  - DEBT-01 closed: 0 occurrences of `cta2` or `contact.loc` in translations.js
  - DEBT-02 closed: 0 occurrences of `fonts.googleapis.com` in scripts/og-template.html; og-image.png regenerated
  - DEBT-03 closed: GA tags in <head>, none after </body>
  - Build green, no runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-tech-debt/05-01-SUMMARY.md` documenting:
- Exact lines removed from translations.js (line numbers + key paths)
- Diff of scripts/og-template.html font block
- Before/after file size of public/og-image.png
- Verification: grep counts, build status, manual smoke result
</output>
