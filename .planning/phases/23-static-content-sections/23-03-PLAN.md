---
phase: 23-static-content-sections
plan: 03
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/components/astro/Projects.astro
  - src/components/astro/Projects.test.ts
  - src/components/astro/Claude.astro
  - src/components/astro/Claude.test.ts
  - src/components/Projects.test.jsx
  - src/components/Claude.test.jsx
autonomous: true
requirements: [STATIC-01, TEST-01]

must_haves:
  truths:
    - "D-01: Projects.astro imports src/data/projects.json and Claude.astro imports src/data/claude.json directly in frontmatter, each with the same {en,es} field shape and the same pick(field, lang) resolver — zero data-shape changes"
    - "D-06: Projects.test.ts and Claude.test.ts are Astro Container API tests with '// @vitest-environment node' as the literal first line, asserting bilingual (en+es) content and preserved data-* / ARIA markup, each porting every assertion of its Projects.test.jsx / Claude.test.jsx counterpart with no assertion-count regression"
    - "Projects and Claude render correct EN and ES content at build time consuming existing JSON unchanged (STATIC-01)"
    - "Projects preserves the featured-vs-rest split via data-featured=\"true\"/\"false\" attributes (Mr. Yoker featured first); Claude renders 4 value cards + 6 offerings"
    - "Projects and Claude ship zero client-side JavaScript — no client: directive, no React import, no hooks"
  artifacts:
    - path: "src/components/astro/Projects.astro"
      provides: "Zero-JS Projects section, build-time bilingual render from projects.json, featured/rest split"
      contains: "section id=\"projects\""
    - path: "src/components/astro/Claude.astro"
      provides: "Zero-JS Claude section, build-time bilingual render from claude.json, 4 values + 6 offerings"
      contains: "section id=\"claude-code\""
    - path: "src/components/astro/Projects.test.ts"
      provides: "Container API test for Projects (parity with Projects.test.jsx)"
      contains: "@vitest-environment node"
    - path: "src/components/astro/Claude.test.ts"
      provides: "Container API test for Claude (parity with Claude.test.jsx)"
      contains: "@vitest-environment node"
  key_links:
    - from: "src/components/astro/Projects.astro"
      to: "src/data/projects.json"
      via: "frontmatter import + featured filter"
      pattern: "import data from '../../data/projects.json'"
    - from: "src/components/astro/Claude.astro"
      to: "src/data/claude.json"
      via: "frontmatter import + values/offerings maps"
      pattern: "import data from '../../data/claude.json'"
---

<objective>
Port `src/components/Projects.jsx` (the largest of the five — two card variants + tech tags) and `src/components/Claude.jsx` (three sub-components) to zero-client-JS `.astro` components, each consuming its existing JSON unchanged (D-01), each with an Astro Container API test porting its RTL counterpart's assertions (D-06).

Purpose: complete the two content-heavy static sections. Runs in parallel with Plan 23-02 (no shared files); both depend on the Container API pattern validated in Plan 23-01.
Output: `Projects.astro`, `Projects.test.ts`, `Claude.astro`, `Claude.test.ts` committed; old `Projects.test.jsx` and `Claude.test.jsx` removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-static-content-sections/23-CONTEXT.md
@.planning/phases/23-static-content-sections/23-PATTERNS.md
@.planning/phases/23-static-content-sections/23-01-SUMMARY.md

<interfaces>
<!-- Container API: renderToString(Component, { props: { locale } }); assert on returned HTML string.
     Pattern proven in Plan 23-01 (src/components/astro/About.test.ts). -->

Projects props contract: receives `locale` via Astro.props.
Projects pick() (Projects.jsx lines 5-8) + filter split (lines 110-111):
  const featured = data.projects.filter((p) => p.featured)
  const rest = data.projects.filter((p) => !p.featured)
Projects preserves `data-featured="true"` (FeaturedProjectCard) / `data-featured="false"` (ProjectCard) attributes — Astro preserves data-* identically.
Conditional CTA block `{(project.liveUrl || project.githubUrl) && (...)}` ports directly (Astro supports the same `{cond && <jsx>}` short-circuit).

Claude props contract: receives `locale` via Astro.props.
Claude pick() (Claude.jsx lines 5-8); two `.map()` loops over data.values (4) and data.offerings (6).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Projects.astro and Projects.test.ts (parity), remove old RTL test</name>
  <files>src/components/astro/Projects.astro, src/components/astro/Projects.test.ts, src/components/Projects.test.jsx</files>
  <read_first>
    - src/components/Projects.jsx (source markup: TechTags, FeaturedProjectCard, ProjectCard, filter split)
    - src/components/Projects.test.jsx (8 it-blocks to port for parity)
    - src/components/astro/About.test.ts (Container API pattern from Plan 23-01)
    - src/data/projects.json (data shape + schema-sanity assertions — do NOT modify)
  </read_first>
  <behavior>
    - Renders `<section id="projects">` with label 'Projects'/'Proyectos', h2 'Selected work'/'Trabajo destacado', intro.
    - Renders one `<article>` per project (count === data.projects.length); each project's title (EN and ES) present; each emoji icon + tech chips present.
    - Featured project first as highlighted variant: exactly one `data-featured="true"` element, containing 'Mr. Yoker' and matching /Featured/i.
    - No live/github CTA text renders while project URLs are null (current data state).
    - Schema sanity: each project has string id, icon, bilingual title/desc, and a tech array.
  </behavior>
  <action>
    Create `src/components/astro/Projects.astro`: frontmatter `import data from '../../data/projects.json'`, `const { locale } = Astro.props`, copy `pick(field, lang)` verbatim, port lines 107-111 (`ctaLive`/`ctaGithub`/`featuredLabel`/`featured`/`rest`) using `locale`. Port the JSX return (Projects.jsx lines 112-135) to Astro template: `className`→`class`, drop all `key={...}`, inline the `FeaturedProjectCard` (lines 25-50), `ProjectCard` (lines 52-103), and `TechTags` (lines 10-23) markup directly into the `featured.map()` / `rest.map()` template blocks (Astro sub-components cannot be called as JS functions), preserve `data-featured="true"`/`data-featured="false"` attributes and the `{(project.liveUrl || project.githubUrl) && (...)}` conditional CTA short-circuit and `aria-hidden="true"` on decorative spans, keep every Tailwind class byte-identical. Then create `src/components/astro/Projects.test.ts` (`// @vitest-environment node` literal line 1, About.test.ts harness pattern), rendering with `{ props: { locale } }`, porting all 8 it-blocks from Projects.test.jsx: count `<article` occurrences === data.projects.length, assert `(result.match(/data-featured="true"/g) || []).length === 1`, assert featured block contains 'Mr. Yoker' and matches /Featured/i, and data-only schema sanity. Delete `src/components/Projects.test.jsx`.
  </action>
  <verify>
    <automated>npx vitest run src/components/astro/Projects.test.ts && test ! -f src/components/Projects.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/astro/Projects.test.ts` passes with >= 8 it-blocks GREEN.
    - Line 1 of Projects.test.ts is exactly `// @vitest-environment node`.
    - `rg -c "useState|useEffect|client:|set:html" src/components/astro/Projects.astro` returns 0.
    - `src/components/Projects.test.jsx` no longer exists.
  </acceptance_criteria>
  <done>Projects renders bilingual content from projects.json at build time, zero JS, featured/rest split preserved, with a parity Container API test (D-01, D-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Claude.astro and Claude.test.ts (parity), remove old RTL test</name>
  <files>src/components/astro/Claude.astro, src/components/astro/Claude.test.ts, src/components/Claude.test.jsx</files>
  <read_first>
    - src/components/Claude.jsx (source markup: PitchHero, ValueCard, OfferingCard, two .map() loops)
    - src/components/Claude.test.jsx (6 it-blocks to port for parity)
    - src/components/astro/About.test.ts (Container API pattern from Plan 23-01)
    - src/data/claude.json (data shape + schema-sanity assertions — do NOT modify)
  </read_first>
  <behavior>
    - Renders `<section id="claude-code">` with the pitch hero (label, split h2, subLead, primary CTA `<a href="#contact">`).
    - EN result matches /AI Engineering · For your team/, /delivered with AI discipline/, /Senior backend engineer/, /Let's talk about your project/.
    - Renders 4 value cards ('Delivery 3–5× faster','Architecture without shortcuts','Tests + observability built-in','Your team keeps the workflow') and 6 offerings ('Agentic delivery','RAG & MCP servers','LLM evals & guardrails','Greenfield builds','Legacy refactor','DevOps automation').
    - ES result matches /AI Engineering · Para tu equipo/, /Hablemos de tu proyecto/, contains 'Entrega 3–5× más rápida','Entrega agéntica'.
    - Schema sanity: values + offerings each have string id + bilingual title/desc.
  </behavior>
  <action>
    Create `src/components/astro/Claude.astro`: frontmatter `import data from '../../data/claude.json'`, `const { locale } = Astro.props`, copy `pick(field, lang)` verbatim. Port the JSX return (Claude.jsx lines 52-70) to Astro template: `className`→`class`, drop all `key={...}`, inline the `PitchHero` (lines 10-29), `ValueCard` (lines 31-39), and `OfferingCard` (lines 41-48) markup directly into the template / `data.values.map()` / `data.offerings.map()` blocks, keep the split `{pick(data.h2Part1, locale)}{' '}<span>{pick(data.h2Part2, locale)}</span>` heading structure and every Tailwind class byte-identical. Then create `src/components/astro/Claude.test.ts` (`// @vitest-environment node` literal line 1, About.test.ts harness pattern), rendering with `{ props: { locale } }`, porting all 6 it-blocks from Claude.test.jsx as `expect(result).toContain(...)` / `toMatch(...)` plus data-only schema sanity (assert data.values.length === 4 and data.offerings.length === 6). Delete `src/components/Claude.test.jsx`.
  </action>
  <verify>
    <automated>npx vitest run src/components/astro/Claude.test.ts && test ! -f src/components/Claude.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/astro/Claude.test.ts` passes with >= 6 it-blocks GREEN.
    - Line 1 of Claude.test.ts is exactly `// @vitest-environment node`.
    - `rg -c "useState|useEffect|client:|set:html" src/components/astro/Claude.astro` returns 0.
    - `src/components/Claude.test.jsx` no longer exists.
  </acceptance_criteria>
  <done>Claude renders bilingual content from claude.json at build time, zero JS, 4 values + 6 offerings, with a parity Container API test (D-01, D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time data → rendered HTML | `projects.json` / `claude.json` (committed, not user input) interpolated at build time; outbound `<a href>` from project liveUrl/githubUrl (currently null) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | Tampering (XSS) | Projects/Claude `{...}` interpolation | mitigate | Astro auto-escapes all `{expr}` output; no `set:html` (grep-gated in acceptance). Content from committed JSON, not runtime input. |
| T-23-03-02 | Information disclosure | Projects outbound CTA links (liveUrl/githubUrl) | mitigate | Existing `rel="noopener noreferrer"` on CTA anchors preserved verbatim in port; links are currently null (no CTA renders). |
| T-23-03-SC | Tampering (supply chain) | npm installs | accept | No package installs in this plan — no new dependency surface. |
</threat_model>

<verification>
- `npm run build` compiles Projects.astro + Claude.astro cleanly.
- `npx vitest run src/components/astro/Projects.test.ts` and `Claude.test.ts` GREEN.
- Full `npx vitest run` GREEN (old RTL test removals introduce no orphaned imports).
</verification>

<success_criteria>
- Projects and Claude render bilingual content at build time with zero client JS (STATIC-01).
- Both have parity Container API tests replacing their former RTL specs (TEST-01).
</success_criteria>

<output>
Create `.planning/phases/23-static-content-sections/23-03-SUMMARY.md` when done.
</output>
