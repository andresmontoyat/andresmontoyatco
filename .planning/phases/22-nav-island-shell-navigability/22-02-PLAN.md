---
phase: 22-nav-island-shell-navigability
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/test/setup.jsx
  - src/components/react/Nav.test.jsx
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "The Nav island's Vitest + React Testing Library suite passes: renders 6 nav links in EN and ES, LangPill anchors carry locale-correct hrefs, active-section highlight applies, theme toggle flips dataset.theme + writes cam-theme, and a LangPill click writes the cam-lang cookie"
    - "The full existing test suite stays green after adding a jsdom IntersectionObserver stub"
  artifacts:
    - path: "src/test/setup.jsx"
      provides: "No-op IntersectionObserver stub so useActiveSection (and future scroll-spy islands) don't throw under jsdom"
      contains: "IntersectionObserver"
    - path: "src/components/react/Nav.test.jsx"
      provides: "RTL coverage for the Nav island — bilingual content, LangPill hrefs, active highlight, theme toggle, cookie write"
      contains: "renderNav"
  key_links:
    - from: "src/components/react/Nav.test.jsx"
      to: "src/components/react/Nav.jsx"
      via: "render(<Nav locale hrefEn hrefEs />)"
      pattern: "render\\(<Nav"
    - from: "src/components/react/Nav.test.jsx"
      to: "src/hooks/useActiveSection"
      via: "vi.mock returns a fixed active id for deterministic highlight assertions"
      pattern: "vi\\.mock"
---

<objective>
Add a jsdom `IntersectionObserver` stub and a full React Testing Library suite for the Nav island, giving Phase 22's interactive island the coverage TEST-02 requires (behavior-equivalent assertions now targeting the island, wrapper changed from `<LanguageProvider>` to a direct `locale` prop).

Purpose: Lock the islands-architecture pattern behind an automated suite before content phases depend on Nav. Delivers TEST-02 (React island keeps Vitest + RTL coverage) and closes the PATTERNS.md open question about scroll-spy testability.
Output: `src/test/setup.jsx` (stub added), new `src/components/react/Nav.test.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-nav-island-shell-navigability/22-CONTEXT.md
@.planning/phases/22-nav-island-shell-navigability/22-PATTERNS.md

# The island under test (created in Plan 22-01)
@src/components/react/Nav.jsx

# RTL analogs — same render-and-assert / bilingual-coverage shape
@src/components/About.test.jsx
@src/components/Skill.test.jsx
@src/test/setup.jsx
@src/hooks/useActiveSection.js

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

Test command: `npm run test:run`  (vitest run; environment jsdom; setupFiles ./src/test/setup.jsx)
Single-file:  `npx vitest run src/components/react/Nav.test.jsx`
Baseline before this plan: 104/104 tests GREEN (Phase 21 close).

Nav island props (from Plan 22-01):
  Nav({ locale, hrefEn, hrefEs })   // render with e.g. locale='en' hrefEn='/en/' hrefEs='/es/'

Existing setup.jsx pattern to mirror (HTMLCanvasElement stub, guarded by typeof check):
  if (typeof HTMLCanvasElement !== 'undefined') { HTMLCanvasElement.prototype.getContext = () => null }

useActiveSection contract: default export, `useActiveSection(ids) -> activeId string`.
  It guards `typeof IntersectionObserver === 'undefined'` and returns '' when unavailable.

Bilingual nav strings to assert (from src/i18n/translations.js):
  EN unique: 'About', 'Experience', 'Projects', 'Contact'
  ES unique: 'Sobre mí', 'Experiencia', 'Proyectos', 'Contacto'
  Theme label EN: 'Switch to dark mode' / 'Switch to light mode'
  Lang group label EN: 'Language'  ES: 'Idioma'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a jsdom IntersectionObserver stub to test setup</name>
  <files>src/test/setup.jsx</files>
  <read_first>
    - src/test/setup.jsx (existing stubs: HTMLCanvasElement.getContext, the Node-22 localStorage bridge — mirror the guarded-stub style)
    - src/hooks/useActiveSection.js (calls `new IntersectionObserver(...)`; degrades when undefined)
    - .planning/phases/22-nav-island-shell-navigability/22-PATTERNS.md (No-Analog table: no IO stub exists today)
  </read_first>
  <action>
    Append a no-op `IntersectionObserver` stub to `src/test/setup.jsx`, mirroring the existing guarded `HTMLCanvasElement` stub style. Guard with `if (typeof globalThis.IntersectionObserver === 'undefined')`. Define a minimal class whose constructor accepts `(callback, options)` and stores nothing meaningful, with no-op `observe()`, `unobserve()`, `disconnect()`, and `takeRecords()` (returns `[]`) methods, then assign it to `globalThis.IntersectionObserver`. Add a short comment explaining WHY (jsdom lacks IntersectionObserver; `useActiveSection` in the Nav island instantiates it on mount; the stub prevents a throw and lets scroll-spy code mount cleanly — active-section highlight is asserted in Task 2 by mocking the hook, not by driving a real observer). Do not alter the existing canvas/localStorage stubs.
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "IntersectionObserver" src/test/setup.jsx` is `>= 2` (guard + assignment).
    - `grep -c "disconnect" src/test/setup.jsx` is `>= 1`.
    - `npm run test:run` reports the full suite GREEN (>= 104 tests passing, 0 failing) — the stub does not regress any existing test.
  </acceptance_criteria>
  <done>A no-op IntersectionObserver is available under jsdom; the pre-existing suite stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Nav island RTL suite</name>
  <files>src/components/react/Nav.test.jsx</files>
  <read_first>
    - src/components/About.test.jsx and src/components/Skill.test.jsx (describe/it structure + bilingual render-and-assert habit)
    - src/components/react/Nav.jsx (the island under test — props, LangPill anchors, ThemeToggle, MobileMenu)
    - src/i18n/translations.js (exact EN/ES nav strings to assert)
  </read_first>
  <action>
    Create `src/components/react/Nav.test.jsx`. Import `React`, `{ describe, it, expect, vi, beforeEach }` from 'vitest', `{ render, screen, fireEvent }` from '@testing-library/react', and `Nav from './Nav'`. Mock the scroll-spy hook for deterministic highlight assertions: `vi.mock('../../hooks/useActiveSection', () => ({ default: () => 'about' }))`. Add a `renderNav(locale = 'en')` helper: `render(<Nav locale={locale} hrefEn="/en/" hrefEs="/es/" />)` — NO `<LanguageProvider>` wrapper (props replace Context). In `beforeEach`, reset `document.documentElement.dataset.theme = 'dark'` and clear `document.cookie` for `cam-lang` where feasible.

    Cover these behaviors (one `it` each): (1) EN render shows the primary nav links — assert `screen.getAllByText('About')`, 'Experience', 'Projects', 'Contact' are present (use getAllByText where a label appears in both desktop nav and mobile menu). (2) ES render translates links — assert 'Sobre mí', 'Experiencia', 'Proyectos', 'Contacto'. (3) LangPill renders anchors with locale-correct hrefs — query the langGroup by `screen.getAllByRole('group', { name: 'Language' })`, assert an EN anchor and an ES anchor exist and the ES anchor's `getAttribute('href')` starts with `/es/`, with `aria-pressed="true"` on the anchor matching the active locale. (4) Active-section highlight — with the hook mocked to `'about'`, assert the About desktop link carries the active `border-brand` class (query by text, check `className` contains `border-brand`). (5) Theme toggle — find the toggle by its accessible name ('Switch to light mode' or 'Switch to dark mode'), `fireEvent.click`, assert `document.documentElement.dataset.theme` flipped from 'dark' to 'light'. (6) LangPill click persists locale — `fireEvent.click` the ES anchor, assert `document.cookie` contains `cam-lang=es` (guard: jsdom supports document.cookie; if the anchor navigation is a jsdom no-op that is fine — the onClick cookie write is what is asserted). Keep assertions behavior-focused; do not assert on internal implementation details beyond the public a11y contract (roles, names, hrefs, dataset).
  </action>
  <verify>
    <automated>npx vitest run src/components/react/Nav.test.jsx 2>&1 | tail -20 && npm run test:run 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "renderNav" src/components/react/Nav.test.jsx` is `>= 2` (helper defined + used).
    - `grep -c "vi.mock" src/components/react/Nav.test.jsx` is `>= 1` (useActiveSection mocked).
    - `grep -c "LanguageProvider" src/components/react/Nav.test.jsx` returns `0` (props, not Context).
    - Test file asserts both EN (`About`/`Contact`) and ES (`Sobre mí`/`Contacto`) strings, an ES href starting `/es/`, a `dataset.theme` flip, and a `cam-lang=es` cookie write.
    - `npx vitest run src/components/react/Nav.test.jsx` passes with `>= 6` tests, 0 failing.
    - `npm run test:run` reports the entire suite GREEN (0 failing).
  </acceptance_criteria>
  <done>Nav island has a passing RTL suite covering bilingual content, hash/locale switcher hrefs, active highlight, theme toggle, and cookie persistence; whole suite is green (TEST-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → globals | The IntersectionObserver stub is installed on `globalThis` for the jsdom test environment only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | Tampering | jsdom IntersectionObserver stub | accept | Test-only global, guarded behind `typeof ... === 'undefined'` and scoped to the vitest/jsdom setup file; never bundled into production output (setup file is excluded from the build). No runtime attack surface. |
| T-22-N/A | Tampering (supply chain) | npm/pip/cargo installs | n/a | No package installs — uses already-present vitest + @testing-library/react; no new dependencies. |
</threat_model>

<verification>
- `npm run test:run` reports the full suite GREEN with the new Nav suite included (>= 110 tests, 0 failing).
- Scroll-spy is testable: the IntersectionObserver stub prevents throws; active-highlight is asserted via a mocked `useActiveSection` return (the PATTERNS.md open question resolved in favor of stub + hook mock, not a real polyfill).
</verification>

<success_criteria>
- Nav island carries behavior-equivalent RTL coverage (bilingual content, LangPill hrefs + hash/locale switch, active highlight, theme toggle, cam-lang cookie) — TEST-02 satisfied.
- No existing test regresses; the whole suite is green.
</success_criteria>

<output>
Create `.planning/phases/22-nav-island-shell-navigability/22-02-SUMMARY.md` when done.
</output>
