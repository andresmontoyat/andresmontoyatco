---
phase: 09-ai-claude-code-section
plan: "01"
type: execute
wave: 1
depends_on: ["07-01", "07-02", "08-01"]
files_modified:
  - src/data/claude.js              # new
  - src/i18n/translations.js        # extend with t.claude.* + t.nav.claudeCode
  - src/components/Claude.js        # new
  - src/App.js                      # add lazy import + Suspense block
  - src/components/Nav.js           # add SECTION_IDS + Desktop/Mobile entries
autonomous: true
requirements: [AI-01, AI-01-CICD]
gap_closure: false
commit_strategy: atomic
task_count: 7
tags: [ai-section, claude-code, sales-pitch, bilingual, lazy-load, scroll-spy, devops]

must_haves:
  truths:
    - "Visitor scrolling past Projects encounters the AI / Claude Code section before reaching Contact"
    - "The section renders the V7 sales-pitch layout: pitch hero + 4 value cards + proof block (7 counters) + 5 service cards + 3 featured-app cards + stack-strip"
    - "Primary CTA scrolls to #contact, secondary CTA scrolls to #projects"
    - "Language toggle flips all 27 new t.claude.* keys live without remount"
    - "Nav (Desktop + Mobile) shows a 'Claude Code' link that scroll-spy highlights when section is in view"
    - "Build emits a separate Claude-*.js lazy chunk under dist/assets/"
    - "ci-templates app card surfaces 47 workflows + 15 starter templates + GitFlow + EC2/EC2-VPN/EKS deploys + SonarQube+OWASP+ArchUnit+Qodana gates (AI-01-CICD evidence)"
  artifacts:
    - path: "src/data/claude.js"
      provides: "VALUES (4), SERVICES (5), APPS (3), COUNTERS (7), STACK_CHIPS (17 strings)"
      contains: "export const VALUES,SERVICES,APPS,COUNTERS,STACK_CHIPS"
    - path: "src/i18n/translations.js"
      provides: "t.nav.claudeCode (EN+ES) and t.claude.* namespace (27 keys × 2 langs = 54 entries)"
      contains: "claude: { label, h2Part1, h2Part2, subLead, ctaPrimary, ctaSecondary, proofLabel, proofHeading, servicesLabel, counters, values, services }"
    - path: "src/components/Claude.js"
      provides: "Lazy-loadable Claude section default export + 6 internal sub-components"
      contains: "export default function Claude / function PitchHero / function ValueCard / function ProofBlock / function ServiceCard / function FeaturedAppCard / function StackStrip"
    - path: "src/App.js"
      provides: "React.lazy(Claude) + Suspense boundary between Projects and Contact"
      contains: "React.lazy(() => import('./components/Claude'))"
    - path: "src/components/Nav.js"
      provides: "claude-code in SECTION_IDS + DesktopNav + MobileMenu link arrays"
      contains: "'claude-code'"
  key_links:
    - from: "src/App.js"
      to: "src/components/Claude.js"
      via: "React.lazy + Suspense"
      pattern: "React\\.lazy\\(.*Claude"
    - from: "src/components/Claude.js"
      to: "src/data/claude.js"
      via: "named imports VALUES, SERVICES, APPS, COUNTERS, STACK_CHIPS"
      pattern: "from '../data/claude'"
    - from: "src/components/Claude.js"
      to: "src/i18n/LanguageContext"
      via: "useLanguage() -> { t, lang }"
      pattern: "useLanguage\\(\\)"
    - from: "src/components/Claude.js"
      to: "src/hooks/useInView"
      via: "default import for scroll-triggered entrance animations"
      pattern: "from '../hooks/useInView'"
    - from: "src/components/Nav.js"
      to: "section#claude-code in src/components/Claude.js"
      via: "scroll-spy via SECTION_IDS + href='#claude-code' anchors"
      pattern: "'claude-code'"
---

<objective>
Ship the V7 sales-pitch AI / Claude Code section between Projects and Contact, positioning Carlos as an AI-disciplined backend engineer-for-hire. Bilingual EN+ES, lazy-loaded chunk, scroll-spy in Desktop + Mobile nav. Surfaces `soldife/ci-templates` as concrete DevOps automation evidence (AI-01-CICD).

Purpose: Convert recruiter visits into engineering conversations. The hero photo (Phase 8) drew them in; this section closes the pitch and routes them to Contact.

Output: New data module, new component, extended translations, App.js wiring, Nav.js scroll-spy entries, verified lazy chunk in build manifest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-ai-claude-code-section/09-CONTEXT.md
@.planning/sketches/v3.6-variants.html
@.planning/phases/06-projects-showcase/06-01-SUMMARY.md
@.planning/phases/06-projects-showcase/06-02-SUMMARY.md
@src/data/projects.js
@src/components/Projects.js
@src/components/Nav.js
@src/App.js
@src/i18n/translations.js
@src/hooks/useInView.js

<interfaces>
<!-- Contracts the executor needs. All extracted from current source. -->

From src/hooks/useInView.js (the signature is `useInView(ref, options) -> boolean`, NOT a tuple):
```js
export default function useInView(ref, { threshold = 0.25 } = {}) { ... }
// Usage pattern (per Projects.js + Experience.js):
const ref = useRef(null)
const inView = useInView(ref, { threshold: 0.15 })
// className={`animate-on-scroll${inView ? ' is-visible' : ''}`}
```

From src/i18n/LanguageContext.js:
```js
export function useLanguage(): { lang: 'en' | 'es', setLang: (l) => void, t: TranslationTree }
```

From src/components/Projects.js (canonical structural template — Claude.js mirrors this pattern):
```js
import React, { useRef } from 'react'
import { useLanguage } from '../i18n/LanguageContext'
import useInView from '../hooks/useInView'
import SectionLabel from './_shared/SectionLabel'   // optional — Claude.js can inline if preferred
import PROJECTS from '../data/projects'

export default function Projects() { /* section with two ref+useInView pairs, header + grid */ }
function ProjectCard({ project, index, lang, t }) { /* uses style={{ transitionDelay: `${index * 100}ms` }} */ }
```

From src/data/projects.js (canonical bilingual data shape):
```js
{
  slug: string,
  title: { en: string, es: string },
  desc: { en: string, es: string },
  tech: string[],
  liveUrl: string | null,
  githubUrl: string | null,
  screenshot: string | null,
}
```

From src/components/Nav.js — three insertion points for 'claude-code':
- Line 7: `const SECTION_IDS = ['about', 'skills', 'experience', 'projects', 'contact']`
- DesktopNav links array (~line 63-69)
- MobileMenu links array (~line 172-178)

From src/App.js — Suspense block ordering inside `<main id="main">`:
- Experience (lazy) → Projects (lazy) → Contact (lazy). Insert Claude between Projects close and Contact open.

From src/i18n/translations.js — translations.js structure:
- Top-level keys: `en: { ... }` (line 2) and `es: { ... }` (line 136).
- Each language block has a `nav: { ... }` sub-block and feature sub-blocks (about, skills, experience, projects, contact, footer).
- Insert new `claudeCode: 'Claude Code'` in BOTH `nav` blocks.
- Insert new `claude: { ... }` namespace after `projects: { ... }` in BOTH language blocks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task A1: Create src/data/claude.js bilingual data module</name>
  <files>src/data/claude.js</files>
  <action>
    Create the file with FIVE named exports per CONTEXT decision-b. Mirror `src/data/projects.js` data shape. NO default export.

    1) `export const VALUES` — 4 entries. Each: `{ id: '01' | '02' | '03' | '04', key: 'velocity' | 'discipline' | 'quality' | 'transfer', desc: { en, es } }`. The `desc[en]` and `desc[es]` are longform 1–2 sentences each (NOT short labels — those live in translations.js).
       - velocity (id '01'): EN = "Agentic workflows (discuss → plan → execute → verify) ship in hours what takes days traditionally, while preserving atomic commits and real tests."; ES = "Workflows agénticos (discuss → plan → execute → verify) entregan en horas lo que toma días con desarrollo tradicional, manteniendo atomic commits y tests reales."
       - discipline (id '02'): EN ≈ "Hexagonal architecture, ports & adapters, ArchUnit gates and ADRs are non-negotiable — agentic speed never comes at the cost of structure."; ES native parallel ("Hexagonal architecture, ports & adapters, ArchUnit gates y ADRs no se negocian — la velocidad agéntica nunca sacrifica estructura.")
       - quality (id '03'): EN ≈ "Tests (JUnit, Karate, Testcontainers), structured logging and OpenTelemetry tracing are wired from day one, not bolted on later."; ES parallel ("Tests (JUnit, Karate, Testcontainers), structured logging y tracing con OpenTelemetry quedan cableados desde el día uno, no parchados después.")
       - transfer (id '04'): EN ≈ "I leave your team the workflow, not a black box: the agents, skills and guidelines stay in your repo and your engineers keep using them after I'm gone."; ES parallel ("Le dejo a tu equipo el workflow, no una caja negra: los agents, skills y guidelines quedan en tu repo y tus ingenieros los siguen usando cuando yo no esté.")

    2) `export const SERVICES` — 5 entries. Each: `{ key: 'greenfield' | 'aiSetup' | 'mcp' | 'legacy' | 'devops', desc: { en, es } }`. Each desc is a single sentence per CONTEXT decision-b example.
       - greenfield: EN = "Spring Boot + hexagonal from scratch with CI/CD, observability and complete testing infra."; ES = "Spring Boot + hexagonal desde cero, con CI/CD, observability y testing infra completas."
       - aiSetup: EN ≈ "Custom agents, skills and slash-commands tailored to your codebase so your team adopts the workflow on day one."; ES parallel ("Subagents, skills y slash-commands a medida de tu codebase para que tu equipo adopte el workflow desde el primer día.")
       - mcp: EN ≈ "Model Context Protocol servers in Java/Kotlin with Spring AI that connect Claude to your internal systems."; ES parallel ("Servidores Model Context Protocol en Java/Kotlin con Spring AI que conectan Claude a tus sistemas internos.")
       - legacy: EN ≈ "Strangler-fig refactor of legacy Spring/Java services toward hexagonal architecture, with full test coverage and zero downtime."; ES parallel ("Refactor strangler-fig de servicios Spring/Java legacy hacia hexagonal architecture, con cobertura completa de tests y cero downtime.")
       - devops: EN ≈ "Production-ready GitHub Actions templates + GitFlow + multi-cloud deploys (EC2, VPN-tunneled EC2, EKS) backed by the soldife/ci-templates toolkit."; ES parallel ("Templates de GitHub Actions listos para producción + GitFlow + deploys multi-cloud (EC2, EC2 sobre VPN, EKS) respaldados por el toolkit soldife/ci-templates.") — explicitly anchor to ci-templates per CONTEXT risk-mitigation.

    3) `export const APPS` — 3 entries (REAL data, no placeholders). Each: `{ slug, name, tag, desc: { en, es }, stack: string[], links: [] }`.
       - ci-templates (tag `'OPEN SOURCE'`): name `'ci-templates'`. desc EN = "47 reusable GitHub Actions workflows + 15 starter templates (Java, Krakend, React). GitFlow strategy, quality gates (SonarQube · OWASP · ArchUnit · Qodana), deploys EC2 / EC2-over-WireGuard-VPN / EKS."; desc ES identical wording (technical English terms preserved per CONTEXT tone constraints). stack = `['github-actions', 'gitflow', 'sonarqube', 'owasp', 'eks', 'ec2', 'wireguard', 'ecr', 'helm', 'jenkins']`. links = `[]`.
       - gsd (tag `'FRAMEWORK'`): slug `'gsd'`, name `'GSD framework'`. desc EN ≈ "Get-Shit-Done agentic methodology for Claude Code: 37 custom agents, 81 skills, 86 orchestrations and 15 architectural guidelines that drive the discuss → plan → execute → verify cycle."; desc ES parallel ("Metodología agéntica Get-Shit-Done para Claude Code: 37 subagents propios, 81 skills, 86 orquestaciones y 15 guidelines arquitectónicos que conducen el ciclo discuss → plan → execute → verify."). stack = `['claude-code', 'subagents', 'skills', 'mcp', 'orchestration', 'tdd', 'hexagonal']`. links = `[]`.
       - spring-ai-qdrant-mcp (tag `'MCP'`): slug `'spring-ai-qdrant-mcp'`, name `'spring-ai-qdrant-mcp'`. desc EN ≈ "Model Context Protocol server built with Spring Boot 3 + Spring AI + Qdrant vector store. Exposes RAG retrieval over private knowledge bases as MCP tools any Claude-compatible client can call."; desc ES parallel ("Servidor Model Context Protocol construido con Spring Boot 3 + Spring AI + Qdrant vector store. Expone retrieval RAG sobre bases de conocimiento privadas como herramientas MCP que cualquier cliente Claude-compatible puede invocar."). stack = `['spring-boot-3', 'spring-ai', 'qdrant', 'mcp', 'rag', 'java-21']`. links = `[]`.

    4) `export const COUNTERS` — 7 entries, EXACT VALUES per CONTEXT decision-b: `[{value: 37, key: 'agents'}, {value: 81, key: 'skills'}, {value: 86, key: 'workflows'}, {value: 15, key: 'guidelines'}, {value: 47, key: 'ciWorkflows'}, {value: 15, key: 'starterTemplates'}, {value: 5, key: 'appsShipped'}]`.

    5) `export const STACK_CHIPS` — exactly 17 strings IN ORDER: `'Java 21'`, `'Kotlin 2.x'`, `'Spring Boot 3'`, `'Hexagonal'`, `'PostgreSQL'`, `'Kafka'`, `'Redis'`, `'AWS'`, `'Docker'`, `'Kubernetes'`, `'Terraform'`, `'GitHub Actions'`, `'Jenkins'`, `'SonarQube'`, `'Claude Code'`, `'MCP'`, `'Spring AI'`.

    Style: no semicolons (per .eslintrc airbnb override `semi: 0`), 2-space indent, LF line endings (per .editorconfig). NO inline comments. NO PropTypes. Use the same plain `export const NAME = [...]` form as `projects.js`.

    Tone reminders (per CONTEXT decision-l):
    - Confident, evidence-backed. NO hyperbole ("rockstar", "world-class", "the best").
    - ES uses "tú" (matches site tone). Keep technical English terms (Spring Boot, hexagonal, MCP, GitFlow, observability, atomic commits, strangler-fig).
    - Velocity claim "3–5× faster" lives in translations.js values.velocity short label — NOT in this file.

    Commit message: `feat(09-01): add src/data/claude.js — bilingual VALUES/SERVICES/APPS/COUNTERS/STACK_CHIPS`
  </action>
  <verify>
    <automated>test "$(grep -c '^export const' src/data/claude.js)" -eq 5 && grep -q "STACK_CHIPS" src/data/claude.js && grep -q "ci-templates" src/data/claude.js && grep -q "ciWorkflows" src/data/claude.js && grep -q "appsShipped" src/data/claude.js && npx --yes eslint src/data/claude.js</automated>
  </verify>
  <done>
    - `src/data/claude.js` exists with exactly 5 top-level `export const` statements.
    - VALUES has 4 entries, SERVICES has 5, APPS has 3 (ci-templates, gsd, spring-ai-qdrant-mcp), COUNTERS has 7, STACK_CHIPS has 17.
    - ci-templates entry references 47 workflows + 15 templates + GitFlow + EC2/EC2-VPN/EKS + SonarQube/OWASP/ArchUnit/Qodana (AI-01-CICD evidence covered).
    - ESLint passes on the file (0 errors).
  </done>
</task>

<task type="auto">
  <name>Task A2: Extend src/i18n/translations.js with t.claude.* namespace + t.nav.claudeCode</name>
  <files>src/i18n/translations.js</files>
  <action>
    Make TWO parallel sets of insertions — one in the `en: { ... }` block (top of file, starts line 2) and one in the `es: { ... }` block (starts line 136). Each block already contains a `nav: { ... }` sub-block and a `projects: { ... }` sub-block.

    Insertion 1 — extend nav (both languages):
    Add `claudeCode: 'Claude Code'` to the `nav` object in BOTH `en` and `es` blocks. Same value in both languages — it's a product name and does not translate (per CONTEXT decision-j).

    Insertion 2 — add `claude` namespace (both languages):
    Insert a `claude: { ... }` sub-block immediately AFTER the `projects: { ... }` sub-block in BOTH `en` and `es`. Use EXACTLY the copy specified in CONTEXT decision-f:

    EN block — `claude: { ... }` shape:
    ```js
    claude: {
      label: 'AI Engineering · For your team',
      h2Part1: 'Backend systems',
      h2Part2: 'delivered with AI discipline',
      subLead: 'Senior backend engineer. Combining hexagonal architecture, Spring Boot, and agentic workflows with Claude Code to ship production-ready features in a fraction of the time — without sacrificing testing, ADRs, or observability.',
      ctaPrimary: "Let's talk about your project →",
      ctaSecondary: 'See projects',
      proofLabel: 'Track record',
      proofHeading: 'I built my own toolkit before selling it',
      servicesLabel: 'Concrete services',
      counters: {
        agents: 'custom agents',
        skills: 'workflow skills',
        workflows: 'orchestrations',
        guidelines: 'guidelines',
        ciWorkflows: 'CI workflows',
        starterTemplates: 'starter templates',
        appsShipped: 'apps shipped',
      },
      values: {
        velocity: 'Delivery 3–5× faster',
        discipline: 'Hexagonal without shortcuts',
        quality: 'Tests + observability built-in',
        transfer: 'Your team keeps the workflow',
      },
      services: {
        greenfield: 'Greenfield builds',
        aiSetup: 'AI workflow setup',
        mcp: 'MCP server development',
        legacy: 'Legacy refactor',
        devops: 'DevOps automation',
      },
    },
    ```

    ES block — `claude: { ... }` shape (parallel, same keys, "tú" register):
    ```js
    claude: {
      label: 'AI Engineering · Para tu equipo',
      h2Part1: 'Sistemas backend',
      h2Part2: 'entregados con disciplina AI',
      subLead: 'Senior backend engineer. Combino hexagonal architecture, Spring Boot y workflows agénticos con Claude Code para entregar features production-ready en una fracción del tiempo — sin sacrificar testing, ADRs ni observability.',
      ctaPrimary: 'Hablemos de tu proyecto →',
      ctaSecondary: 'Ver proyectos',
      proofLabel: 'Track record',
      proofHeading: 'Construí mi propio toolkit antes de venderlo',
      servicesLabel: 'Servicios concretos',
      counters: {
        agents: 'subagents propios',
        skills: 'workflow skills',
        workflows: 'orquestaciones',
        guidelines: 'guidelines',
        ciWorkflows: 'CI workflows',
        starterTemplates: 'starter templates',
        appsShipped: 'apps shipped',
      },
      values: {
        velocity: 'Entrega 3–5× más rápida',
        discipline: 'Hexagonal sin atajos',
        quality: 'Tests + observability built-in',
        transfer: 'Tu equipo se lleva el workflow',
      },
      services: {
        greenfield: 'Greenfield builds',
        aiSetup: 'AI workflow setup',
        mcp: 'MCP server development',
        legacy: 'Legacy refactor',
        devops: 'DevOps automation',
      },
    },
    ```

    Style:
    - No semicolons inside object literals (file already uses no-semi style).
    - 2-space indent. LF.
    - Use straight double quotes for the EN ctaPrimary because of the apostrophe in "Let's" (or escape with `'Let\\'s ...'`).
    - Use en-dash `–` and `×` exactly as shown (not `--` or `x`).
    - Do NOT touch existing `projects.*`, `experience.*`, `about.*`, `skills.*`, `contact.*`, `footer.*`, or `nav.*` keys other than adding the new `claudeCode` and `claude` ones.

    Commit message: `feat(09-01): add t.claude.* namespace + t.nav.claudeCode to translations.js (EN+ES)`
  </action>
  <verify>
    <automated>test "$(grep -c 'claudeCode:' src/i18n/translations.js)" -eq 2 && test "$(grep -c 'claude: {' src/i18n/translations.js)" -eq 2 && test "$(grep -c 'ciWorkflows:' src/i18n/translations.js)" -eq 2 && test "$(grep -c 'starterTemplates:' src/i18n/translations.js)" -eq 2 && test "$(grep -c 'appsShipped:' src/i18n/translations.js)" -eq 2 && test "$(grep -c 'subLead:' src/i18n/translations.js)" -eq 2 && test "$(grep -c 'proofHeading:' src/i18n/translations.js)" -eq 2 && npx --yes eslint src/i18n/translations.js</automated>
  </verify>
  <done>
    - `nav.claudeCode` present in BOTH `en` and `es` blocks (value `'Claude Code'`).
    - `claude` namespace present in BOTH blocks with all 9 top-level scalar keys (label, h2Part1, h2Part2, subLead, ctaPrimary, ctaSecondary, proofLabel, proofHeading, servicesLabel) plus 3 sub-objects (counters with 7 keys, values with 4 keys, services with 5 keys).
    - Total NEW string entries: 27 per lang × 2 = 54.
    - ESLint passes (0 errors; existing warnings tolerated).
  </done>
</task>

<task type="auto">
  <name>Task B1: Create src/components/Claude.js with all 6 internal sub-components</name>
  <files>src/components/Claude.js</files>
  <action>
    Create the file as a single default-exported `function Claude()` component PLUS 6 internal helper components (NOT exported). Mirror `src/components/Projects.js` structural conventions exactly.

    Imports (top of file):
    ```js
    import React, { useRef } from 'react'
    import { useLanguage } from '../i18n/LanguageContext'
    import useInView from '../hooks/useInView'
    import { VALUES, SERVICES, APPS, COUNTERS, STACK_CHIPS } from '../data/claude'
    ```

    Default export — `function Claude()`:
    - Calls `const { lang, t } = useLanguage()`.
    - Two `useRef(null)` + `useInView(ref, { threshold: 0.15 })` pairs is acceptable (one for header / pitch hero, one for the grids), but a SINGLE section-level ref is the locked simpler shape per CONTEXT decision-p. Use ONE section-level ref + inView for the staggered grids, matching the CONTEXT example.
    - Section root: `<section id="claude-code" ref={sectionRef} className="py-24 sm:py-32 bg-ink-950">`.
    - Inner wrapper: `<div className="max-w-6xl mx-auto px-6">`.
    - Children order (CONTEXT decision-p): `<PitchHero t={t} />` → values grid (4 cards) → `<ProofBlock t={t} counters={COUNTERS} />` → services grid (5 cards) → apps grid (3 cards) → `<StackStrip chips={STACK_CHIPS} />`.
    - Pass `inView` (or per-grid inView) down so cards can flip `is-visible` class — Task B2 wires the actual `animate-on-scroll` / `is-visible` toggle. For B1, scaffold the className strings as `animate-on-scroll` on each grid wrapper (no `is-visible` yet — B2 adds the conditional).

    Sub-components (each defined below `export default function Claude` in this file, NOT exported):

    1) `function PitchHero({ t })`:
       - Centered block (`text-center max-w-3xl mx-auto`).
       - **Wrapper backdrop:** the PitchHero outer wrapper class must include `bg-gradient-to-br from-accent/5 to-brand/5 rounded-2xl p-8 md:p-12 mb-12` (or equivalent subtle theme-aware gradient backdrop) so the pitch hero sits on a tinted container that mirrors the V7 sketch's hero-card treatment (V7 used a `linear-gradient(135deg, rgba(16,185,129,0.04) 0%, rgba(59,130,246,0.02) 100%)` rule on the `.pitch-hero` container — see `.planning/sketches/v3.6-variants.html` lines 677–786). Tailwind's opacity modifier syntax (`/5`) is compatible with the CSS-var-backed `accent` and `brand` colors from Phase 7. Per CONTEXT risk-mitigation: "V7 sketch uses distinct visual treatments per card type" — this gradient backdrop is the PitchHero's distinct treatment versus the flat ValueCard / ServiceCard / FeaturedAppCard surfaces.
       - Small label `<p>` using `t.claude.label` (uppercase, tracked, `text-brand font-mono text-xs uppercase tracking-widest`).
       - `<h2>` with `t.claude.h2Part1` then `<span className="bg-brand-gradient bg-clip-text text-transparent">{t.claude.h2Part2}</span>` (gradient half-line, matches V7 sketch styling). Heading classes `text-4xl md:text-5xl lg:text-6xl font-extrabold text-text-primary mt-4 leading-tight`.
       - `<p>` sub-lead: `t.claude.subLead`, `text-text-secondary text-lg mt-6 max-w-2xl mx-auto`.
       - Two CTAs wrapped in `<div className="flex flex-col sm:flex-row gap-4 justify-center mt-10">`:
         - Primary `<a href="#contact">` with `bg-brand-gradient text-ink-900 font-extrabold px-8 py-4 rounded-lg motion-safe:transition-transform motion-safe:hover:-translate-y-0.5 shadow-brand`. Text: `t.claude.ctaPrimary`.
         - Secondary `<a href="#projects">` with `border border-ink-400 text-text-primary px-8 py-4 rounded-lg motion-safe:transition-colors hover:border-brand hover:text-brand`. Text: `t.claude.ctaSecondary`.

    2) `function ValueCard({ id, title, desc, index })`:
       - Numbered card: large monospace `id` ("01"..."04") in `text-brand font-mono text-2xl font-extrabold`.
       - `<h3>{title}</h3>` `text-text-primary font-extrabold text-lg mt-3`.
       - `<p>{desc}</p>` `text-text-secondary text-base mt-2 leading-relaxed`.
       - Card wrapper: `bg-ink-500 border border-ink-400 rounded-xl p-6 motion-safe:transition-all motion-safe:duration-300 hover:border-brand hover:-translate-y-1`.
       - Per-card stagger: `style={{ transitionDelay: \`${index * 100}ms\` }}`.

    3) `function ProofBlock({ t, counters })`:
       - Outer wrapper: `mt-20 bg-ink-500/60 border border-ink-400 rounded-2xl p-8 md:p-12`.
       - Small label `t.claude.proofLabel` (`text-brand font-mono text-xs uppercase tracking-widest`).
       - Heading `t.claude.proofHeading` `text-2xl md:text-3xl font-extrabold text-text-primary mt-3`.
       - Counter grid: `grid grid-cols-2 sm:grid-cols-4 lg:grid-cols-7 gap-6 mt-8` (7 columns on lg, wraps below).
       - For each counter `{value, key}`: `<div className="text-center">` with big number `<div className="text-3xl md:text-4xl font-extrabold text-brand">{value}</div>` and label `<div className="text-xs text-text-secondary uppercase tracking-wide mt-1">{t.claude.counters[counter.key]}</div>`.

    4) `function ServiceCard({ title, desc, index })`:
       - Distinct treatment from ValueCard (per CONTEXT risk-mitigation: visual fatigue).
       - Left-border accent: `border-l-4 border-brand bg-ink-500 rounded-r-xl p-6 motion-safe:transition-all motion-safe:duration-300 hover:-translate-y-1`.
       - `<h3>{title}</h3>` `text-text-primary font-extrabold text-base`.
       - `<p>{desc}</p>` `text-text-secondary text-sm mt-2`.
       - Per-card stagger: `style={{ transitionDelay: \`${index * 100}ms\` }}`.

    5) `function FeaturedAppCard({ name, tag, desc, stack, index })`:
       - Distinct treatment (bordered with tag badge).
       - Wrapper: `bg-ink-500 border border-ink-400 rounded-xl p-6 motion-safe:transition-all motion-safe:duration-300 hover:border-brand`.
       - Top row: `<div className="flex items-center justify-between">` containing `<h3 className="font-mono text-base font-extrabold text-text-primary">{name}</h3>` and a tag badge `<span className="font-mono text-xs px-2 py-1 rounded-md bg-brand-gradient text-ink-900 font-extrabold">{tag}</span>`.
       - Description `<p>{desc}</p>` `text-text-secondary text-sm mt-3 leading-relaxed`.
       - Stack chips grid: `<div className="flex flex-wrap gap-2 mt-4">` mapping `stack` to `<span className="font-mono text-xs px-2 py-1 rounded-md bg-ink-400 text-text-secondary border border-ink-400">{tag}</span>`.
       - Per-card stagger via `transitionDelay`.

    6) `function StackStrip({ chips })`:
       - Outer: `mt-16 border-t border-ink-400 pt-10`.
       - Flex-wrap chip row: `<div className="flex flex-wrap justify-center gap-2">`.
       - Each chip: `<span className="font-mono text-xs px-3 py-1.5 rounded-full bg-ink-500 text-text-secondary border border-ink-400">{chip}</span>`.

    Section grids (inside `Claude`):
    - Values grid: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mt-16 animate-on-scroll">` mapping `VALUES` to `<ValueCard key={v.key} id={v.id} title={t.claude.values[v.key]} desc={v.desc[lang]} index={i} />`.
    - Services grid: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mt-8 animate-on-scroll">` mapping `SERVICES` to `<ServiceCard key={s.key} title={t.claude.services[s.key]} desc={s.desc[lang]} index={i} />`. The `t.claude.servicesLabel` text appears above this grid (`<p className="text-brand font-mono text-xs uppercase tracking-widest mt-12">{t.claude.servicesLabel}</p>`).
    - Apps grid: `<div className="grid grid-cols-1 md:grid-cols-3 gap-6 mt-8 animate-on-scroll">` mapping `APPS` to `<FeaturedAppCard key={a.slug} name={a.name} tag={a.tag} desc={a.desc[lang]} stack={a.stack} index={i} />`.

    Style:
    - No semicolons. 2-space indent. LF.
    - No PropTypes. No JSDoc.
    - All user-facing strings come from `t.claude.*` or `*.desc[lang]` from data — no hardcoded text anywhere except section-id `'claude-code'` and href anchors `'#contact'` / `'#projects'`.
    - Use Tailwind utilities only — NO new CSS file, NO inline color hex.
    - Use `motion-safe:` prefix on every `transition-*` or `hover:-translate-*` utility (existing project convention; Task B2 verifies).
    - Single export: `export default function Claude()`. All sub-components are file-local `function` declarations BELOW the default export (matches Projects.js convention where `ProjectCard` lives below `Projects`).

    Reference visual sketch: `.planning/sketches/v3.6-variants.html` — locate the variant labeled "7 ✓ LOCKED" or with id `claude-code`. Adapt JSX:
    - `class` → `className`
    - Inline styles become `style={{ camelCase: value }}` objects.

    Commit message: `feat(09-01): Claude.js component + 6 internal sub-components (PitchHero/ValueCard/ProofBlock/ServiceCard/FeaturedAppCard/StackStrip)`
  </action>
  <verify>
    <automated>test "$(grep -c 'export default function Claude' src/components/Claude.js)" -eq 1 && test "$(grep -cE 'function (PitchHero|ValueCard|ProofBlock|ServiceCard|FeaturedAppCard|StackStrip)' src/components/Claude.js)" -eq 6 && grep -q "from '../data/claude'" src/components/Claude.js && grep -q "useInView" src/components/Claude.js && grep -q "useLanguage" src/components/Claude.js && grep -q 'id="claude-code"' src/components/Claude.js && grep -q 'href="#contact"' src/components/Claude.js && grep -q 'href="#projects"' src/components/Claude.js && npx --yes eslint src/components/Claude.js</automated>
  </verify>
  <done>
    - File default-exports `Claude` and declares 6 file-local sub-components.
    - All copy sourced from `t.claude.*` or `*.desc[lang]`.
    - Section root has `id="claude-code"`.
    - Primary CTA `href="#contact"` and secondary CTA `href="#projects"` both present.
    - PitchHero wrapper carries the V7-style gradient backdrop (`bg-gradient-to-br from-accent/5 to-brand/5 rounded-2xl p-8 md:p-12 mb-12` or equivalent subtle tinted container) — mirrors locked sketch.
    - ESLint passes (0 errors; pre-existing warnings tolerated).
  </done>
</task>

<task type="auto">
  <name>Task B2: Wire useInView + animate-on-scroll/is-visible toggle in Claude.js</name>
  <files>src/components/Claude.js</files>
  <action>
    Extend the scaffold from B1: convert the placeholder `animate-on-scroll` strings into the full inView-conditional pattern matching `src/components/Projects.js` line 30.

    Two valid implementations — pick the cleaner one:

    Option A (single section-level inView) — matches CONTEXT decision-p simplest shape:
    ```js
    const sectionRef = useRef(null)
    const inView = useInView(sectionRef, { threshold: 0.15 })
    // ... then on each grid wrapper:
    <div className={`grid ... animate-on-scroll${inView ? ' is-visible' : ''}`}>
    ```

    Option B (per-block refs) — matches Projects.js exactly (two ref/inView pairs):
    ```js
    const headerRef = useRef(null)
    const headerInView = useInView(headerRef, { threshold: 0.25 })
    const gridsRef = useRef(null)
    const gridsInView = useInView(gridsRef, { threshold: 0.15 })
    ```

    Pick Option A for this task (single ref → 3 grids all flip together; simpler and matches CONTEXT decision-p exemplar code). Apply `${inView ? ' is-visible' : ''}` template suffix to the className of EACH of the THREE grid wrappers (values grid, services grid, apps grid). The PitchHero, ProofBlock, and StackStrip wrappers DO NOT need the animate-on-scroll toggle (they render statically — only the card grids stagger in).

    Stagger:
    - Each card sub-component (ValueCard, ServiceCard, FeaturedAppCard) already receives `index` from B1 and applies `style={{ transitionDelay: \`${index * 100}ms\` }}`. Verify these inline styles render.

    `motion-safe:` audit:
    - All `transition-*` utility classes (transform, colors, all) MUST carry the `motion-safe:` prefix so `prefers-reduced-motion` suppresses them. Grep target: at least 3 occurrences in the file (one per card type's hover state at minimum).

    Section-level ref attachment:
    - `<section id="claude-code" ref={sectionRef} className="py-24 sm:py-32 bg-ink-950">` — ensure the `ref={sectionRef}` binding is on the `<section>` root so IntersectionObserver attaches at the right element.

    Style: no semicolons, 2-space indent.

    Commit message: `feat(09-01): Claude.js entrance animations via useInView + stagger 100ms`
  </action>
  <verify>
    <automated>test "$(grep -c 'useInView' src/components/Claude.js)" -ge 1 && test "$(grep -c 'animate-on-scroll' src/components/Claude.js)" -ge 3 && grep -q "is-visible" src/components/Claude.js && grep -q "transitionDelay" src/components/Claude.js && test "$(grep -c 'motion-safe:' src/components/Claude.js)" -ge 3 && grep -q "ref={sectionRef}" src/components/Claude.js && npx --yes eslint src/components/Claude.js</automated>
  </verify>
  <done>
    - `useInView` called with `{ threshold: 0.15 }` on the section ref.
    - All 3 grid wrappers (values / services / apps) carry `animate-on-scroll${inView ? ' is-visible' : ''}` className.
    - `transitionDelay` inline style present on at least one card sub-component (verifies stagger wiring).
    - `motion-safe:` prefix appears on at least 3 transition/hover utilities.
    - ESLint passes (0 errors).
  </done>
</task>

<task type="auto">
  <name>Task C1: Add Claude lazy import + Suspense block to src/App.js</name>
  <files>src/App.js</files>
  <action>
    Two surgical edits to `src/App.js`, preserving all existing structure:

    Edit 1 — lazy import:
    After the existing `const Projects = React.lazy(() => import('./components/Projects'))` line (currently line 11), insert a new line:
    ```js
    const Claude = React.lazy(() => import('./components/Claude'))
    ```
    Position it BETWEEN `Projects` and `Contact` lazy imports to preserve visual reading order matching the section flow.

    Edit 2 — Suspense block:
    Inside `<main id="main">`, insert a new Suspense block BETWEEN the existing Projects Suspense block (closes at `</Suspense>` near line 44) and the Contact Suspense block (opens near line 45). The new block:
    ```jsx
    <Suspense fallback={SectionFallback}>
      <Claude />
    </Suspense>
    ```

    Resulting Suspense block order inside `<main>`:
    1. Hero (eager)
    2. About (eager)
    3. Skill (eager)
    4. Experience (lazy via Suspense)
    5. Projects (lazy via Suspense)
    6. **Claude (lazy via Suspense) ← NEW**
    7. Contact (lazy via Suspense)

    Footer Suspense block (outside `<main>`) stays unchanged.

    Do NOT touch: SkipLink, ThemeProvider, LanguageProvider, the outer div className, or any other existing Suspense boundary.

    Style: no semicolons, 2-space indent.

    Commit message: `feat(09-01): App.js lazy-load Claude component between Projects and Contact`
  </action>
  <verify>
    <automated>test "$(grep -c "React.lazy(() => import('./components/Claude'))" src/App.js)" -eq 1 && test "$(grep -c '<Claude' src/App.js)" -eq 1 && test "$(grep -c 'Suspense' src/App.js)" -ge 10 && LINE_CLAUDE=$(grep -n '<Claude' src/App.js | head -1 | cut -d: -f1) && LINE_CONTACT=$(grep -n '<Contact' src/App.js | head -1 | cut -d: -f1) && [ -n "$LINE_CLAUDE" ] && [ -n "$LINE_CONTACT" ] && [ "$LINE_CLAUDE" -lt "$LINE_CONTACT" ] && npx --yes eslint src/App.js</automated>
  </verify>
  <done>
    - Exactly one `React.lazy(() => import('./components/Claude'))` line exists.
    - Exactly one `<Claude />` element exists inside a Suspense block.
    - Suspense count increases by 1 (was 8 — Experience + Projects + Contact + Footer wrappers; now 10 occurrences of the string "Suspense" counting opens+closes for 5 blocks).
    - `<Claude />` element appears at a strictly lower line number than `<Contact />` (order-correct: Claude renders before Contact in the JSX flow).
    - ESLint passes (0 errors).
  </done>
</task>

<task type="auto">
  <name>Task C2: Add 'claude-code' to SECTION_IDS + DesktopNav + MobileMenu in src/components/Nav.js</name>
  <files>src/components/Nav.js</files>
  <action>
    Three surgical insertions in `src/components/Nav.js`. ORDER MATTERS — `'claude-code'` goes BETWEEN `'projects'` and `'contact'` in every list.

    Edit 1 — `SECTION_IDS` (currently line 7):
    Change:
    ```js
    const SECTION_IDS = ['about', 'skills', 'experience', 'projects', 'contact']
    ```
    to:
    ```js
    const SECTION_IDS = ['about', 'skills', 'experience', 'projects', 'claude-code', 'contact']
    ```

    Edit 2 — `DesktopNav` `links` array (currently lines 63-69):
    Insert a new entry between the `projects` and `contact` entries:
    ```js
    { id: 'claude-code', label: t.nav.claudeCode },
    ```
    Result: 6-entry array `[about, skills, experience, projects, claude-code, contact]`.

    Edit 3 — `MobileMenu` `links` array (currently lines 172-178):
    Insert the same entry in the same position:
    ```js
    { id: 'claude-code', label: t.nav.claudeCode },
    ```
    Result: 6-entry array `[about, skills, experience, projects, claude-code, contact]`.

    Do NOT touch: Logomark, ProgressBar, LangPill, ThemeToggle, hamburger button JSX, aria attributes, focus-trap effect, portal rendering, or any other existing code. The `href` template literal `\`#${l.id}\`` automatically renders `href="#claude-code"` once the entry is added — no manual href edit needed.

    Style: no semicolons, 2-space indent.

    Commit message: `feat(09-01): Nav adds claude-code SECTION_ID + DesktopNav + MobileMenu link entries`
  </action>
  <verify>
    <automated>test "$(grep -c "'claude-code'" src/components/Nav.js)" -eq 3 && test "$(grep -c 't.nav.claudeCode' src/components/Nav.js)" -eq 2 && grep -q "'about', 'skills', 'experience', 'projects', 'claude-code', 'contact'" src/components/Nav.js && npx --yes eslint src/components/Nav.js</automated>
  </verify>
  <done>
    - `'claude-code'` literal appears 3 times: once in SECTION_IDS, once in DesktopNav links, once in MobileMenu links.
    - `t.nav.claudeCode` referenced exactly 2 times (DesktopNav + MobileMenu).
    - SECTION_IDS reads exactly `['about', 'skills', 'experience', 'projects', 'claude-code', 'contact']`.
    - ESLint passes (0 errors; pre-existing warnings tolerated).
  </done>
</task>

<task type="auto">
  <name>Task D1: Build, verify Claude lazy chunk emission and bundle size budget</name>
  <files></files>
  <action>
    Run the production build and validate four properties:

    1) Tailwind `bg-brand-gradient` utility still defined (defensive sanity — Phase 7 Plan 07-02 Task 3 updated `tailwind.config.js` `backgroundImage.brand-gradient` to the new blue+emerald palette; this check catches any regression where the utility goes missing before runtime):
       ```sh
       grep -q "'brand-gradient'" tailwind.config.js
       ```
       Must exit 0. If missing, the PitchHero gradient half-line and the FeaturedAppCard tag badge will silently lose their gradient — block and fix in Phase 7 before proceeding.

    2) Build succeeds:
       ```sh
       npm run build
       ```
       Must exit 0. Any TypeScript/Vite errors fail the task — go back and fix in B1/B2 before retrying.

    3) Claude chunk emits as a separate file:
       ```sh
       ls dist/assets/Claude-*.js
       ```
       Must list exactly one matching file (Vite hash-suffixed). If zero files match, the lazy boundary in App.js (Task C1) is broken — investigate.

    4) Chunk size within budget:
       ```sh
       wc -c dist/assets/Claude-*.js
       ```
       Assert byte count ≤ 12_000 (12 KB raw — 8 KB target with 50% margin per CONTEXT decision-p risk-mitigation). If over 12 KB: WRITE a one-line warning in the commit message body (e.g. "Note: Claude chunk is {N} bytes — exceeds 8 KB target by {pct}%, within 12 KB ceiling. Phase 10 may tune."). Do NOT block the commit unless > 12 KB ceiling is exceeded — then split sub-components into separate files (refactor needed).

    5) Lint still clean (final check across all touched files):
       ```sh
       npm run lint
       ```
       Must exit 0. Warnings tolerated; errors block.

    6) Confirm no unrelated changes leaked into `dist/index.html`:
       - The Phase 8 hero photo preload tag (if present in current `dist/index.html`) should still be there.
       - No new `<link>` or `<script>` tags for Claude — Vite handles chunk loading via the existing module bootstrap.

    NO new files created in this task. This is verification + commit only.

    Commit message: `chore(09-01): verify Claude lazy chunk emits and stays within size budget`
    (Append size note in body if chunk > 8 KB but ≤ 12 KB.)
  </action>
  <verify>
    <automated>grep -q "'brand-gradient'" tailwind.config.js && npm run build 2>&1 | tail -20 && ls dist/assets/Claude-*.js && SIZE=$(wc -c < $(ls dist/assets/Claude-*.js | head -1)) && echo "Claude chunk size: ${SIZE} bytes" && test "${SIZE}" -le 12000 && npm run lint</automated>
  </verify>
  <done>
    - `grep "'brand-gradient'" tailwind.config.js` exits 0 (utility intact post-Phase-7).
    - `npm run build` exits 0.
    - `dist/assets/Claude-*.js` exists (exactly one file).
    - File size ≤ 12_000 bytes (target ≤ 8_000; ceiling 12_000).
    - `npm run lint` exits 0.
    - Commit recorded; if chunk > 8 KB, body note documents the overage.
    - **Doc-drift note for SUMMARY.md (executor must include in `09-01-SUMMARY.md` deviations section so Phase 10 UAT does not flag as regressions):**
      1. REQUIREMENTS.md AI-01 lists 5 apps; CONTEXT + locked V7 sketch + this plan ship 3 (ci-templates, GSD, spring-ai-qdrant-mcp). Record: "Deviation from REQUIREMENTS.md: ships 3 featured-app cards per locked V7 sketch + CONTEXT decision; original REQUIREMENTS draft listed 5 (claude-kanban + caveman deferred to backlog / v3.7 candidate)."
      2. ROADMAP.md Phase 9 SC2 entry says "5 counters, 4 service cards"; this plan ships 7 counters + 5 services. Record: "ROADMAP Phase 9 SC2 entry stale (predates 09-CONTEXT discuss-phase); ships 7 counters + 5 services per locked CONTEXT + sketch."
  </done>
</task>

</tasks>

<verification>
Phase 9 has no internal verification gate — Phase 10 owns the full real-browser sweep (visual layout, scroll-spy, WCAG AA contrast, CTAs scroll-to behavior, language toggle, reduced-motion, Lighthouse). For this plan, the automated verification in each task plus the build/lint check in Task D1 is sufficient.

Sanity checks before Phase 10 hand-off:
- `dist/assets/Claude-*.js` emitted as a separate chunk (proves lazy boundary works).
- `grep -c "claude:" src/i18n/translations.js` returns 2 (EN + ES namespaces).
- `grep -c "claudeCode:" src/i18n/translations.js` returns 2 (EN + ES nav entries).
- `grep "'claude-code'" src/components/Nav.js | wc -l` returns 3 (SECTION_IDS + DesktopNav + MobileMenu).
- `src/data/claude.js` exports VALUES, SERVICES, APPS, COUNTERS, STACK_CHIPS — five named exports, no default export.
- `src/components/Claude.js` is the only file in `src/components/` matching `Claude*.js`.
</verification>

<success_criteria>
Plan 09-01 is COMPLETE when ALL of the following are true:

1. **Data layer (Task A1):** `src/data/claude.js` exists with 5 named exports (VALUES × 4, SERVICES × 5, APPS × 3 including ci-templates/gsd/spring-ai-qdrant-mcp, COUNTERS × 7, STACK_CHIPS × 17). ESLint passes.

2. **Translations (Task A2):** `src/i18n/translations.js` has `nav.claudeCode = 'Claude Code'` in both EN and ES, plus a complete `claude: { ... }` namespace in both languages totalling 27 keys × 2 langs = 54 entries. ESLint passes.

3. **Component (Tasks B1 + B2):** `src/components/Claude.js` default-exports `Claude` and declares 6 internal sub-components (PitchHero, ValueCard, ProofBlock, ServiceCard, FeaturedAppCard, StackStrip). Imports `useInView` and `useLanguage` and pulls all data from `../data/claude`. Section root has `id="claude-code"`. PitchHero wrapper carries the V7-style gradient backdrop (`bg-gradient-to-br from-accent/5 to-brand/5 rounded-2xl p-8 md:p-12 mb-12`). Three grid wrappers carry `animate-on-scroll${inView ? ' is-visible' : ''}`. Card sub-components stagger via `style={{ transitionDelay: \`${index * 100}ms\` }}`. `motion-safe:` prefix on every transition/hover utility. ESLint passes.

4. **App wiring (Task C1):** `src/App.js` adds `React.lazy(() => import('./components/Claude'))` and inserts a `<Suspense fallback={SectionFallback}><Claude /></Suspense>` block between the Projects and Contact Suspense blocks. ESLint passes.

5. **Nav wiring (Task C2):** `src/components/Nav.js` SECTION_IDS array reads exactly `['about', 'skills', 'experience', 'projects', 'claude-code', 'contact']`. DesktopNav and MobileMenu links arrays both include `{ id: 'claude-code', label: t.nav.claudeCode }` in the same position. ESLint passes.

6. **Build verified (Task D1):** `npm run build` exits 0. `dist/assets/Claude-*.js` exists as a separate lazy chunk. Chunk size ≤ 12 KB (target ≤ 8 KB). `npm run lint` exits 0. `tailwind.config.js` still defines `'brand-gradient'` utility.

7. **AI-01-CICD evidence:** The `ci-templates` app card in `src/data/claude.js` explicitly references 47 workflows + 15 starter templates + GitFlow + EC2/EC2-VPN-WireGuard/EKS + SonarQube/OWASP/ArchUnit/Qodana. The `devops` service entry exists and anchors back to ci-templates. Two of the seven counters (`ciWorkflows: 47`, `starterTemplates: 15`) appear in COUNTERS. AI-01-CICD sub-feature is fully surfaced.

8. **Atomic commits:** 7 separate commits, one per task, conventional-commits prefixed (`feat:` for A1/A2/B1/B2/C1/C2; `chore:` for D1), tagged with `09-01` scope where applicable.

9. **Plan goal satisfied:** The visitor scrolling past Projects encounters the V7 sales-pitch AI section; the section renders bilingual EN+ES; CTAs route to #contact and #projects; the section is reachable via Nav scroll-spy (Desktop + Mobile); it ships as a separate lazy chunk. Phase 10 will UAT-verify visual layout, WCAG, Lighthouse, and reduced-motion behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-claude-code-section/09-01-SUMMARY.md` capturing:
- One-liner summary of what was built
- 7-task completion table (task → commit hash → files modified)
- All 5 named exports in `src/data/claude.js` with counts (VALUES 4, SERVICES 5, APPS 3, COUNTERS 7, STACK_CHIPS 17)
- Final Claude chunk byte size (from `wc -c dist/assets/Claude-*.js`)
- Any deviations from this plan (auto-fixes, scope-preserving substitutions) — MUST include the two doc-drift items flagged in Task D1 `<done>`: (a) REQUIREMENTS.md AI-01 lists 5 apps but plan ships 3 per locked V7 + CONTEXT (extras deferred to backlog), and (b) ROADMAP Phase 9 SC2 says 5 counters + 4 services but plan ships 7 + 5 per locked CONTEXT (entry stale, predates discuss-phase). Phase 10 UAT should not treat these as regressions.
- Known stubs (none expected — content is REAL data, not placeholders)
- Hand-off note to Phase 10 listing what UAT must verify (visual layout @ 4 breakpoints in dark+light, scroll-spy active state, CTAs smooth-scroll, lang toggle flips all 54 entries, prefers-reduced-motion suppresses stagger, WCAG AA contrast)
</output>
