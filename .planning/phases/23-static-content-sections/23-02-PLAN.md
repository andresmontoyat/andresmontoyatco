---
phase: 23-static-content-sections
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/components/astro/Skill.astro
  - src/components/astro/Skill.test.ts
  - src/components/astro/Footer.astro
  - src/components/astro/Footer.test.ts
  - src/components/Skill.test.jsx
autonomous: true
requirements: [STATIC-01, TEST-01]

must_haves:
  truths:
    - "D-01: Skill.astro imports src/data/skills.json directly in frontmatter with the same {en,es} field shape and the same pick(field, lang) resolver — zero data-shape changes"
    - "D-02: Footer.astro sources its copy from translations[locale].footer (rights + tagline), and copies the hardcoded module-level `social` array (GitHub, LinkedIn) verbatim from Footer.jsx — it has NO dedicated JSON file and uses no pick() resolver"
    - "D-03: Footer.astro reads new Date().getFullYear() in frontmatter (build-time) instead of at React runtime — same logic, different execution timing"
    - "D-06: Skill.test.ts and Footer.test.ts are Astro Container API tests with '// @vitest-environment node' as the literal first line, asserting bilingual (en+es) content and ARIA/landmark presence; Skill.test.ts ports every assertion of Skill.test.jsx with no assertion-count regression"
    - "Skill and Footer render correct EN and ES content at build time consuming existing data unchanged (STATIC-01)"
    - "Skill and Footer ship zero client-side JavaScript — no client: directive, no React import, no hooks"
  artifacts:
    - path: "src/components/astro/Skill.astro"
      provides: "Zero-JS Skill section, build-time bilingual render from skills.json"
      contains: "section id=\"skills\""
    - path: "src/components/astro/Footer.astro"
      provides: "Zero-JS Footer, build-time render from translations[locale].footer + social array + build-time year"
      contains: "footer"
    - path: "src/components/astro/Skill.test.ts"
      provides: "Container API test for Skill (parity with Skill.test.jsx)"
      contains: "@vitest-environment node"
    - path: "src/components/astro/Footer.test.ts"
      provides: "Container API test for Footer (fresh assertions — no RTL analog exists)"
      contains: "@vitest-environment node"
  key_links:
    - from: "src/components/astro/Skill.astro"
      to: "src/data/skills.json"
      via: "frontmatter import"
      pattern: "import data from '../../data/skills.json'"
    - from: "src/components/astro/Footer.astro"
      to: "src/i18n/translations.js"
      via: "translations[locale].footer resolution in frontmatter"
      pattern: "translations\\[locale\\]"
---

<objective>
Port `src/components/Skill.jsx` and `src/components/Footer.jsx` to zero-client-JS `.astro` components, each with an Astro Container API test. Skill consumes `src/data/skills.json` unchanged (D-01); Footer resolves copy from `translations[locale].footer`, copies its hardcoded `social` array verbatim, and reads the copyright year at build time (D-02, D-03).

Purpose: Skill is the simplest of the five (zero hooks) and Footer is the only one with no dedicated JSON file and no prior RTL test — both are pure structural ports building on the Container API pattern validated in Plan 23-01.
Output: `Skill.astro`, `Skill.test.ts`, `Footer.astro`, `Footer.test.ts` committed; old `Skill.test.jsx` removed. (No `Footer.test.jsx` exists to remove.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-static-content-sections/23-CONTEXT.md
@.planning/phases/23-static-content-sections/23-PATTERNS.md
@.planning/phases/23-static-content-sections/23-01-SUMMARY.md

<interfaces>
<!-- Container API: renderToString(Component, { props: { locale } }); assert on returned HTML string.
     Pattern proven in Plan 23-01 (src/components/astro/About.test.ts). -->

Skill props contract: receives `locale` via Astro.props (consistent with About/sibling components).
Skill pick() + maxYears() to port verbatim (Skill.jsx lines 5-12).
Skill preserves `data-core="true"`/`data-core="false"` attributes on chip tiles — Astro preserves data-* identically (6 core tiles expected).

Footer copy source (translations subtree, translations.js lines 50-53 EN / 103-106 ES):
  EN footer.rights = 'All rights reserved'; footer.tagline = "I write code. Let's build something amazing together."
  ES footer.rights = 'Todos los derechos reservados'; footer.tagline = 'Escribo código. Construyamos algo increíble juntos.'
Footer social array (verbatim from Footer.jsx lines 4-7):
  { name: 'GitHub', href: 'https://github.com/andresmontoyat', label: 'gh' }
  { name: 'LinkedIn', href: 'https://www.linkedin.com/in/carlos-andres-montoya-tobon-8b508033/', label: 'in' }
Footer resolution idiom (from BaseLayout.astro lines 8-17): `const footerCopy = translations[locale]?.footer`.

NOTE: no `src/components/Footer.test.jsx` exists — Footer.test.ts assertions are authored fresh from Footer.jsx source (there is no assertion-count baseline to diff).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Skill.astro and Skill.test.ts (Container API parity), remove old RTL test</name>
  <files>src/components/astro/Skill.astro, src/components/astro/Skill.test.ts, src/components/Skill.test.jsx</files>
  <read_first>
    - src/components/Skill.jsx (source markup, pick(), maxYears(), MeterRow/Category sub-components)
    - src/components/Skill.test.jsx (8 it-blocks to port for parity)
    - src/components/astro/About.test.ts (Container API pattern from Plan 23-01)
    - src/data/skills.json (data shape + schema-sanity assertions — do NOT modify)
  </read_first>
  <behavior>
    - Skill.astro renders `<section id="skills">` with label 'Skills'/'Skills', h2 'Technical stack'/'Stack técnico', intro, and 4 category cards.
    - Renders all 30 chips (each with its label) across 4 categories; each chip tile carries `data-core="true"` (6 total) or `data-core="false"`.
    - Test EN: result contains 'Skills','Technical stack', matches /Tools and technologies/; contains 'Backend','Cloud & Infrastructure','DevOps & Tools','AI & Productivity'; every chip label present; matches /18\s*y/.
    - Test ES: result contains 'Stack técnico','Nube e Infraestructura','DevOps y Herramientas','IA y Productividad', matches /Herramientas y tecnologías/.
    - Test data-core count: (result.match(/data-core="true"/g) || []).length === 6.
    - Test schema sanity: skills.json has 4 categories, each with bilingual title + non-empty chips; Kotlin backend chip has years 8 and core true.
  </behavior>
  <action>
    Create `src/components/astro/Skill.astro`: frontmatter `import data from '../../data/skills.json'`, `const { locale } = Astro.props`, copy `pick(field, lang)` (Skill.jsx lines 5-8) and `maxYears()` (lines 10-12) verbatim, `const suffix = pick(data.yearsSuffix, locale)`, `const max = maxYears()`. Port the JSX return (Skill.jsx lines 52-67) to Astro template: `className`→`class`, drop `key={...}`, inline the `MeterRow` and `Category` markup (lines 14-45) into `.map()` blocks in the template (Astro components cannot be called as plain JS functions the way React sub-components were), preserve the `data-core={chip.core ? 'true' : 'false'}` attribute and every Tailwind class byte-identical, keep the `style={\`width: ${fill}%\`}` inline width. Then create `src/components/astro/Skill.test.ts` with `// @vitest-environment node` as literal line 1, importing the Container API harness (mirror About.test.ts), rendering with `{ props: { locale } }`, porting all 8 it-blocks from Skill.test.jsx as `expect(result).toContain(...)` / `toMatch(...)` and data-only schema assertions. Delete `src/components/Skill.test.jsx`.
  </action>
  <verify>
    <automated>npx vitest run src/components/astro/Skill.test.ts && test ! -f src/components/Skill.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/astro/Skill.test.ts` passes with >= 8 it-blocks GREEN.
    - Line 1 of Skill.test.ts is exactly `// @vitest-environment node`.
    - `rg -c "useState|useEffect|client:|set:html" src/components/astro/Skill.astro` returns 0.
    - `src/components/Skill.test.jsx` no longer exists.
  </acceptance_criteria>
  <done>Skill renders bilingual content from skills.json at build time, zero JS, with a parity Container API test (D-01, D-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Footer.astro (translations + social array + build-time year) and Footer.test.ts (fresh assertions)</name>
  <files>src/components/astro/Footer.astro, src/components/astro/Footer.test.ts</files>
  <read_first>
    - src/components/Footer.jsx (source markup, hardcoded social array, year read)
    - src/layouts/BaseLayout.astro (translations[locale] resolution idiom, lines 8-17)
    - src/i18n/translations.js (footer subtree lines 50-53 EN / 103-106 ES)
    - src/components/astro/About.test.ts (Container API pattern)
  </read_first>
  <behavior>
    - Footer.astro renders a `<footer>` landmark with the `<a href="#hero">` brand link, both social `<a>` links, tagline, rights, and the copyright year.
    - Both social links render with `href` and `aria-label` (GitHub → github.com/andresmontoyat, aria-label="GitHub"; LinkedIn → linkedin.com/..., aria-label="LinkedIn") and their visible label text 'gh'/'in'.
    - EN result contains "I write code. Let's build something amazing together." and 'All rights reserved'.
    - ES result contains 'Escribo código. Construyamos algo increíble juntos.' and 'Todos los derechos reservados'.
    - Result contains the current year (String(new Date().getFullYear())) in the copyright line.
  </behavior>
  <action>
    Create `src/components/astro/Footer.astro`: frontmatter `import translations from '../../i18n/translations.js'`, `const { locale } = Astro.props`, `const footerCopy = translations[locale]?.footer` (BaseLayout idiom), `const year = new Date().getFullYear()` (D-03 build-time read), and copy the `social` array (Footer.jsx lines 4-7) verbatim as a module-level const in the frontmatter (D-02). Port the JSX return (Footer.jsx lines 12-43) to Astro template: `className`→`class`, drop `key={s.name}`, replace `{t.footer.tagline}`→`{footerCopy.tagline}` and `{t.footer.rights}`→`{footerCopy.rights}`, keep the `social.map(...)` loop and every Tailwind class byte-identical, keep `target="_blank" rel="noreferrer" aria-label={s.name}` on each social anchor. No pick() resolver needed (footer strings are already flat per-locale). Then create `src/components/astro/Footer.test.ts` with `// @vitest-environment node` as literal line 1; author fresh assertions per `<behavior>` above (no RTL baseline exists) — render EN and ES via `{ props: { locale } }`, assert the `<footer` landmark, both social hrefs + aria-labels, both taglines/rights strings, and the current year.
  </action>
  <verify>
    <automated>npx vitest run src/components/astro/Footer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/astro/Footer.test.ts` passes (>= 5 it-blocks: footer landmark, social links + aria-labels, EN copy, ES copy, current year).
    - Line 1 of Footer.test.ts is exactly `// @vitest-environment node`.
    - `rg -c "useState|useEffect|client:|useLanguage|set:html" src/components/astro/Footer.astro` returns 0.
    - `rg -c "getFullYear" src/components/astro/Footer.astro` returns 1 (build-time year read present, D-03).
  </acceptance_criteria>
  <done>Footer renders bilingual copy from translations + verbatim social array + build-time year, zero JS, with fresh Container API test (D-02, D-03, D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time data → rendered HTML | `skills.json` and `translations.js` (committed, not user input) interpolated at build time; outbound `<a href>` to github.com/linkedin.com |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Tampering (XSS) | Skill/Footer `{...}` interpolation | mitigate | Astro auto-escapes all `{expr}` output; no `set:html` (grep-gated in acceptance). Content from committed skills.json/translations.js, not runtime input. |
| T-23-02-02 | Information disclosure | reverse-tabnabbing on Footer social links | mitigate | Social `<a target="_blank">` carry `rel="noreferrer"` verbatim from Footer.jsx (preserved in port). |
| T-23-02-SC | Tampering (supply chain) | npm installs | accept | No package installs in this plan — no new dependency surface. |
</threat_model>

<verification>
- `npm run build` compiles Skill.astro + Footer.astro cleanly.
- `npx vitest run src/components/astro/Skill.test.ts` and `Footer.test.ts` GREEN.
- Full `npx vitest run` GREEN (Skill.test.jsx removal introduces no orphaned imports).
</verification>

<success_criteria>
- Skill and Footer render bilingual content at build time with zero client JS (STATIC-01).
- Skill has a parity Container API test; Footer has a fresh Container API test authored from source (TEST-01).
</success_criteria>

<output>
Create `.planning/phases/23-static-content-sections/23-02-SUMMARY.md` when done.
</output>
