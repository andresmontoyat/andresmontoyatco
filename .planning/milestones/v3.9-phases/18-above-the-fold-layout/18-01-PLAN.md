---
phase: 18-above-the-fold-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/GameMode.js
  - src/game/SkillFilters.js
  - src/game/ConstellationFallback.js
  - src/game/GameMode.test.js
autonomous: true
requirements: [POLISH-01]

must_haves:
  truths:
    - "Visitor landing on game mode at 1024px wide sees ≥1 SVG/WebGL constellation node within the first viewport without scroll"
    - "Visitor landing at 768px (tablet portrait) sees the constellation above the fold"
    - "Visitor landing at 390px (iPhone 14) sees the constellation above the fold — filters bar is repositioned to bottom, not above"
    - "SkillFilters chips, year slider, and reset button remain operable on all 3 viewports (not hidden behind a toggle)"
    - "ExperienceCard mobile bottom-sheet still overlays the new fixed filter bar (z-index discipline preserved)"
    - "Lighthouse mobile HARD gate (Perf ≥95 / A11y 100 / BP 100 / SEO 100) does not regress"
    - "All 253 existing tests still pass; new layout-positioning tests pass"
  artifacts:
    - path: "src/game/GameMode.js"
      provides: "Above-the-fold layout — compacted H1, sub-copy relocated to sr-only, renderer slot fills remaining viewport"
      contains: "flex-1 min-h-0"
    - path: "src/game/SkillFilters.js"
      provides: "Filter bar repositioned as fixed bottom bar"
      contains: "fixed bottom-0"
    - path: "src/game/ConstellationFallback.js"
      provides: "Sub-copy injected into existing sr-only block (SEO + a11y preserved)"
      contains: "sr-only"
    - path: "src/game/GameMode.test.js"
      provides: "Layout-positioning assertions (RED first, then GREEN)"
      contains: "toHaveClass"
  key_links:
    - from: "src/game/GameMode.js (section root)"
      to: "renderer-slot wrapper"
      via: "section.min-h-screen.flex.flex-col → wrapper.flex-1.min-h-0"
      pattern: "flex-1.*min-h-0"
    - from: "src/game/SkillFilters.js root (line 207)"
      to: "viewport bottom edge"
      via: "fixed bottom-0 left-0 right-0 positioning"
      pattern: "fixed\\s+bottom-0\\s+left-0\\s+right-0"
    - from: "src/game/ConstellationFallback.js"
      to: "screen readers + SEO crawlers"
      via: "sr-only <p> with t.game.subCopy text"
      pattern: "sr-only.*subCopy|subCopy.*sr-only"
---

<objective>
Restructure game-mode above-the-fold layout so the constellation is visible without scroll on every standard viewport (desktop ≥1024px, tablet ~768px, mobile ~390px). Pure layout/positioning change — no new components, no new translation keys, no new design tokens, no new deps.

Purpose: POLISH-01 — first real post-v3.8 session surfaced that on desktop the filter bar pushes the constellation below the fold; the H1+sub-copy block + in-flow SkillFilters together consume ~50–60% of the 768px viewport height before the renderer slot mounts. Recruiters land on filters instead of the constellation, defeating the whole "stop them mid-scroll" core value.

Output: Single MVP slice — RED test → GREEN restructure across 3 source files + 1 test file → verification. 4 files touched, ~30–50 LOC net change, 0 new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-above-the-fold-layout/18-CONTEXT.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-CONTEXT.md
@.planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md
@.planning/phases/17-webgl-desktop-renderer-lighthouse-gate/17-CONTEXT.md

# Source files touched by this plan
@src/game/GameMode.js
@src/game/SkillFilters.js
@src/game/ConstellationFallback.js
@src/game/GameMode.test.js

# ExperienceCard — read-only verification (already uses z-[200], well above planned z-30 bar)
@src/game/ExperienceCard.js

<interfaces>
<!-- Key contracts the executor needs. Extracted from the touched files. -->
<!-- No exploration required — these are the only contracts in play. -->

From src/game/GameMode.js (current shape):
- `<section id="game-mode" className="min-h-screen flex flex-col items-center px-6 pt-12 pb-8">` — root; already has flex-col + min-h-screen, only children change.
- H1 container `<div className="text-center mb-8 md:mb-12 max-w-2xl">` — margin shrinks to `mb-4`.
- H1 className `text-2xl md:text-4xl font-bold text-text-primary leading-tight mb-3` — becomes `text-xl md:text-2xl font-bold text-text-primary leading-tight mb-1`.
- Sub-copy `<p className="text-base text-text-secondary leading-relaxed">{t.game.subCopy}</p>` — DELETED from visible region (moved into ConstellationFallback).
- `<SkillFilters ... />` — sits between H1 block and renderer slot in current DOM. Stays in same DOM position (D-18-STACKING: single DOM order, no per-viewport branching); its rendered position becomes `fixed bottom-0` so it visually overlays the bottom edge.
- Renderer-slot wrapper `<div data-game-interactive className="w-full max-w-3xl relative" data-testid="renderer-slot" data-theme={theme} data-renderer={capability}>` — className becomes `w-full max-w-3xl relative flex-1 min-h-0 pb-20 md:pb-24`. data-game-interactive + data-testid + data-theme + data-renderer all preserved (Phase 16 + 17 contracts intact).
- `<ConstellationFallback experiences={EXPERIENCE} lang={lang} t={t} />` — already mounted as sr-only sibling.

From src/game/SkillFilters.js (current shape — line 202–208):
- Root `<div role="group" aria-label={t.game.filterBarLabel} data-game-interactive className="w-full max-w-3xl bg-ink-900/80 backdrop-blur-sm border-t border-ink-600 px-4 py-4 flex flex-wrap items-center gap-4 rounded-b-xl">`
- Becomes: `<div role="group" aria-label={t.game.filterBarLabel} data-game-interactive className="fixed bottom-0 left-0 right-0 bg-ink-900/95 backdrop-blur-sm border-t border-ink-600 px-4 py-3 flex flex-wrap items-center gap-3 z-30">`
- Changes: drop `w-full max-w-3xl rounded-b-xl` (incompatible with full-width fixed); bump bg opacity `/80 → /95` (matches Nav backdrop); `py-4 gap-4 → py-3 gap-3` (shrink vertical footprint); add `fixed bottom-0 left-0 right-0 z-30`. role/aria-label/data-game-interactive preserved verbatim.

From src/game/ConstellationFallback.js (current shape):
- `<section aria-labelledby="constellation-fallback-heading" className="sr-only">` wraps `<h2>` + `<ol>`.
- Add `<p>{t.game.subCopy}</p>` immediately AFTER the `<h2>` and BEFORE the `<ol>` (inside the existing sr-only section, so it inherits the sr-only class — no className needed on the `<p>`).

From src/game/ExperienceCard.js (verification only — no change):
- Card root className already includes `z-[200]` (line 107) — well above filter bar z-30. No z-index regression possible.
- Overlay backdrop uses `z-[199]`. Stack: constellation (z-auto) < bar (z-30) < Nav (z-40 existing) < overlay (z-199) < card (z-200). Verified clean.

Tailwind utilities used (all stock, no config changes):
- `fixed`, `bottom-0`, `left-0`, `right-0` — positioning
- `z-30` — bar (z-50/100/200 already in use elsewhere; 30 sits cleanly between auto and Nav z-40)
- `flex-1`, `min-h-0` — slot fills remaining flex-col space
- `pb-20` (5rem ≈ 80px desktop) / `pb-24` (6rem ≈ 96px mobile) — breathing room above bar
- `text-xl`, `md:text-2xl`, `mb-1`, `mb-4` — H1 compaction
- `bg-ink-900/95`, `backdrop-blur-sm`, `border-t`, `border-ink-600` — existing design tokens
</interfaces>

<phase-context-decisions>
LOCKED decisions from 18-CONTEXT.md (NON-NEGOTIABLE — implement exactly as specified):
- D-18-BAR-POS: SkillFilters → fixed bottom-0 left-0 right-0 + bg-ink-900/95 backdrop-blur-sm border-t border-ink-600 z-30. Same on mobile + desktop.
- D-18-H1-SIZE: H1 → text-xl md:text-2xl + mb-1; container margin → mb-4.
- D-18-SUBCOPY-RELOC: Sub-copy injected inside ConstellationFallback sr-only block; REMOVED from visible region.
- D-18-SLOT-H: Renderer-slot wrapper gains flex-1 min-h-0 + pb-20 md:pb-24.
- D-18-STACKING: Single DOM order all viewports (no md:flex-col-reverse etc.).

Claude's Discretion (finalized for this plan):
- D-18-Z-INDEX-MAP: filter bar z-30 (chosen — between auto and Nav z-40, leaves z-40 for Nav untouched). ExperienceCard z-[200] (existing, verified — no centralization in this phase; keep deferred).
- D-18-BOTTOM-PADDING: pb-20 desktop / pb-24 mobile (matches recommendation; gives constellation visual breathing room above the bar).
- D-18-FOCUS-MGMT: NO tabindex changes needed. Reading order = DOM order = H1 → filter bar (visually bottom but logically next in DOM) → renderer slot → ConstellationFallback (sr-only). Tab order follows DOM; users tabbing from H1 land on first filter chip — natural for "configure then explore" mental model. FpsCounter is dev-only (`import.meta.env.DEV`) — tree-shaken from prod, no a11y/layout concern.

Carry-forward contracts MUST NOT regress:
- D-15-LAND-COPY: H1 derives from live data (`${yearsActive} ${t.game.h1Years}. ${skillCount} ${t.game.h1Skills}. ${t.game.h1Tagline}`) — preserve verbatim, only className changes.
- D-16-CLICK-OUTSIDE-RENDERER: data-game-interactive attribute stays on BOTH SkillFilters root AND renderer-slot wrapper.
- D-16-CARD-STRUCTURE: ExperienceCard mobile bottom-sheet (`fixed bottom-0 ... z-[200]`) — verify still overlays filter bar.
- D-17-CLICK-OUTSIDE-RENDERER: renderer-slot wrapper attributes (data-game-interactive, data-testid, data-theme, data-renderer) all preserved.
- ConstellationFallback contract (Phase 15): sr-only section with h2 + ol of experiences — add sub-copy <p> WITHOUT touching the existing h2 or ol.
</phase-context-decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Extend GameMode.test.js with layout-positioning assertions</name>
  <read_first>
    - .planning/phases/18-above-the-fold-layout/18-CONTEXT.md §"<decisions>" (D-18-BAR-POS, D-18-H1-SIZE, D-18-SUBCOPY-RELOC, D-18-SLOT-H, D-18-STACKING)
    - src/game/GameMode.test.js (lines 1–290 — existing 18 it() blocks + Phase 17 capability mock — extend, do not replace)
    - src/game/GameMode.js (lines 93–155 — current section structure to assert against)
    - src/game/SkillFilters.js (line 202–208 — root container className to assert)
    - src/game/ConstellationFallback.js (lines 6–24 — sr-only section to assert sub-copy injection into)
  </read_first>
  <files>src/game/GameMode.test.js</files>
  <behavior>
    Append a new `describe('GameMode - Phase 18 above-the-fold layout (RED)')` block to the end of the existing file (after the Phase 17 SC-5 describe). Five new `it()` cases — all MUST fail before Task 2 (proving the test actually exercises the layout, not just mocks):
    - Test 1: section root has `min-h-screen` and `flex` and `flex-col` classes (no regression — passes today, locks the contract).
    - Test 2: renderer-slot wrapper has `flex-1` AND `min-h-0` classes (FAILS today — wrapper only has `w-full max-w-3xl relative`).
    - Test 3: H1 has `text-xl` AND `md:text-2xl` classes; H1 does NOT have `text-2xl` (without `md:` prefix) and does NOT have `md:text-4xl` (FAILS today — H1 has `text-2xl md:text-4xl`).
    - Test 4: SkillFilters root (queried by role=group + filterBarLabel) has `fixed`, `bottom-0`, `left-0`, `right-0`, and `z-30` classes; does NOT have `rounded-b-xl` class (FAILS today — current root has `rounded-b-xl` and no fixed positioning).
    - Test 5: ConstellationFallback sr-only section contains a `<p>` whose textContent equals `translations.en.game.subCopy` (FAILS today — sub-copy lives in visible region, not in fallback).
    - Test 6 (regression guard): visible region (the section, excluding the sr-only fallback) does NOT contain the sub-copy text — query the section by id="game-mode", filter out the sr-only descendant, assert textContent does not include `translations.en.game.subCopy` (FAILS after GREEN if sub-copy stays in visible region; PASSES after GREEN moves it to fallback).

    Use existing `renderWithProviders`, existing `screen`, `container.querySelector`, and `translations` imports already present in the file. No new test utilities. Default capability stays 'svg' (existing default) — these tests don't depend on WebGL path.
  </behavior>
  <action>
    Append the new describe block at the end of src/game/GameMode.test.js (after the closing brace of `describe('GameMode - Phase 17 SC-5 capability-based renderer selection', ...)` at line ~290). The block MUST:
    1. Import nothing new — `translations` already imported on line 11.
    2. Use `renderWithProviders(<GameMode />, { lang: 'en' })` for each test (matches existing pattern).
    3. For className assertions, use `expect(element).toHaveClass('flex-1', 'min-h-0')` (jest-dom matcher — already configured via @testing-library/jest-dom in setup).
    4. For Test 4, query the filter bar via `screen.getByRole('group', { name: translations.en.game.filterBarLabel })` (mirrors existing Phase 16 test on line 137).
    5. For Test 5 + 6, use `container.querySelector('section[aria-labelledby="constellation-fallback-heading"]')` to scope to the sr-only fallback section; use `document.getElementById('game-mode')` for the visible section.
    6. Run `npm run test:run -- src/game/GameMode.test.js` after writing — expect exit code ≠ 0 with 5 of the 6 new tests failing (Test 1 passes since it asserts current contract). If all 6 pass, the tests are not exercising the right thing — fix before Task 2.
    7. Commit with message: `test(18-01): RED — add layout-positioning assertions for above-the-fold restructure`. Use existing commit conventions (no semicolons noted in CLAUDE.md but commit messages are prose, not code).
  </action>
  <verify>
    <automated>npm run test:run -- src/game/GameMode.test.js 2>&1 | rg -e "(FAIL|✓|✗)" | head -40 ; npm run test:run -- src/game/GameMode.test.js 2>&1 | tail -10</automated>
    Expect: 5 of the 6 new tests FAIL (Test 1 passes — it's a regression-guard for current state). Existing 18 tests still pass (no regression from test additions alone). Exit code ≠ 0.
  </verify>
  <done>
    GameMode.test.js gains a `describe('GameMode - Phase 18 above-the-fold layout (RED)')` block with 6 `it()` cases. `npm run test:run -- src/game/GameMode.test.js` exits non-zero with at least 5 failures matching the new test names. RED commit pushed. No source files (GameMode.js, SkillFilters.js, ConstellationFallback.js) modified yet.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Apply layout restructure across GameMode.js + SkillFilters.js + ConstellationFallback.js</name>
  <read_first>
    - .planning/phases/18-above-the-fold-layout/18-CONTEXT.md §"<decisions>" + §"<specifics>"
    - src/game/GameMode.js (lines 93–155 — section structure + H1 + slot wrapper)
    - src/game/SkillFilters.js (line 202–208 — root container only; internal layout untouched)
    - src/game/ConstellationFallback.js (lines 6–24 — sr-only section to inject sub-copy into)
    - src/game/ExperienceCard.js (line 107 — confirm z-[200] still holds; READ-ONLY verification, no edit)
  </read_first>
  <files>src/game/GameMode.js, src/game/SkillFilters.js, src/game/ConstellationFallback.js</files>
  <behavior>
    After this task, all 6 Phase 18 tests added in Task 1 MUST pass, AND all 253 pre-existing tests MUST still pass (no regression). Specifically:
    - Renderer-slot wrapper exposes `flex-1 min-h-0 pb-20 md:pb-24` classes alongside existing `w-full max-w-3xl relative` and all data-* attributes.
    - H1 className becomes `text-xl md:text-2xl font-bold text-text-primary leading-tight mb-1` (drops `text-2xl md:text-4xl` and `mb-3`).
    - H1 container becomes `text-center mb-4 max-w-2xl` (drops `mb-8 md:mb-12`).
    - SkillFilters root className becomes `fixed bottom-0 left-0 right-0 bg-ink-900/95 backdrop-blur-sm border-t border-ink-600 px-4 py-3 flex flex-wrap items-center gap-3 z-30` (drops `w-full max-w-3xl rounded-b-xl py-4 gap-4`, bumps bg opacity, adds positioning). role/aria-label/data-game-interactive preserved verbatim.
    - ConstellationFallback gains `<p>{t.game.subCopy}</p>` inside its existing sr-only section, after `<h2>` and before `<ol>`. No new className (inherits sr-only from parent section).
    - Visible region no longer contains `t.game.subCopy` text.
    - All Phase 15/16/17 contracts intact: data-game-interactive on both filter bar root AND renderer-slot wrapper; data-testid="renderer-slot"; data-theme; data-renderer; ConstellationFallback's existing h2 + ol untouched.
  </behavior>
  <action>
    Apply three coordinated edits — execute in order, but commit as a single atomic GREEN commit at the end.

    **Edit 1 — src/game/GameMode.js (lines 94–100, 118–124):**
    - Line 95: change `<div className="text-center mb-8 md:mb-12 max-w-2xl">` to `<div className="text-center mb-4 max-w-2xl">`.
    - Line 96: change `<h1 className="text-2xl md:text-4xl font-bold text-text-primary leading-tight mb-3">` to `<h1 className="text-xl md:text-2xl font-bold text-text-primary leading-tight mb-1">`.
    - Line 99: DELETE the entire line `<p className="text-base text-text-secondary leading-relaxed">{t.game.subCopy}</p>`.
    - Line 120: change `className="w-full max-w-3xl relative"` to `className="w-full max-w-3xl relative flex-1 min-h-0 pb-20 md:pb-24"`.
    - Do NOT touch: section root className (already `min-h-screen flex flex-col items-center px-6 pt-12 pb-8` — correct), data-* attributes on the slot wrapper, RendererErrorBoundary wrapping, Suspense+lazy WebGL boundary, ExperienceCard mount, ConstellationFallback mount, FpsCounter dev-only mount.

    **Edit 2 — src/game/SkillFilters.js (line 207):**
    - Replace line 207 className entirely.
    - FROM: `className="w-full max-w-3xl bg-ink-900/80 backdrop-blur-sm border-t border-ink-600 px-4 py-4 flex flex-wrap items-center gap-4 rounded-b-xl"`
    - TO:   `className="fixed bottom-0 left-0 right-0 bg-ink-900/95 backdrop-blur-sm border-t border-ink-600 px-4 py-3 flex flex-wrap items-center gap-3 z-30"`
    - Do NOT touch: role="group", aria-label={t.game.filterBarLabel}, data-game-interactive, internal SkillChips/CategoryChips/YearRangeSlider/ResetButton children. Internal flex layout (gap, flex-wrap, items-center) preserved with shrunk gap (4→3).

    **Edit 3 — src/game/ConstellationFallback.js (lines 7–9):**
    - Insert one new line between the existing `<h2>` (line 8) and the existing `<ol>` (line 9).
    - New line: `<p>{t.game.subCopy}</p>`
    - Resulting block:
      ```
      <section aria-labelledby="constellation-fallback-heading" className="sr-only">
        <h2 id="constellation-fallback-heading">{t.game.fallbackHeading}</h2>
        <p>{t.game.subCopy}</p>
        <ol>
          ...
      ```
    - The new `<p>` inherits `sr-only` from the parent section's className — no className on the `<p>` itself.

    **No-edit verification — src/game/ExperienceCard.js (line 107):**
    - Confirm via rg that ExperienceCard root className still contains `z-[200]` and overlay contains `z-[199]`. Both are above filter bar z-30 → no overlay regression. NO edit to this file.

    **After all three edits:**
    - Run `npm run test:run -- src/game/GameMode.test.js` — expect ALL tests pass (the 6 new Phase 18 tests now GREEN, plus the 18 pre-existing GameMode tests still GREEN).
    - Run full `npm run test:run` — expect 259 total tests pass (253 pre-existing + 6 new), 0 failures.
    - Commit with message: `feat(18-01): GREEN — above-the-fold restructure (compact H1, sub-copy → sr-only, fixed bottom filter bar, flex-1 renderer slot)`.
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | tail -15 ; rg -n "flex-1\s+min-h-0" src/game/GameMode.js ; rg -n "fixed\s+bottom-0\s+left-0\s+right-0" src/game/SkillFilters.js ; rg -n "text-xl\s+md:text-2xl" src/game/GameMode.js ; rg -n "z-\[200\]" src/game/ExperienceCard.js ; rg -n "subCopy" src/game/ConstellationFallback.js ; rg -c "subCopy" src/game/GameMode.js | grep -v '^#' || echo "GameMode no longer references subCopy in visible region"</automated>
    Expect: full test suite exit 0 with 259 tests passing; rg finds `flex-1 min-h-0` in GameMode.js (1 match on slot wrapper); rg finds `fixed bottom-0 left-0 right-0` in SkillFilters.js (1 match on line ~207); rg finds `text-xl md:text-2xl` in GameMode.js (1 match on H1); rg finds `z-[200]` in ExperienceCard.js (preserved); rg finds `subCopy` in ConstellationFallback.js (1 match on new <p>); rg finds 0 matches for `subCopy` in GameMode.js (removed from visible region).
  </verify>
  <done>
    GameMode.js, SkillFilters.js, ConstellationFallback.js modified per Edit 1/2/3 above. ExperienceCard.js untouched but z-[200] re-confirmed via rg. Full test suite passes (259/259). GREEN commit pushed. All Phase 15/16/17 contracts (data-game-interactive on both bar + slot, ConstellationFallback h2+ol shape, H1 live-data derivation, renderer-slot data-* attributes) intact via existing tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bundle gate + lint + write SUMMARY</name>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY.md template)
    - .planning/phases/17-webgl-desktop-renderer-lighthouse-gate/17-05-SUMMARY.md (most recent SUMMARY for tone/structure reference, if it exists; otherwise the template above is sufficient)
  </read_first>
  <files>.planning/phases/18-above-the-fold-layout/18-01-SUMMARY.md</files>
  <action>
    Final verification + SUMMARY. NO source code changes in this task — only verification commands + SUMMARY authorship.

    1. Run `npm run lint` and confirm exit 0 (no new lint errors introduced by Task 2's className changes). If lint fails on max-len or class-string formatting, fix in src/ then re-run; do NOT disable rules.
    2. Run `npm run build:gate` and confirm:
       - Vite build succeeds (exit 0 from `vite build`).
       - Bundle-gate script (`scripts/check-bundle-gate.mjs`) exits 0 — mobile GameMode chunk should still be ≤ 38.82 kB gz (current 8.91 kB; layout-only changes add ~0 kB). Capture the gz-size line from script output for the SUMMARY.
    3. Manual viewport spot-check (NON-BLOCKING for this task — flagged in SUMMARY as UAT carry-over):
       - Run `npm run dev` in background, open http://localhost:5173.
       - At browser widths 1024×768, 768×1024, 390×844: visually confirm constellation has ≥1 visible node above the fold without scroll.
       - Visually confirm filter bar sits at bottom; chips/slider/reset still operable.
       - Visually confirm clicking a node opens ExperienceCard above the filter bar (no z-index clash).
       - Kill the dev server.
       - Note results in SUMMARY (pass/fail per viewport).
    4. Lighthouse mobile re-verification — DEFERRED to milestone-close UAT. Note in SUMMARY: "Lighthouse mobile HARD gate re-verify is a v3.9 milestone-close concern (POLISH-01 SC-5 + REQUIREMENTS.md constraints), not per-plan. Layout-only changes should add 0 kB to mobile chunk (verified via build:gate above)."
    5. Write `.planning/phases/18-above-the-fold-layout/18-01-SUMMARY.md` following the template. MUST include:
       - **Files modified:** src/game/GameMode.js, src/game/SkillFilters.js, src/game/ConstellationFallback.js, src/game/GameMode.test.js (4 files).
       - **Files verified unchanged:** src/game/ExperienceCard.js (z-[200] preserved — no clash with new z-30 bar).
       - **Tests:** 259/259 pass (253 pre-existing + 6 new Phase 18 layout-positioning tests).
       - **Bundle:** Mobile chunk gz (capture from build:gate output).
       - **Decisions applied:** D-18-BAR-POS, D-18-H1-SIZE, D-18-SUBCOPY-RELOC, D-18-SLOT-H, D-18-STACKING (all locked); D-18-Z-INDEX-MAP (bar z-30, ExperienceCard z-[200] preserved), D-18-BOTTOM-PADDING (pb-20 desktop / pb-24 mobile), D-18-FOCUS-MGMT (no tabindex changes — DOM order = reading order) (all discretion zones resolved).
       - **Contracts preserved:** D-15-LAND-COPY (H1 derivation untouched), D-16-CLICK-OUTSIDE-RENDERER (data-game-interactive on both bar + slot), D-16-CARD-STRUCTURE (ExperienceCard z-[200] still overlays bar z-30), D-17-CLICK-OUTSIDE-RENDERER (renderer-slot data-* attrs intact).
       - **POLISH-01 success criteria status:** SC-1..SC-4 verified via tests + manual viewport spot-check (record results); SC-5 (Lighthouse mobile HARD gate non-regression) flagged for milestone-close UAT.
       - **Carry-forward to Phase 19:** None — Phase 18 standalone; Phase 19 depends on this restructure for its motion work.
    6. Commit with message: `docs(18-01): SUMMARY — above-the-fold layout shipped; POLISH-01 SC-1..SC-4 verified; Lighthouse re-verify flagged for milestone close`.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -5 ; npm run build:gate 2>&1 | tail -20 ; ls -la .planning/phases/18-above-the-fold-layout/18-01-SUMMARY.md</automated>
    Expect: lint exit 0; build:gate exit 0 with mobile chunk gz size reported and ≤ 38.82 kB; SUMMARY file exists.
  </verify>
  <done>
    `npm run lint` exits 0. `npm run build:gate` exits 0; mobile chunk gz recorded in SUMMARY (expect ~9 kB, unchanged from baseline). 18-01-SUMMARY.md written with all sections above. Manual viewport spot-check results recorded in SUMMARY. Lighthouse re-verify explicitly deferred to milestone close (POLISH-01 SC-5). SUMMARY commit pushed.
  </done>
</task>

</tasks>

<verification>
- All 6 new Phase 18 layout-positioning tests pass (Task 1 RED → Task 2 GREEN).
- All 253 pre-existing tests still pass (no regression).
- `rg` confirms each layout change is present in the expected file (Task 2 verify block).
- `npm run lint` exits 0 (Task 3).
- `npm run build:gate` exits 0; mobile chunk gz ≤ 38.82 kB ceiling (Task 3).
- Manual viewport spot-check at 1024×768, 768×1024, 390×844 confirms constellation visible above the fold on all 3 viewports (Task 3 — recorded in SUMMARY).
- ExperienceCard `z-[200]` preserved; clicking a node opens card that overlays filter bar (Task 3 spot-check + existing E16 test).
- ConstellationFallback sr-only block now contains both h2 heading AND sub-copy text — SEO + screen-reader contract intact (Task 1 Test 5 + Task 2 GREEN).
- Phase 16 D-16-CLICK-OUTSIDE-RENDERER preserved: existing test on GameMode.test.js line 156–184 ("click-outside discrimination") still passes — data-game-interactive on both filter bar root AND renderer-slot wrapper.
- Phase 17 D-17-CLICK-OUTSIDE-RENDERER preserved: renderer-slot wrapper retains data-game-interactive + data-testid + data-theme + data-renderer (existing tests on lines 217–227 cover this).
</verification>

<success_criteria>
This plan is complete when:
1. All 5 of POLISH-01 ROADMAP success criteria are satisfied:
   - SC-1: ≥1 visible node within first viewport at 1024×768 desktop — verified via manual spot-check.
   - SC-2: ≥1 visible node within first viewport at 768×1024 tablet — verified via manual spot-check.
   - SC-3: ≥1 visible node within first viewport at 390×844 mobile — verified via manual spot-check.
   - SC-4: Filter bar remains discoverable + operable on all 3 viewports — verified via existing Phase 16 filter tests + manual spot-check (chips clickable, slider keyboard-operable, reset enabled when filters active).
   - SC-5: No regression in Lighthouse mobile HARD gate or 253 tests — tests verified (259/259 pass); Lighthouse formally re-verified at v3.9 milestone close (SUMMARY notes this).
2. POLISH-01 traceability: this plan ships POLISH-01 in its entirety (no follow-on plans needed for Phase 18; the only POLISH-01 lever is layout, fully addressed here).
3. v3.8 Phase 14–17 contracts (D-15-LAND-COPY, D-16-CLICK-OUTSIDE-RENDERER, D-16-CARD-STRUCTURE, D-17-CLICK-OUTSIDE-RENDERER) preserved — verified via existing test suite passing.
4. Bundle budget preserved: mobile chunk gz ≤ 38.82 kB ceiling (build:gate exit 0).
5. SUMMARY.md committed to git; STATE.md update + ROADMAP Phase 18 closure handled by /gsd:close-phase at milestone close (not this plan's responsibility).
</success_criteria>

<output>
Create `.planning/phases/18-above-the-fold-layout/18-01-SUMMARY.md` when done (Task 3).
</output>
