---
phase: 02-shell-hero
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/translations.js
autonomous: true
requirements:
  - I18N-01
  - SEO-04
user_setup: []

must_haves:
  truths:
    - "translations.js exports nav.menuOpen in both en and es"
    - "translations.js exports nav.menuClose in both en and es"
    - "translations.js exports meta.title and meta.description in both en and es"
    - "All other existing keys preserved unchanged"
  artifacts:
    - path: "src/i18n/translations.js"
      provides: "Bilingual copy with menuOpen/menuClose for mobile menu a11y and meta.title/description for per-language head tags"
      contains: "menuOpen"
  key_links:
    - from: "Nav.js mobile hamburger button (Plan 02-04)"
      to: "t.nav.menuOpen / t.nav.menuClose"
      via: "useLanguage() hook lookup"
      pattern: "t\\.nav\\.menu(Open|Close)"
    - from: "LanguageContext useEffect (Plan 02-02)"
      to: "translations[lang].meta.title and meta.description"
      via: "DOM writes to document.title and meta[name=description]"
      pattern: "translations\\[[^\\]]+\\]\\.meta"
---

<objective>
Add the four bilingual key sets that downstream Phase 2 plans depend on:
- `nav.menuOpen` / `nav.menuClose` — mobile menu aria-labels (consumed by Plan 02-04)
- `meta.title` / `meta.description` — per-language head tag content (consumed by Plan 02-02 LanguageContext useEffect)

Purpose: Centralizing all translation additions in one tiny plan makes Wave 1 a single-file edit, keeps the diff trivial, and avoids file-conflict serialization with Plan 02-02 (which only touches LanguageContext.js).
Output: 8 new string values (4 keys × 2 languages) in `src/i18n/translations.js`. No structural changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shell-hero/02-CONTEXT.md
@.planning/phases/02-shell-hero/02-UI-SPEC.md
@src/i18n/translations.js
@index.html

<interfaces>
<!-- Existing translations.js shape (no schema change — only additive keys) -->

```js
const translations = {
  en: {
    nav:  { about, skills, experience, contact /* + new: menuOpen, menuClose */ },
    meta: { /* NEW namespace: title, description */ },
    hero: { ... },
    /* other namespaces unchanged */
  },
  es: { /* same shape */ }
}
export default translations
```

Existing access patterns the new keys must support:
```js
const { t } = useLanguage()
t.nav.menuOpen   // "Open menu" (en) | "Abrir menú" (es)
t.nav.menuClose  // "Close"     (en) | "Cerrar"     (es)

translations[lang].meta.title       // for document.title sync (Plan 02-02)
translations[lang].meta.description // for meta[name=description] sync (Plan 02-02)
```

<!-- Existing index.html (project root, NOT public/) — DO NOT MODIFY -->
- `<html lang="en">` (line 2)
- `<title>Carlos Andrés Montoya Tobón · Solutions Architect &amp; Senior Backend Engineer</title>` (line 14)
- `<meta name="description" content="Carlos Andrés Montoya Tobón — Solutions Architect and Senior Backend Engineer with 18+ years of experience in Java, Spring, microservices and cloud." />` (lines 8-11)

The EN values below MUST exactly match the current index.html content (so first-paint EN content is identical for English users — no churn).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nav.menuOpen, nav.menuClose, and meta namespace to translations.js</name>
  <read_first>
    - src/i18n/translations.js (full file — note existing nav block at lines 3-8 (en) and 76-81 (es); en block ends at line 73, es block ends at line 147)
    - .planning/phases/02-shell-hero/02-UI-SPEC.md (Copywriting Contract table — exact strings for menuOpen/menuClose; Mobile Menu Overlay section — confirms aria-label-only usage)
    - .planning/phases/02-shell-hero/02-CONTEXT.md (D-07 locks the menuOpen/menuClose keys; Claude's Discretion permits per-language title/meta)
    - index.html (lines 8-14 — exact EN strings to mirror in en.meta)
  </read_first>
  <files>src/i18n/translations.js</files>
  <action>
    Make TWO additive edits inside the `en` block AND TWO inside the `es` block. No reordering, no deletions.

    === EN BLOCK (`en: { ... }`) ===

    1. Inside `en.nav`, AFTER `contact: 'Contact',` add:
    ```
        menuOpen: 'Open menu',
        menuClose: 'Close',
    ```

    2. After the closing brace of `en.nav` (i.e., between `nav` and `hero` blocks), ADD a new `meta` namespace:
    ```
        meta: {
          title: 'Carlos Andrés Montoya Tobón · Solutions Architect & Senior Backend Engineer',
          description: 'Carlos Andrés Montoya Tobón — Solutions Architect and Senior Backend Engineer with 18+ years of experience in Java, Spring, microservices and cloud.',
        },
    ```

    === ES BLOCK (`es: { ... }`) ===

    3. Inside `es.nav`, AFTER `contact: 'Contacto',` add:
    ```
        menuOpen: 'Abrir menú',
        menuClose: 'Cerrar',
    ```

    4. After the closing brace of `es.nav`, ADD:
    ```
        meta: {
          title: 'Carlos Andrés Montoya Tobón · Arquitecto de Soluciones e Ingeniero Backend Senior',
          description: 'Carlos Andrés Montoya Tobón — Arquitecto de Soluciones e Ingeniero Backend Senior con +18 años de experiencia en Java, Spring, microservicios y cloud.',
        },
    ```

    Constraints:
    - Use the literal `&` character (not `&amp;`) — these are JS strings, not HTML
    - 2-space indent for the `nav` / `meta` properties; 4-space indent for nested keys (matches existing file structure observed at lines 4-8)
    - Trailing commas on every property (matches existing file style)
    - NO semicolons (matches the airbnb override `semi: 0`)
    - DO NOT touch any other key (hero, stats, about, skills, exp, contact, footer all unchanged)
    - DO NOT delete `hero.cta2` ("Download CV" / "Descargar CV") even though Plan 02-05 retires it — leave that decision to Plan 02-05 to keep diffs surgical
    - The `&` and `—` (em-dash) characters MUST be preserved as UTF-8 in the EN meta strings (these match index.html)
  </action>
  <verify>
    <automated>grep -E "menuOpen: 'Open menu'" src/i18n/translations.js &amp;&amp; grep -E "menuOpen: 'Abrir menú'" src/i18n/translations.js &amp;&amp; grep -E "menuClose: 'Close'" src/i18n/translations.js &amp;&amp; grep -E "menuClose: 'Cerrar'" src/i18n/translations.js &amp;&amp; grep -c "meta: {" src/i18n/translations.js &amp;&amp; grep -E "Solutions Architect &amp; Senior Backend Engineer" src/i18n/translations.js &amp;&amp; grep -E "Arquitecto de Soluciones e Ingeniero Backend Senior" src/i18n/translations.js &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "menuOpen" src/i18n/translations.js` returns 2
    - `grep -c "menuClose" src/i18n/translations.js` returns 2
    - `grep -E "menuOpen: 'Open menu'" src/i18n/translations.js` matches exactly once
    - `grep -E "menuOpen: 'Abrir menú'" src/i18n/translations.js` matches exactly once
    - `grep -E "menuClose: 'Close'" src/i18n/translations.js` matches exactly once
    - `grep -E "menuClose: 'Cerrar'" src/i18n/translations.js` matches exactly once
    - `grep -c "meta: {" src/i18n/translations.js` returns 2 (one en, one es)
    - `grep -E "Solutions Architect & Senior Backend Engineer" src/i18n/translations.js` matches exactly once
    - `grep -E "Arquitecto de Soluciones e Ingeniero Backend Senior" src/i18n/translations.js` matches exactly once
    - `grep -E "with 18\+ years of experience in Java" src/i18n/translations.js` matches exactly once
    - `grep -E "con \+18 años de experiencia en Java" src/i18n/translations.js` matches exactly once
    - All previously existing nav keys still present: `grep -c "about: '" src/i18n/translations.js` returns at least 2 (en+es), `grep -c "experience: '" src/i18n/translations.js` returns at least 2
    - Existing `hero.cta2` still present: `grep -E "cta2: 'Download CV'" src/i18n/translations.js` and `grep -E "cta2: 'Descargar CV'" src/i18n/translations.js` each match once
    - `npm run build` exits 0 (proves the file still parses)
  </acceptance_criteria>
  <done>Both languages expose `t.nav.menuOpen`, `t.nav.menuClose`, `t.meta.title`, `t.meta.description`; existing translations untouched; production build succeeds.</done>
</task>

</tasks>

<verification>
- File parses as a valid JS module (Vite build succeeds)
- All 8 new strings present at the exact expected paths
- No regression on existing keys (grep counts above)
</verification>

<success_criteria>
`npm run build` succeeds AND grep finds all 8 new strings (4 nav additions + 4 meta strings) AND no existing keys removed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-shell-hero/02-01-SUMMARY.md` with:
- What changed (translations.js only)
- Lines/keys added (`nav.menuOpen`, `nav.menuClose`, full `meta` namespace × 2 languages)
- Confirmation `npm run build` passed
</output>
