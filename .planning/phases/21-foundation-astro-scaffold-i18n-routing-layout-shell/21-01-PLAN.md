---
phase: 21-foundation-astro-scaffold-i18n-routing-layout-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - vitest.config.ts
  - vite.config.js
autonomous: true
requirements: [ROUTE-01, DEPLOY-01, DEPLOY-03]
must_haves:
  truths:
    - "astro and @astrojs/react are installed; three is no longer a dependency"
    - "package.json pins engines.node >=22.12.0"
    - "npm run dev/build/preview invoke astro, not vite"
    - "astro:i18n is configured for symmetric /en + /es trees with prefixDefaultLocale true"
    - "The existing Vitest suite still runs green under Astro's getViteConfig wiring"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Astro build config with astro:i18n and react integration"
      contains: "prefixDefaultLocale"
    - path: "vitest.config.ts"
      provides: "Vitest config via astro/config getViteConfig, preserving setupFiles"
      contains: "getViteConfig"
    - path: "package.json"
      provides: "Astro deps, engines.node pin, astro scripts, three removed"
      contains: "\"astro\""
  key_links:
    - from: "vitest.config.ts"
      to: "src/test/setup.jsx"
      via: "test.setupFiles array"
      pattern: "setupFiles.*setup\\.jsx"
    - from: "astro.config.mjs"
      to: "@astrojs/react"
      via: "integrations: [react()]"
      pattern: "integrations.*react"
---

<objective>
Stand up the Astro build toolchain that every later Phase 21 plan depends on: install `astro` + `@astrojs/react`, remove the unused `three` dependency, pin `engines.node`, swap the `dev`/`build`/`preview` scripts to Astro, author `astro.config.mjs` with `astro:i18n` (symmetric `/en`/`/es`), and move the Vitest `test` block into a dedicated `vitest.config.ts` using Astro's `getViteConfig()` so the existing React test suite keeps running.

Purpose: Nothing else in this phase can build, render, or test until Astro is the build tool. This plan is the foundation the route tree, BaseLayout, middleware, and 404 all sit on.
Output: A repo that builds with `astro build`, tests with `vitest`, has `astro:i18n` configured, and carries no `three` dependency (DEPLOY-01) with `engines.node >=22.12.0` (DEPLOY-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-RESEARCH.md
@.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-PATTERNS.md

<interfaces>
<!-- Exact target syntax lives in 21-RESEARCH.md. Executor must use it verbatim: -->
<!-- astro.config.mjs: 21-RESEARCH.md § "astro.config.mjs — exact syntax" (lines ~135-153) -->
<!-- vitest.config.ts: 21-RESEARCH.md § "vitest.config.ts wiring" (lines ~326-337) -->
<!-- package.json engines: 21-RESEARCH.md § "package.json — exact engines.node syntax" (lines ~378-386) -->
<!-- Package legitimacy audit (all 4 packages [OK]): 21-RESEARCH.md § "Package Legitimacy Audit" -->

Current package.json scripts (to modify — from live read):
  "dev": "vite"            → "astro dev"
  "build": "vite build"    → "astro build"
  "preview": "vite preview"→ "astro preview"
  "test": "vitest"         → unchanged (Vitest is preserved)
  "test:run": "vitest run" → unchanged
  "coverage": "vitest run --coverage" → unchanged

Current vite.config.js `test` block (to migrate verbatim into vitest.config.ts):
  environment: 'jsdom'
  environmentOptions: { jsdom: { url: 'http://localhost/' } }
  globals: true
  setupFiles: ['./src/test/setup.jsx']   ← LOAD-BEARING, must survive
  coverage: { provider: 'v8', reporter: ['text','lcov'],
              include: ['src/**/*.{js,jsx}'], exclude: ['src/test/**','src/index.jsx'] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Astro deps, remove three, pin engines.node, swap core scripts</name>
  <files>package.json</files>
  <read_first>
    - package.json (current deps, scripts, no engines field today)
    - .planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-RESEARCH.md § "Package Legitimacy Audit" and § "Installation" and § "package.json — exact engines.node syntax"
  </read_first>
  <action>
    Run `npm install astro @astrojs/react` then `npm install --save-dev @types/react @types/react-dom` then `npm uninstall three` (DEPLOY-01 — `three@^0.169.0` is confirmed unused in `src/`). All four installed packages passed slopcheck `[OK]` per 21-RESEARCH.md Package Legitimacy Audit — no legitimacy checkpoint required. Add a new top-level `"engines": { "node": ">=22.12.0" }` block as a sibling of `"dependencies"` (DEPLOY-03), matching the file's 2-space JSON indent. In `"scripts"`, change `"dev"` to `astro dev`, `"build"` to `astro build`, `"preview"` to `astro preview`. Leave `"test"`, `"test:run"`, `"coverage"`, `"lint"`, `"og:gen"`, `"hero:process"`, `"cv:gen"` unchanged. Note in the SUMMARY that `build:analyze`, `build:gate`, and the `lighthouse:*` scripts still reference `vite`/`stats.html` and are now stale — they are out of Phase 21 scope and are flagged for the Phase 27 cleanup; do NOT rewrite them here.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.dependencies.three) throw new Error('three still present'); if(!p.dependencies.astro||!p.dependencies['@astrojs/react']) throw new Error('astro deps missing'); if(p.engines.node!=='>=22.12.0') throw new Error('engines.node wrong'); if(p.scripts.build!=='astro build'||p.scripts.dev!=='astro dev'||p.scripts.preview!=='astro preview') throw new Error('scripts not swapped'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `require('./package.json').dependencies.three` is undefined (DEPLOY-01).
    - `dependencies.astro` and `dependencies['@astrojs/react']` are present; `devDependencies['@types/react']` and `['@types/react-dom']` present.
    - `engines.node === '>=22.12.0'` (DEPLOY-03).
    - `scripts.dev === 'astro dev'`, `scripts.build === 'astro build'`, `scripts.preview === 'astro preview'`.
    - `scripts.test` unchanged (`vitest`).
    - `package-lock.json` updated (install ran for real, not hand-edited).
  </acceptance_criteria>
  <done>Astro deps installed, three removed, engines pinned, core scripts on astro; the node -e assertion above prints OK.</done>
</task>

<task type="auto">
  <name>Task 2: Author astro.config.mjs with astro:i18n and react integration</name>
  <files>astro.config.mjs</files>
  <read_first>
    - .planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-RESEARCH.md § "astro.config.mjs — exact syntax for this phase"
    - vite.config.js (existing ESM defineConfig + no-semicolon style to match)
  </read_first>
  <action>
    Create `astro.config.mjs` at repo root using the exact block from 21-RESEARCH.md: `import { defineConfig } from 'astro/config'`, `import react from '@astrojs/react'`, and `export default defineConfig({ output: 'static', integrations: [react()], i18n: { locales: ['en','es'], defaultLocale: 'en', routing: { prefixDefaultLocale: true, redirectToDefaultLocale: false } } })`. `prefixDefaultLocale: true` is required so both `/en/*` and `/es/*` trees exist with no bare default-locale tree (ROUTE-01). `redirectToDefaultLocale: false` is set explicitly to document that the Vercel Edge Middleware (Plan 21-03) owns `/`, not Astro's i18n redirect. Follow the repo's no-semicolon, single-quote style (matches `vite.config.js`). Do NOT set a `site` field yet — the canonical base-URL wiring is resolved in Plan 21-02 (env-var `PUBLIC_SITE_URL`, D-06).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('astro.config.mjs','utf8'); ['prefixDefaultLocale: true','redirectToDefaultLocale: false',\"locales: ['en', 'es']\".replace(/ /g,'')].forEach(t=>{ if(!s.replace(/\s+/g,'').includes(t.replace(/\s+/g,''))) throw new Error('missing '+t) }); if(!s.includes('@astrojs/react')) throw new Error('react integration missing'); console.log('OK')" && npx astro sync</automated>
  </verify>
  <acceptance_criteria>
    - `astro.config.mjs` exists and exports `defineConfig` with `i18n.locales` = `['en','es']`, `i18n.defaultLocale` = `'en'`.
    - `i18n.routing.prefixDefaultLocale === true` and `redirectToDefaultLocale === false` present in source.
    - `integrations` includes `react()`.
    - `npx astro sync` completes without error (validates the config parses under the installed Astro version).
  </acceptance_criteria>
  <done>astro.config.mjs present with the i18n block; `npx astro sync` exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Move Vitest config to vitest.config.ts via getViteConfig; retire vite.config.js</name>
  <files>vitest.config.ts, vite.config.js</files>
  <read_first>
    - vite.config.js (full — the `test` block being extracted, and the `@` alias + visualizer being dropped)
    - src/test/setup.jsx (LOAD-BEARING setup — must remain referenced)
    - .planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-RESEARCH.md § "vitest.config.ts wiring"
    - .planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-PATTERNS.md § "vitest.config.ts"
  </read_first>
  <action>
    Create `vitest.config.ts` with `/// <reference types="vitest/config" />`, `import { getViteConfig } from 'astro/config'`, and `export default getViteConfig({ test: { ... } })`. Inside `test`, copy the current `vite.config.js` `test` block verbatim: `environment: 'jsdom'`, `environmentOptions: { jsdom: { url: 'http://localhost/' } }`, `globals: true`, `setupFiles: ['./src/test/setup.jsx']` (this file bridges Node-22 localStorage/sessionStorage into jsdom and stubs `HTMLCanvasElement.getContext` — every existing component test depends on it, do NOT drop it), and the `coverage` object unchanged. Then delete `vite.config.js` (Astro owns the Vite pipeline now via `astro.config.mjs`; the `rollup-plugin-visualizer` and `@` alias are not needed this phase — the `build:analyze` script that used the visualizer is already flagged stale in Task 1). `.ts` may use semicolons per Astro/TS convention (author's discretion, per 21-RESEARCH.md style note).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('vitest.config.ts','utf8'); if(!s.includes('getViteConfig')) throw new Error('no getViteConfig'); if(!s.includes('./src/test/setup.jsx')) throw new Error('setupFiles dropped'); const fs=require('fs'); if(fs.existsSync('vite.config.js')) throw new Error('vite.config.js not removed'); console.log('OK')" && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` exists, uses `getViteConfig` from `astro/config`, and its `test.setupFiles` includes `./src/test/setup.jsx`.
    - `test.coverage.include` = `['src/**/*.{js,jsx}']` preserved.
    - `vite.config.js` no longer exists.
    - `npx vitest run` passes the full existing suite (57 tests GREEN baseline per STATE.md) with zero failures.
  </acceptance_criteria>
  <done>Vitest runs green through vitest.config.ts; vite.config.js removed; setup.jsx still wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Third-party packages enter the build via `npm install` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-SC | Tampering | npm installs (`astro`, `@astrojs/react`, `@types/react`, `@types/react-dom`) | mitigate | All 4 packages pre-verified `[OK]` by slopcheck in 21-RESEARCH.md Package Legitimacy Audit (official withastro / DefinitelyTyped repos, years-old); no `[ASSUMED]`/`[SUS]`/`[SLOP]` packages → no blocking human checkpoint required |
| T-21-01-02 | Denial of Service | build tooling swap | accept | Config-only change; if `astro sync`/`vitest` fail they fail loudly at build time, no runtime exposure — no user-facing surface in this plan |
</threat_model>

<verification>
- `npx astro sync` exits 0 (config valid).
- `npx vitest run` full suite GREEN (no regression from the 57-test baseline).
- `three` absent from `package.json`; `engines.node` pinned; core scripts on `astro`.
</verification>

<success_criteria>
Astro is the build tool, `astro:i18n` is configured for `/en`+`/es`, the Vitest suite runs unchanged through `vitest.config.ts`, `three` is gone (DEPLOY-01), and `engines.node >=22.12.0` is pinned (DEPLOY-03).
</success_criteria>

<output>
Create `.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-01-SUMMARY.md` when done
</output>
