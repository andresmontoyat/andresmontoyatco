# v3.11 Mario-World — Real-Browser UAT Checklist

**Status:** PENDING operator sign-off. Auto-gates already cleared (see below).

## Automated gates (passing as of Phase 24 close)

- Test suite: **150/150 GREEN** (`npm test -- --run`)
- Bundle gate: **exit 0**
  - `index-*.js` = 66.49 kB gz (WARN band 50–90 kB)
  - `WebGLWorldMap-*.js` = 116.50 kB gz (INFO; under 125 soft, under 130 HARD)
- Lighthouse mobile HARD gate: **PASS**
  - Performance: 0.97 (≥ 0.95)
  - Accessibility: 1.00 (= 1.00)
  - Best Practices: 1.00 (= 1.00)
  - SEO: 1.00 (= 1.00)
  - Target-size audit: 1 (= 1)

## Manual UAT — operator sweep

Run a real-browser pass on Chrome stable (macOS) and at least one mobile
real device (iPhone or Android). Mark PASS / FAIL inline; populate notes
below.

| # | Scenario | Result | Notes |
|---|----------|--------|-------|
| 1 | Hero gate first paint shows both buttons; `Enter the world` / `Skip to CV` route correctly | | |
| 2 | Game view: WebGL canvas mounts; 5 biome backdrops visible; world sprites placed | | |
| 3 | Arrow keys walk the avatar; bbox clamp holds at edges | | |
| 4 | Drag (mouse / touch) pans the camera offset | | |
| 5 | Click world → cinematic 600 ms zoom-in → overlay appears with correct content | | |
| 6 | Esc closes overlay → 400 ms zoom-out → map intact at last avatar position | | |
| 7 | Type a registered secret command anywhere (after operator adds entries to `secret-worlds.js`) → hidden world appears in its biome; Backspace / Escape clear the buffer | | |
| 8 | Reduced-motion users land in SvgWorldMap; keyboard tab + arrow navigation works | | |
| 9 | Mobile drag + tap functional on real device | | |
| 10 | WebGL context loss (DevTools `Lose context` extension) falls back to SvgWorldMap silently | | |
| 11 | Bilingual switch (EN ↔ ES) propagates everywhere (Hero, DevView, overlays, SecretCommandHint) | | |
| 12 | Dev view: classic scroll; Hero + sections + Experience + Footer present | | |

## Sign-off

- [ ] Operator reviewed all 12 items above; result column populated.
- [ ] Tag `v3.11.0` cut on signed-off commit.
- [ ] Branch `v3.11-mario-world` pushed and merged into `main`.

## Deferred / carried debt

- High-fidelity sprite art (OPEN-ART-1 in `.planning/seeds/SEED-MARIO-WORLD-ART.md`) — placeholders shipping.
- `SECRET_WORLDS` catalog ships empty; operator adds entries as narratives are written.
- v3.9 + v3.10 carried UAT items absorbed into this sweep.
