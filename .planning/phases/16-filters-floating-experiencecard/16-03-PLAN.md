---
phase: 16-filters-floating-experiencecard
plan: 03
type: execute
wave: 2
depends_on: ["16-02"]
files_modified:
  - src/game/useConstellation.js
  - src/game/renderers/SvgConstellation.js
autonomous: true
requirements: [GAME-03, GAME-04]
tags: [phase-16, hook-extension, renderer-consumer, tdd-green]
must_haves:
  truths:
    - "useConstellation owns filter state — selectedSkills, yearRange, category — and exposes toggleSkill / setYearRange / setCategory / resetFilters / isFilterActive setters"
    - "highlightedSkillIds is DERIVED via composeFilters + visibleSkillIds whenever filter state changes (no stale memoization, Pitfall 7 guarded)"
    - "yearBounds is computed once at module load from live EXPERIENCE data and exposed via the hook (D-16-YEAR-BOUNDS honesty rule)"
    - "SvgConstellation consumes highlightedSkillIds[] to dim non-matching nodes at fill-opacity 0.35 (D-16-INTERSECT-HIGHLIGHT, reuses Phase 15 dim treatment)"
    - "SvgConstellation consumes yearRange to dim nodes whose .years field does not intersect [from, to] (D-16-YEAR-EFFECT)"
    - "Edges incident to dimmed nodes also dim regardless of weight when any filter is active"
    - "Phase 15 single-select dim behavior is unchanged — extending, not replacing"
    - "Reduced-motion path skips the fill-opacity transition (existing motion-safe pattern preserved)"
  artifacts:
    - path: "src/game/useConstellation.js"
      provides: "Extended hook with full Phase 16 filter state"
      contains: "toggleSkill"
    - path: "src/game/renderers/SvgConstellation.js"
      provides: "Renderer with highlightedSkillIds + yearRange consumer logic"
      contains: "shouldDimNode"
  key_links:
    - from: "src/game/useConstellation.js"
      to: "src/game/filters.js"
      via: "named import of composeFilters, visibleSkillIds, yearBounds"
      pattern: "from './filters"
    - from: "src/game/renderers/SvgConstellation.js"
      to: "highlightedSkillIds, yearRange props"
      via: "render-loop dim logic on each node circle + edge line"
      pattern: "highlightedSkillIds"
---

<objective>
Wire the pure-logic foundation into runtime state + renderer behavior: extend useConstellation with full filter state ownership and extend SvgConstellation to dim nodes/edges based on highlightedSkillIds + yearRange.

Purpose: After this plan, the constellation visualization responds to filter state — even without the SkillFilters UI shipping yet, the useConstellation hook tests are GREEN and the renderer tests assert correct dim behavior. UI bar (Plan 04) and dialog (Plan 05) can be developed against a working state surface.

Output: 2 extended files. Wave 0 hook + renderer RED suites flip GREEN. Phase 15 single-select dim still works (Phase 16 is additive, not replacing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md
@.planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md
@.planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md
@.planning/phases/16-filters-floating-experiencecard/16-UI-SPEC.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-02-SUMMARY.md
@.planning/phases/15-accessible-constellation-seo-fallback/15-03-SUMMARY.md
@src/game/filters.js
@src/game/useConstellation.js
@src/game/useConstellation.test.js
@src/game/renderers/SvgConstellation.js
@src/game/renderers/SvgConstellation.test.js
@src/data/experience.js
@src/data/skills.js

<interfaces>
<!-- Current Phase 15 useConstellation (src/game/useConstellation.js) — placeholders for Phase 16 -->
const [highlightedSkillIds] = useState([])   // line 8 — replace with derived useMemo
const [yearRange] = useState(null)           // line 9 — replace with [state, setter]

Phase 16 final return object:
{
  // Phase 15 (preserved):
  selectedSkillId, hoveredSkillId, onSelectSkill, onHoverSkill,
  // Phase 16 (new):
  selectedSkills,       // string[] — toggled via toggleSkill
  yearRange,            // [from, to] | null — set via setYearRange
  category,             // string | null — set via setCategory
  yearBounds,           // [2007, 2026] — module-derived, never changes
  highlightedSkillIds,  // string[] — derived from composeFilters + visibleSkillIds
  isFilterActive,       // boolean — true when any filter set
  toggleSkill(id),
  setYearRange(range | null),
  setCategory(key | null),
  resetFilters(),
}

<!-- Current Phase 15 SvgConstellation dim logic (line 224) -->
const fillOpacity = selectedSkillId !== null && !isSelected ? 0.35 : 1

<!-- Phase 16 extended dim logic — must preserve Phase 15 behavior as a fallback -->
function nodeMatchesYearRange(node, yearRange)
function shouldDimNode(node, { selectedSkillId, highlightedSkillIds, yearRange })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend useConstellation.js with full Phase 16 filter state + derived highlightedSkillIds</name>
  <files>src/game/useConstellation.js</files>
  <read_first>
    - src/game/useConstellation.js (existing Phase 15 hook — lines 1-27; placeholders at lines 8-9 to REPLACE)
    - src/game/useConstellation.test.js (RED tests from Plan 01 — the contract: toggleSkill / setYearRange / setCategory / resetFilters / isFilterActive / highlightedSkillIds derivation)
    - src/game/filters.js (composeFilters, visibleSkillIds, yearBounds — newly available from Plan 02)
    - src/data/experience.js (EXPERIENCE source for yearBounds derivation)
    - src/data/skills.js (SKILLS map needed by composeFilters)
    - src/game/GameMode.js lines 13-16 (Phase 15 module-level YEAR_BOUNDS derivation pattern to mirror)
    - .planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md §1 (filters.js contracts), §"Pitfall 7" (stale memo guard)
    - .planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md Wave 2 useConstellation.js section (lines 570-657)
    - .planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md (D-16-INTERSECT-AND, D-16-PERSIST-MEMORY — in-memory state only, no localStorage)
  </read_first>
  <behavior>
    - Replace Phase 15 placeholders: `const [highlightedSkillIds] = useState([])` → derived useMemo; `const [yearRange] = useState(null)` → [state, setter] tuple
    - Add 3 useState hooks: selectedSkills (string[] default []), yearRange ([from,to]|null default null), category (string|null default null)
    - Add 4 useCallback setters: toggleSkill (push/remove from selectedSkills array), setYearRange (state setter passthrough), setCategory (state setter passthrough), resetFilters (clears all 3 to defaults)
    - Add 1 useMemo deriving highlightedSkillIds: when no filter active returns []; otherwise calls composeFilters(EXPERIENCE, {skillIds: selectedSkills, yearRange, category}, SKILLS) → visibleSkillIds
    - Add module-level YEAR_BOUNDS = yearBounds(EXPERIENCE) once at module load (mirrors GameMode.js Phase 15 pattern)
    - Expose isFilterActive boolean (selectedSkills.length > 0 || yearRange !== null || category !== null)
    - Final useMemo on the returned object includes ALL filter dependencies in deps array (Pitfall 7 guard)
    - NO localStorage writes — D-16-PERSIST-MEMORY mandates in-memory only
    - Phase 15 selectedSkillId / hoveredSkillId / onSelectSkill / onHoverSkill UNCHANGED
  </behavior>
  <action>EDIT src/game/useConstellation.js. Add imports at top: `import EXPERIENCE from '../data/experience.js'`; `import { SKILLS } from '../data/skills.js'`; `import { composeFilters, visibleSkillIds, yearBounds } from './filters.js'`. Add module-level constant `const YEAR_BOUNDS = yearBounds(EXPERIENCE)` BEFORE the hook function (per GameMode.js line 14-16 pattern). Inside the hook function: REMOVE the Phase 15 frozen placeholders (lines 8-9). ADD three useState hooks for selectedSkills/yearRange/category. ADD useCallback setters per the implementation in 16-PATTERNS.md lines 609-622 (toggleSkill toggles inclusion via prev.includes ? prev.filter : [...prev, id]; resetFilters calls all three state setters). ADD `const highlightedSkillIds = useMemo(() => { if (selectedSkills.length === 0 && !yearRange && !category) return []; const matching = composeFilters(EXPERIENCE, { skillIds: selectedSkills, yearRange, category }, SKILLS); return visibleSkillIds(matching); }, [selectedSkills, yearRange, category])` — CRITICAL: include all three filter-state values in the deps array per Pitfall 7. ADD `const isFilterActive = selectedSkills.length > 0 || yearRange !== null || category !== null`. UPDATE the final useMemo return object to include all new fields (selectedSkills, yearRange, category, yearBounds: YEAR_BOUNDS, highlightedSkillIds, isFilterActive, toggleSkill, setYearRange, setCategory, resetFilters) and EXTEND its deps array to match (PATTERNS.md lines 633-652). Phase 15 fields (selectedSkillId, hoveredSkillId, onSelectSkill, onHoverSkill) remain. Per D-16-PERSIST-MEMORY: NO localStorage.setItem calls anywhere. No semicolons.</action>
  <verify>
    <automated>npx vitest run src/game/useConstellation.test.js 2>&amp;1 | tee /tmp/wave2-hook.log; grep -E "passed|failed" /tmp/wave2-hook.log | tail -3; grep -c "localStorage\\.setItem" src/game/useConstellation.js</automated>
  </verify>
  <acceptance_criteria>
    - src/game/useConstellation.js imports composeFilters, visibleSkillIds, yearBounds from './filters.js'
    - Module-level `const YEAR_BOUNDS = yearBounds(EXPERIENCE)` declared OUTSIDE the hook function
    - Hook declares useState for selectedSkills, yearRange, category (3 new state hooks)
    - Hook declares useCallback for toggleSkill, setYearRange, setCategory, resetFilters
    - toggleSkill body uses `prev.includes(id) ? prev.filter((s) => s !== id) : [...prev, id]` (AND-toggle semantics)
    - highlightedSkillIds is a useMemo (NOT useState) and its deps array contains exactly `[selectedSkills, yearRange, category]`
    - Return object includes selectedSkills, yearRange, category, yearBounds, highlightedSkillIds, isFilterActive, toggleSkill, setYearRange, setCategory, resetFilters (in addition to Phase 15 fields)
    - `grep -c "localStorage" src/game/useConstellation.js` returns 0 (D-16-PERSIST-MEMORY)
    - `npx vitest run src/game/useConstellation.test.js` exits 0 with all 18+ tests PASSING (Phase 15 10 + Phase 16 8+)
    - `npx vitest run` shows no regression on filters.test.js, spatialNav.test.js, constellation.graph.test.js, constellation.layout.test.js
  </acceptance_criteria>
  <done>useConstellation.js extended. Hook RED tests are now GREEN; Phase 15 tests remain GREEN. Commit: `feat(16): extend useConstellation with filter state + derived highlightedSkillIds`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend SvgConstellation.js to consume highlightedSkillIds + yearRange (dim logic)</name>
  <files>src/game/renderers/SvgConstellation.js</files>
  <read_first>
    - src/game/renderers/SvgConstellation.js (current dim logic at line 224; placeholder consumers at lines 72-74; edge logic lines 188-195; prefersReducedMotion line 81; transition pattern line 206)
    - src/game/renderers/SvgConstellation.test.js (new Phase 16 RED tests from Plan 01 — must turn GREEN)
    - src/game/constellation.graph.js (node shape — confirm `.years` field is `[start, end ?? CURRENT_YEAR]`; if not present, this task must surface that as a blocker for Plan 03 retry)
    - .planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md (D-16-YEAR-EFFECT, D-16-INTERSECT-HIGHLIGHT)
    - .planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md §"Architectural Responsibility Map" (renderer consumer responsibility)
    - .planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md Wave 2 SvgConstellation.js section (lines 660-722)
  </read_first>
  <behavior>
    - Add helper `nodeMatchesYearRange(node, yearRange)`: true when yearRange null OR node.years intersects [from, to]; uses node.years[0] <= to && node.years[1] >= from
    - Add helper `shouldDimNode(node, { selectedSkillId, highlightedSkillIds, yearRange })`: returns true when filters active AND node fails any criterion; falls back to Phase 15 single-select dim when no filters
    - Replace render-loop fillOpacity line (current line 224) with `const fillOpacity = shouldDimNode(node, {...}) ? 0.35 : 1`
    - Extend edge-render logic: when filters active, dim any edge whose source OR target is dimmed (multiply final opacity by 0.35) — D-16-YEAR-EFFECT explicit
    - Remove `// eslint-disable-line no-unused-vars` from highlightedSkillIds and yearRange prop declarations (lines 72, 74) — they ARE used now
    - Preserve Phase 15 transition pattern (line 206): `transition: prefersReducedMotion ? 'none' : 'fill-opacity 200ms ease-out'`
    - Preserve Phase 15 single-select behavior: when no Phase 16 filter is active, selectedSkillId dims non-selected per Phase 15 — additive logic only
    - Optional Phase 16 prop: justFilteredId (chip-flash trigger) — accept gracefully but DO NOT implement flash here (Wave 4+5 plumbing); just accept the prop to satisfy linting if Plan 06 passes it
  </behavior>
  <action>EDIT src/game/renderers/SvgConstellation.js. Remove the `eslint-disable-line no-unused-vars` comments on the highlightedSkillIds (line 72) and yearRange (line 74) prop declarations — they are now consumed. Add two pure helper functions at the module top (above the component) per 16-PATTERNS.md lines 673-693: `function nodeMatchesYearRange(node, yearRange)` and `function shouldDimNode(node, { selectedSkillId, highlightedSkillIds, yearRange })`. shouldDimNode logic: compute `filtersActive = (highlightedSkillIds?.length > 0) || yearRange !== null`; if filtersActive: dim when node is NOT in highlightedSkillIds (or highlight list empty) AND not in yearRange; if NOT filtersActive: fall back to Phase 15 single-select dim (`selectedSkillId !== null && node.id !== selectedSkillId`). In the render loop, replace the current fillOpacity computation (line 224) with `const fillOpacity = shouldDimNode(node, { selectedSkillId, highlightedSkillIds, yearRange }) ? 0.35 : 1`. In the edge render loop (around lines 188-195), add an additional dim multiplier when filtersActive AND (shouldDimNode(sourceNode) || shouldDimNode(targetNode)): final edge opacity = (existing computed opacity) × 0.35. Preserve the existing transition: prefersReducedMotion ? 'none' : ... pattern verbatim. Do NOT add chip-flash logic in this task (deferred — Plan 06 wires the trigger if needed). No semicolons.</action>
  <verify>
    <automated>npx vitest run src/game/renderers/SvgConstellation.test.js 2>&amp;1 | tee /tmp/wave2-renderer.log; grep -E "passed|failed" /tmp/wave2-renderer.log | tail -3; grep -c "shouldDimNode\\|nodeMatchesYearRange" src/game/renderers/SvgConstellation.js</automated>
  </verify>
  <acceptance_criteria>
    - src/game/renderers/SvgConstellation.js defines `function nodeMatchesYearRange` and `function shouldDimNode` (module-level helpers)
    - The `eslint-disable-line no-unused-vars` comments are removed from highlightedSkillIds (line ~72) and yearRange (line ~74) prop declarations
    - The render-loop fillOpacity expression uses shouldDimNode (`grep "shouldDimNode" src/game/renderers/SvgConstellation.js` returns ≥2 — one in helper definition, ≥1 in render loop)
    - Edge render code reduces opacity when both endpoints dim under active filters (search for `shouldDimNode` in edge rendering section)
    - `npx vitest run src/game/renderers/SvgConstellation.test.js` exits 0 with all 25+ tests PASSING (Phase 15 22 + Phase 16 3)
    - The Phase 15 "does not dim before selectedSkillId is set" test (line 122-129) still PASSES — Phase 16 is additive
    - The Phase 15 single-select dim test (whatever line) still PASSES — fallback path preserved
    - `npx vitest run` shows no regression on useConstellation.test.js (which is GREEN from Task 1)
    - Phase 15 transition-on-reduced-motion behavior preserved: `grep "prefersReducedMotion" src/game/renderers/SvgConstellation.js` still finds the fill-opacity transition guard
  </acceptance_criteria>
  <done>SvgConstellation.js extended. Renderer RED tests are GREEN; Phase 15 renderer tests remain GREEN. Commit: `feat(16): extend SvgConstellation to consume highlightedSkillIds + yearRange (dim logic)`</done>
</task>

</tasks>

<verification>
- `npx vitest run src/game/useConstellation.test.js src/game/renderers/SvgConstellation.test.js src/game/filters.test.js` — all GREEN
- `npx vitest run` — no regression across the full suite
- `npm run build` — exits 0; bundle size logged (no significant growth — hook + renderer extensions are < 1 kB gz)
- SkillFilters.test.js + ExperienceCard.test.js + GameMode.test.js (new tests) still RED — expected, waiting on Plans 04/05/06
</verification>

<success_criteria>
- useConstellation.js exposes the full Phase 16 filter state surface (10+ public fields, 4 new setters)
- SvgConstellation.js dims nodes/edges based on highlightedSkillIds + yearRange while preserving Phase 15 single-select behavior
- 2 atomic commits (one per task)
- The constellation now visually responds to filter state — even before SkillFilters UI ships
</success_criteria>

<output>
Create `.planning/phases/16-filters-floating-experiencecard/16-03-SUMMARY.md` — list new hook fields/setters, the helper functions in SvgConstellation, the dim threshold (0.35), and confirm Phase 15 single-select behavior unchanged.
</output>
