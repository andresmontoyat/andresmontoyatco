---
phase: 24-hero
plan: 05
type: execute
wave: 4
depends_on: [24-04]
files_modified: []
autonomous: false
requirements: [STATIC-02]

must_haves:
  truths:
    - "D-07: the CV dropdown's Escape-key close and outside-click close are verified by REAL manual QA in Chrome, Firefox, and Safari (minimum) — not by code review or automated grep/Container-API assertions alone, since native <details> Escape behavior was only ASSUMED by research (A1/A2), not confirmed from an authoritative source"
    - "D-05: on all three browsers the CV <details> opens/closes via clicking <summary> (native, zero JS) and both CV PDF download links work"
    - "D-01: on all three browsers the CSS steps() typewriter reveal on the H1 middle line completes without a visible half-character clip or trailing gap (Pitfall 1 / Press Start 2P ch-width check)"
    - "D-04: on all three browsers the count-up stats do not visibly start counting before Hero's 850ms entrance fade-in completes (if they do, count-up.js needs a fixed start offset per the D-04 seam left in Plan 24-01)"
    - "D-08: the hero photo paints as the LCP element at or better than v4.1's 2.1s baseline (no LCP regression)"
  artifacts:
    - path: ".planning/phases/24-hero/24-UAT.md"
      provides: "Recorded cross-browser QA results (Chrome/Firefox/Safari) for Escape, outside-click, typewriter, count-up timing, LCP"
  key_links:
    - from: "manual QA"
      to: "STATIC-02 success criterion #2 (Escape-key close cross-browser verified)"
      via: "human keypress + click test in 3 browsers"
      pattern: "Escape"
---

<objective>
Execute the mandatory manual cross-browser QA gate for the migrated Hero (D-07). The ROADMAP success criterion "Escape-key close cross-browser verified" and the Pattern-3 assumptions A1/A2 (native `<details>` does NOT close on Escape/outside-click without the enhancer) can only be discharged by a real human keypress/click test in Chrome, Firefox, and Safari — no automated check substitutes.

Purpose: Confirm the progressive-enhancement layer (details-dismiss.js) actually works cross-browser and the CSS typewriter / count-up timing / LCP read correctly on real engines before the phase closes.
Output: A completed `24-UAT.md` record; phase-close signal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-hero/24-RESEARCH.md
@.planning/phases/24-hero/24-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 24-UAT.md scaffold</name>
  <files>.planning/phases/24-hero/24-UAT.md</files>
  <read_first>
    - .planning/phases/24-hero/24-RESEARCH.md (Pitfalls 1-4, Assumptions A1/A2, Open Questions 1-2)
    - .planning/phases/24-hero/24-CONTEXT.md (D-07)
  </read_first>
  <action>Create .planning/phases/24-hero/24-UAT.md with a checklist table covering, per browser column (Chrome / Firefox / Safari), the rows: (1) CV dropdown opens/closes via clicking summary (native, D-05); (2) Escape key closes the open dropdown and returns focus to summary (D-07/D-06); (3) outside-click closes the open dropdown (D-06); (4) both CV PDF links download the correct file on /en and on /es; (5) H1 middle-line typewriter completes with no half-character clip/gap (Pitfall 1, D-01); (6) count-up stats do not start before the 850ms fade-in completes (D-04, Open Question 2); (7) hero photo is the visible LCP element, no regression vs v4.1 2.1s (D-08, run the site via `npm run build && npx serve dist` or `npm run preview`). Include an instructions block: how to launch the built site, that Safari-on-a-real-device or simulator is preferred but note it as a gap if unavailable, and a Notes section for observed defects + any count-up start-offset tuning needed.</action>
  <verify>
    <automated>test -f .planning/phases/24-hero/24-UAT.md && grep -c "Escape" .planning/phases/24-hero/24-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/24-hero/24-UAT.md exists with a 3-browser column layout and all 7 rows above
    - Includes launch instructions and a Notes section
  </acceptance_criteria>
  <done>24-UAT.md scaffold ready for the human QA pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Manual cross-browser QA — Escape/outside-click/typewriter/count-up/LCP (D-07)</name>
  <action>Manually execute the cross-browser QA sweep described in <how-to-verify> below across Chrome, Firefox, and Safari on both /en and /es, then record PASS/FAIL per row per browser in .planning/phases/24-hero/24-UAT.md. This is a human-driven verification of behaviors (Escape close, outside-click close, typewriter completion, count-up timing, LCP) that automated tests cannot cover per D-07.</action>
  <what-built>Hero.astro is live on /en and /es as a zero-island static section: native <details> CV dropdown (roles dropped), CSS steps() typewriter H1, IntersectionObserver count-up stats, and details-dismiss.js providing Escape + outside-click close. This checkpoint verifies the browser-dependent behaviors that research only ASSUMED (A1/A2) and that automated tests cannot cover.</what-built>
  <how-to-verify>
    1. Build and serve the production output: `npm run build` then `npx serve dist` (or `npm run preview`). Note the local URL.
    2. In EACH of Chrome, Firefox, and Safari, open both `/en` and `/es`:
       a. Click the "Download CV" summary — the dropdown opens; click it again — it closes (native, zero JS).
       b. Open the dropdown, press Escape — it MUST close and focus returns to the summary button (D-07 core criterion).
       c. Open the dropdown, click anywhere outside it — it MUST close.
       d. Click "English" and "Spanish" links — the correct CV PDF downloads.
       e. Watch the H1 middle line reveal — confirm no last-character clip or trailing gap after the animation settles (Pitfall 1).
       f. Watch the stats bar — confirm the numbers do NOT visibly count up before the stats block finishes fading in (~850ms). If they do, note it — count-up.js needs a fixed start offset added at the D-04 seam (small follow-up in Plan 24-01's file).
       g. Confirm the hero photo paints promptly as the largest element (LCP), no obvious regression vs the current v4.1 site.
    3. Record PASS/FAIL per row per browser in `.planning/phases/24-hero/24-UAT.md`. If Safari is unavailable on this machine, mark it explicitly as a gap (Chrome + Firefox minimum) rather than silently skipping.
  </how-to-verify>
  <resume-signal>Type "approved" once Escape-close + outside-click-close PASS in Chrome and Firefox (and Safari if available) and 24-UAT.md is filled in; or describe the failures (e.g. "Escape does not close in Firefox", "count-up starts during fade-in") so a fix follow-up can be planned.</resume-signal>
  <acceptance_criteria>
    - 24-UAT.md shows Escape-close and outside-click-close PASS in at least Chrome + Firefox (Safari PASS or explicitly marked as a gap)
    - CV PDF links verified downloading on both /en and /es
    - Typewriter completion + count-up timing + LCP observations recorded
  </acceptance_criteria>
  <done>Cross-browser QA complete and recorded; STATIC-02 success criterion #2 (Escape-key close cross-browser verified) discharged by real testing, not code review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard/pointer → <details> enhancer | details-dismiss.js reads only e.key and e.target identity; no data extracted or persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-01 | Repudiation / correctness | Escape/outside-click behavior assumed, not proven | mitigate | This entire plan IS the mitigation — real cross-browser manual QA converts assumptions A1/A2 into verified behavior before phase close |
| T-24-05-02 | Information Disclosure | Pitfall 3 — closed <details> panel links in tab order | mitigate | During QA, tab through the closed Hero section and confirm the CV links are NOT reachable before the LinkedIn/GitHub icons (native <details> excludes closed content from tab order) |
| T-24-05-SC | Tampering | npm installs | accept | Zero packages installed this plan |
</threat_model>

<verification>
- 24-UAT.md completed with per-browser results
- Escape-close + outside-click-close PASS (Chrome + Firefox minimum; Safari PASS or noted gap)
- No LCP regression observed
</verification>

<success_criteria>
- D-07 discharged by real manual cross-browser QA, recorded in 24-UAT.md
- STATIC-02 (Hero half) success criteria #2 met on real browser engines
- Any count-up-timing follow-up (D-04 offset) identified if needed
</success_criteria>

<output>
Create `.planning/phases/24-hero/24-05-SUMMARY.md` when done
</output>
