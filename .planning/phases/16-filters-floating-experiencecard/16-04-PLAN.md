---
phase: 16-filters-floating-experiencecard
plan: 04
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/game/SkillFilters.js
autonomous: true
requirements: [GAME-03]
tags: [phase-16, ui-component, filter-bar, dual-thumb-slider, wai-aria]
must_haves:
  truths:
    - "SkillFilters renders a chip cluster for all skills (toggle multi-select), 8 category chips, dual-thumb year-range slider, and reset button"
    - "Skill chips and category chips use aria-pressed to expose toggle state to assistive tech"
    - "Year-range slider is WAI-ARIA APG slider-multithumb compliant: two role='slider' buttons with dependent aria-valuemin/max, ArrowLeft/Right ±1 year, Home/End jump to bounds, e.preventDefault() prevents page scroll"
    - "Reset button is disabled (native + aria-disabled='true') when isFilterActive=false; enabled state triggers onReset; clears all 3 dimensions"
    - "Whole filter bar root carries data-game-interactive so click-outside-to-close on the card doesn't fire when interacting with filters (D-16-FLOW-CLOSE-EMPTY allow-list)"
    - "Bilingual labels via t.game.* keys — never inline EN/ES literals; category chip labels resolve via SKILL_CATEGORIES[k][lang]"
    - "All clickable elements have a 44×44px touch target via min-h-[44px] min-w-[44px] (WCAG 2.5.5)"
    - "AA contrast holds for chip states in both dark + light themes (per UI-SPEC color table)"
    - "Reduced-motion path: animation utilities are gated by motion-safe: prefix; existing global rule already suppresses durations"
  artifacts:
    - path: "src/game/SkillFilters.js"
      provides: "Filter UI bar with chips + dual-thumb slider + reset"
      contains: "role=\"slider\""
  key_links:
    - from: "src/game/SkillFilters.js"
      to: "props onToggleSkill, onYearRangeChange, onCategoryChange, onReset"
      via: "controlled-component pattern — all state owned by parent (useConstellation in GameMode)"
      pattern: "onToggleSkill\\(node.id\\)"
---

<objective>
Build the SkillFilters UI bar — chip clusters, dual-thumb year-range slider, reset button — entirely as a controlled component receiving state and callbacks via props.

Purpose: This is the single filter-input surface that maps GAME-03. The dual-thumb slider is the only widget without an in-repo analog; it follows the WAI-ARIA APG slider-multithumb pattern (two role="slider" buttons with dependent ranges). All other chips reuse the LangPill / ViewModeToggle pattern with aria-pressed. After this plan, the filter bar tests are GREEN even though it isn't yet wired into GameMode (Plan 06).

Output: 1 new file. SkillFilters.test.js RED → GREEN. Bundle impact estimate: ~3 kB gz.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md
@.planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md
@.planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md
@.planning/phases/16-filters-floating-experiencecard/16-UI-SPEC.md
@src/game/SkillFilters.test.js
@src/components/_shared/ViewModeToggle.js
@src/components/Nav.js
@src/i18n/translations.js
@src/data/skills.js
@tailwind.config.js
@src/index.css

<interfaces>
<!-- SkillFilters component props (from PATTERNS.md + RESEARCH.md) -->
SkillFilters({
  nodes,                   // [{ id, label, category, count, color, years }]  — full constellation node list (for skill chips)
  selectedSkills,          // string[]
  yearRange,               // [from, to] | null
  yearBounds,              // [2007, 2026]
  category,                // string | null
  isFilterActive,          // boolean
  onToggleSkill(id),
  onYearRangeChange([from, to]),
  onCategoryChange(key | null),
  onReset(),
  lang,                    // 'en' | 'es'
  t,                       // translations[lang]
})

<!-- SKILL_CATEGORIES shape (Phase 14 — confirmed in skills.js) -->
SKILL_CATEGORIES = {
  lang:     { en: 'Languages',         es: 'Lenguajes',         color: '#3B82F6' },
  cloud:    { en: 'Cloud',             es: 'Nube',              color: '...' },
  // ... 8 entries total
}

<!-- WAI-ARIA APG slider-multithumb contract (RESEARCH §2 lines 396-525) -->
- Each thumb: <button role="slider"> in tab sequence
- aria-valuemin, aria-valuemax, aria-valuenow per thumb
- DEPENDENT: start.aria-valuemax = end - 1; end.aria-valuemin = start + 1
- aria-valuetext for human-readable announcement
- ArrowRight/Up: +1; ArrowLeft/Down: -1; Home: aria-valuemin; End: aria-valuemax
- e.preventDefault() in every keyboard handler
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement SkillFilters.js — chip clusters + reset button + filter-bar layout</name>
  <files>src/game/SkillFilters.js</files>
  <read_first>
    - src/game/SkillFilters.test.js (RED tests from Plan 01 — the contract; chip toggle, slider keyboard, reset disabled, role=group with aria-label)
    - src/components/_shared/ViewModeToggle.js (analog: aria-pressed pill button — line 28 aria-pressed={viewMode === 'game'}; line 9 className with min-h-[44px] focus-visible:ring + motion-safe:transition-colors)
    - src/components/Nav.js LangPill lines 92-123 (analog: segmented role=group + base/active/inactive className triple — exact copy lines 93-95)
    - src/data/skills.js (SKILL_CATEGORIES export with 8 entries — keys + bilingual labels + color)
    - src/i18n/translations.js (t.game.filterBarLabel, filterSkillsLabel, filterCategoryLabel, filterReset — all available from Plan 02)
    - tailwind.config.js (chip color tokens from Plan 02: bg-chip-activeBg, text-chip-activeText, bg-chip-outlineBg, border-chip-outlineBorder, text-chip-outlineText)
    - .planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md (D-16-FLAYOUT-DESKTOP persistent bottom bar; D-16-FLAYOUT-MOBILE top compact toolbar; D-16-FLAYOUT-RESET clears all 3 dimensions; bilingual labels)
    - .planning/phases/16-filters-floating-experiencecard/16-UI-SPEC.md §Layout Contract (filter bar dimensions, padding, gap) + §Sizing (chip height 32px, h-8; touch target 44px via min-h)
    - .planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md Wave 3 SkillFilters.js section (lines 727-810; chip group pattern lines 750-774; category chip pattern lines 778-795; reset button pattern lines 800-810)
  </read_first>
  <behavior>
    - Root <div role="group" aria-label={t.game.filterBarLabel} data-game-interactive> — D-16-FLOW-CLOSE-EMPTY allow-list
    - Skill chip cluster: <div role="group" aria-label={t.game.filterSkillsLabel}> containing one button per node. Each chip: aria-pressed={selectedSkills.includes(node.id)}, aria-label={node.label}, onClick={() => onToggleSkill(node.id)}, type="button", className base+active/inactive triple from LangPill pattern. Active state uses bg-chip-activeBg text-chip-activeText font-extrabold; inactive uses text-chip-outlineText bg-chip-outlineBg border border-chip-outlineBorder.
    - Category chip cluster: <div role="group" aria-label={t.game.filterCategoryLabel}> with Object.entries(SKILL_CATEGORIES).map. Each chip: aria-pressed={category === key}, aria-label={cat[lang]}, onClick={() => onCategoryChange(category === key ? null : key)}, style={isActive ? { backgroundColor: cat.color, color: '#FFFFFF' } : undefined}, type="button". Renders all 8 categories. The category color is a DATA value (cat.color from skills.js) — applied via inline style, not Tailwind token (documented exception).
    - Reset button: <button type="button" disabled={!isFilterActive} aria-disabled={!isFilterActive} aria-label={t.game.filterReset} onClick={onReset}> with className base + conditional (enabled: text-brand border-brand; disabled: text-text-muted border-ink-400). Native disabled + aria-disabled both required per D-16-FLAYOUT-RESET.
    - All clickable elements use the shared base className: 'px-3 py-1.5 rounded-full font-mono text-xs min-h-[44px] min-w-[44px] inline-flex items-center justify-center focus:outline-none focus-visible:ring-2 focus-visible:ring-brand motion-safe:transition-colors duration-200'
    - Layout: filter bar root uses flex flex-wrap items-center gap-4 with bg-ink-900/80 backdrop-blur-sm border-t border-ink-600 px-4 py-4 — desktop persistent bottom bar. Mobile responsive variant relies on flex-wrap + horizontal scroll fallback (UI-SPEC §Layout Contract). The mobile-specific top-toolbar positioning is enforced by GameMode.js placement in Plan 06; this file only handles internal layout.
    - NOT in this task: YearRangeSlider implementation (handled in Task 2 below — extracted for clarity within the same plan)
  </behavior>
  <action>Create src/game/SkillFilters.js. Imports: `import React from 'react'`; `import { SKILL_CATEGORIES } from '../data/skills.js'`. Default export `function SkillFilters({ nodes, selectedSkills, yearRange, yearBounds, category, isFilterActive, onToggleSkill, onYearRangeChange, onCategoryChange, onReset, lang, t })`. Build the root wrapper per PATTERNS.md lines 740-748. Build the shared base className constant per PATTERNS.md line 753. Render skill chip cluster per PATTERNS.md lines 757-774 (map over nodes, aria-pressed, base+activeSkill/inactiveSkill classNames). Render category chip cluster per PATTERNS.md lines 778-795 (Object.entries(SKILL_CATEGORIES), inline backgroundColor when active). Render reset button per PATTERNS.md lines 800-810 (disabled + aria-disabled both, conditional brand-color styling when enabled). Render the YearRangeSlider inline as a child component (defined in Task 2) — for Task 1 leave a placeholder `<YearRangeSlider value={yearRange ?? yearBounds} bounds={yearBounds} onChange={onYearRangeChange} t={t} />` that Task 2 will fill in within the same file. Mobile horizontal scroll: wrap chip groups in `<div className="flex gap-2 overflow-x-auto scrollbar-hide md:flex-wrap md:overflow-visible">`. No semicolons, no comma-dangle on imports, JSX-in-.js.</action>
  <verify>
    <automated>npx vitest run src/game/SkillFilters.test.js -t "chip\\|reset\\|role=group\\|aria-pressed" 2>&amp;1 | tee /tmp/wave3-chips.log; grep -E "passed|failed" /tmp/wave3-chips.log | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/game/SkillFilters.js exists with `export default function SkillFilters` signature
    - Root <div> has `role="group"`, `aria-label={t.game.filterBarLabel}`, and `data-game-interactive` attribute (D-16-FLOW-CLOSE-EMPTY allow-list)
    - Skill chip cluster wrapped in `<div role="group" aria-label={t.game.filterSkillsLabel}>` containing N buttons (where N === nodes.length)
    - Each skill chip has `aria-pressed={selectedSkills.includes(node.id)}` (D-16-INTERSECT-AND multi-select)
    - Category chip cluster maps over Object.entries(SKILL_CATEGORIES) producing exactly 8 buttons
    - Category chips use `style={isActive ? { backgroundColor: cat.color, color: '#FFFFFF' } : undefined}` — data-driven category color exception documented
    - Reset button has BOTH `disabled={!isFilterActive}` (native) AND `aria-disabled={!isFilterActive}` (D-16-FLAYOUT-RESET)
    - Reset button onClick={onReset} fires when enabled
    - All buttons include `min-h-[44px] min-w-[44px]` in className (WCAG 2.5.5 touch target)
    - All buttons include `focus:outline-none focus-visible:ring-2 focus-visible:ring-brand` (focus ring contract from UI-SPEC)
    - Animation classes use `motion-safe:transition-colors duration-200` prefix (never bare animate-*)
    - NO inline EN/ES literals in the file: `grep -E "'Reset'|'Limpiar'|'Years'|'Años'" src/game/SkillFilters.js` returns 0 lines
    - NO semicolons in file: `grep -c "^[^/]*;$" src/game/SkillFilters.js` returns 0 (excluding URL/template-literal embeddeds)
    - At least the chip-toggle, reset-disabled, reset-enabled, role=group tests in SkillFilters.test.js PASS
  </acceptance_criteria>
  <done>SkillFilters.js chip + reset surface built. Chip/reset tests in SkillFilters.test.js GREEN; slider tests still RED until Task 2. Commit: `feat(16): add SkillFilters chip clusters + reset button`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement inline YearRangeSlider (dual-thumb WAI-ARIA APG widget) inside SkillFilters.js</name>
  <files>src/game/SkillFilters.js</files>
  <read_first>
    - src/game/SkillFilters.js (the file from Task 1 — extend by adding the YearRangeSlider component in the same file; inline per RESEARCH.md §2 recommendation unless LOC > 280)
    - src/game/SkillFilters.test.js (slider-specific RED tests: ArrowRight increments, Home/End jumps, dependent valuemax)
    - .planning/phases/16-filters-floating-experiencecard/16-CONTEXT.md (D-16-YEAR-INPUT keyboard contract; D-16-YEAR-BOUNDS live data)
    - .planning/phases/16-filters-floating-experiencecard/16-RESEARCH.md §2 (full WAI-ARIA contract lines 407-433, implementation sketch lines 437-509, anti-patterns lines 521-525), §"Pitfall 4" (start==end lock guard)
    - .planning/phases/16-filters-floating-experiencecard/16-UI-SPEC.md §Sizing (slider track h-1; thumb 4×4 visual + 44×44 touch via before:absolute pseudo-pad)
    - .planning/phases/16-filters-floating-experiencecard/16-PATTERNS.md "YearRangeSlider inline" section (lines 812-823)
    - tailwind.config.js (slider color tokens bg-slider-track, bg-slider-range, bg-slider-thumb, border-slider-thumbBorder from Plan 02)
  </read_first>
  <behavior>
    - Inline `function YearRangeSlider({ value, bounds, onChange, t })` in the same file as SkillFilters (not a separate file unless this pushes total beyond 280 LOC — per RESEARCH.md §2)
    - value = [start, end]; if parent passes null, SkillFilters substitutes yearBounds before passing (handled in Task 1 wrapper)
    - bounds = yearBounds; range = max - min; startPct/endPct = percentages for track fill positioning
    - Two <button role="slider" type="button"> elements — startThumb and endThumb. Each is in the tab order.
    - Each thumb's ARIA: aria-orientation="horizontal", aria-valuemin/max DEPENDENT (start.max = end - 1; end.min = start + 1), aria-valuenow = current value, aria-valuetext = bilingual string (e.g., `${t.game.yearFrom} ${start}`), aria-label from t.game.yearStartLabel / t.game.yearEndLabel
    - Keyboard handler `handleKey(thumb, e)`: switch on e.key. ArrowRight/Up: next = min(current+1, opposite-thumb-bound); ArrowLeft/Down: next = max(current-1, opposite-thumb-bound); Home: next = thumb's aria-valuemin; End: next = thumb's aria-valuemax. If next differs from current, call e.preventDefault() then onChange([newStart, newEnd]).
    - PITFALL 4 guard: enforce STRICT `start < end` — start cannot equal end; if both at same edge, allow the OTHER thumb to move first; document with a comment.
    - Track + range fill: <div className="relative h-1 bg-slider-track rounded-full"> with inner <div className="absolute h-1 bg-slider-range rounded-full" style={{ left: `${startPct}%`, width: `${endPct - startPct}%` }} />
    - Thumb visual: w-4 h-4 bg-slider-thumb border-2 border-slider-thumbBorder rounded-full; 44px touch via before:absolute before:inset-[-14px] before:content-['']; positioned via style={{ left: `${pct}%` }} + -translate-x-1/2 -translate-y-1/2 top-1/2
    - Live region: hidden <span aria-live="polite" aria-atomic="true" className="sr-only">{`${start} — ${end}`}</span> announces changes
    - Visual range label below: text-xs font-mono text-text-secondary showing "{start} — {end}" plus min/max anchors as aria-hidden spans
    - Reduced-motion: thumb position transition is purely position-driven (style left:%) — no animation utility; existing global rule covers any transition automatically. Do not add motion-safe: classes to thumb position.
    - DEFER pointer-drag per RESEARCH.md §2 lines 511-519 — keyboard only in v1. Drag is a v2 follow-up if UAT demands.
  </behavior>
  <action>EXTEND src/game/SkillFilters.js. Above the default export `SkillFilters` function (or below it in same file scope), add `function YearRangeSlider({ value, bounds, onChange, t })`. Use useRef for startThumbRef and endThumbRef. Compute startPct/endPct from value + bounds. Build handleKey(thumb, e) per RESEARCH.md §2 lines 448-461 — derive lo/hi based on thumb identity (start: lo=min, hi=to-1; end: lo=from+1, hi=max); on ArrowRight/Up/ArrowLeft/Down/Home/End compute next; if next changed, e.preventDefault() then call onChange. Render <div role="group" aria-label={t.game.yearLabel} className="relative w-full max-w-[200px] py-3"> wrapping the track. Inside: the bg-slider-track div + bg-slider-range filler div + two <button role="slider"> elements per PATTERNS.md lines 813-823 + RESEARCH.md §2 lines 468-508. Use aria-valuetext={`${t.game.yearFrom} ${start}`} for start and {`${t.game.yearTo} ${end}`} for end. Add the sr-only aria-live="polite" announcement region. Add the visual range label "{start} — {end}" + min/max aria-hidden anchors. Inside SkillFilters render (Task 1), replace the placeholder with `<YearRangeSlider value={yearRange ?? yearBounds} bounds={yearBounds} onChange={onYearRangeChange} t={t} />`. Note: when yearRange is null at init, pass bounds as default value so the slider renders at full range (start=min, end=max); when user moves a thumb, onChange fires with a non-null range. The "no range filter active" state is detectable by parent comparing range[0] === yearBounds[0] && range[1] === yearBounds[1] OR yearRange=== null. Keep this contract — parent (useConstellation via setYearRange in Plan 03) accepts both null and full-range as "no filter". No semicolons.</action>
  <verify>
    <automated>npx vitest run src/game/SkillFilters.test.js 2>&amp;1 | tee /tmp/wave3-slider.log; grep -E "passed|failed" /tmp/wave3-slider.log | tail -3; grep -c "role=\"slider\"\\|aria-valuemin\\|aria-valuemax\\|aria-valuenow\\|aria-valuetext" src/game/SkillFilters.js</automated>
  </verify>
  <acceptance_criteria>
    - src/game/SkillFilters.js contains `function YearRangeSlider` definition
    - Exactly 2 elements with `role="slider"` rendered (one for start, one for end) — `grep -c 'role="slider"' src/game/SkillFilters.js` returns 2
    - Each slider thumb has aria-valuemin, aria-valuemax, aria-valuenow, aria-valuetext, aria-label, aria-orientation="horizontal"
    - Dependent ranges: start thumb's aria-valuemax = `end - 1`; end thumb's aria-valuemin = `start + 1` (verify by grep for `end - 1` and `start + 1` or `from + 1`/`to - 1` in component)
    - Keyboard handler calls `e.preventDefault()` before invoking onChange (mandatory per APG to prevent page scroll)
    - handleKey responds to: ArrowRight, ArrowUp, ArrowLeft, ArrowDown, Home, End
    - sr-only <span aria-live="polite" aria-atomic="true"> announces "{start} — {end}" on changes
    - Touch target via before:inset-[-14px] before:absolute before:content-[''] OR an equivalent 44px hit area
    - Slider track uses bg-slider-track, range fill uses bg-slider-range, thumb uses bg-slider-thumb (token-driven; no raw hex)
    - `npx vitest run src/game/SkillFilters.test.js` exits 0 with all 10+ tests PASSING (chip, slider keyboard, dependent max, reset states)
    - Pointer-drag is NOT implemented (deferred): `grep -c "onPointerDown\\|setPointerCapture" src/game/SkillFilters.js` returns 0 — keyboard-only v1 per RESEARCH.md §2
    - No localStorage writes: `grep -c "localStorage" src/game/SkillFilters.js` returns 0
    - No inline EN/ES literals
  </acceptance_criteria>
  <done>YearRangeSlider implemented inline. All SkillFilters.test.js tests GREEN. Commit: `feat(16): add YearRangeSlider dual-thumb WAI-ARIA widget (GREEN)`</done>
</task>

</tasks>

<verification>
- `npx vitest run src/game/SkillFilters.test.js` — all 10+ tests GREEN
- `npx vitest run` — no regression on Phases 14/15 tests, no regression on filters.test.js / useConstellation.test.js / SvgConstellation.test.js
- `npm run build` — exits 0; game-mode chunk size logged (expected: +2 to +3 kB gz over Plan 03 baseline; total ≤ 8 kB gz after this plan)
- ExperienceCard.test.js + GameMode.test.js (new tests) still RED — expected until Plans 05/06
- Open Question 3 (RESEARCH.md): if total LOC exceeds 280, extract YearRangeSlider to src/game/YearRangeSlider.js. Per checker discretion if LOC threshold hit.
</verification>

<success_criteria>
- SkillFilters.js delivered with chips + slider + reset
- SkillFilters.test.js fully GREEN
- WAI-ARIA APG slider-multithumb pattern compliance: two role="slider" buttons, dependent valuemin/max, keyboard contract complete
- 44×44px touch targets on every interactive element
- AA contrast holds in dark + light themes (per UI-SPEC table — verified at /gsd:verify-work browser UAT)
- 2 atomic commits, one per task
</success_criteria>

<output>
Create `.planning/phases/16-filters-floating-experiencecard/16-04-SUMMARY.md` — list SkillFilters props consumed, slider keyboard map, count of buttons rendered for the live constellation, bundle size delta from `npm run build`, and confirm the YearRangeSlider stayed inline (LOC under 280) or was extracted (with reason).
</output>
