---
phase: 23-static-content-sections
plan: 04
type: execute
wave: 3
depends_on: [23-01, 23-02, 23-03]
files_modified:
  - src/pages/en/index.astro
  - src/pages/es/index.astro
autonomous: false
requirements: [STATIC-01, TEST-01]

must_haves:
  truths:
    - "D-05: src/pages/en/index.astro and es/index.astro replace the placeholder <h1>Carlos Montoya</h1> with the 5 static components in the exact order About -> Skill -> Projects -> Claude -> Footer (Hero/Experience/Contact/SectionPager slots stay absent — not stubbed — until their own phases)"
    - "About, Skill, Projects, Claude, and Footer all render correct bilingual content on /en and /es with zero client-side JS shipped for these five components — only Nav's astro-island survives in the built HTML (STATIC-01)"
    - "The full Container API test suite for all 5 components (TEST-01) stays GREEN after mounting"
  artifacts:
    - path: "src/pages/en/index.astro"
      provides: "EN page mounting all 5 static sections after Nav, in D-05 order"
      contains: "About"
    - path: "src/pages/es/index.astro"
      provides: "ES page mounting all 5 static sections after Nav, in D-05 order"
      contains: "About"
  key_links:
    - from: "src/pages/en/index.astro"
      to: "src/components/astro/About.astro"
      via: "import + <About locale={locale} /> in <main>"
      pattern: "components/astro/About.astro"
    - from: "src/pages/es/index.astro"
      to: "src/components/astro/Footer.astro"
      via: "import + <Footer locale={locale} /> in <main>"
      pattern: "components/astro/Footer.astro"
---

<objective>
Mount the five static components (About, Skill, Projects, Claude, Footer) into `src/pages/en/index.astro` and `src/pages/es/index.astro`, replacing the placeholder `<h1>` in the exact D-05 order (About -> Skill -> Projects -> Claude -> Footer). Verify the built HTML carries correct bilingual content with zero client-side JS for these five (only Nav remains a hydrated island), then confirm in a real browser.

Purpose: this is the phase's observable-outcome plan — it makes the migrated sections actually appear on the live locale routes and proves the STATIC-01 zero-JS guarantee end to end.
Output: both page files updated; `dist/` build verified zero-JS for the five; browser confirmation captured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-static-content-sections/23-CONTEXT.md
@.planning/phases/23-static-content-sections/23-PATTERNS.md
@.planning/phases/23-static-content-sections/23-01-SUMMARY.md
@.planning/phases/23-static-content-sections/23-02-SUMMARY.md
@.planning/phases/23-static-content-sections/23-03-SUMMARY.md

<interfaces>
<!-- Current en/index.astro (20 lines) mounts only Nav client:load + placeholder <h1>.
     es/index.astro is identical except the frontmatter fallback ?? 'es' on line 9. -->

Target mounting block inside <main> (from 23-PATTERNS.md, D-05 order):
  <About locale={locale} />
  <Skill locale={locale} />
  <Projects locale={locale} />
  <Claude locale={locale} />
  <Footer locale={locale} />

Imports to add to both page frontmatters (paths relative to src/pages/{en,es}/):
  import About from '../../components/astro/About.astro'
  import Skill from '../../components/astro/Skill.astro'
  import Projects from '../../components/astro/Projects.astro'
  import Claude from '../../components/astro/Claude.astro'
  import Footer from '../../components/astro/Footer.astro'

The existing const locale = Astro.currentLocale ?? 'en' (en) / ?? 'es' (es) line already resolves
the locale to pass as the locale prop. Keep Nav, BaseLayout, and the getRelativeLocaleUrl href
computation unchanged. Nav is the ONLY client:load island on these pages, so the built HTML must
contain exactly one astro-island element per page after mounting.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount 5 static components in both locale pages + build zero-JS verification</name>
  <files>src/pages/en/index.astro, src/pages/es/index.astro</files>
  <read_first>
    - src/pages/en/index.astro (current 20-line file — incremental edit, not a rewrite)
    - src/pages/es/index.astro (current 20-line file — only diff is the ?? 'es' fallback)
    - .planning/phases/23-static-content-sections/23-PATTERNS.md (target mounting pattern, D-05 order)
  </read_first>
  <action>
    In `src/pages/en/index.astro`: add the five import lines (About/Skill/Projects/Claude/Footer from `../../components/astro/*.astro`) to the frontmatter after the existing Nav import; replace the `<h1>Carlos Montoya</h1>` placeholder inside `<main>` with the five component tags in D-05 order (About -> Skill -> Projects -> Claude -> Footer), each passed `locale={locale}`. Leave BaseLayout, the Nav `client:load` mount, and the `getRelativeLocaleUrl` href computation untouched. Do NOT add Hero/Experience/Contact/SectionPager slots (they stay absent until their own phases, per D-05). Apply the identical edit to `src/pages/es/index.astro`, preserving its `?? 'es'` frontmatter fallback as the only difference. Then run `npm run build` and inspect the built `dist/en/index.html` and `dist/es/index.html`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && rg -q 'id="about"' dist/en/index.html && rg -q 'id="skills"' dist/en/index.html && rg -q 'id="projects"' dist/en/index.html && rg -q 'id="claude-code"' dist/en/index.html && rg -q '<footer' dist/en/index.html && [ "$(rg -c 'astro-island' dist/en/index.html)" = "1" ] && [ "$(rg -c 'astro-island' dist/es/index.html)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes without error.
    - `dist/en/index.html` contains `id="about"`, `id="skills"`, `id="projects"`, `id="claude-code"`, and `<footer`.
    - `dist/es/index.html` contains the Spanish strings 'Sobre mí' and 'Proyectos' (bilingual mount confirmed).
    - Exactly ONE `astro-island` element exists in each of `dist/en/index.html` and `dist/es/index.html` (only Nav hydrates — the 5 new sections ship zero client JS, proving STATIC-01).
    - Both page files still import BaseLayout and mount `<Nav client:load .../>` unchanged; no Hero/Experience/Contact/SectionPager tags added.
  </acceptance_criteria>
  <done>Both locale pages render all 5 static sections in D-05 order; built HTML confirms zero client JS for the five (D-05, STATIC-01).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser confirmation of bilingual render + zero per-section JS on /en and /es</name>
  <action>Human verification checkpoint — no code is written here. The executor has already mounted the five sections and confirmed the build-level zero-JS assertion (exactly one astro-island per page) in Task 1. Present the verification steps in `<how-to-verify>` to the developer and wait for the resume signal before marking the phase complete.</action>
  <what-built>All 5 migrated sections (About, Skill, Projects, Claude, Footer) are now mounted on `/en` and `/es`, rendered as static HTML with zero client-side JS (only Nav hydrates). The build-level zero-JS assertion already passed in Task 1 (exactly one astro-island per page).</what-built>
  <how-to-verify>
    1. Run `npm run preview` and open the served URL, then visit `/en`.
    2. Open DevTools -> Network -> filter JS. Confirm the only hydration script served corresponds to the Nav island; no per-section JS chunk loads for About/Skill/Projects/Claude/Footer.
    3. Scroll the page: About (paragraphs + quick-facts), Skill (4 categories, chip meters), Projects (Mr. Yoker featured card first, then rest), Claude (pitch + 4 values + 6 offerings), Footer (brand link + GitHub/LinkedIn + current year) all render correctly, in that order.
    4. Visit `/es` and confirm every section shows Spanish copy ('Sobre mí', 'Stack técnico', 'Proyectos', 'Hablemos de tu proyecto', 'Todos los derechos reservados').
    5. Confirm quick-facts numbers render as static final values (e.g. '18+ years') with NO count-up animation (D-04 — count-up is deferred to Phase 24).
  </how-to-verify>
  <resume-signal>Type "approved" once both locales render all 5 sections correctly with no per-section JS, or describe any issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time page composition -> static HTML | Page frontmatter imports committed `.astro` components; no runtime/user input crosses into the mounted output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-01 | Tampering (XSS) | mounted section output on /en, /es | mitigate | All five components use Astro auto-escaped interpolation (verified in Plans 23-01..03); mounting adds no new dynamic sinks. |
| T-23-04-02 | Elevation (unintended hydration) | client JS surface | mitigate | Build-verify asserts exactly one astro-island per page (Nav only) — the five sections ship zero client JS, keeping the attack surface flat. |
| T-23-04-SC | Tampering (supply chain) | npm installs | accept | No package installs in this plan — no new dependency surface. |
</threat_model>

<verification>
- `npm run build` succeeds; `dist/en/index.html` and `dist/es/index.html` contain all 5 sections with exactly one astro-island each.
- Full `npx vitest run` GREEN (all 5 Container API suites + Nav island suite).
- Human browser check confirms bilingual render + zero per-section JS.
</verification>

<success_criteria>
- About, Skill, Projects, Claude, Footer render correct bilingual content on /en and /es with zero client JS for these five (STATIC-01 success criterion 1 — including Claude).
- All 5 components consume their existing data unchanged and have passing Container API tests (TEST-01).
- Phase 23 goal met: the bulk of the page ships as zero-client-JS HTML consuming existing JSON unchanged.
</success_criteria>

<output>
Create `.planning/phases/23-static-content-sections/23-04-SUMMARY.md` when done.
</output>
