---
phase: 21-foundation-astro-scaffold-i18n-routing-layout-shell
plan: 04
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - src/pages/404.astro
  - src/pages/404.test.ts
autonomous: true
requirements: [DEPLOY-02]
must_haves:
  truths:
    - "A build emits dist/404.html — the authored custom 404, not an SPA fallback"
    - "The 404 page uses only real dark-palette tokens (bg-ink-900, text-brand, text-text-primary/secondary), never the nonexistent text-neon/bg-grad-neon/shadow-neon"
    - "The 404 shows bilingual copy inferred from an /en or /es URL prefix, defaulting to EN + / CTA"
    - "The 404 page offers a single non-destructive link back home, keyboard-reachable, ≥44px touch target"
    - "An Astro Container API test renders 404.astro and asserts on real output — the phase's test-harness proof-of-life"
  artifacts:
    - path: "src/pages/404.astro"
      provides: "Custom bilingual 404 page, minimal dark-palette design (D-10, D-11)"
      min_lines: 25
    - path: "src/pages/404.test.ts"
      provides: "experimental_AstroContainer proof-of-life test (harness stand-up)"
      contains: "experimental_AstroContainer"
  key_links:
    - from: "src/pages/404.test.ts"
      to: "src/pages/404.astro"
      via: "container.renderToString(NotFoundPage)"
      pattern: "renderToString"
---

<objective>
Author the custom `404.astro` page (DEPLOY-02) and stand up the Astro Container API test harness with a proof-of-life test against it. The 404 is minimal per D-10/D-11 (single generic route, dark-palette tokens, bilingual, link home, no new animation) — and critically must use the REAL Tailwind tokens (`bg-ink-900`, `text-brand`, `text-text-primary`/`text-text-secondary`, `border-ink-400`/`border-ink-600`), not the `text-neon`/`bg-grad-neon`/`shadow-neon` tokens named in D-11/stale CLAUDE.md that return zero grep hits. The Container API test is the phase's test-harness "proof-of-life" — the first `.astro` component test in the repo, the pattern Phase 23 will scale to full TEST-01 coverage.

Purpose: Removing the SPA rewrite (Plan 21-03) means a bad path now needs a real authored 404 instead of `index.html`. Standing up the Container API harness now (against the simplest possible `.astro` file) de-risks the experimental API before Phase 23 depends on it for real content tests.
Output: `dist/404.html` from a token-correct bilingual 404, plus a passing `experimental_AstroContainer` test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-RESEARCH.md
@.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-PATTERNS.md
@.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-UI-SPEC.md
@.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-02-SUMMARY.md

<interfaces>
<!-- 404.astro convention: 21-RESEARCH.md § "404.astro — exact convention" (lines ~292-308) -->
<!-- Container API: 21-RESEARCH.md § "Astro Container API" (import experimental_AstroContainer from 'astro/container', renderToString) (lines ~312-323) -->
<!-- UI contract: 21-UI-SPEC.md § Color / Typography / Copywriting Contract -->

REAL Tailwind tokens (verified against tailwind.config.js + src/index.css — USE THESE):
  bg-ink-900        → var(--color-ink-900)  page background (#0B1020)
  text-text-primary → var(--color-text-primary)   heading
  text-text-secondary → var(--color-text-secondary) body copy
  text-brand / hover:text-brand → var(--color-brand)   accent (CTA hover)
  border-ink-400 / border-ink-600 / hover:border-brand → borders
  bg-brand-gradient + bg-clip-text text-transparent → optional 404 numeral gradient
  font-mono (JetBrains Mono) eyebrow · font-sans (Inter) body/heading
DO NOT USE (zero grep hits — do not appear anywhere in src/): text-neon, bg-grad-neon,
  shadow-neon, animate-fadein, animate-pulse2 (pulse2 exists but is out of scope / motion here)

Copy contract (21-UI-SPEC.md):
  eyebrow EN "404 · Not Found" / ES "404 · No encontrado"
  heading EN "Page not found" / ES "Página no encontrada"
  body    EN "The page you're looking for doesn't exist or has moved. Let's get you back on track."
          ES "La página que buscas no existe o fue movida. Volvamos al inicio."
  CTA     EN "Back to home" → /en if path started /en, else / ; ES "Volver al inicio" → /es
  locale inference: pathname startsWith /es → ES + /es CTA ; /en → EN + /en CTA ; else EN + / CTA
  CTA touch target: min-h-[44px] (existing site-wide exception, reused verbatim)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 404.astro — bilingual, locale-inferred, real dark-palette tokens</name>
  <files>src/pages/404.astro</files>
  <read_first>
    - src/layouts/BaseLayout.astro (decide whether 404 wraps BaseLayout or is a minimal standalone doc per D-11)
    - src/components/Footer.jsx (live token usage: border-ink-400, text-text-primary, text-text-secondary, hover:text-brand, hover:border-brand)
    - tailwind.config.js (confirm ink/brand/text token definitions)
    - .planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-UI-SPEC.md (full — color, typography, spacing, copy contract)
    - .planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-RESEARCH.md § "404.astro — exact convention"
    - .planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-PATTERNS.md § "src/pages/404.astro"
  </read_first>
  <action>
    Create `src/pages/404.astro` (single generic route, no `/en/404` or `/es/404` — D-10). In frontmatter, infer locale from `Astro.url.pathname`: startsWith `/es` → ES copy + CTA target `/es`; startsWith `/en` → EN copy + CTA target `/en`; otherwise EN copy + CTA target `/` (which Plan 21-03 middleware then redirects). Render the UI-SPEC copy for the inferred locale: a `font-mono uppercase tracking-wide text-xs font-extrabold` eyebrow, a large `text-6xl font-extrabold` "404" numeral (optionally `bg-brand-gradient bg-clip-text text-transparent` per UI-SPEC accent), a `text-2xl font-extrabold text-text-primary` heading, a `text-base text-text-secondary` body paragraph, and a single "back home" `<a>` CTA with `min-h-[44px] px-6 py-3 rounded-full border border-ink-400 text-text-secondary hover:text-brand hover:border-brand` (matches Hero/Footer secondary-link pattern). Page background `bg-ink-900`, centered content block, vertical padding `py-8` mobile / `py-12` desktop (xl/2xl scale). Wrap in `<BaseLayout>` if BaseLayout renders cleanly for a standalone error page — otherwise author a minimal self-contained `<html lang>` doc per D-11's "no personality" minimalism (record which you chose and why in SUMMARY). Use ONLY the real tokens listed in the interfaces block. Do NOT use `text-neon`/`bg-grad-neon`/`shadow-neon` (zero grep hits — they do not exist). Meet WCAG AA: the CTA is keyboard-focusable with a visible focus state; contrast holds on `bg-ink-900`.
  </action>
  <verify>
    <automated>npx astro build && node -e "const fs=require('fs'); if(!fs.existsSync('dist/404.html')) throw new Error('dist/404.html not built'); const h=fs.readFileSync('dist/404.html','utf8'); ['text-neon','bg-grad-neon','shadow-neon'].forEach(t=>{ if(h.includes(t)) throw new Error('forbidden token '+t) }); if(!/ink-900/.test(h)) throw new Error('bg-ink-900 missing'); if(!/href=\"\/(en|es)?\"/.test(h)) throw new Error('home CTA link missing'); if(!/404/.test(h)) throw new Error('404 text missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `astro build` emits `dist/404.html` (DEPLOY-02 — real authored 404, not SPA fallback).
    - Built HTML contains `ink-900` and a home CTA `href` to `/`, `/en`, or `/es`; contains "404".
    - Built HTML contains NONE of `text-neon`, `bg-grad-neon`, `shadow-neon`.
    - Copy matches the UI-SPEC bilingual contract; locale inferred from URL prefix per D-11.
    - CTA carries `min-h-[44px]` (44px touch target) and a visible focus state (WCAG AA).
    - SUMMARY records the BaseLayout-wrap vs standalone decision.
  </acceptance_criteria>
  <done>dist/404.html builds from a token-correct bilingual, locale-inferred 404 with a keyboard-reachable home CTA; assertion prints OK.</done>
</task>

<task type="auto">
  <name>Task 2: Stand up the Container API harness with a 404.astro proof-of-life test</name>
  <files>src/pages/404.test.ts</files>
  <read_first>
    - src/pages/404.astro (the component under test)
    - src/seo/jsonld.test.js (describe/it naming with a requirement-ID suffix; assert-on-real-structure philosophy)
    - src/App.test.jsx (renders-without-throwing smoke-test-first pattern)
    - vitest.config.ts (getViteConfig wiring the Container API test resolves through)
    - .planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-RESEARCH.md § "Astro Container API — exact import path"
    - .planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-PATTERNS.md § "src/pages/404.test.ts"
  </read_first>
  <action>
    Create `src/pages/404.test.ts` using `import { experimental_AstroContainer as AstroContainer } from 'astro/container'`, `import { expect, test } from 'vitest'` (or describe/it to match repo style), and `import NotFoundPage from './404.astro'`. Title the suite referencing the harness's future requirement anchor (e.g. `describe('404 page (Container API proof-of-life — TEST-01 harness)')`). First assertion: renders without throwing — `const container = await AstroContainer.create(); await expect(container.renderToString(NotFoundPage)).resolves.toBeTruthy()` (async equivalent of App.test.jsx's not-to-throw smoke test). Second assertion: `renderToString` output contains a `href="/"`-family home link and the string `404`. This is proof-of-life for the experimental API only (no bilingual/ARIA depth — that scales in Phase 23); note in a comment that `experimental_AstroContainer` is experimental and subject to breaking changes per 21-RESEARCH.md, so downstream harness use should not assume API stability. No React renderer registration is needed here (404.astro has no nested island).
  </action>
  <verify>
    <automated>npx vitest run src/pages/404.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/404.test.ts` imports `experimental_AstroContainer` from `astro/container` and imports `./404.astro`.
    - `npx vitest run src/pages/404.test.ts` passes (harness resolves Astro's compiler pipeline through `getViteConfig`).
    - Test asserts renderToString resolves truthy AND output contains a home link and `404`.
    - `npx vitest run` (full suite) remains GREEN — the new Astro test coexists with existing React tests.
  </acceptance_criteria>
  <done>The Container API test passes and the full suite stays green — the .astro test harness is proven working.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requested URL path → 404 render | An arbitrary unmatched path reaches `404.astro`; its prefix drives locale inference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Spoofing / Injection | locale inference + CTA href from `Astro.url.pathname` | mitigate | Only a fixed `/^\/es`/`/^\/en` prefix check drives a hardcoded `/es` `/en` `/` CTA target; the raw path is never reflected into the href or page body, so a crafted path cannot inject markup or an arbitrary redirect target |
| T-21-04-02 | Information Disclosure | 404 error page content | accept | Static generic message, no stack traces, no path echo — reveals nothing about the app internals |
| T-21-04-03 | Tampering | experimental Container API instability | accept | Documented as experimental; proof-of-life scope only, isolated to one test file, no production runtime dependency |
</threat_model>

<verification>
- `npx astro build` emits `dist/404.html` with real tokens and a home CTA; no forbidden tokens.
- `npx vitest run` full suite GREEN including the new Container API test.
</verification>

<success_criteria>
A minimal token-correct bilingual `404.astro` builds to `dist/404.html` (DEPLOY-02), and the Astro Container API harness is stood up and proven by a passing proof-of-life test.
</success_criteria>

<output>
Create `.planning/phases/21-foundation-astro-scaffold-i18n-routing-layout-shell/21-04-SUMMARY.md` when done
</output>
