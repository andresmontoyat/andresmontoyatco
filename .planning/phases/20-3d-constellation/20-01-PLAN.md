---
phase: 20-3d-constellation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/constellation.layout.js
  - src/game/constellation.layout.test.js
  - scripts/check-bundle-gate.mjs
autonomous: true
requirements: [DEPTH-01]
tags: [layout, bundle-gate, data-layer]

must_haves:
  truths:
    - "computeLayout() returns {x, y, z} for every node — z is a number"
    - "Nodes in the same category share an identical z value (deterministic clustering)"
    - "At least 2 distinct z values exist across the constellation (defends CRIT-04 z=0 trap)"
    - "All z values fall within [-200, 200] (defends MOD-01 z-clipping at near=10/far=2000)"
    - "Layout is deterministic — same input nodes produce same {x, y, z} output across calls"
    - "SVG renderer path is visually unchanged at dev server (silently ignores .z per D-20-PROPS-CONTRACT)"
    - "scripts/check-bundle-gate.mjs HARD-FAILS when mobile chunk contains any 'three/...' subpath import (regression fixture verifies)"
    - "All 261 existing Vitest tests stay GREEN; ≥3 new z-tests added"
  artifacts:
    - path: "src/game/constellation.layout.js"
      provides: "CATEGORY_Z constants block + extended computeLayout returning {x, y, z}"
      contains: "const CATEGORY_Z"
    - path: "src/game/constellation.layout.test.js"
      provides: "3+ new z-storytelling tests appended to existing suite"
      contains: "z storytelling"
    - path: "scripts/check-bundle-gate.mjs"
      provides: "Widened THREE_JS_PATTERN regex that catches three/addons/* and three/examples/jsm/* leaks"
      contains: "three(\\\\/"
  key_links:
    - from: "src/game/constellation.layout.js CATEGORY_Z map"
      to: "src/game/renderers/WebGLConstellation.js position attribute write"
      via: "Plan 20-02 reads layout[id].z via pos.z ?? 0 fallback"
      pattern: "CATEGORY_Z\\[cat\\] \\?\\? 0"
    - from: "scripts/check-bundle-gate.mjs:12 widened regex"
      to: "Plan 20-02 OrbitControls import from three/addons/controls/OrbitControls.js"
      via: "Continuous invariant: no three.js leak to mobile chunk"
      pattern: "three\\(\\\\/\\[\\^'\"\\]\\+\\)\\?"
---

<objective>
Lay the data-layer foundation for genuine 3D: extend the pure `computeLayout()` function to return `{x, y, z}` with deterministic `CATEGORY_Z` constants mapped front-to-back as an architecture-stack narrative (per D-20-CONTEXT-ZMAP), and fix the bundle-gate regex bug at `scripts/check-bundle-gate.mjs:12` BEFORE the OrbitControls import lands in Plan 20-02 — so "no three.js leak to mobile chunk" stays a continuously enforced invariant.

Purpose:
- Defends CRIT-04 (z=0 trap) at the source — layout asserts z presence + clustering + range via tests, so Plan 20-02 cannot silently regress to a flat disc.
- Defends CRIT-03 (bundle-gate regex BUG) BEFORE the first `three/addons/*` import in Plan 20-02 — preserves GAME-07 Lighthouse mobile HARD gate as a continuous invariant, not a one-time check.
- Locks the D-20-CONTEXT-ZMAP narrative (`ai +150 → lang +75 → arch 0 → data −25 → cloud −75 → devops −100 → security −125 → hardware −150`) with comment-block "UAT-tunable" annotation per Alert A14.
- Preserves D-14-01-LAYOUT determinism (no `d3-force-3d`) and the GAME-01-reframed-as-D-20-PROPS-CONTRACT (props identical, SVG silently ignores `.z`).

Output: Extended `constellation.layout.js` + 3+ new layout tests + widened bundle-gate regex with regression fixture verification. SVG dev-server render visually identical to v3.9 close (renderer ignores `.z` silently). 261 existing tests stay GREEN; new tests pass; npm run build:gate exits 0 on a v3.9-shaped mobile chunk but HARD-FAILS the regression fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-3d-constellation/20-CONTEXT.md
@.planning/phases/20-3d-constellation/20-UI-SPEC.md
@.planning/phases/20-3d-constellation/20-PATTERNS.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@src/game/constellation.layout.js
@src/game/constellation.layout.test.js
@scripts/check-bundle-gate.mjs

<interfaces>
<!-- Locked invariants and contracts the executor must honor. No code exploration required. -->

CATEGORY_Z map (D-20-CONTEXT-ZMAP) — append after NODE_CLUSTER_RADIUS at module scope:

```
const CATEGORY_Z = {
  ai: 150,
  lang: 75,
  arch: 0,
  data: -25,
  cloud: -75,
  devops: -100,
  security: -125,
  hardware: -150,
}
```

Comment-block prefix REQUIRED above the constant per Alert A14:
`// CATEGORY_Z values are starting points — UAT-tunable. Consult v3.10-UAT.md`
`// before adjusting after milestone close.`

Layout assignment sites (lines 63 and 72-75 of current file) — both must be updated identically:

- Line 63 (single-node branch): `layout[catNodes[0].id] = { x: centroid.x, y: centroid.y, z: CATEGORY_Z[cat] ?? 0 }`
- Lines 72-75 (multi-node branch): add `z: CATEGORY_Z[cat] ?? 0` as third property of the object literal.

`?? 0` defensive fallback defends future category additions without map updates.

JSDoc @returns line (current line 23) updates from `{x, y}` to `{x, y, z}`.

Bundle-gate regex fix (scripts/check-bundle-gate.mjs:12) — single-line widening:

- BEFORE: `const THREE_JS_PATTERN = /THREE\.|from\s*['"]three['"]/`
- AFTER:  `const THREE_JS_PATTERN = /THREE\.|from\s*['"]three(\/[^'"]+)?['"]/`

This adds an optional `(\/[^'"]+)?` capture group after the literal `three`, catching `three`, `three/addons/...`, AND `three/examples/jsm/...` paths. Tested against three exact strings:

- `from 'three'` → MATCHES (existing behavior preserved)
- `from 'three/addons/controls/OrbitControls.js'` → MATCHES (NEW — the bug fix)
- `from 'three/examples/jsm/controls/OrbitControls.js'` → MATCHES (NEW — defensive)

Existing Vitest test invariant: `npm test` runs `src/test/setup.js` which stubs `HTMLCanvasElement.prototype.getContext = () => null` — layout tests do not touch canvas, so they are jsdom-safe. Pure-function tests follow the existing pattern at `constellation.layout.test.js` lines 12-103.

Build-gate CLI invariant: `npm run build` produces `dist/assets/GameMode-*.js` + `dist/assets/WebGLConstellation-*.js`. `npm run check:bundle-gate` (or equivalent) invokes `checkBundleGate('dist/assets')` and exits 0 on PASS, non-zero on HARD FAIL.

Regression-fixture pattern for the new regex test (Plan 20-01 deliverable inside a new test file `scripts/check-bundle-gate.test.mjs` — OR inline self-test in the script):

- Fixture string 1: `"from 'three/addons/controls/OrbitControls.js'"` → THREE_JS_PATTERN.test(...) === true (HARD FAIL expected)
- Fixture string 2: `"from 'three/examples/jsm/controls/OrbitControls.js'"` → true (HARD FAIL expected)
- Fixture string 3: `"const x = 1"` → false (PASS expected)

Forbidden anti-patterns (PITFALLS §"Decision Violations to Flag"):
- DO NOT propose `d3-force-3d` for organic z-spread (D-14-01-LAYOUT — deterministic required)
- DO NOT propose putting CATEGORY_Z in `src/data/skills.js` (skill-data layer ≠ visual layout per CONTEXT §domain)
- DO NOT change layout to return BOTH `{x, y}` AND `{x, y, z}` schemas — single shape per D-20-PROPS-CONTRACT
- DO NOT use values outside `[-200, 200]` — defends MOD-01 z-clipping
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen bundle-gate regex + add regression fixture verification</name>
  <files>scripts/check-bundle-gate.mjs, scripts/check-bundle-gate.test.mjs (NEW)</files>
  <read_first>
    - scripts/check-bundle-gate.mjs (current — line 12 regex bug)
    - .planning/phases/20-3d-constellation/20-PATTERNS.md §"scripts/check-bundle-gate.mjs"
    - .planning/research/PITFALLS.md §CRIT-03 (full)
    - .planning/research/SUMMARY.md §"Critical Pitfalls" #1
  </read_first>
  <behavior>
    - Test 1: regex matches `from 'three'` (existing behavior preserved — no regression on the literal import path)
    - Test 2: regex matches `from 'three/addons/controls/OrbitControls.js'` (NEW — the CRIT-03 bug fix)
    - Test 3: regex matches `from 'three/examples/jsm/controls/OrbitControls.js'` (NEW — defensive coverage of the legacy alias path)
    - Test 4: regex matches `THREE.PerspectiveCamera` (existing namespace-access alternation preserved)
    - Test 5: regex does NOT match unrelated source like `from 'react'` or `const x = 1` (no false positives)
  </behavior>
  <action>
    RED (write failing tests first). Create `scripts/check-bundle-gate.test.mjs` using Vitest. Import the regex via re-exporting it OR by re-reading the source file as text and extracting the literal (preferred: add a named export `export const THREE_JS_PATTERN` in `check-bundle-gate.mjs` to make it test-importable without invoking the CLI gate). Five `it()` cases per the behavior block above. Run `npm test -- check-bundle-gate` and confirm tests for the three new strings FAIL with the current narrow regex.

    GREEN. In `scripts/check-bundle-gate.mjs` line 12, replace the regex literally:
    - FROM: `const THREE_JS_PATTERN = /THREE\.|from\s*['"]three['"]/`
    - TO: `export const THREE_JS_PATTERN = /THREE\.|from\s*['"]three(\/[^'"]+)?['"]/`

    Add `export` to make it test-importable (preserves named-default-export shape — existing `export default async function checkBundleGate` stays). Add a comment immediately above the constant: `// Pitfall CRIT-03 mitigation: widened to catch three/addons/* and three/examples/jsm/* leaks. Phase 20 Plan 01.`

    Run `npm test -- check-bundle-gate` again and confirm all 5 tests pass. Run the full suite `npm test` to confirm no regression on the other 261 tests (this is a pure-additive named-export change — should be zero impact).

    Verify CLI still works end-to-end against the v3.9 build artifacts: `npm run build && npm run check:bundle-gate` (or whichever existing script invokes `node scripts/check-bundle-gate.mjs`) — must exit 0 with PASS on mobile chunk = ~8.91 kB gz and INFO on webgl chunk.
  </action>
  <verify>
    <automated>npm test -- check-bundle-gate</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-bundle-gate.test.mjs` exists with ≥5 test cases per the behavior block.
    - `rg "from\\s*\\['\\\"\\]three\\(\\\\/" scripts/check-bundle-gate.mjs` returns the new regex on line 12 (or wherever it now lives after the `export` keyword addition).
    - `rg "^export const THREE_JS_PATTERN" scripts/check-bundle-gate.mjs` returns exactly 1 hit (named export added).
    - `npm test -- check-bundle-gate` exits 0; all 5 cases pass.
    - `npm test` exits 0; total green count is 261 (existing) + 5 (new) = 266 tests OR equivalent (RED-then-GREEN: if any test was originally GREEN with the old regex and stays GREEN now, the delta is still net +5).
    - `npm run build && node scripts/check-bundle-gate.mjs` exits 0 against the current v3.9 build (mobile chunk does not yet contain any `three/...` import — Plan 20-02 has not landed).
  </acceptance_criteria>
  <done>
    Bundle-gate regex catches `three/addons/*` and `three/examples/jsm/*` import strings; regression fixture proves it; existing PASS path on the v3.9 build artifact unchanged; named export exists for downstream tests; commit `feat(20-01): widen bundle-gate regex to catch three/addons/* leaks (CRIT-03)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend computeLayout() to return {x, y, z} with CATEGORY_Z constants + z-tests</name>
  <files>src/game/constellation.layout.js, src/game/constellation.layout.test.js</files>
  <read_first>
    - src/game/constellation.layout.js (current — full file, 81 LOC; assignment sites at lines 63 + 72-75)
    - src/game/constellation.layout.test.js (current — 4 existing tests; append after the last describe block)
    - .planning/phases/20-3d-constellation/20-PATTERNS.md §"src/game/constellation.layout.js" + §"src/game/constellation.layout.test.js"
    - .planning/phases/20-3d-constellation/20-CONTEXT.md §"Category-Z Storytelling" (D-20-CONTEXT-ZMAP)
    - .planning/phases/20-3d-constellation/20-UI-SPEC.md §"Layout — z-Storytelling (D-20-CONTEXT-ZMAP)"
    - .planning/research/PITFALLS.md §CRIT-04 (z=0 trap) + §MOD-01 (z-clipping) + §MOD-09 (props-contract divergence)
  </read_first>
  <behavior>
    - Test A (z present): every node in `computeLayout(nodes)` returns an entry with `typeof pos.z === 'number'` — mirrors the existing line 17-22 pattern that asserts `typeof pos.x === 'number'`.
    - Test B (z clustered by category): all nodes in category `lang` share `pos.z === 75`; all nodes in category `ai` share `pos.z === 150`; all nodes in category `hardware` share `pos.z === -150`. Defends D-20-CONTEXT-ZMAP narrative integrity.
    - Test C (≥2 distinct z values + z range ≥100): collect the set of `pos.z` values across all nodes; assert `set.size >= 2` AND `Math.max(...values) - Math.min(...values) >= 100`. Directly defends CRIT-04 z=0 trap per PATTERNS Alert A2.
    - Test D (z range bounded): every `pos.z` satisfies `-200 <= pos.z <= 200`. Defends MOD-01 z-clipping at `near=10, far=2000`.
    - Test E (deterministic): `computeLayout(nodes)` called twice returns identical `pos.z` values for every node id. Mirrors the existing determinism test at line 34 (which asserts x/y; extend with z comparison).
  </behavior>
  <action>
    RED. In `src/game/constellation.layout.test.js`, append a new describe block AFTER the existing `describe('computeLayout - category clustering', ...)` block (current line 103):

    ```
    describe('computeLayout - z storytelling (D-20-CONTEXT-ZMAP)', () => {
      it('should assign a numeric z to every node', ...)
      it('should cluster nodes by category in z (lang=75, ai=150, hardware=-150)', ...)
      it('should produce >=2 distinct z values and range >=100 across the constellation (CRIT-04)', ...)
      it('should keep every z in range [-200, 200] (MOD-01)', ...)
      it('should be deterministic across calls (z values identical)', ...)
    })
    ```

    Use the existing `buildTestNodes()` fixture at lines 5-8 — no new fixture needed. Run `npm test -- constellation.layout` and confirm all 5 new tests FAIL (computeLayout still returns `{x, y}`, no `.z`).

    GREEN. Edit `src/game/constellation.layout.js`:

    1. AFTER `const NODE_CLUSTER_RADIUS = 80` (current line 17), append exactly:

    ```
    // CATEGORY_Z values are starting points — UAT-tunable. Consult v3.10-UAT.md
    // before adjusting after milestone close. (D-20-CONTEXT-ZMAP, Alert A14)
    const CATEGORY_Z = {
      ai: 150,
      lang: 75,
      arch: 0,
      data: -25,
      cloud: -75,
      devops: -100,
      security: -125,
      hardware: -150,
    }
    ```

    2. Update the JSDoc `@returns` line (current line 23) from `Map of node.id → { x, y }` to `Map of node.id → { x, y, z }`.

    3. At the single-node assignment (current line 63), change:
       `layout[catNodes[0].id] = { x: centroid.x, y: centroid.y }`
       TO:
       `layout[catNodes[0].id] = { x: centroid.x, y: centroid.y, z: CATEGORY_Z[cat] ?? 0 }`

    4. At the multi-node assignment (current lines 72-75), change the object literal:
       ```
       layout[catNodes[i].id] = {
         x: centroid.x + radius * Math.cos(angle),
         y: centroid.y + radius * Math.sin(angle),
       }
       ```
       TO:
       ```
       layout[catNodes[i].id] = {
         x: centroid.x + radius * Math.cos(angle),
         y: centroid.y + radius * Math.sin(angle),
         z: CATEGORY_Z[cat] ?? 0,
       }
       ```

    Run `npm test -- constellation.layout` — all 5 new tests must pass, plus the existing 4 tests stay GREEN (the existing tests assert `typeof pos.x === 'number'` and `typeof pos.y === 'number'` — adding a third property `.z` does not invalidate them).

    Run full suite `npm test` — confirm 261 existing + 5 new = 266 GREEN (or whatever the existing baseline + 5 yields).

    Sanity check the dev server: `npm run dev` then open the game-mode page. Visual must be identical to v3.9 close — `SvgConstellation.js` reads only `pos.x` and `pos.y`, silently ignoring `.z` per D-20-PROPS-CONTRACT (Alert A2 not yet triggered — that's Plan 20-02's flat-disc-on-WebGL defense).
  </action>
  <verify>
    <automated>npm test -- constellation.layout</automated>
  </verify>
  <acceptance_criteria>
    - `rg "^const CATEGORY_Z" src/game/constellation.layout.js` returns exactly 1 hit.
    - `rg "UAT-tunable" src/game/constellation.layout.js` returns ≥1 hit (Alert A14 comment-block present).
    - `rg "CATEGORY_Z\\[cat\\] \\?\\? 0" src/game/constellation.layout.js` returns exactly 2 hits (one per assignment site — lines ~73 single-node and ~85 multi-node).
    - `rg "z storytelling" src/game/constellation.layout.test.js` returns ≥1 hit (new describe block present).
    - `npm test -- constellation.layout` exits 0; ≥7 tests pass in the file (4 existing + ≥3 new — target is 5 new but Test E may merge with the existing determinism test).
    - `npm test` exits 0; full suite GREEN.
    - `npm run dev` — manually verify SVG path renders identically to v3.9 close (no visual regression; layout `.z` silently ignored by `SvgConstellation.js`).
    - `npm run build && node scripts/check-bundle-gate.mjs` exits 0 (mobile chunk still ≤38.82 kB gz; layout module is in eager bundle but adds <100 bytes of CATEGORY_Z constants).
  </acceptance_criteria>
  <done>
    `computeLayout()` returns `{x, y, z}` with 8 categories mapped per D-20-CONTEXT-ZMAP; 5 new layout tests assert z presence + clustering + range + determinism + ≥2 distinct values; CRIT-04 z=0 trap is defended at the source layer; SVG dev-server render visually unchanged; commit `feat(20-01): extend computeLayout to {x,y,z} with CATEGORY_Z architecture-stack narrative (D-20-CONTEXT-ZMAP, CRIT-04)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI build → mobile chunk | Build-time emission of `dist/assets/GameMode-*.js`; bundle-gate is the guard preventing `three/...` import strings from reaching mobile users |
| Pure-function layout module → renderer consumers | `computeLayout()` produces deterministic `{x,y,z}`; SVG and WebGL renderers consume identical shape (D-20-PROPS-CONTRACT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-SC | Tampering / Supply-chain | `scripts/check-bundle-gate.mjs` regex (CRIT-03) | mitigate | Widen `THREE_JS_PATTERN` to `/THREE\.|from\s*['"]three(\/[^'"]+)?['"]/` BEFORE OrbitControls import lands in Plan 20-02. Add 5-case regression fixture in `scripts/check-bundle-gate.test.mjs`. CI-blocking on regex match. |
| T-20-01-DoS | Denial of Service | `CATEGORY_Z` value bounds | accept | Project-controlled skill data with ~26 nodes today; max projected ~50-80 nodes ever. No user input flows into layout. z values are hard-coded module constants in `[-150, +150]`. |
| T-20-01-Info | Information Disclosure | Layout test fixtures | N/A | Tests consume `EXPERIENCE` + `SKILLS` from project source files — no PII, no secrets, no external data. |
| T-20-01-Repud | Repudiation | Layout determinism | mitigate | Test E (deterministic across calls) asserts no implicit randomness; defends against silent regression to `Math.random()` or `Date.now()` in future. |
| T-20-01-XSS | XSS via translation / copy | N/A | N/A | Plan 20-01 introduces ZERO new i18n keys, ZERO new user-facing strings. Comment-block "UAT-tunable" lives in source-only — never rendered. |

**No package install tasks in this plan** — no `[ASSUMED]`/`[SUS]` packages, no new dependencies. Bundle-gate widening is supply-chain hygiene (T-20-01-SC) and is the entire point of Task 1.
</threat_model>

<verification>
**Phase-level checks for Plan 20-01:**

1. `npm test` exits 0 with net +5+ tests over the v3.9 baseline of 261.
2. `npm run build` succeeds without error.
3. `node scripts/check-bundle-gate.mjs` exits 0 against the post-Plan-01 build:
   - Mobile chunk `GameMode-*.js` ≤ 38.82 kB gz (HARD ceiling) — expected ~8.91 kB unchanged.
   - WebGL chunk `WebGLConstellation-*.js` reported as INFO (current Phase 17 size, no three.js leak to mobile).
4. `rg "CATEGORY_Z" src/game/` returns hits only in `constellation.layout.js` (NOT in `src/data/skills.js` per CONTEXT boundary).
5. Manual dev-server visual smoke: open the game-mode page at `npm run dev`, confirm SVG renders identically to v3.9. No flat disc on WebGL path — that's the Plan 20-02 trap (`positions[i*3+2] = 0` line still hardcodes 0 until Plan 20-02 lands; current rendering uses `OrthographicCamera` where z is meaningless anyway).
6. `git log --oneline -3` shows two atomic commits with `(20-01)` scope per the task done blocks.
</verification>

<success_criteria>
- [ ] `THREE_JS_PATTERN` regex in `scripts/check-bundle-gate.mjs` is widened to `/THREE\.|from\s*['"]three(\/[^'"]+)?['"]/` and exported as a named export.
- [ ] `scripts/check-bundle-gate.test.mjs` exists with ≥5 test cases covering: literal `'three'` path, `'three/addons/*'` path, `'three/examples/jsm/*'` path, `THREE.` namespace access, and a no-match negative case.
- [ ] `CATEGORY_Z` constants block exists in `src/game/constellation.layout.js` with the 8 categories at the exact values per D-20-CONTEXT-ZMAP, preceded by the UAT-tunable comment block per Alert A14.
- [ ] `computeLayout()` returns `{x, y, z}` at both assignment sites (single-node and multi-node branches).
- [ ] JSDoc `@returns` line updated to reflect new shape `{ x, y, z }`.
- [ ] ≥3 new tests (target 5) appended to `constellation.layout.test.js` under a `describe('computeLayout - z storytelling (D-20-CONTEXT-ZMAP)', ...)` block.
- [ ] `npm test` exits 0 with all 261 existing tests still GREEN plus new layout + bundle-gate tests.
- [ ] `npm run build && node scripts/check-bundle-gate.mjs` exits 0 against the current build (no three.js leak yet — Plan 20-02 has not landed).
- [ ] Dev-server visual smoke confirms SVG path renders identically to v3.9 close.
- [ ] Two atomic commits land on the branch: one for the bundle-gate regex widening, one for the layout z extension.
</success_criteria>

<output>
Create `.planning/phases/20-3d-constellation/20-01-SUMMARY.md` when done. The SUMMARY must report:
- Final test count (was 261, target ≥266 GREEN).
- Mobile chunk size after build (target ≤8.91 kB gz, unchanged from v3.9).
- Bundle-gate exit status (must be 0 / PASS).
- Confirmation that the regression fixture tests would HARD FAIL the gate if a `three/addons/...` import string appeared in the mobile chunk.
- Visual smoke note: dev server SVG path renders identically to v3.9 (`renderer-slot` data-renderer attribute = `svg` or `webgl` — verify both paths if capability-tier permits).
- Decision log: confirm D-20-CONTEXT-ZMAP and D-20-PROPS-CONTRACT are now backed by code + tests; decisions to log at milestone close stay queued for Plan 20-03.
</output>
